# WORKLOG — now-here-demo

> 여러 PC를 오가며 작업하기 위한 **단일 상태 소스**입니다.
> 새 PC에서 시작할 때 이 파일을 먼저 읽고, 작업을 마치면 아래 규칙대로 갱신·push 하세요.

> ✅ **정식 repo: `gihoon-mx/now-here-demo`** — https://github.com/gihoon-mx/now-here-demo
> 새 PC에서는 이것만 clone 하세요: `gh repo clone gihoon-mx/now-here-demo`
> (구 repo `now-here-map-demo-pages`는 **폐기(deprecated)** — 더 쓰지 않음. 상세는 아래 이전 이력 참고.)

---

## v2.70.0 — 매장이 저장에서 잘리고 있었다 · 무조건 실사진 · 자리에 지터 (2026-08-20, 콘솔 D237 짝)

사용자: "여전히 매장 라벨이 안 뜨고 (정확히는 now-here-demo 앱에서), 사진이 아닌 일러스트라
사실감이 떨어지고, 배치 간격이 일정해서 인위적이다. 사진은 무조건 받아줘."

- **매장이 안 뜨던 진짜 이유는 저장 상한이었다** — saveDeals 가 timeDeals.slice(0,20).
  콘솔에서 매장 12+딜 12=24 를 가져오면 push 는 되는데 **저장은 앞 20 개만** 남아(새 것은
  배열 뒤에 붙는다) 새로고침하면 사라졌다. 120 으로 올렸다. 렌더·게이트 문제가 아니었다.
- **sgImportStage 가 v2.69 의 사진 폴백을 안 타고 있었다** — 그 판은 무대 **재생** 경로
  (nhLayFeed)만 고쳤고, 관리자 **가져오기**는 여전히 '콘솔 주소 없으면 곧장 색 카드' 였다.
  이제 콘솔 주소 → 근처 장소 사진 → 테마별 실사진(Wikimedia) → 색 카드 순이고, 매장·딜
  카드에도 사진을 붙인다. 가져오기 직전에 photoKw(또는 지역 이름)로 프리페치도 청한다.
- **자리에 결정적 지터** — 황금각만 쓰면 최근접 간격이 거의 균일해(변동계수 0.028) 기계가
  뿌린 티가 났다. 각 ±0.55rad · 반지름 0.72~1.22배를 heatJitter 로 얹어 0.515 로.
  같은 번호는 여전히 같은 자리다(시연 규칙). nhSpread(재생 경로)도 같은 손질.
- 가져오기 상한을 NH_MAX 로 (여태 40/40/10/12 하드코딩) · 라이브카메라(kind:'cam')가
  가져오기에서 post 로 굳던 것 수리.

## v2.69.0 — 무대 사진은 웹에서 · 상한 상향 · 가져온 무대 표시 토글 (2026-08-20, 콘솔 D236 짝)

- **사진**: 무대 시드 피드에 img 가 없으면 nhBurstPhoto 로 채운다 — seed.photoKw(새 칸)로
  회차 시작에 근처 장소 사진을 미리 받고(nhPlacePhotoPrefetch, 표기는 약관대로 nhPlaceCredit,
  회차 끝에 nhSweepTemp 가 걷음), 없으면 테마별 실사진 풀, 그것도 없으면 여태의 테마 색.
- **상한**: NH_MAX 를 req 10→24 · spot 40→60 · feed 40→60 · deal 10→24 · page 12→16 으로.
  콘솔 MAX_SEED_* 와 짝이다(한쪽만 올리면 조용히 잘린다).
- **그룹 숨김의 비대칭 수리**: sgGroupSetHidden 이 스팟·피드에만 전파돼 매장·Request 는
  그룹을 숨겨도 지도에 남았다. 이제 timeDeals·fieldRequests 에도 전파하고 렌더 필터
  (renderDealMarkers·renderRequestMarkers)가 hidden 을 본다.
- **가져온 무대 전부 숨기기/보이기** (sgPvcToggleAll) — 관리자 › 지역 시드 생성 › 무대
  가져오기 옆 버튼. src:'pvc' 그룹 전체를 한 번에 감춘다.

## v2.68.0~2.68.1 — 픽 실클릭 수리 · 행정동 다중 선택 · 영역(폴리곤) 배치 · 존 목록 (2026-08-20, 콘솔 D233 짝)

**찍기가 안 되던 원인**: 폰 지도가 동 경계 GeoJSON(data 레이어)로 덮여 있어 서울 안쪽
탭이 map click 이 아니라 data click 으로 갔다 — v2.66 픽 리스너는 map click 에만 걸려
폴리곤 위에서 영영 안 울렸다. 이제 픽은 **두 리스너**(map+data)에 걸리고, 무장 중에는
동 선택(selectPhoneDong 카메라 밀림)·강조 해제·롱프레스 추가 메뉴가 끼지 않는다.

- **nh:pick mode:'dong'** — 행정동 여러 개 토글 (regionAt 판정, data 스타일 하이라이트,
  선택 전체를 nh:picked 로 회신). point 모드는 계속 무장(옮겨 찍기).
- **nh:shape** — 폴리곤 미리보기 (원의 폴리곤판, rings/clear).
- **areaPlaces 항목에 dongs(행정동 키)·rings(링들)** — nhSpread 가 원 대신 그 안에
  결정적으로 편다 (R2 수열 + pointInRing 리젝션, nhSpreadRegion).
- **nh:ready 에 zones**(publicSettings 의 zoneBook) — 콘솔이 트렌드 존을 고를 재료.
- **sgImportStage 가 region(dongs·rings) 존중** — 재생과 같은 배치 규칙.
- **청소 누락 수리** — nhReset 이 픽 리스너·원·영역 미리보기를 걷는다.
- **2.68.1**: zoneBook 의 shape 는 외곽 링이 아니라 **헥사 셀 중심들**이었다 (라이브 실측:
  1~4점) — cells{centers,radiusKm} 어휘를 더해 hexVertices 로 링을 세운다 (배치·미리보기·
  관리자 가져오기 셋 다). 1~2칸 존도 성립한다.

## v2.67.0 — 픽커 모드: 지도만 남긴 임베드 (2026-08-20, 콘솔 D232 짝)

콘솔 「직접 지정」이 큰 모달로 띄우는 변형. embed=1&clean=1&pick=1 로 뜨면
- body.pick-mode 가 .phone-screen 의 지도 형제를 전부 걷는다 (내비·지면·피드·앱바·팝업)
- 지도 제스처를 연다 (임베드의 '스크립트 전용' 예외 — 사람이 끌어 보며 고르는 자리)
- 커서 crosshair. 나머지는 여태 어휘 그대로 (nh:pick/picked/circle, v2.66).

## v2.66.0 — 직접 지정: 지점 찍기 · 반경 원 · 반경대로 흩기 · 코드로 불러오기 (2026-08-19, 콘솔 D231 짝)

콘솔 컨텐츠 채우기의 「직접 지정」 짝. 넷:
- **nh:pick / nh:picked / nh:circle** — 콘솔이 켜면 폰 지도의 다음 탭 한 번을 좌표로
  돌려주고, 반경 원(google.maps.Circle, 첫 사용)을 그려 미리 보인다. 재생 중에는 거절.
  수신 사슬 nh:peek 뒤에 additive 로 붙었다 — 옛 콘솔은 모르는 채로 그대로 돈다.
- **nhAreaFrom 이 r(m) 을 나른다** — areaPlaces[0].r 이 SEED_AREAS.custom.r 이 되고,
  nhSpread 가 c.r 로 반경을 편다 (기본 0.0015/0.0008° 비율 유지, r 없으면 여태 동작).
  100~3000m 클램프.
- **sgImportStage 가 center/radM 을 존중** — 콘솔이 직접 지정으로 만든 무대는 그 자리
  그대로. 없으면 여태처럼 보고 있는 지역 + 반경 입력값.
- **코드로 불러오기(sgImportCode)** — 콘솔의 공유 코드 nhs1.<파이어베이스>.<프로젝트>.<토큰> 을
  Firestore REST 로 읽어 텍스트영역에 채우고 같은 가져오기 길을 탄다. 콘솔↔앱이 다른
  Firebase 프로젝트라(콘솔 D230) 코드가 주소를 통째로 나른다 — 앱 설정 불필요.
  문서는 콘솔 규칙이 status=='open' 인 것만 연다.

검증: app.js 구문 OK · 브라우저 육안은 콘솔 쪽 세션에서 (이 판은 콘솔 v0.185.0 과 짝,
앱 먼저 배포).

## 🔁 다른 PC에서 이어가기 (매번 이 순서)

```bash
# 1) 최신 상태 받기 (작업 시작 전 항상)
git pull

# 2) ...작업...

# 3) 작업 끝나면: WORKLOG 갱신 → 커밋 → push
git add -A
git commit -m "작업 내용 요약 vX.Y.Z"
git push
```

- **push 전 반드시 `git pull`** 먼저 (다른 PC에서 올린 변경과 충돌 방지).
- 배포: **GitHub Actions 워크플로우(`.github/workflows/pages.yml`)로 배포**(2026-07-03 legacy→Actions 전환). `main` push마다 자동 실행.
- 배포 URL: **https://gihoon-mx.github.io/now-here-demo/**

> 🛠 **배포가 라이브에 안 뜰 때**:
> 1. Actions 탭에서 최근 run 확인: `gh run list --workflow=pages.yml` / 재실행: `gh workflow run pages.yml --ref main`
> 2. `Deploy to GitHub Pages` 단계가 **`Deployment failed, try again later`**로 실패하면 = **GitHub Pages 백엔드 일시 장애**(코드 문제 아님). 몇 분~수시간 뒤 자동 회복되며, 회복 후 재실행하면 배포됨.
> 3. 폰/브라우저에서 옛 버전이면 URL에 `?x=1` 붙여 캐시 우회(그래도 안 바뀌면 아직 미배포).
> - (참고) 2026-07-03 오전, legacy·Actions **양쪽 모두** 배포 지연/실패 관측 → GitHub Pages 측 이슈로 판단. Actions 방식이 로그·상태가 보여 진단이 쉬움.

> ⚠️ **repo 이전됨(2026-07-03)**: `gihoon-mx/now-here-map-demo-pages` → **`gihoon-mx/now-here-demo`**.
> 다른 PC에서는 remote를 새 repo로 바꾸거나 새로 clone:
> ```bash
> git remote set-url origin https://github.com/gihoon-mx/now-here-demo.git   # 기존 clone 재지정
> # 또는:  gh repo clone gihoon-mx/now-here-demo
> ```
> (이전 repo는 GitHub Pages 배포 큐가 꼬여서 새 repo로 이전. 같은 `gihoon-mx.github.io` 호스트라 GCP/Firebase 도메인은 그대로 유효 — 경로만 `/now-here-demo/`로 바뀜.)

### 🔢 버전 규칙 (코드/스타일 바꿀 때마다 필수)
제작(코드·스타일·기능 변경)마다 버전을 올리고 **3곳을 동기화**:
1. `index.html` → `<span id="app-version">vX.Y.Z</span>`
2. asset 캐시버스트 → `style.css?v=X.Y.Z`, `app.js?v=X.Y.Z`, `config.js?v=X.Y.Z`
3. 커밋 메시지에 `vX.Y.Z`
- 증가: 일반 변경 = 패치(+0.0.1), 큰 기능 = 마이너(+0.1.0). 문서(WORKLOG 등)만 바뀌면 버전 유지.
- **현재 최신: v2.65.0** (변경 이력은 아래 📝 변경 이력 절이 항상 최신이다 — 아래 📸 스냅샷 절은 v1.66 시점에서 멈춰 있다)
- ⚠️ v1.65부터 **admin.html도 버전 동기 대상**(check.js가 index/admin의 #app-version·?v= 일치를 강제). index/admin에 **공통 요소**(폰 화면 마크업·모달·설정 섹션)를 수정하면 **두 파일 모두** 반영할 것.

---

## 📸 현재 상태 스냅샷 (2026-07-09)

**최신 v1.66.0 · 라이브 정상.** 완료된 기능:
- **v1.66.0 관리자/폰 설정 UI 다듬기 4종 (M11+M14 + M09 CSS ⚠️교차)**: ①**관리자 메뉴 팝업 내비 카테고리 강조** — 상위 카테고리(컨텐츠/스타일/시스템) 글자 확대(.66→.84rem·잉크색)+그룹 경계선(border-top), 하위 항목 들여쓰기(18px) ②**관리자 지도 좌측 상단 햄버거 제거** — admin.html에서 #pc-menu-btn·#pc-drawer 삭제(전 참조 가드 확인, 둘러보기=폰 미리보기 햄버거·관리=탑바 팝업으로 일원화) ③**관리자 설정 블록 기본 전부 펼침** — `initSettingsAccordion`에 IS_ADMIN_PAGE 분기(팝업 내비가 섹션을 하나씩 보여주므로 접기 불필요; 아코디언·h4 클릭 토글은 폰 드로어 전용) ④**PC/폰 설정 화면 분리 + 폰 최적화** — 화면은 분리(admin.html 팝업 vs index.html 폰 드로어, 마크업 독립), **설정값만 공유**(같은 DRAFT→cloudSave→Firestore 실시간 동기). 폰 드로어 설정 터치 최적화 CSS(`body.page-app #phone-drawer-body` 스코프): 아코디언 헤더 13px 패딩·설정 로우 38px 최소높이·레인지 썸 20px·셀렉트/버튼 확대·색상 스와치 38×26. 검증: check.js PASS·양 페이지 콘솔 에러 0·관리자 스타일 패널 아코디언 미적용(전부 펼침)·내비 캡션 스타일 실측 PASS.
- **v1.65.0 서비스/관리자 페이지 분리 + 관리자 대형 메뉴 팝업 + 색상 팝업 통일 (M09+M11+M12+M14 + M03/M04 ⚠️교차)**: ①**페이지 분리** — `index.html`=**서비스(폰 앱 전용)**: PC 지도·설정 숨김, 데스크톱=폰 프레임 중앙 무대(390×844 스테이지), 모바일=기존 풀스크린, 폰 지도가 항상 주 지도(gestureHandling greedy·primaryMap), 데스크톱에서도 드로어 관리자 탭 사용 가능, 프로필 메뉴에 🛠 관리자 페이지 링크(관리자만) / `admin.html`=**관리자 전용(신설)**: 탑바(브랜드·모드 토글·메뉴 3버튼·📱서비스 열기·계정)+PC 지도 전면+폰 미리보기(우측, 표시영역 오버레이 유지), 데모유저 접근 시 차단+서비스 안내. 같은 app.js/style.css 공유 — `window.PAGE_MODE`('app'/'admin') 분기(IS_APP_PAGE/IS_ADMIN_PAGE), 관리자 페이지는 드로어=둘러보기 전용(설정 이동·관리자 탭 생략) ②**관리자 대형 메뉴 팝업**(`initAdminMenu`, #admin-menu 1080×780): 좌측 카테고리 내비(🗂컨텐츠: 스팟/존/피드/소셜/지면/시드 · 🎨스타일: 스팟 말풍선/동 구역·라벨/존·라벨/렌즈 · 🛠시스템: 미리보기·백업/계정·권한/문서·개발) + 우측 패널 — 기존 .settings-section 블록을 data-adm 키로 재배치(id·배선 그대로), 스타일 그룹=적용 바(드래프트→전체 적용) 포함, 🧩 기능 보기 설정 점프도 팝업으로 연결(jumpToSetting 분기), 지면(뉴스)은 컨텐츠 그룹으로 이동해 컨텐츠 관리 일원화 ③**색상 팝업 통일**: 모든 색상 설정 팝업에 **기본 팔레트=온도 4색(#DE2F2A/#F2862E/#F2C53D/#9DC64C)+브랜드 블루 #1428A0**(PALETTE 단일 소스, 존 생성 팔레트도 5색) + **투명도 조절**: 글자색 3종(스팟/동 라벨/존 라벨)=`textOpacity`(additive, SPOT/LLABEL/ZLABEL_FIELDS 클라우드 동기, 지도 위 스팟 텍스트는 색조=모드 규칙·투명도=설정 존중), 스팟 개별 색=`alpha`(liveSpots/mapContent additive), **존 색=네이티브 입력 3곳(생성 폼·편집 바·존 카드) 전부 통일 팝업으로 교체**하고 알파=**존 채움 투명도 fillA**(기본 0.35, zones 직렬화 4곳 additive) ④check.js에 admin.html 검사 추가(#app-version·?v= 3종·PAGE_MODE 스탬프 — CI 강제). 검증: 라이브 프리뷰 양 페이지 부팅 콘솔 에러 0, 서비스 데스크톱 폰 스테이지 390×844 센터링, 관리자 탑바 52px+메뉴 팝업(그룹 전환·패널 표시·닫기)·색상 팝업(프리셋 5색 선두·알파 슬라이더·#1428A0 콜백)·txA/zoneFillA(기본값·클램프·하위호환) 전수 PASS. ⚠️로그인 뒤 실지도 동작(존 fillA 렌더·관리 플로우)은 라이브에서 확인.
- **v1.64.0 트렌드존 라벨 줌 위치 버그픽스 + 라벨 표시 토글 (M03 + M00 MapLabel 버그픽스 + M11 ⚠️교차)**: ①**(버그) 줌인/아웃 시 존 라벨 위치가 계속 밀리던 문제 픽스** — v1.62에서 `MapLabel.draw`에 CSS `zoom`(라벨 크기 줌 연동)을 넣으며 **같은 div에 `left/top`과 `zoom`을 동시에** 걸었던 게 원인. CSS `zoom`은 요소 크기뿐 아니라 **`left/top` 오프셋까지 `s`배로 곱해 렌더**하므로 앵커가 `pos.x → pos.x*s`로 밀리고, 줌마다 `s`(0.7~1.6)가 달라져 라벨이 계속 흔들렸음 → **`left/top`을 `s`로 나눠 보정**(렌더 위치=`(pos/s)*s`=pos, 중심 정렬은 CSS `translate(-50%,-50%)`). 브라우저 실측: 기존=130px 드리프트, 수정=100px 정확 고정(s=1.3, anchor=100). ②**존 라벨 표시/숨김 토글**(관리자 › 트렌드 존 설정 '라벨 표시') — 동 라벨 `localLabelConfig.enabled` 패턴 미러링: `zoneLabelConfig.show`(기본 true) + `ZLABEL_FIELDS`에 추가(클라우드 동기)+`zoneLabelsShown()` 게이트(메인맵 `renderZoneOnMap`·폰맵 `syncPhoneZones` 라벨 생성부), 토글 변경 시 `rerenderZones()`로 라벨 생성/제거, 미니 프리뷰 칩도 반영. **additive-only**: 구버전 클라우드 데이터(필드 없음)=`show!==false`로 표시 유지. 검증: node check.js(3곳 동기화·스탬프·문법)+브라우저 실측(CSS zoom 오프셋 130→보정 100, 배선 전수, show true/false/undef 하위호환) PASS·콘솔 에러 0. ⚠️지도 위 실제 존 라벨 렌더·줌 고정은 로그인 게이트 뒤라 라이브에서 최종 확인.
- **v1.63.0 팝업 헤더·의견 뱃지·버그픽스 3종·모드 전환 연출 (M04+M05+M07+M09 ⚠️교차)**: ①**팝업 헤더 재구성**: [제목 | 📍 지도보기·✏️ 수정(권한자) | 닫기] 한 줄 얼라인(cpop-head/headAct, ✕→'닫기' 텍스트, 하단 액션 행 제거) ②**스팟 의견 수 뱃지**: 버블 우상단 💬n(색=모드 규칙 그레이/온도색, liveChat 스냅샷·로컬 전송 시 즉시 갱신 — .spot-cmt) ③**(버그) Request 핀 탭 무반응 픽스**: v1.60에 핸들러만 달고 `.req-pin{pointer-events:none}`을 안 풀었던 것 → **rp-drop만 클릭 허용**(펄스 링·컨테이너는 지도 제스처 통과) ④**팝업 프로스트**: .cpop-card=네비바와 같은 유리(0.72+blur 26px) ⑤**베이직↔트렌드 전환 연출**: AI 아이콘 스핀(기존 aiSpin 재사용, 선글라스 착탈 느낌)+지도 펄스(#map/#phone-map mapMorph scale 1→1.02→1, reduced-motion 존중) ⑥**(버그) 피드 썸네일 크기 덜컹임 픽스**: **스팟 이모지와 완전 동일 로직** — 기준 크기=spotConfig.emojiSize(26), 곡선=spotScale 클램프 없음(z16=26px·z18=104px 이모지와 1:1 비례), 점 전환 기준(spotDotScaleM)도 동일(.fp-dot 12px·뱃지 숨김), width/height CSS 트랜지션 제거(덜컹임 원인) — CONTENT_PIN_BASE 폐기 ⑦**(버그) 트렌드존 리스트 썸네일 안 뜸 픽스**: zoneBestPhoto가 존 태깅(f.zone)만 봐서 zone:null인 시드/일반 피드가 전부 제외됐던 것 → **존 범위 안 좌표 피드도 포함**(feedItemLatLng+ptInZone, 하트합산과 기준 통일). 검증: 라이브(헤더 정렬·프로스트 0.72·의견 뱃지 카운트·rp-drop pointer/cursor·핀 수식 z15/16/18=13/26/104·mapMorph keyframes·스크린샷)+콘솔 에러 0 PASS.
- **v1.62.0 라벨 스케일 안정화 + 탭 UX 통일 + 포커스 규칙 재정립 (M00 수정+M02+M03+M05+M07+M09 ⚠️교차)**: ①**지도 라벨(존 이름·동 선택) 크기 안정화** — 고정 px라 줌마다 지면 대비 상대 크기가 들쭉날쭉하던 것 → **MapLabel.draw에 spotScale 곡선 줌 연동**(CSS zoom, 클램프 0.7~1.6) = 스팟과 같은 리듬으로 부드럽게. ⚠️M00 '수정 금지' 예외 적용: 호출처 2곳(존 라벨·동 라벨) 모두가 의도 대상이라 draw 직접 수정(사유 기록) ②**컨텐츠 탭 UX 통일**: 규칙="컨텐츠(스팟/피드/Request)는 어디서 탭하든 **상세 팝업**, 지도 이동은 팝업 안 **📍 지도에서 보기**(cpopGoMap), 지역(동/존) 탭=포커스 이동, 지면 캐러셀=스와이프 열람 전용" — 드로어 스팟 칩(기존 지도이동→팝업), 피드 카드 싱글탭(기존 지도이동→팝업, 더블탭 좋아요 유지), 드로어 Request 카드(기존 지도이동→팝업, 내부 버튼 stopPropagation 유지), 팝업 3종에 📍 버튼 추가, 죽은 focusFeedEntry 제거 ③**포커스 모드 재정립**: (a)탭 선택=지도 이동+핀 고정(기존) (b)**드래그로 선택 지역/존을 벗어나면 자동 해제**(autoReleaseFocus, idle에서 센터가 선택 영역 밖이면 해제→자동 렌즈가 센터 추종) (c)**지도 탭 선택은 화면에 지역/존 3개 이상 보일 때만**(visibleRegionCount bbox 교차·visibleZoneCount 센트로이드 포함, 줌인 오터치 방지 — 재탭 해제·드로어/리스트 선택은 항상 허용). 검증: node(가시 카운트 3조기종료/줌인1개/게이트 가드·라벨 클램프 0.7~1.6·잔존 참조 0)+라이브(팝업 3종 📍 버튼·헬퍼 5종·콘솔 에러 0) PASS. ⚠️지도 위 실동작(라벨 스케일·자동 해제·탭 게이트)은 라이브 로그인 후 확인.
- **v1.61.0 UX 5종 — 시드 전량 실사진·존 좋아요순 정렬·핀 크기 상한·AI 선글라스·스팟 의견 (M03+M05+M07+M08+M09+M13 ⚠️교차)**: ①**시드 생성(SVG) 이미지 17건 전량 실사진 교체**(M13): Commons API 검색→브라우저 실로드 검증 16장 신규(parkRun·mural·nightRoad·cherryStreet·foodAlley·shopStreet·roastery·dogWalk·gukbap·bookNight·flavin·burger·bakery·climb·cherryCampus·barista)+garosu 재활용, '지하상가 쇼핑'은 사진에 맞게 '쇼핑 득템'으로 문구 조정 — **⚠️라이브 반영=🧹 비우기→🌱 채우기 재실행** ②**존 리스트=합산 좋아요순**(M03 sortedZonesForList): 포커스 존 맨앞 이동 폐지(순서 유지, 액센트 테두리·✓ 표시만) ③**핀 크기 상한**(M05/M07): 줌인 시 사진 핀·Request 핀이 스팟을 압도하던 문제 → 표시 배율 클램프 2.4→**1.3**(z17부터 고정 ~57px, 스팟은 축척 고정이라 계속 커짐=의도) ④**트렌드 모드 AI 캐릭터=선글라스**(M08): SVG .ai-shades 그룹(렌즈 2+브릿지+템플+하이라이트), body.mode-trend(.ai-flame)에서 표시+눈 숨김 ⑤**스팟 상세 팝업 의견**(M09): 메시지 버블 리스트(본인=우측 파랑)+입력/Enter 전송 — **liveChat 컬렉션 room='spot:<id>' 재사용**(규칙 변경 불필요·계정 간 실시간 공유·소셜 탭엔 안 섞임, 팝업 열린 중 스냅샷 갱신 시 실시간 재렌더 cpopRefresh), 폴백=localStorage. 검증: 이미지 16장 브라우저 실로드(960w)+dogWalk/쇼핑거리 시각 확인, 선글라스 렌더 스크린샷(트렌드/베이직 비교), 의견 전송→me 버블→localStorage persist, 클램프 z20=1.3, 콘솔 에러 0 PASS.
- **v1.60.0 UX 7종 — 흔들림 픽스·상세 팝업·스팟 지도배경·Request 카운트다운·온도 수동·투명 스플래시·라운드 아이콘 (M00+M04+M05+M07+M09 ⚠️교차)**: ①**(버그) 줌 시 스팟 위치 흔들림 수정** — v1.59 declutter가 idle마다 방향을 재계산하며 근소한 차로 방향이 뒤바뀌던 churn → **현재 방향 유지 우선 안정화**(`declutterBoxes`에 cur 인자, 겹침이 사실상 동급이면 유지) ②**지도 컨텐츠 탭=상세 팝업**(`openContentPop`, #content-pop 모달): 스팟(큰 이모지+전체 텍스트+동, 권한자 ✏️ 편집 진입 — 기존 즉시 편집 모달을 팝업 경유로 변경), 피드(원본 사진+설명+만든이+♥+시간+LIVE — **비편집 핀도 탭 가능해짐**, 전 핀 overlayMouseTarget), Request(질문+장소+남은시간+답변 목록/응답 버튼 — 핀 pointer-events 활성) ③**피드 탭 스팟 카드=위치 지도 배경**(`staticMapUrl`, Maps Static API): 카드 128px≈축척(m) 기준 줌 계산, 관리자 옵션 **투명도**(끔/옅게/보통/진하게)+**축척**(50/100/200m, 기본 100m) — 클라우드 동기(spotMapBg), 로드 실패=onerror로 배경 제거. **⚠️ API 키에 'Maps Static API' 허용 필요** ④**Request 남은 시간**: `reqRemainLabel`(1분 이상=분·미만=초), 드로어 카드+상세 팝업에 ⏱ 칩, **1초 티커**(`tickReqRemain` — data-rq-left 텍스트만 갱신, 만료 시 1회 재렌더) ⑤**온도 수동 오버라이드**(`heatTOf`): **존 온도**(존 카드 편집 zi-temp 0~100, 존 안 컨텐츠 기본값) + **컨텐츠 개별 온도**(피드 관리 리스트 온도 입력·스팟 편집 모달 se-temp) — 빈값=자동(좋아요/하트합산), 수동값이 항상 우선. 직렬화: zone.temp(클라우드 zones)·feed temp(liveFeed additive)·spot temp(liveSpots/로컬) ⑥**인증 스플래시 박스 제거**: .auth-card 배경/테두리/그림자 제거 — 그라디언트 배경 위 직접 표시 ⑦**앱 아이콘 라운드 컷**: 정사각 원본 → **모서리 22% 라운드 PNG**(PowerShell System.Drawing 생성: favicon.png 64·icon-192/512.png), 파비콘·매니페스트 any용=라운드 PNG, **maskable=정사각 유지**(런처가 마스킹), apple-touch도 정사각 유지(iOS 자체 라운딩 — 미리 깎으면 모서리 검게 됨). 검증: node(안정화 유지/전환·remainLabel 분/초/올림/만료·staticMapUrl 줌 17/축척비례·heatTOf 0값/클램프/빈값)+라이브(팝업 3종 렌더·티커 ⏱10분·투명 카드·설정 UI·콘솔 에러 0) PASS.
- **v1.59.0 지도 마커 겹침 방지 + 말풍선 자유 방향 + 핀 줌 스케일 통일 (M00 declutter 추가, M04+M05+M07 ⚠️교차)**: ①**겹침 방지(declutter)** — `[M00] declutterBoxes`(순수 기하): 말풍선을 앵커 기준 4방향(위/아래/좌/우) 중 이웃(다른 말풍선)·고정 장애물(피드 핀·Request 핀·점)과 **가장 안 겹치는 쪽**으로 배치, 동점이면 지도 센터에서 바깥으로 향하는 방향 선호(자연스러운 부채꼴). idle·렌더 후 디바운스(60ms) 재계산, try/catch로 지도 안전. ②**말풍선 자유 방향** — SpotBubble이 `_dir`(기본 up)에 따라 origin/translate와 꼬리(`tl-b/tl-t/tl-r/tl-l` 4방향 CSS)를 전환. 기존 '항상 위' → 앵커 기준 어느 방향이든 꼬리가 위치를 정확히 가리킴 ③**피드 썸네일·Request 핀 = 스팟과 동일 줌 스케일** — 기존 계단식(44/30/18)·고정(48×56) → **`spotScale(z)`**(기준 줌 16·줌당 2배, 스팟 메시지와 동일 곡선)에 연동, 핀 가시성 위해 표시 배율만 0.34~2.4 클램프(`CONTENT_PIN_BASE` 44 기준). 검증(node 단위테스트+라이브): dirBox/boxOverlap/outwardDir 기하, 겹치는 말풍선 방향 분리·장애물 회피·센터 바깥 선호, 꼬리 4방향 CSS 실렌더, 핀 스케일(z16=44·z15=22·z17=88·클램프) PASS, 콘솔 에러 0. ⚠️지도 위 실제 배치는 로그인 게이트 뒤 — 라이브에서 스팟 밀집 지역 줌인/모드 전환으로 확인.
- **v1.58.0 지도 컨텐츠 모드 컬러 규칙 + 크기 통일 (M04+M05+M07 ⚠️교차, M00 온도 팔레트 추가)**: ①**베이직 모드=무채색 통일** — 스팟 버블(화이트 프로스트+잉크 텍스트+그레이 점), 피드 핀 뱃지(그레이, 링은 화이트 유지), Request 핀(그레이 그라디언트). 사진 컨텐츠 자체는 컬러 유지(크롬만 무채색). 개별 스팟 색·설정 색은 지도 위에선 모드 규칙이 우선(드로어 칩 등 리스트엔 개별 색 유지) ②**트렌드 모드=온도색** — `[M00] HEAT_STOPS`(앰버 #ffc24a→주황 #ff6a4d→빨강 #ff2e1f, AI_PALETTE 화염 톤 계열)+`heatColor(t)` 보간. 온도 기준=**좋아요**: 피드 핀=`feedHeatT`(좋아요/최다 좋아요, 클러스터=멤버 최고 온도), 스팟·Request=`zoneHeatT`(속한 트렌드 존 하트합산/최다 존, 존 밖=식은 색). 핀들은 인라인 `--heat` 변수 주입+`body.mode-trend` CSS 스코프라 **모드 전환 시 재렌더 불필요**(스팟은 switchMode의 renderSpots 경유) ③**크기 통일(기준 줌)**: Request 핀 드롭 26→34px(컨테이너 48×56, ? 17px, 링 비례) — 피드 핀 44px·스팟(z16 기준 ~45px)과 시각 균형. 스팟의 지도 축척 고정·피드 핀 줌 계단(44/30/18)은 기존 결정 유지 ④검증: heatColor 양끝/중간/클램프, 합성 핀 DOM으로 베이직(그레이)/트렌드(화염+--heat 발현) computed style 전수, 빈 데이터 안전(0), 콘솔 에러 0 PASS. ⚠️지도 위 실핀 확인은 로그인 게이트 뒤 — 라이브에서 모드 토글로 확인.
- **v1.57.1 Google Maps API 키 일원화 (config.js)**: 구 지도전용 키(`hot-hot-map` 프로젝트, `AIzaSyAA6A…`)를 폐기하고 **Firebase 키(`now-here-demo` 프로젝트, `AIzaSyCF63…`)에 Maps JavaScript API를 추가해 단일 키로 통합**. config.js에 `GCP_API_KEY` 상수 1곳 → `GOOGLE_MAPS_API_KEY`·`FIREBASE.apiKey`가 함께 참조(향후 로테이션도 1곳만). Map ID도 새 값(`b14f18…`, now-here-demo 프로젝트)으로 교체. 클라이언트 키라 노출은 정상 — 보안은 Cloud Console HTTP 리퍼러/API 제약이 담당(아래 ☁️ 외부 설정 갱신). 검증: config 배선(맵키=Firebase키 일치·구키 제거·새 MapID)·콘솔 에러 0·네트워크 실패 0 PASS. ⚠️**지도 타일 실렌더는 로그인 게이트 뒤라 로컬 프리뷰로 미검증** — 라이브 배포 후 로그인해 확인 필요. ⚠️**구 키(`AIzaSyAA6A…`)는 라이브 확인 후 Console에서 삭제/비활성**(git 히스토리에 영구 잔존 → 제약 없으면 악용 가능).
- **v1.57.0 Twemoji 통일 렌더링 (M00 신설 섹션, ⚠️교차: M10)**: iOS/Android/PC 기기별로 다르게 보이던 네이티브 이모지를 **Twemoji SVG로 전역 통일**. ①index.html에 `@twemoji/api@15.1.0` CDN 스크립트(app.js보다 먼저 동기 로드, **로드 실패 시 네이티브 이모지 폴백**) ②app.js `[M00] initTwemoji`: 초기 `twemoji.parse(document.body)` + **MutationObserver**(childList+characterData)로 이후 렌더되는 모든 DOM 자동 치환 — rAF 배치(프레임당 1회·중복 제거), IMG/SCRIPT/STYLE/**svg 내부 스킵**(SVG `<text>`에 이모지 넣으면 깨지므로 금지 주석) ③style.css `img.emoji` 사이징(1em, 카드류 범용 img 규칙에 안 밀리게 `!important` 방어) + `#cp-placeholder::before`의 CSS `content:'📰'`는 Twemoji가 못 바꿔 **배경이미지(1f4f0.svg)로 교체**(⚠️교차: M10 컴포넌트 CSS). 대상 아님(네이티브 유지): input/textarea 입력값·native alert/confirm·seedImg 생성 이미지(data URI) 내부 이모지. 하트(♥·❤)도 twemoji 레드 하트로 치환됨 — 기존 디자인이 레드 계열(#ff4d6d)이라 유지 판단. 검증: 초기 파싱 25개 치환·신규 노드/textContent 갱신 옵저버 경로 치환·사이징 1em(11.52px)·이모지 픽커 클로저 전달이라 안전(코드 확인)·콘솔 에러 0·네트워크 실패 0 PASS.
- **v1.56.0 시드 3지역 확장 + 수량·밀집도 옵션 (M13)**: ①시드 데이터를 **지역별 구조**(`SEED_AREAS` 앵커 + `SEED_FEED/SPOTS/REQS = {gangnam,jamsil,seongsu}`)로 재편 — **잠실·석촌호수**(피드 10·스팟 7·Request 1: 새내 먹자골목·석촌호수 러닝/벚꽃·롯데타워·송리단길)와 **성수·서울숲**(피드 10·스팟 7·Request 1: 서울숲·붉은벽돌 카페·창고 전시·뚝섬) 추가, 실사진 5장 신규(Wikimedia HEAD 200 검증: lotteTower·lotteWorld·seongsuBrick·seongsuShop·ttukPark), 채팅(local:잠실2동·잠실본동·성수2가1동+주제방 2)·지면(ns_4·ns_5) 확장 → 총 **피드 40·스팟 30·Request 4** ②채우기 옵션 2종(관리자 › 데모 데이터): **수량**(전체/⅔/⅓ — 지역별 균등 샘플링 `seedPick`으로 지리 분산 유지) + **밀집도**(촘촘 0.55/보통/넓게 1.4 — 지역 앵커 기준 좌표 오프셋 스케일 `seedFlat`, 이동한 좌표는 `dongAt`로 동 라벨 재판정·경계 밖=원 라벨 유지) ③문서 id=전체 목록 기준 고정 인덱스(gi) — 재채우기 시 같은 문서 덮어씀(**수량 줄일 땐 🧹 비우기 먼저**, confirm에 안내) ④기존 강남 피드 3건 이미지 정리(석촌호수·서울숲 실사진을 해당 지역 아이템으로 이관, 강남 쪽은 생성 이미지 폴백). 검증(node·dong_boundary.geojson 전수): 전 좌표×밀집도 3단(0.55/1/1.4) 동 경계 내 판정 PASS·표기 동 라벨 실제 행정동과 전수 일치(9건 교정)·피드 간 최소거리(강남 85m/잠실 256m/성수 111m)·수량 샘플링(40/27/13)·gi 유일성 PASS. ⚠️라이브 반영 = 관리자로 라이브 접속 → 🧹 비우기 → 🌱 채우기 재실행.
- **v1.55.1 스플래시 앱 아이콘 (M12)**: 첫 화면 📍 이모지 → **앱 아이콘 이미지**(icon-192.jpg 구름 마스코트, `<img.auth-mark>`) — 일반 앱 아이콘처럼 모서리 R컷(68px·radius 15px≈한 변의 22%, object-fit:cover).
- **v1.55.0 모듈 인프라 — 섹션 태깅·버전 검사 CI·페이지 스탬프**: ①app.js 전 섹션 주석에 **[M##] 태그 59곳**(`grep -n "\[M07\]" app.js`로 모듈 위치 즉시 파악 — 크레딧 절약 핵심) ②**`tools/check.js`**: 버전 3곳 동기화+dev/diagram `data-app-ver` 스탬프=앱 버전+app.js 문법 검사, **pages.yml 배포 전 자동 실행**(실패=배포 중단 → dev/diagram 매 push 최신화 강제) ③3개 정적 페이지에 반영 버전 표시(dev/diagram=push마다 갱신 의무, 소개 덱=지연 허용 v1.44.0) ④MODULES.md에 **M00 공용 헬퍼(동결)**·**공유 상태 계약**(전역 소유권 테이블)·**안전 규칙**(시그니처 동결·Firestore additive-only·토큰 값 변경 금지) 문서화 ⑤diagram 블럭 현행화(Ask Map·Request 10분 팝업).
- **v1.54.0 Request 핀·드로어 정돈 + 존 리스트 정렬 + Ask Map (M07+M03+M08+M09 ⚠️교차)**: ①**Request 전용 맵 핀**(ReqPin 오버레이): 말풍선 폐지 → 보라 그라디언트 ? 티어드롭+펄스 링 2겹(현장에 질문 신호를 쏘는 특성, 스팟/피드 핀과 구분) ②드로어 섹션 타이틀 **이모지 제거+타이포 강화**(.ds-tt .92rem/900, 스팟 지역명은 .ds-sub 보조 라벨) ③Request 카드 위치의 📍 제거 ④**Request 삭제**(deleteRequest — 본인·관리자 🗑) ⑤**존 리스트 정렬**: 포커스 존 맨 앞 → ❤ 합산 → 가까운 순(sortedZonesForList), 포커스 카드=액센트 테두리+✓ 뱃지, idle·존 선택 시 재정렬 ⑥**Ask Map 패널**: 타이틀 'Ask Map'(그라디언트), 상단 [🗺 현재 지도 요약하기](aiMapSummary — 존/피드/스팟/Request 실데이터 브리핑), 템플릿 풀 **~50개 중 5개 랜덤**, 하단 **채팅 입력**(aiChatAnswer — 키워드 매칭→템플릿 답변, 미매칭=데모 안내). 트렌드 모드=패널 요소 화염 톤.
- **v1.53.1 현장 Request 라이프사이클 (M07)**: ①**요청자 본인에겐 수신 팝업 안 뜸**(등록 직후 2.6s 자기 시뮬레이션 제거 — 팝업은 실시간 리스너의 타겟 지역 타인에게만) ②**10분 타임아웃**(`REQ_TTL_MS`): 만료 시 지도 마커·타인 드로어에서 숨김+답변 차단(alert), 시드(seed:true)는 데모 연출용 상시 활성, 30s 주기 재렌더로 경과 반영 ③드로어 **'🙋 내 Request' 뱃지**+상태(⏳ 답변 받는 중/⏱ 종료, 만료돼도 내 것은 계속 표시) + **💬 답변 N개 보기** 토글 — 카드 안에서 답변 목록(텍스트·📷사진 썸네일·시간) 확인.
- **v1.53.0 현장 Request 실시간 팝업 개편 + 개발 모듈 체계**: ①Request의 **응답 대기/결과 상시 노출 제거**(드로어 카드 ⏳/💬 상태줄·지도 마커 답변 텍스트 폐지 — 등록 후 일정 시간 답변을 받는 모델이므로) ②**AI Agent 실시간 수신 팝업**: liveRequests onSnapshot docChanges에서 새 Request(3분 내·내 것 제외) 감지 → **타겟 지역**(`reqNearMe`: 보는 위치 1.5km 이내 또는 같은 동) 사용자에게 팝업 — AI Agent 칩+질문+📍위치+**응답 버튼 2개**(💬 답하기/📷 사진 올리기), 하단 네비바와 동일 프로스트 유리 문법(트렌드 모드=AI 칩 화염 그라디언트) ③답변 도착=**요청자에게만** 실시간 ai-bubble 알림(reqAnsSeen 카운트 비교, 첫 스냅샷 제외), 답변자는 '전송' 확인 ④**개발 모듈 체계**: `MODULES.md`(모듈 15종 레지스트리+세션 규칙) + `dev.html`(개발 관리 페이지 — 모듈 카드·상태·앵커·의존·**세션 프롬프트 복사**), 기능 보기에 🛠 개발 관리 링크. **앞으로 세션은 모듈 단위로 작업**(아래 🧱 참고).
- **v1.52.0 UX 7종 — 스플래시 라이트·AI 프리셋·트렌드 불꽃·네비 스와이프·드로어 관리자 탭·Request 응답·피드 칩**: ①피드 존 칩=**Trend Zone 스코프에서만**(그 외엔 fc-region 동 표시) ②**스플래시 서비스 톤**(라이트 프로스트+브랜드 그라디언트 로고+📍마크) + **버전 노출**(#auth-ver, #app-version에서 JS 복사 — 버전 3곳 규칙 유지) ③**AI Agent 프리셋**(#ai-presets 패널): 클릭 시 상황(모드/탭/위치/존/Request 유무/시간대) 맞춤 질문 풀에서 **10개 랜덤**, 선택 시 데모 답변 말풍선 ④**트렌드 모드 AI 버튼=불꽃 톤**: body.mode-trend + .ai-flame(플리커 글로우 애니, 눈/입 다크레드), 그라디언트 팔레트 AI_PALETTE(트렌드 idle 주황→활성 화염 노랑·빨강 — 클릭 색상도 트렌드 전용) ⑤**하단 네비 좌우 스와이프**=지도↔피드↔소셜 전환(수평 40px+, 수직 우세 제외) ⑥**드로어 관리자 탭 분리**(#drawer-tabs 🧭둘러보기/🛠관리자, 관리자만 노출·localStorage 유지): 모바일 실기기 관리자도 데모 메뉴(존/Request/스팟) 접근 가능 — 데스크톱 폰 미러는 기존대로 데모 고정 ⑦**현장 Request 응답 버튼**: 드로어 카드에 💬답하기/📷사진(rqc-btn), 수신 버블에도 📷 사진 올리기 — 사진 답변은 answers[].img(compressNews 압축, 카드 썸네일 표시).
- **v1.51.0 시드 분산·존 시드 제거 + 피드 핀 클러스터 + 롱프레스 이동 + 피드 view 옵션**: ①시드 좌표 분산(피드끼리 최소 85m·전체 69m, 동 라벨=dongAt 전수검증) + **존 시드 폐지**(SEED_ZONES 삭제, 🧹 비우기가 기존 tzs_* 존 삭제는 유지) ②**피드 썸네일 핀 클러스터**(월드픽셀 56px 근접 그룹, 대표사진+개수 뱃지, 탭=fitBounds 펼침, 줌 idle 재클러스터 — z13≈4그룹→z17 전부 낱개) ③**콘텐츠 이동=터치 롱프레스(450ms) 후 드래그**(스팟·피드핀, 롱프레스 전 움직임=지도 팬, 진동 피드백, 마우스는 기존 즉시 드래그) ④**피드탭 view 버튼**(1·2·3 버튼 대체) → 팝오버: 가로 배열 1~3 + 컨텐츠 종류 필터(피드 작성/라이브 카메라/스팟/지면, nowhere_feedtypes). ⚠️라이브 시드 갱신 = 관리자로 접속해 🧹 비우기 → 🌱 채우기 재실행 필요.
- **v1.50.0 시드 확장 — 실사진 + shoomerion 소유**: 피드 16→**20**(칼국수·에스프레소바·전집·벚꽃 추가)·스팟 12→**16**(존 안 4개 추가). 이미지=**Wikimedia Commons 실사진 직링크 19장**(핫링크 허용·영구 보존, 브라우저 검증)+생성 1(벽화) — 존 사진·지면 3장도 실사진. 좋아요 총 ~200개 분산. **소유자=shoomerion@gmail.com**: 콘텐츠에 `byEmail` 저장 + 소유 판정 `ownsContent`(uid ∥ 이메일) — 해당 계정 로그인 시 시드 피드·스팟 수정/드래그/삭제 가능. ⚠️**라이브 반영 = 관리자로 라이브 접속 → 컨텐츠 설정 → 데모 데이터 → 🌱 채우기 클릭**(코드 배포만으로는 데이터 안 들어감).
- **v1.49.0 데모 시드 데이터(강남·역삼·논현)**: 관리자 컨텐츠 설정 › 데모 데이터 — **🌱 채우기**(트렌드 존 3 + 피드 16(설명·좋아요·시간 분산) + 스팟 12 + Request 2 + 채팅 시드, Firestore 공유 컬렉션에 `seed:true` 태깅) / **🧹 시드만 비우기**(직접 만든 컨텐츠 보존). 이미지=일관 스타일 생성 SVG(`seedImg`, ~1KB, 존 카드 편집·피드 관리에서 URL 교체 가능). ⚠️라이브 시딩은 **관리자로 라이브 사이트에서 버튼 클릭** 1회 필요.
- **v1.48.1 드로어 스팟 칩 정돈**: v1.48.0 워드클라우드의 크기 3티어가 `hashStr(id)%3` 랜덤 배정이라 무질서해 보이던 것 → **단일 텍스트 스타일**(2.9cqw·600·--ink) + **필(pill) 버블**(얇은 --line 테두리 + 스팟 색 7% 옅은 채색, hover=액센트)로 일원화.
- **v1.48.0 스팟 선명도·흐림 수정 + 드로어 워드클라우드 + 본인 콘텐츠 수정**: ①줌인 시 저해상도 원인=transform scale(1배 래스터 GPU 확대) → **CSS zoom(레이아웃 스케일)**로 교체(미지원 브라우저는 기존 폴백) ②흐림 미적용 원인=배경 불투명도 0.92(거의 불투명) → **버블 배경 알파 상한 0.82**로 blur(6px) 항상 가시화 ③드로어 스팟 리스트=**현재 지역(동/존)만 워드 클라우드**(무채색 3티어, 색 말풍선 폐지) ④**demo도 본인이 올린 컨텐츠 수정 가능**: 스팟=지도 드래그 이동(모바일 터치 포함)+클릭 편집 모달(텍스트·이모지·색·삭제), 피드=드래그(기존)+카드 ✏️설명 수정·🗑삭제 칩(본인 것만 노출).
- **v1.47.1 스팟 버블 = 그림자 제거 + 배경 흐림**: `.spot-bubble` box-shadow 제거(기본+hover/선택 확대 시 그림자도), 이모지 drop-shadow 제거 → 대신 **backdrop-filter blur(6px)**(반투명 배경 설정일 때 뒤 지도가 살짝 흐려지는 유리 느낌).
- **v1.47.0 피드 컨셉 개편 (속성 모델·Feed 작성·지면 연동·드래그)**: ①+메뉴에 **✍️ Feed 작성**(갤러리 사진+설명글 120자) 추가 — 컨텐츠 속성 `kind`(cam=라이브 카메라/post=피드 작성)·`desc`(설명글)·`name`(만든이)·ts 저장(liveFeed 문서 포함) ②피드 카드 **1:1 통일**(1열 4:5 폐지) + **LIVE 칩**(cam)·존 칩·**올린 시간**(상대 n분 전/시각/숨김 — 관리자 옵션, 클라우드 동기 feedTimeMode) ③**지역 컨텐츠 지면**(지도 탭)=관리자 수동 이미지(유지)+**현 위치 연관 피드 최대 4장 자동 노출**(가까운 순+최신, 스팟 제외, 위치·설명·존/LIVE/♥ 칩) ④**지도 핀 드래그 이동**: 만든이+관리자만(pointer 이벤트, 이동 시 동/존 자동 재태깅) ⑤관리자 피드 컨텐츠 관리 확장: 종류/존/동/설명 편집 + 만든이·시각·♥ 표시. ※+버튼 위치는 기존(좌하단) 유지 — '우하단' 요구는 AI 버튼과 충돌해 보류.
- **v1.46.1 🔥설정 초기화 버그 근본 수정 + 설정 백스톱 파일**: 새 버전 push(리로드)마다 관리자 설정이 초기화되던 원인 = v1.41 에코 가드가 `updatedBy===내 이메일`이라 **관리자가 새로 접속해도 클라우드 설정을 영영 안 읽고**, 이후 아무 편집 시 cloudSave가 코드 기본값 설정으로 클라우드를 덮어쓰던 것. → 에코 판별을 **세션 ID(`SESSION_SID`/`updatedSid`)**로 교체(새 접속·다른 기기=적용, 같은 세션 에코만 무시). 백스톱: `settings-default.json`(repo, 코드기본값<파일<클라우드 순) + 관리자 설정의 **📋 설정 JSON 복사** 버튼으로 언제든 백업 가능.
- **v1.46.0 피드↔지도 연결 4종**: ①피드 가로 배열 **최대 3칸**(버튼/설정/핀치 모두 1~3 클램프) ②피드 카드에 **트렌드존 칩**(존 태그 우선, 없으면 좌표 ptInZone 판정 — 존 색상 배경 `.fc-zonechip`, 좌상단) ③**피드 사진=지도 위 원형 썸네일 핀**(`FeedThumb` 오버레이, Apple Maps 무드: 흰 링+옅은 섀도·글로우 없음, 비인터랙티브 pane, 줌아웃 시 44→30→18px 축소, 메인+폰 동시, 최근 30장) — 사진 업로드/링크/카메라 시 **좌표(lat/lng) 저장**(liveFeed 문서 포함), 구버전 사진은 존 중심→동 중심 폴백(`feedItemLatLng`) ④**피드 카드 싱글탭=지도 탭 전환+해당 위치로 팬**(줌<15→16, 폰 인셋 보정, 스팟 항목은 focusSpot) — 더블탭 좋아요와 공존(360ms 탭 타이머).
- **v1.45.0 소셜 채팅 실시간 공유(liveChat)** ⚠️**Firestore 규칙 배포 필요**: 마지막 남은 미공유 요소였던 소셜 채팅 메시지를 계정 간 실시간 공유로 전환 → **이로써 전 콘텐츠 실시간 공유 완료**(관리자 콘텐츠 + 피드/좋아요/스팟/Request/채팅. 피드 열수·접기 등 UI 개인설정은 의도적으로 기기별 유지). firestore.rules에 `liveChat`(signedIn read+write) 추가 — **콘솔 배포 필수**(미배포 시 채팅 쓰기 permission-denied→liveWriteErr 안내. v1.42의 liveFeed/liveSpots/liveRequests 배포 여부도 함께 확인).
- **v1.44.0 소개 덱 + 블럭 다이어그램 페이지**: ①deck.html — 8슬라이드 가로 스냅 덱(컨셉/두 렌즈/포커스 렌즈/컨텐츠 5종/탭 3종/실시간 아키텍처/링크), 키보드 화살표+도트 내비, 앱 디자인 토큰 톤 ②diagram.html — SVG 기능 블럭 다이어그램(코어→탭→컨텐츠→데이터 4계층, 실선=사용 흐름/보라 점선=실시간 데이터/빨강=좋아요→존 랭킹 파이프라인, 범례+상호 링크) ③기능 보기 상단에 [📽 소개 덱][🧩 블럭 다이어그램] 링크(.ft-docs, 새 탭). 두 페이지 모두 독립 정적 파일(인증 불필요·공유 가능).
- **v1.43.0 하트 개선 + 기능 점검/정리 + 기능보기 갱신**: ①하트 애니 1.15s 자연화(팝→반동→머무름→위로 떠오르며 페이드) ②**전체 기능 리뷰 결과 반영**: 죽은 코드 제거(persistSpotChange), 중복 통일(inZoneAt→ptInZone, 존 중심 인라인 2곳→zoneCentroid), **모드 간 끊긴 고리 2건 수정** — 트렌드 위치명 존 밖=동 이름 폴백 / 피드 '현재 동네' 필터가 트렌드 모드에서 존 이름↔동 비교로 항상 비던 버그→존 기준(태그 우선+좌표 ptInZone) 매칭 ③**기능보기 전면 갱신**(13종): 모드 카드에 동↔존 렌즈 관계·폴백 명시, ❤️좋아요 카드 신설(존 하트합산·썸네일 연결), 실시간 공유 상태 반영(spot/cam/req/feed=live, social=demo·메시지 실시간 예정). 검증: 정리 4건·트렌드 로컬 필터·애니 1.15s·13카드 PASS.
- **v1.42.0 유저 생성 콘텐츠 실시간 공유(피드·좋아요·스팟·Request)** ⚠️**Firestore 규칙 배포 필요**: firestore.rules에 liveFeed/liveSpots/liveRequests(signedIn read+write) 추가 → **콘솔 배포 필수**(안 하면 쓰기 permission-denied, liveWriteErr 안내). ①**피드**: feedItems=liveFeed 컬렉션 실시간(onSnapshot orderBy ts desc limit48), 추가/수정/삭제 라우팅(feedAdd/feedUpdate/feedDelete), **좋아요=문서 likes 맵(uid→true)** 계정당 1개(toggleLike 낙관적+FieldValue.delete), rebuildLikes로 feedLikes 재구성, zoneTotalHearts 그대로 동작 ②**스팟**: adminSpots(mapContent)+demoSpots(liveSpots 실시간) 분리, rebuildSpots 병합, 데모 생성/삭제/편집=liveSpots 문서(persistSpotEdit), 관리자=mapContent 유지, cloudSave는 adminSpots ③**Request**: liveRequests 실시간, 생성=문서 set, 답변=arrayUnion. 모두 hasLive() 가드로 미인증/오프라인 시 localStorage 폴백. 검증: 로컬 폴백 경로(피드 추가·좋아요 토글·수정·삭제·스팟 분리·request 가드) PASS, 콘솔 에러 0. (소셜 채팅은 이번 범위 제외)
- **v1.41.0 실시간 동기화(관리자 공유 콘텐츠)**: shared/mapContent·shared/news 를 .get() 1회 읽기 → **.onSnapshot() 실시간 리스너**로 전환. 관리자가 올린 존/스팟/설정/소식/채팅방/피드(클라우드 저장분)/카드스타일이 접속 중인 모든 사용자에게 **즉시 반영**(새로고침 불필요). 자기 저장 에코 가드(metadata.hasPendingWrites + updatedBy===내이메일 스킵 → 편집 중 방해 없음), 로그아웃 시 리스너 해제(detachLiveListeners). ※데모 유저 본인 생성 콘텐츠(피드/좋아요/스팟/채팅/Request)는 아직 기기 localStorage → 계정 간 공유엔 Firestore 규칙 추가(signedIn write) 필요, 별도 결정 대기.
- **v1.40.0 피드 컨텐츠 추가 UX + 트렌드존 속성**: ①피드 컨텐츠 섹션 재구성 — **＋ 사진 추가(업로드)** 전체폭 버튼(feed-add-btn/feed-file, 여러 장 compressNews 압축) + 링크 입력 상단 배치, 그리드/간격 옵션은 아래로 ②**(버그) 트렌드존 select가 renderFeedColList에서 생성만 되고 row에 append 안 되던 문제 수정** — ni-fields에 존 select+동 입력 2단 배치 ③**존 선택 시 속한 동 자동 채움**(zoneRegionName: 존 중심 zoneCentroid→dongAt). 촬영/링크/업로드 모두 현 위치 존 자동 태깅 유지. 검증: 추가버튼/파일input·존 select DOM 노출·존→동 자동채움(강남→역삼1동) PASS, 콘솔 에러 0.
- **v1.39.1 리스트 카드 하트/거리 하단 정렬**: 트렌드존 리스트 스타일에서 이름·카테고리 길이가 달라도 ❤ 합산·거리 행이 어긋나지 않게 → .tz-scroll-list align-items:stretch(카드 높이 통일) + .tzl-meta margin-top:auto(하단 밀착). 검증: 짧은/긴 카테고리 카드 높이 동일(159px)·메타 하단 정렬 PASS.
- **v1.39.0 존 카드 리스트 스타일 + 트렌드 요약 존 리스트**: ①**존 카드 스타일 옵션**(트렌드 존 관리 › 목록 카드: 글래스 캡션 / 리스트, nowhere_zonecard+클라우드 zoneCardStyle 동기): 리스트 스타일=썸네일+이름+카테고리(설명)+**하트 합산**(별점 대체)+**거리/Here**. 하트=존 태깅 사진 + 존에 속한 동 컨텐츠(regionCenterByName→ptInZone) 좋아요 총합(zoneTotalHearts). 거리=현재 지도 센터 기준 직선거리(haversineM), 존 안이면 'Here'(zoneDistLabel, 지도 idle마다 갱신). 공용 빌더 makeZoneCard/buildZoneScroll(드로어·요약 공유) ②**트렌드 모드 지도 탭 요약 공간=존 리스트**(#cp-zones, 세로폭 44cqw 유지·비율만 조정, 캐러셀/접기버튼 숨김, switchMode/switchTab/renderNews 연동). 버그: #cp-zones display:none 기본값에 inline '' 폴백 → 'block' 명시. 검증: 하트합산(z1=8)·리스트/글래스 렌더·요약 165px·모드 토글 PASS, 콘솔 에러 0.
- **v1.38.3 존 편집 UI 수정 + 카드 글래스화**: ①**(버그)** 트렌드존 인라인 편집 폼이 flex 부모(.zone-item) 자식으로 끼어 찌그러지던 문제 → .zone-item flex-wrap + .zone-inline-edit flex:0 0 100%(전체폭), .zi-url flex 스타일 누락 보완(링크 버튼 한 줄 정렬) ②드로어 **트렌드존 카드=글래스 캡션 스타일**(요약 카드 V3 톤): 이미지 풀채움(42×50cqw)+하단 그라디언트+프로스트 유리 캡션 바(볼드 이름+얇은 설명), 사진 없으면 존 컬러 ⬡ 플레이스홀더, 가로 스크롤 유지. 검증: 편집폼 전체폭·URL 한줄·카드 캡션 3장 렌더 PASS, 콘솔 에러 0.
- **v1.38.2 트렌드존 카드 편집 발견성 개선**: 존 사진(업로드/링크)·설명 편집이 '이름 더블클릭'으로만 숨어 있던 문제 → 존 목록에 **🖼️ 카드 편집 버튼** 추가(관리자 설정 아님, 🗂 컨텐츠 설정 → 트렌드 존 관리 → 존 항목). 버튼 구분 명확화: 📍이동 / 🖼️카드 편집(사진·설명·이름·색) / ✏️영역 편집(헥사곤 범위) / 🗑️삭제. 더블클릭 진입도 유지. 검증: 버튼 4종·🖼️→인라인 편집기(설명/사진/링크) 열림 PASS.
- **v1.38.1 드로어 정리 + 존 포커스 버그**: ①**(버그)** 사이드바에서 단일 트렌드존 선택 시 3존 전체뷰로 덮이던 문제 수정 — switchMode(mode,{noNearby}) 옵션 추가, 존 카드는 noNearby:true로 focusNearbyZones(80ms) 억제 → selectPhoneZone의 단일 존 fitBounds 유지 ②기능 보기를 드로어 **헤더(닫기 옆)**로 이동(.pdh-feature, 폰/PC 공통), body 내 feature-btn 제거 ③닫기 버튼 ✕→**'닫기' 텍스트** ④드로어 3섹션(트렌드존/현장Request/스팟)을 **카드 블록으로 구분**(흰 배경+보더+그림자) + **상시 표시**(Request도 항상, 없으면 '등록된 ~가 없어요' 안내). 검증: 닫기텍스트/헤더기능버튼/3블록/빈안내/switchMode 가드 PASS, 콘솔 에러 0.
- **v1.38.0 피드 존 태깅 + 좋아요**: ①피드 컨텐츠에 **트렌드존 할당**(zone 필드): 촬영/링크 시 현 위치 존 자동 태깅 + 피드 컨텐츠 관리에 존 셀렉트(존 없음/존 목록), 존 범위 필터는 태그 우선 매칭 ②**더블탭 좋아요**(기기당 1개 토글, nowhere_likes): ♥n 뱃지(사진=글래스 다크/텍스트카드=핑크), 하트 팝 애니, 토글 후 탭 타이머 리셋으로 연타 오작동 방지 ③**존 썸네일 = 태깅 사진 중 최다 좋아요**(zoneBestPhoto, 없으면 존 photo 폴백) — 좋아요 변경 시 드로어 즉시 갱신 ④소셜 서브탭=피드 범위 필과 동일 pill 스타일 + 아이콘 제거(동네 채팅방/주제방/프라이빗). 검증: 좋아요 토글/연타/뱃지/베스트 썸네일/필 탭 PASS.
- **v1.37.0 드로어 카드화 + 존 속성**: ①**트렌드존 속성 photo/desc**(로컬·클라우드 zones 필드, 인라인 편집기 확장: 설명 입력 + 📷 업로드(compressNews 압축)/링크(https 프로브) + 썸네일 미리보기, updateZone desc 지원) ②**드로어 재구성**(renderDrawerDemo 재작성, 순서: 트렌드존→현장 Request→스팟): 트렌드존=말풍선(볼드 이름+thin 설명, 꼬리)+썸네일 카드 **가로 스크롤**(레퍼런스 무드, 사진 없으면 존 컬러 ⬡ 플레이스홀더), 탭→맵 이동+포커스 / **현장 Request 카드**(있을 때만, 가로 스크롤: 장소·질문·첫 응답/대기 상태, 탭→해당 위치 팬) / 스팟=**컬러 말풍선 세로 리스트**(비대칭 코너, 탭→포커스) ③**섹션 접이식**(ds-head 볼드+셰브론, nowhere_drawerfold 유지) + 메인 UI 타이포 톤 통일. Request 변경 시 드로어 자동 갱신. 검증: 3섹션 순서/카드 수/접기/편집기 필드·썸네일 PASS.
- **v1.36.1 데모 미리보기 + 설치 유도**: ①**데스크톱 관리자의 폰 미러=데모 로그인 기준** — 폰 드로어는 데모 목록(존/스팟)+기능 보기만, 관리자 설정 숨김(min-width:769px 스코프 CSS — 모바일 실기기 관리자는 기존대로), 스팟 admin 커서 메인 지도 한정, 폰 프로필 라벨 '뷰어 (데모 미리보기)' ②**웹앱 설치 유도 배너**(initInstallPrompt, 모바일 브라우저 한정·standalone 제외·닫기 시 localStorage로 재노출 차단): Android=beforeinstallprompt 가로채 [설치] 네이티브 프롬프트, iOS=공유→홈 화면에 추가 안내(1.8초 후), 프로스트 카드(#a2hs-bar). 검증: 폰/PC 드로어 역할 분리·iOS 배너 렌더 PASS.
- **v1.36.0 설정 통합 + 전환 존 N개**: ①베이직→트렌드 전환=근접 존 N개 fitBounds(기본 3, 관리자 '전환 존 개수' 1~6, lens.switchZoomN 클라우드 동기, setTimeout 80ms로 전환 마무리 후 실행, 데스크톱=메인만/모바일=폰 직접으로 미러 충돌 제거) ②**관리자 설정 12→6블록**: 동 구역 스타일(비선택+선택+스무딩+선택 라벨, row-cap 소구분)·트렌드 존 스타일(헥사 기본/선택+반경+병합+존 라벨) 통합 — BLOCK_DEFS/MINI_RENDER 복합 블록(프리뷰 공유: region=스무딩 반영 블롭+라벨칩, trendzone=기본/선택 헥사+실병합 아웃라인+라벨칩+반경) ③접기 셰브론 **자동 대비색**(이미지 우상단 8×8 캔버스 밝기 샘플링, CORS 실패=흰색, 접힘/빈지면=검정, 캐시) ④소셜 서브탭 재디자인: 언더라인 탭+구분선(채팅 버블과 명확 분리) ⑤'기능 관리'→'기능 보기' 개명 + FEATURES 설명 현행화(전환 N뷰·핀 고정·피드 범위·소셜 관리자 개설 등). 검증: 6섹션/4타일/블록키/병합 렌더/톤 기본 PASS.
- **v1.35.2 피드 간격 옵션 + 최근접 존 포커스**: ①피드 사진 간격 옵션(없음/최소/좁게/보통, 기본 1.2cqw, 피드 컨텐츠 설정, nowhere_feedgap) ②베이직→트렌드 전환=**가장 가까운 존 하나만**(내 위치 등 다른 요소 제외) 존 스팬 0.8배 사방 확장으로 화면 ~1/3 크기 컨텍스트 뷰(`focusNearestZone`) ③접기 셰브론 글로우(drop-shadow) 제거 ④**지역 선택=맵 보기 전환+포커스** — selectPhoneZone/selectPhoneDong에 탭 전환 훅(피드/소셜에서 드로어로 존 선택해도 맵 탭 복귀). ※사용자 요청 중 '처음 시작화면에서 이전에 만들고 업데이트'는 문장 미완성으로 보류(확인 요청함).
- **v1.35.1 지도=본문 전용 + 간격 안정화**: ①**지도를 본문(헤더 아래)에만 배치**(`layoutPhoneMap`: #phone-map top=헤더 높이, 변경 시 resize+센터 보존) — 헤더/요약 뒤 지도 제거로 트렌드 전환 시 근접 존 3~5개 fitBounds가 실제 보이는 영역 기준으로 정확해짐. `phoneMapInsets` 재정의(top=지도 위 모드 필 오버행만+6, bottom=네비) → 가시중심/fit패딩/스팟포커스/뷰포트 오버레이 전부 새 기준으로 일관(더블체크 완료) ②**소셜 입력바 간격 단일화**: 페이지 padding(네비+12px)만 기준, 입력바 자체 env(safe-area) 이중가산 제거 → 기기 무관 네비로부터 정확히 12px(검증값 12) ③**cqw 높이 트랜지션 고착 버그 수정**: #cp-frame 상시 transition:height 제거 → 접기 토글 순간에만 .anim 부여(뷰포트/컨테이너 변화 시 이전 px 스냅샷에 카드가 660px로 고착되던 브라우저 이슈 재현·해결). 검증: 헤더 247/지도 top 일치/인셋(46·61)/입력바 갭 12 PASS.
- **v1.35.0 카드 레퍼런스 반영 + 피드 범위**: ①**카드 3버전 재설계**(첨부 레퍼런스): V1=인셋 라운드 이미지+화이트 바디, V2=풀블리드 이미지가 화이트로 마스크 페이드, V3=풀이미지+하단 다크 오버레이(흰 텍스트). 제목→위치 순서(전 버전) ②**접기 반응성**: 피드/소셜 페이지가 카드 높이 트랜지션을 rAF 프레임 단위로 추적(늦은 점프 제거) ③접기 아이콘=배경 없는 셰브론(흰 드롭섀도 헤일로), 접힘 시 카드 세로 중앙 정렬 ④피드 LIVE 뱃지 제거 ⑤요약 위치 📍 아이콘 제거 ⑥**피드 보기 범위**(좌측 필터: 전체보기/현재 동네/Trend Zone, nowhere_feedscope): 전체=포커스 위치 기준 **거리순+최신순**(항목 좌표: 스팟=실좌표, 사진·지면=동 중심 `regionCenterByName`) + **무한 스크롤**(12개 청크, 바닥 -160px 트리거), 현재 동네=포커스 구역만(기존), Trend Zone=근처 존 5개 내 컨텐츠(`inZoneAt`). 검증: 스코프 3버튼·LIVE 제거·아이콘 제거·무한로딩(12→21)·V1/접힘 스크린샷 PASS.
- **v1.34.0 위치 마커 + 요약 카드 리디자인**: ①**현재 위치 마커** — 브랜드 블루 점(흰 링 2.5)+옅은 헤일로(15%), 메인·폰 동시(`renderMyLocation`, geolocation 성공 시) ②**요약 카드** — 아이템에 `title`(카드 제목) 추가, 위치=region 재사용, **카드 스타일 3버전**(전역 select, nowhere_newsver+클라우드 cardVer): V1 풀이미지+하단 그라디언트/흰 타이틀, V2 분할(좌 이미지 42%+우 텍스트/파란 위치 pill), V3 글라스 캡션 바. 관리 목록 2단 필드(탭+위치 / 카드 제목), 클라우드 items에 title 포함 ③**접기 리디자인** — 촌스러운 화살표 폐기 → SVG 셰브론 원형 글래스(회전 애니), 접으면 **컴팩트 카드**(높이 44→15cqw 트랜지션, 썸네일 11cqw 좌측+제목 1줄+위치, 도트 숨김), 스와이프 유지, 높이 전환 후 렌즈/축척/레이아웃 재계산. 검증: cv1/cv2 렌더·접힘/회전·복원 스크린샷 PASS, 콘솔 에러 0.
- **v1.33.0 탭 UX 마감 8종**: ①**베이직 동 탭=존과 동일 UX** — phoneMap.data 클릭→`selectPhoneDong`(bbox fitBounds+렌즈 핀 고정, 재탭/빈곳 탭 해제, `phoneSelectedDongKey`) ②피드/소셜 탭에서 모드 토글 숨김 ③**피드 보기 컨트롤**: 본문 우측 상단 컴팩트(1·2·3·4) + **핀치 줌으로 열 변경**(벌리면 열 감소, touch-action:pan-y) ④**피드=포커스 지역 컨텐츠만**(focusedRegionName 매칭, 지역별 빈 상태 문구) + 피드 컨텐츠에 **링크 업로드**(https 검증+프로브) ⑤**요약 공간 접기**(#sum-collapse 글래스 버튼, nowhere_sumfold 유지, 접힘 시 레이아웃/렌즈/축척 재계산) ⑥**시작 위치=내 위치**(geolocation 줌15, 실패/미지원 시 서울시 전역 줌11, 폰은 미러 동기) ⑦**베이직→트렌드 전환 시 가까운 존 3~5개 fitBounds**(`focusNearbyZones`: 존 중심 거리순+내 위치 포함) ⑧관리자 메뉴: 컨텐츠(파랑)/관리자(보라) 좌측 컬러바 구분 + **최상위 토글 배타**(하나 열면 다른 쪽 닫힘) + 컨텐츠-스팟 메시지에 **미리보기 타일**(data-prev=spot-view, MINI_RENDER 재사용, 블록바 없음—BLOCK_DEFS 가드) + jumpToSetting 셀렉터를 설정 그룹으로 한정. 검증 9항목 PASS(배타 토글은 수동 바인딩으로 확인), 콘솔 에러 0.
- **v1.32.0 탭 UX 통일 + 컨텐츠 관리 확장**: ①**요약 공간 3탭 공용화** — 별도 칩 패널 제거, 캐러셀(cp-frame) 하나를 지면 이미지의 `tab` 속성(지도/피드/소셜)으로 필터해 공용(`newsView`), 플레이스홀더도 탭별 문구 → 탭 높이 동일=전환 완성도+지도 비침 근본 해결(+탭페이지 top-2px 오버랩) ②**지면 관리 이동/확장** — '동네소식'→관리자 설정 '요약 공간 지면(탭별 이미지)', 이미지별 탭 셀렉트(지도/피드/소셜) 추가, 클라우드에 tab 필드 포함. 피드 컨텐츠와 별개(관리자 UI 목업용) ③**피드 그리드 열 1~4** — 피드 상단 보기(1·2·3·4) + 설정-컨텐츠 '피드 컨텐츠'(그리드 열 + 라이브 사진 목록/지역/삭제), 1열=4:5 인스타 비율, nowhere_feedcols ④**소셜 컨텐츠 관리** — 방=관리자 개설(설정: 이름+타입 주제방/프라이빗 추가·수정·삭제, `socRoomList`), 유저측 크레딧 개설 버튼 제거, **클라우드 동기화**(shared/mapContent.social=rooms+seedLocal) ⑤**동네 채팅** — 서브탭 라벨='현 위치명+채팅방'(베이직=동/트렌드=존), **JSON/CSV 시드 채우기**(`parseChatSeed`: [{who,t}] 또는 "닉,내용" 줄) + 비우기 ⑥**트렌드 존 선택=렌즈 핀 고정** — 별도 스포트라이트 폴리곤 제거, 선택 시 존 렌즈를 축척 무관 유지+fitBounds(줌아웃 선택 시 포커스 미동작 버그 해결), 해제 시 자동 렌즈 복귀 ⑦**모드 토글↔위치 텍스트 스왑** — 앱바 중앙=현 위치명, 헤더 아래 프로스트 필=모드 토글(전 탭 노출, z해결+탭페이지 동적 상단여백). 검증 13항목+겹침 검사 PASS, 콘솔 에러 0.
- **v1.31.0 서비스 탭 완성 + 기능 맵 (하이퍼로컬 SNS 데모 풀셋)**: ①**피드 탭**(`switchTab`/`renderFeed`) — 라이브 사진+스팟+동네소식 그리드, **포커스 구역(렌즈 동/선택·렌즈 존) 우선 정렬**(`focusedRegionName`), LIVE 뱃지·지역 태그 ②**소셜 탭** — 소탭 3종(📍동네 채팅=포커스 구역 단체방 / #주제방 / 🔒프라이빗=크레딧 3개로 개설), 채팅 UI+시드+데모 응답, localStorage(nowhere_chat) ③**라이브 카메라**(구 사진 올리기) — 촬영→압축(재사용 compressNews)→피드 첫 카드+위치 태그, capture=environment ④**현장 Request** — 롱프레스/+메뉴→질문 작성→지도에 ❓라벨, 2.6초 후 **현장 유저 수신 버블**(우하단 글래스, 장소+질문+퀵응답 3종+직접답장)→응답 시 요청자에게 AI버블로 답 도착, nowhere_requests ⑤**요약 공간 탭별 전환** — 지도=동네소식 캐러셀 / 피드=추천 칩 / 소셜=핫토픽 칩 ⑥**🧩 기능 맵**(드로어 최상단, 컨텐츠 설정 앞, 데모도 열람) — FEATURES 12종 그룹 카드(구현/데모/예정 뱃지 + 설명 + **관계 칩 ↔ 클릭 점프** + 관리자 **⚙설정**=해당 설정 블록 자동 펼침·스크롤·플래시 `jumpToSetting`). 탭 페이지는 헤더/네비 사이 오버레이(`layoutTabPages`). 검증: 피드 카드/추천칩·소셜 채팅/핫토픽·Request 응답 왕복·기능맵 12카드/25칩/관리자 3버튼 전부 PASS, 콘솔 에러 0.
- **v1.30.0 설정 = 블록 단위 드래프트/적용**: 옵션 변경이 실제 지도에 즉시 반영되지 않음 — 모든 설정 컨트롤이 **DRAFT 버퍼**(`initDraft`, 라이브 딥카피)를 편집하고 미니 프리뷰(`MINI_RENDER`)·입력 동기화(`syncSettingsUI`)는 DRAFT를 읽음. **각 옵션 블록 하단에 [기본값][취소][적용] 바**(`initBlockBars`): 적용=해당 블록 필드만 라이브 복사+리프레시+cloudSave / 취소=마지막 적용값(라이브→드래프트) / 기본값=공장값을 드래프트에 스테이징(적용 눌러야 반영). 블록 엔진 `BLOCK_DEFS` 10종(필드 스코프 분리 — highlight의 spotScaleM은 렌즈 블록 소속), 스칼라 블록(스무딩/헥사반경/존병합)은 전용 apply(변경 시에만 applyGeoJsonToMap/rezone/rerender). 상단 요약 바=전체 적용/취소/기본값(dirty 블록 일괄). cloudSave는 항상 라이브(=적용본) 저장 → 드래프트 유출 원천 차단(구 savedSettings 로직 제거). 이모지 목록은 콘텐츠 성격이라 라이브+자동저장 유지(드래프트 미러). 검증: 10항목(드래프트 격리·적용·취소·기본값·스칼라) 브라우저 테스트 PASS, 콘솔 에러 0.
- **v1.29.1 미니 프리뷰 개선**: ①타일 높이 64→128px(2배) ②설정 아코디언 **배타화** — 그룹(컨텐츠/관리자)당 항상 1개만 펼침, 열면 나머지 자동 접힘 + scrollIntoView, 컨텐츠 그룹도 첫 블록만 기본 펼침 ③타일 배경=**자체 SVG 동네 지도**(100~200m 축척 느낌: 공원·건물 블록·도로 케이싱, `mpMapBg()`) — 외부 이미지/Static Maps 과금 없이. DOM 타일(스팟/라벨)에도 `.mp-bg` 절대배치로 동일 배경. 검증: 배타 동작(항상 1개)·타일 128px·배경 렌더 PASS.
- **v1.29.0 설정 인라인 미니 프리뷰**: 관리자 설정 각 블록 상단에 그 옵션만 보여주는 64px 예시 타일(`.mini-prev`, `data-prev` 10종: 스팟 스타일(실제 버블 DOM)/포커스 렌즈(포그+헤어라인+전환ms)/비선택·선택 구역/경계 스무딩(원본 점선 vs `chaikinSmooth` 결과)/선택 라벨/헥사 그리드(+반경km)/선택 헥사/존 표시(**실제 `zoneOutlineLoops`로 병합 아웃라인**)/존 라벨). 값 변경 시 `markStyleDirty`→`renderMiniPreviews()` 실시간 갱신, 적용/취소/기본값 시 `syncSettingsUI` 경유 갱신. 지도 없이도 렌더(순수 SVG/DOM). 병합 타일 인접 좌표를 격자 공식으로 정확 계산(반올림 좌표는 꼭짓점 불일치로 루프 분리됐음 → 12꼭짓점 단일 루프 검증).
- **v1.28.0 렌즈 버그픽스 + 트렌드 렌즈 + 설정 드래프트**: ①**포커스 동에 포그 덮이던 버그 수정** — 행정동 GeoJSON 링이 CW(스펙 위반)라 구멍이 안 뚫렸음 → `holeRing()` 감김 정규화(shoelace 부호 검사, CW면 반전). 탭 스포트라이트 구멍에도 적용 ②**트렌드 렌즈**: 줌아웃=채움+아웃라인+라벨 / 줌인(축척≤trendScaleM, 기본 300m)=존 채움·라벨 페이드아웃 + 주변 포그 + 아웃라인만(`lensBuildZone`, 존 오버레이와 크로스페이드 `lensApply`의 zoneRef). 베이직/트렌드 공용 엔진(`updatePhoneLens` 디스패처), 탭 스포트라이트가 자동 렌즈보다 우선 ③**설정 드래프트 시스템**: 스타일 계열 변경(markStyleDirty 14곳)=폰 미러 라이브 프리뷰(내 화면만) → 관리자 설정 상단 **적용바**(`#settings-apply-bar` sticky: 미리보기 중 표시 + [전체 적용]=cloudSave / [취소]=마지막 적용본 복원 / [기본값]=코드 기본치 프리뷰). 콘텐츠(존/스팟/소식) 자동저장은 유지하되 저장 시 설정은 '마지막 적용본' 사용 → 드래프트 유출 없음(`snapshotSettings`/`applySettingsSnapshot`/`FACTORY_SETTINGS`) ④렌즈 수치 관리자화: 베이직 발동(m)/트렌드 발동(m)/전환 속도(ms)/주변 안개/구역 테두리. 프리뷰 검증: 감김 정규화·드래프트 플로우(dirty→취소 복원) PASS, 콘솔 에러 0.
- **v1.27.0 포커스 렌즈 (구역 단위 몰입 시스템)**: 베이직 폰의 축척 강조를 스타일 교체 → **마스크 렌즈**로 재구축(`phoneLens`, 트렌드 스포트라이트와 동일 문법). ①주변=**화이트 포그** 1장 + 현재 동 브랜드블루 헤어라인(1.6px) — 컨텐츠 몰입 유지 ②**부드러운 전환**: 페이드 250ms(rAF, easeOut)+히스테리시스(켜짐≤thr·꺼짐≥1.3thr)+렌즈 이동 시 딥&페이드 ③수동(자동 렌즈)=옅게 / 능동(존 탭)=진하게(+0.22) 위계, 존 스포트라이트도 화이트 포그로 통일 ④**스팟 포커스 연동**: 렌즈/선택존 밖 스팟 42% 투명(.spot-out, 폰만) ⑤**동네소식 연동**: 보는 동 변경 시 region 태그 일치 이미지로 캐러셀 자동 슬라이드(숫자 무시 정규화 '논현동'≈'논현1동', 스와이프 중 방해 금지). 설정 UI: '강조 구역'→'포커스 렌즈'(발동 축척/주변 안개/구역 테두리, `styleConfig.lens` — 구 dim은 클라우드에서 무시). 단위테스트: 히스테리시스·소식 매칭 PASS.
- **v1.26.1 글래스 질감 고도화(핀터레스트풍 세련화)**: 프로스트 불투명도 0.86→0.6/0.78(지도가 실제로 비침) + `--glass-blur`(26px·채도1.8)/`--glass-line`(밝은 헤어라인)/`--glass-hi`(유리 상단 광택 inset) 토큰. **폰 헤더=프로스트 글래스**(Apple Maps 문법), 하단네비/＋/AI·팝업·프로필메뉴·드로어(블러34px)·모달·컬러피커·컴포저·축척범례 전부 진짜 유리로. 활성칩=동일색 미묘 그라디언트+컬러 섀도+상단광택, 모드칩 동일 처리, 캐러셀 플레이스홀더 그라디언트+📰, 프로필 아바타 화이트 링. 지도 패턴 시뮬 배경으로 유리 비침 프리뷰 검증 완료.
- **v1.26.0 UI/UX 통일 (디자인 시스템 · style.css 토큰 기반 재작성)**: ① `:root` 디자인 토큰 도입(--acc 브랜드블루 #2f7bff 단일 액센트, --hot 트렌드 그라디언트, --ink 텍스트 3단 위계, --line/--frost/--sh-* 라인·프로스트·그림자 스케일, --nav-* 네비 3색) ② **라이트 프로스트 통일**: 다크였던 하단네비 칩·AI버튼·+버튼·컨텐츠추가팝업·AI말풍선·폰/PC 드로어·스팟 컴포저·모달·컬러피커·축척범례·로딩 pill → 전부 화이트/프로스트 화이트(흰 헤더와 한 몸, 네이버·카카오·당근 문법) ③ 다크 드로어 안 라이트 설정패널 이질감 해소(베이스 컴포넌트 자체를 라이트화, `#left-panel` 오버라이드 블록 60줄 삭제) ④ 사이드바·바디 배경 라이트(#eef1f6) ⑤ 액센트 #6ec6ff/#2563eb/#1d4ed8 난립 → --acc 단일화, iOS식 토글(체크=파랑 채움) ⑥ 죽은 CSS 제거(.pm-ic/pmFlame, .zone-io-row, #spot-form, #drawer-account 등). 인증 스플래시는 브랜드 다크 유지. 프리뷰 검증(모바일/데스크톱/드로어/팝업) 완료.
- **v1.25.0 코드 최적화(리팩터, 동작 동일)**: ① 헥사 그리드 생성 시 존 점유 판정 O(N×M)→O(1) 키맵(`occupiedHexKeys`, 신/구 동치성 25,205케이스 노드 검증 ✅) ② 존 재매핑 로직 2벌→`remapZoneToGrid()` 통합 ③ 헥사 스타일 4벌→`hexOpts()` ④ 이모지 추가 프롬프트 3벌→`promptAddEmoji()` ⑤ 축척 렌더 2벌→`renderScale()` ⑥ `escHtml` DOM 없이 정규식 + 따옴표 이스케이프(존 이름 `"` 시 인라인 편집 input 깨지던 버그 수정). app.js 순 -33줄.
- **스팟 생성 위치 버그 수정**: 벡터 지도(mapId)에서 컴포저 팝업이 실제 생성점과 어긋나던 문제 → 생성점에 **펄스 점** + 그 위에 입력 팝업. 이모지 **꾹 눌러 삭제**.
- **폰 햄버거 메뉴**: 데모도 **트렌드 존/스팟 메시지 리스트**(탭→포커스/강조), 관리자는 **관리자 설정 메뉴**가 드로어에. ⚠️ 관리자 설정이 사이드바→**폰 햄버거 드로어로 이동**(데스크톱도 햄버거로 접근).
- **앱 아이콘/파비콘 = 마스코트 이미지**(구름 캐릭터). 페이지 타이틀 'Now Here Demo'. **UI 약한 글래스**(설정패널·폰 헤더 반투명+블러).
- **스팟 롤오버/선택 시 살짝 커지며 강조**(테두리 대신 스케일). **스팟 추가 = 화면 롱프레스/우클릭 → 좌측하단 컨텐츠 추가 팝업**, 또는 좌측하단 **+버튼** → 현재 보는 지도 센터에 추가(클릭 배치 폐지).
- **스팟**: 점 색상 = 버블 색상 자동(개별 색 포함). **데모(뷰어)도 스팟 추가 가능**(이 기기 localStorage 저장). **스팟 목록 관리**(컨텐츠 설정 > 스팟 메시지: 이동/삭제). 버블 좌우 여백 축소.
- **폰 상단바 3분할**: 햄버거(좌)·**모드토글(가운데)**·**위치(우)**. 하단 네비 살짝 위로. 스팟 외 모든 수치 설정 범위 시스템 최대로 확대.
- **PWA(홈 화면에 추가 → 전체화면 앱)**: manifest+아이콘+메타태그. 폰에서 브라우저 크롬 없이 standalone 실행, 노치/홈인디케이터 안전영역 처리.
- **설정 = 숫자 직접입력 전용**(슬라이더 제거) + 정수/소수·범위 주의문구. **설정 패널 Light 테마**. 스팟은 **지도에 고정된 실제 크기**(줌해도 같은 미터 범위 유지). 스팟은 **베이직·트렌드 양쪽 모두 표시**(모드는 지도 구획 방식일 뿐).
- **폰 하단 네비**: 지도/피드/소셜(미션 제거) + **네비 왼쪽 컨텐츠 추가(+) 버튼**(누르면 스팟 메시지/사진 올리기 팝업).
- **모드 이름: 로컬→베이직**(UI만, 내부 식별자 `local` 유지). 모드 토글=지도 전환 전용, 스타일 설정은 관리자 설정에 통합 상시 표시.
- **사이드바 관리자 UI = 두 최상위 메뉴**(블록 카드로 가독성↑): **🗂 컨텐츠 설정**(스팟 추가·트렌드 존 관리) + **⚙ 관리자 설정**(스타일 9블록 아코디언). 슬라이더 옆 숫자 직접입력.
- **스팟 추가**: 커서 crosshair + 클릭 포인트 옆 지도 팝업 입력. **스팟은 지도 배율에 붙음**(줌인 확대·줌아웃 축소), 점 전환 줌 미만은 **점 또는 이모지**(옵션)로, **점 색상**도 설정.
- **상단 위치 라벨**: 베이직=화면 센터의 **동**(예 논현1동), 트렌드=보고 있는 **트렌드 존 이름**.
- **트렌드 존(데모)**: 폰에서 존 터치→**존 전체가 한 화면에 맞춰지고 주변은 그레이 처리**로 강조(재터치/빈곳 터치 해제).
- **인증/역할**: Google 로그인(Firebase) + allowlist 접근제어. `관리자`(admin) / `데모유저`(user) 역할.
- **레이아웃**: 좌 전체화면 지도 / 우 사이드바(폰 미러). 사이드바 폭 드래그 조절 → 폰 크기 변경(내부 UI는 `cqw`로 비율 유지).
- **폰 미러(앱처럼)**: 상단 좌 햄버거(→설정 드로어)·우 로컬/트렌드 모드토글, 하단 네비(지도/피드/미션/커뮤니티/AI), 접기 버튼.
- **로컬 모드**: 동 경계 하이라이트 + 선택 라벨 + **스팟 메시지**(이모지+말풍선, 관리자 생성).
- **트렌드 모드**: 헥사곤 그리드 + 트렌드 존.
- **데모유저**: 뷰잉 + 로컬/트렌드 전환만. 관리자가 만든 존·스팟 **열람 가능**(편집 불가).
- **모바일 접속**: 폰 화면만 전체표시(폰맵 터치 조작).
- **저장**: `shared/mapContent` 공유 문서(관리자 쓰기 / 로그인 사용자 모두 읽기). **Firestore `shared` 규칙 콘솔 배포 완료.**
- **외부 설정(GCP/Firebase)**: `gihoon-mx.github.io` 호스트 기준 완료 — 추가 조치 불필요.

---

## 🤝 다른 PC에서 이어서 작업 시작하기

**1) 터미널 준비:**
```bash
gh auth switch --user gihoon-mx           # push 권한 계정으로
gh repo clone gihoon-mx/now-here-demo      # 처음이면 clone (기존 clone이면 생략)
cd now-here-demo && git pull
git config user.name "gihoon-mx" && git config user.email "gihoon.mx@gmail.com"
```

> 🛠 **push가 아무 출력 없이 멈출 때(수십 분도 가능)**: 시스템에 깔린 **GCM(git-credential-manager)이 자격증명 창을 띄우려다 헤드리스에서 대기**하는 것 (2026-07-06 트레이스로 확인 — `-c credential.helper='!gh ...'`는 헬퍼를 *추가*만 해서 GCM이 먼저 돌 수 있음). 해결 2가지:
> ```bash
> # A) 헬퍼 체인 리셋 후 gh만 사용 (빈 -c가 기존 헬퍼 제거)
> git -c credential.helper= -c credential.helper='!gh auth git-credential' push origin main
> # B) gh 토큰을 URL에 직접 (헬퍼 완전 우회)
> git push "https://x-access-token:$(gh auth token)@github.com/gihoon-mx/now-here-demo.git" main
> ```

**2) Claude Code에 붙여넣을 시작 프롬프트 (예시 — 대괄호만 바꿔 사용):**
> now-here-demo 프로젝트를 이어서 작업할 거야. 먼저 repo 루트의 `WORKLOG.md`를 읽고 현재 상태(v1.6.0)와 규칙을 파악해줘:
> - 버전: 코드/스타일 바꿀 때마다 3곳(app-version, `?v=` 캐시버스트, 커밋메시지) 동기화해서 올리기
> - 배포: `main`에 push하면 GitHub Pages 자동 배포 (반영 1~15분)
> - 저장: 관리자 콘텐츠는 `shared/mapContent` 공유 문서 (데모유저 열람용)
> - 데모유저는 뷰잉+모드전환만, 편집 UI는 `role-user`로 숨김
>
> 그다음 **[여기에 하고 싶은 작업]**을 진행해줘. 작업 중 로컬 프리뷰로 검증하고, 끝나면 버전 올리고 WORKLOG 갱신 후 commit·push 해줘.
>
> _작업 예시_: "스팟 메시지에 목록·편집 기능 추가" / "데모 로그인 화면 디자인 개선" / "트렌드 존 색상 프리셋" / "폰 피드/미션 탭 실제 화면 구현" 등

**핵심 흐름**: ① WORKLOG 먼저 읽기 → ② 작업(+프리뷰 검증) → ③ 버전업 + WORKLOG 갱신 + commit/push.
- GCP/Firebase는 손댈 것 없음(같은 호스트). 로그인이 안 되면 아래 ☁️ 외부설정의 `firebaseapp.com` 리퍼러 주의 참고.

---

## 🔐 계정 / 인증 (중요)

- GitHub repo 소유: **`gihoon-mx`** (2026-07-02에 `gihoonmx-source`에서 rename됨).
- 이 Mac은 gh CLI에 계정 2개가 있음. **push하려면 gihoon-mx가 active여야 함**:
  ```bash
  gh auth switch --user gihoon-mx     # 이 프로젝트 작업 시
  # (HAOS 등 shoomerion 작업으로 돌아갈 땐 gh auth switch --user shoomerion)
  ```
- 커밋 identity(로컬 repo 한정): `gihoon-mx <gihoon.mx@gmail.com>`.
  - 새 PC에서 clone 후 필요하면:
    ```bash
    git config user.name "gihoon-mx"
    git config user.email "gihoon.mx@gmail.com"
    ```

---

## 🗂️ 프로젝트 구조

| 파일 | 역할 |
|---|---|
| `index.html` | 진입점 |
| `app.js` | 앱 로직 (지도, 로그인, 관리자 뷰 등) |
| `style.css` | 스타일 |
| `config.js` | 설정값 (Maps/Firebase 키, ADMIN_EMAIL 등) |
| `firestore.rules` | Firestore 보안 규칙 (소스 오브 트루스 — 콘솔과 동기화 유지) |
| `dong_boundary.geojson` | 동 단위 행정구역 경계 데이터 |

---

## ☁️ 외부 설정 (코드 밖 — GCP/Firebase 콘솔)

계정: `gihoon.mx@gmail.com` / GCP 프로젝트 2개

- **`now-here-demo`**: Firebase 프로젝트. **단일 키(`AIzaSyCF63…` = config.js `GCP_API_KEY`)로 Maps JavaScript API + Firebase 공용**(2026-07-07 v1.57.1 일원화), OAuth 웹 클라이언트, Firebase Authentication(Google 로그인), Map ID `b14f18…`.
- ~~**`hot-hot-map`**: Maps Platform API 키~~ — **폐기(v1.57.1)**. 구 지도전용 키(`AIzaSyAA6A…`)는 더 안 씀. 라이브 지도 확인 후 Console에서 삭제/비활성화할 것(git 히스토리에 잔존).

⚠️ **배포 도메인(github.io)이 바뀌면** 아래 3곳에 새 도메인을 추가해야 지도·로그인이 안 깨짐:
1. now-here-demo → **단일 키 · HTTP 리퍼러**(이제 이 한 키가 지도+Firebase 둘 다 — 앱 도메인 + 아래 firebaseapp.com/web.app 모두 필요)
2. now-here-demo → OAuth 웹 클라이언트 · 승인된 JavaScript 원본
3. Firebase Console → Authentication → Settings → **승인된 도메인**

> 🔑 **단일 키 제약 (2026-08-11 v2.9 확정판)**
>
> **애플리케이션 제한 = 웹사이트(HTTP 리퍼러)** — 아래가 **전부** 있어야 한다:
> `https://gihoon-mx.github.io/*` · `https://now-here-demo.firebaseapp.com/*` ·
> `https://now-here-demo.web.app/*` (+ 로컬에서 지도를 보려면 `http://localhost:8765/*`,
> `http://localhost:8766/*`)
>
> **API 제한 = 아래 6개.** 키 하나가 지도·로그인·Firestore를 다 여니 **하나만 빠져도 그 기능이 죽는다**:
>
> | API | 없으면 |
> |---|---|
> | Maps JavaScript API | 지도 전체 |
> | **Places API** (New 아님 — 레거시 `PlacesService`) | 지역 시드 생성 |
> | **Maps Static API** | 피드 스팟 카드의 지도 배경 |
> | Identity Toolkit API | Google 로그인 |
> | Token Service API | 로그인 유지(토큰 갱신) |
> | Cloud Firestore API | 콘텐츠·설정 저장/읽기 + 데모 임베드 설정 전달 |
>
> ⚠️ 증상으로 가르는 법: `REQUEST_DENIED` + 콘솔에 *"not authorized to use this service or API …
> check the API restrictions settings of your API key"* 가 뜨면 **프로젝트가 아니라 이 키의
> API 제한 목록** 문제다 (2026-08-11 실측으로 확인). 격리 테스트는 API 제한을 잠시
> "키 제한 안함" 으로 두고 재시도 → 되면 목록 문제가 확정된다.
>
> 🧯 **요금 방어는 키 제한이 아니라 할당량이다.** 리퍼러는 위조가 가능하다 — API 및 서비스 →
> Places API → 할당량에서 **일일 요청 수 상한**(200 정도)을 걸어 두는 것이 실질 방어선이다.
> 예산 알림은 알려만 주고 막지 않는다.

⚠️⚠️ **Firebase 브라우저 키(1번)의 HTTP 리퍼러에는 앱 도메인과 별개로 아래가 항상 있어야 함** (없으면 Google 로그인이 403 `API_KEY_HTTP_REFERRER_BLOCKED` → "The requested action is invalid."로 깨짐. 로그인 팝업이 이 도메인에서 돌기 때문):
- `https://now-here-demo.firebaseapp.com/*`
- `https://now-here-demo.web.app/*`

### Firestore 규칙
- 소스 오브 트루스: repo의 **`firestore.rules`**. 콘솔(Firebase → Firestore → 규칙)에 배포하며, 양쪽을 항상 같게 유지.
- ✅ **2026-08-11 게시 완료** — `shared/publicSettings` 비로그인 읽기가 열렸다(실측: 인증 없이 REST 200). 이로써 관리자가 적용한 스킨·설정이 **persona-vc 데모 임베드에 자동 반영**된다(v2.5 경로). localStorage 캐시는 cross-site iframe 에서 안 보이므로(D91 ③) 이 문서가 유일한 자동 경로다.
- 규칙의 관리자 이메일(`gihoon.mx@gmail.com`)은 `config.js`의 `ADMIN_EMAIL`과 일치해야 함.

---

## 🧱 모듈 단위 작업 (2026-07-07~)

구현은 **모듈 단위**로 진행: 모듈 맵·세션 규칙·공유 상태 계약은 **`MODULES.md`**, 시각화·세션 프롬프트 생성은 **`dev.html`**(개발 관리 페이지).
- 한 세션 = 한 모듈. dev.html에서 [세션 프롬프트 복사] → 작업 내용만 채워 시작.
- 코드 탐색은 `grep -n "\[M07\]" app.js`(섹션 태그)·앵커 grep으로만 — 파일 통독 금지(크레딧 절약).
- 다른 모듈 코드 수정 금지(교차 시 WORKLOG에 ⚠️교차: M## 표기). 완료 시 MODULES.md 상태/최근 버전 + **dev.html·diagram.html 반영(`data-app-ver` 갱신, 소개 덱 제외)**.
- **push 전 `node tools/check.js`** — 버전 3곳·dev/diagram 스탬프·app.js 문법 검사. pages.yml 배포 전에도 자동 실행되어 실패 시 배포 중단.

---

## 📝 변경 이력

### 2026-08-19 (36)
- **v2.65.0 — 무대 가져오기: Persona VC 의 페르소나 무대를 이 지역의 시드 그룹으로 (콘솔 v0.184.0 · D230 짝)**
  사용자: "페르소나로 데모 컨텐츠 채우기를 만들고, 이걸 now-here-demo 관리자 콘솔에서도 활용".
  - **관리자 › 컨텐츠 › 지역 시드 생성** 아래에 **「무대 가져오기 (Persona VC)」** — 콘솔
    STEP 05 「컨텐츠 채우기」가 내보낸 JSON(`{"nhstage":1,"name","seed"}`)을 붙여넣으면
    **지금 보고 있는 지역**에 그 컨텐츠를 깐다. seed 알맹이만 붙여넣어도 받는다.
  - **생성기와 같은 길**(`sgImportStage` — sgCommit 과 같은 저장 경로): 지도 글→spots ·
    피드→feeds(사진 주소가 없으면 `seedImg` 테마 이미지) · Request→reqs ·
    매장(`store:true`)/타임딜→timeDeals. 전부 `sgroup` 을 달아 **그룹 하나**가 되고,
    기존 그룹 관리(지도 이동·숨김·삭제)를 그대로 탄다. 그룹 메타에 `src:'pvc'` —
    목록에 「Persona VC」로 찍힌다 (AI/템플릿과 구분).
  - **좌표는 골든앵글 나선으로 결정적** — 포커스 중심에서 반경(#sg-radius) 안에 퍼진다.
    같은 JSON 은 언제나 같은 자리 (Math.random 이면 재렌더마다 움직인다 — sgCommit 의
    jit 주석과 같은 이유). 수량은 NH_MAX 안쪽으로 자른다 (spots·feeds 40 · reqs 10 · deals 12).
  - **nh:* 계약은 안 건드렸다** — JSON 은 사람이 복사해 나르는 것이라 postMessage 어휘가
    아니다. 콘솔 check:contract 무관, 배포 순서 제약(D91) 없음. ⚠️ 처음에 콘솔 짝을 v0.173.0·D216 으로 적었는데, 콘솔 쪽이 병행 세션과 번호가 겹쳐 v0.184.0·D230 으로 재발행됐다 — 여기 표기도 그에 맞춘 것.
  - 검증(로컬 :8766, 지도는 없는 환경): 역할 가드("관리자만") · 지도 가드("지도가 아직") ·
    `sgFocusPoint` 스텁 후 실주입 — 6개 항목이 개별 좌표로 서고(counts 정확, store 와 딜
    구분, pct·secs 클램프), 그룹 라벨 「Persona VC」, 그룹 삭제로 전량 회수. app.js
    `node --check` 통과. ⚠️ 지도 위 실렌더는 로그인 게이트 뒤 — 라이브에서 확인.
  - ⚠️ v2.64.0 이 v2.65.0 으로 찍힌 역사 주석 4곳(딜 전환·프로필 원·라이트/다크·팝업 스크림)은
    일괄 버전 범프의 오염이라 되돌렸다 — 그 기능들은 v2.64.0 의 것이다.

### 2026-08-18 (35)
- **v2.64.0 — 모든 액션에 어울리는 소리 · 단계마다 켜고 끄기 (콘솔 v0.161.0 · D196 짝)**
  사용자: "모든 액션마다 어울리는 효과음들을 넣어주고, 효과음 액션 개별 단계별로 켜고 끄기".
  - **먼저 셌다.** NH_ACTIONS 41개의 실행 경로(디스패치 → 도우미 → 콜백)를 `nhSfxPlay` 까지
    추적했더니 **16개가 완전히 조용**했다: like·hearts·react(리액션) · reward(코인) ·
    answers·adopt·ai·chat(도착) · zoom·focus·area(카메라) · tab·scope·theme(화면 전환) ·
    drawer·bubbleclose. 나머지 25개는 이미 어딘가에서 소리에 닿았다(pop·open·close·tap·type·shot).
    ⚠️ 첫 감사 스크립트가 addphoto/addpost 를 조용하다고 했는데 **재귀 두 단계**를 못 본
    것이었다(nhAddFeedCard → nhBounceMark → pop). 손으로 다시 따라가 확인했다.
  - **슬롯 다섯을 늘렸다** (7 → 12): `heart`(리액션) · `coin`(코인·리워드) · `ding`(답 도착·
    채택·AI·채팅) · `whoosh`(카메라 이동) · `alarm`(딜 시작 ⏰). 기존 7슬롯으로는 이 소리들이
    다 "팝" 아니면 "탭" 이 되어 종류가 안 갈린다. 음원 다섯은 기존 파일과 **같은 형식**
    (PCM 16bit · 모노 · 22050Hz)으로 합성했다 — 로더가 형식을 안 가리게.
    화면 전환(tab·scope·theme)은 새 슬롯 없이 `mode` 를, 서랍은 `open`, 말풍선 닫기는 `close`
    를 쓴다 — 같은 종류의 일이면 같은 소리다.
    ⚠️ **`dealon` 은 `pop` 이었는데 `alarm` 으로 바꿨다** — 등장음이 아니라 ⏰ 와 같은 말이어야 한다.
    ⚠️ 슬롯을 늘리면 **네 자리**를 다 고쳐야 한다: NH_SFX_KEYS(단일 기준) · NH_SFX_GAP ·
    NH_SFX_DEFAULT · admin.html 의 패널 행(행이 없으면 initSfxPanel 이 조용히 건너뛴다).
    콘솔 `check:contract` ⑫ 가 이제 넷을 다 센다 — 일부러 빼고 돌려 잡히는 것까지 확인했다.
  - **단계 `mute`** — 콘솔 단계 편집기의 `효과음` 체크(기본 켜짐). 앱은 `nhAct` 가 단계
    시작에 `nhStepMute` 를 심고(말풍선 유지와 같은 자리) `nhSfxPlay` 한 곳에서 거른다.
    ⚠️ **새는 곳이 없는지 셌다** — 이 저장소에서 소리를 내는 곳은 전부 nhSfxPlay 를 지난다.
    Audio 를 직접 만드는 곳은 관리자 미리듣기 ▶ 하나뿐이고 그건 재생이 아니다.
    `nhSanitize` 가 새 객체를 지으므로 `mute:!!s.mute` 를 **거기 먼저** 넣었다(무대 밝기 사고).
  - 검증(로컬 :8765, 단계 경로 · **실제 재생**을 `HTMLMediaElement.play` 가로채기로 잼):
    18개 액션이 의도한 슬롯으로 울고(zoom→whoosh · like→heart×3 · answers→ding×2 ·
    reward→coin · dealon→alarm · tab→mode · drawer→open · bubbleclose→close · theme→mode ·
    chat send→ding), 같은 값 재적용(theme dark→dark)은 조용하며, **mute 단계는 실제로 안 울고**
    다음 단계에서 다시 운다. `chat` 은 방만 여는 `local` 은 mode 만, `send` 라야 ding — 검증
    조건을 잘못 잡았다가 바로잡았다.
    ⚠️ 첫 측정기는 **가로챈 래퍼가 원본보다 앞에서 세어** mute 단계도 소리 난 것처럼 보였다.
    "불렸는가" 가 아니라 "재생됐는가" 를 재야 한다 — 그래서 play 를 가로챘다.

### 2026-08-18 (34)
- **v2.63.3 — 지도 위 라이브 핀에 `LIVE` · 시계 효과는 라벨 위에서**
  사용자 요청 둘.
  - **① 지도 피드 핀에 `LIVE`** — 여태 `LIVE` 는 피드 목록 카드(`.fc-live`)·팝업(`.cpf`)·
    지면 칩(`.cpc-live`)에만 있어서 **지도 위 핀만 라이브인지 아닌지를 못 말했다.**
    자리는 **사진 아래 가운데**다 — 우측 상단은 리액션 라벨(`.fp-tag`)과 클러스터 개수
    (`.fp-n`)가 이미 쓴다.
    ⚠️ **클러스터에는 안 붙인다** — 대표 사진 하나가 라이브라고 그 무더기가 라이브인 것은
    아니다(그쪽은 탭의 뜻이 '펼치기' 라 리액션 라벨도 안 단다).
    ⚠️ **점 모드에서는 감춘다** — 핀이 12px 로 줄면 이 알약이 핀보다 커진다(`.fp-tag` 와
    같은 규칙). 실측: 34px 핀에서 뱃지 26.8×13.4, 가운데 정렬, 점 모드 `display:none`.
  - **② 전환 효과(⏰)를 라벨 위로** — v2.62.6 이 앵커를 `-20px` 로 옮겼는데 그것은 미니
    라벨과 **같은 줄**이었다(라벨이 -20~-3 을 쓴다). 실측한 라벨 높이 17px 위로 여유 6px,
    이모지가 앵커 안에서 다시 9px 내려붙는 것까지 빼서 **-37px**.
    ⚠️ **정지 상태로 재면 겹쳐 보인다** — 0% 프레임이 라벨 자리에서 시작하기 때문이다.
    그 프레임은 **투명도 0** 이라 눈에 안 보인다. 검증은 음수 `animation-delay` + `paused`
    로 진행 지점마다 세워 재야 한다(이 세션은 프레임 합성이 없어 그냥 두면 시작값에 멈춘다).
    실측: 보이는 프레임(22~70%)이 전부 -25.8 ~ -49.2 로 **라벨(-20) 위**, 사라지는
    100% 프레임은 -57.4.

### 2026-08-18 (33)
- **v2.63.2 — 방금 낸 판을 적대적으로 되짚어 결함 둘을 닫았다**
  v2.63.1 을 낸 뒤 **조사 워크플로(에이전트 14, 반증 단계 포함)** 를 돌려 내가 쓴 코드를
  반증하게 했다. 그중 앱에서 **실제로 성립하는 결함 둘**이 나왔다.
  - **① 채택 연출이 끝난 뒤 450ms 동안 얼룩이 남았다** — `animation:rqaSweep .95s … 1;` 에
    **fill-mode 가 없었다.** 0.95초에 애니메이션이 끝나면서 `background-position` 이 기본값
    (0% 0%)으로 **되돌아가** 줄 오른쪽에 파란 얼룩이 생기고, JS 가 클래스를 걷는 1.4초까지
    그대로 멈춰 있다가 툭 사라졌다. `forwards` 로 마지막 프레임(220%)을 붙들게 했다.
    ⚠️ 이건 **픽셀로만 보이는 종류**라 내 DOM 검증이 못 잡았다 — 클래스도 애니메이션 이름도
    맞게 붙어 있었기 때문이다. 값(fill-mode)까지 재야 잡힌다.
  - **② 주제로 좁혀 0건이면 앞 주제의 사진이 그대로 떴다** — 콜백이 `status!==OK` 에서 그냥
    돌아나가서 `nhPlacePhotos` 를 안 덮었다. 그러면 앞 배치가 살아남아 **화면은 새 주제라고
    말하는데 사진은 옛 주제**다. "못 받으면 저장소 묶음으로 떨어진다" 는 약속이 깨지는
    유일한 구멍이었다. 이제 비우고(키는 적어 둬 같은 주제를 또 묻지 않는다) 출처 표기도
    함께 걷는다.
    검증: 0건을 흉내 낸 응답에서 캐시 0장·표기 사라짐·사진이 **저장소 묶음으로** 떨어짐.
  - 함께 확인하고 **안 고친 것**: `keep:true` 미리보기 경로는 `nhReset` 을 건너뛰어 회차
    값(테마·존 카드·컨텐츠 배율·피드 가로칸)이 안 되돌아온다 — **이번 판이 만든 게 아니라
    넷이 공유하는 옛 성질**이라 따로 잡을 일로 남긴다.

### 2026-08-18 (32)
- **v2.63.1 — 안내문은 연하게 · 쿠폰은 버튼에서 · 채택의 순간 · 피드 가로칸 · 라이브 카드 · burst 주제 (콘솔 v0.160.0 짝)**
  사용자 요청 일곱. 앱이 여섯, 콘솔이 하나(스크린샷)다.
  - **① 타이핑 칸의 안내문을 아주 연하게** — 재생이 글자를 박아 넣는 칸(스팟 컴포저·Request
    컴포저·Request 답장·스팟 팝업 의견·소셜 채팅·AI 물어보기)의 `::placeholder` 를
    `rgba(15,23,42,.16)` 로. ⚠️ **지우지 않았다**: 이 앱은 시연용이자 실제 앱이라, 지우면
    손으로 쓰는 사람이 그 칸이 무엇을 받는 자리인지 알 길이 없다. 다크는 밝은 쪽으로 같은 만큼.
  - **② 쿠폰이 '쿠폰 받기' 버튼에서 출발한다** — 원인은 타이밍이었다. `coupon` 액션은
    `nhAfterClose` 안에서 받는데(v2.59.1), **그때 시트는 이미 닫혀 있어** `#ds-claim` 의
    rect 가 0 이다 → `couponFly` 가 그것을 버리고 **화면 한가운데**에서 날렸다.
    이제 누르는 순간(`nhTouch`) **자리를 값으로 재 두고** 그것을 넘긴다. `couponFly` 는
    요소든 rect 든 받는다. 실측: 출발 x 가 버튼과 같은 412.6 (화면 가운데는 639.7).
  - **③ 채택되는 순간의 연출** — `.rqa-item.best` 의 파란 바탕·테두리는 **상태**만 말했다
    (다시 열어도 같다). 빛이 줄을 한 번 훑고(`rqaSweep`) ✅ 뱃지가 뿅 뜬다(`rqaBadge`).
    ⚠️ `nhReqRepaint` **뒤**에 걸어야 한다 — 그 함수가 `cpopRefresh` 로 목록을 통째로 다시
    그리므로 앞에 걸면 방금 붙인 클래스가 날아간다.
  - **⑤ 탭 바꾸기 · 피드 가로 칸** — `tab` 액션의 `e` 칸(콘솔은 `v='feed'` 일 때만 세운다).
    ⚠️ **탭 전환보다 먼저** 건다 — 뒤에 걸면 옛 배열로 한 번 그려진 뒤 바뀌는 것이 보인다.
    ⚠️ **되돌린다**(`nhFeedColsRestore`): `applyFeedCols` 는 localStorage 까지 쓰는 관리자
    설정이라 시연 한 판이 기기 설정을 영구히 바꾸면 안 된다.
  - **⑥ 피드 카드의 라이브 여부** — ⚠️ 앱 `nhLayFeed` 는 `kind` 를 **v2.34 부터 읽고
    있었는데** `nhSanitize` 의 feeds map 이 안 실어 보내서 정할 길이 아예 없었다(무대
    밝기와 **같은 자리·같은 병**). 네 자리를 다 뚫었다.
  - **⑦ burst 사진을 주제로도** — `nearbySearch` 에 `keyword` 를 얹는다. ⚠️ **textSearch 로
    바꾸지 않았다**: 그러면 검색이 동네를 벗어나 엉뚱한 도시 사진이 와서 "이 동네가
    깨어난다" 가 깨진다. 자리는 이 동네, 주제만 좁힌다.
    ⚠️ **새 단계 칸을 안 뚫었다** — `n` 한 칸에 `nearby:루프탑` 으로 담는다(D94·D95 가
    종류별 개수를 `v` 에 담은 것과 같은 판단: 칸을 늘리면 다섯 자리가 갈린다).
    캐시 키에도 주제를 넣었다 — 안 넣으면 주제를 바꿔도 앞 사진이 재사용된다.
  - 검증(로컬 :8765 임베드, 전부 **단계 경로**로): ② 출발 x=412.6(버튼)·화면 가운데 아님
    ③ `rqaSweep`+`rqaBadge` 가 붙고 1.6초 뒤 걷히며 `.best` 는 남는다 ⑤ 3칸으로 그리드가
    실제 3트랙·빈 값은 안 건드림·`nhReset` 이 되돌림 ⑥ `cam` 카드에만 LIVE 뱃지
    ⑦ `루프탑`·`베이커리` 가 검색에 실리고 주제를 바꾸면 다시 받으며 주제 없으면 keyword 없음.
    콘솔 화면(:3010): 📷 한 장 버튼 · 가로 칸이 **피드일 때만** · 주제 칸.
    `node tools/check.js` PASS.
  - ⚠️ **④ 스크린샷은 콘솔 기능이고 이 세션에서 실제로 못 찍어 봤다** — `getDisplayMedia`
    는 사람이 공유 대화상자에서 탭을 골라야 열린다. 코드 경로(캡처→크롭→한 프레임→PNG→저장)
    는 녹화가 쓰던 것과 같은 길이다.

### 2026-08-18 (31)
- **v2.63.0 — 무대가 까는 지면에도 `사진만` (콘솔 v0.159.0 짝)**
  사용자: "상단 지면 게시할 때 텍스트 옵션을 체크 해제하면 텍스트 가독성 때문에 추가된
  검은색의 그라데이션 효과 삭제 (persona-vc 컨텐츠 탭에서 지면 개별 항목마다)".
  - v2.62.5 가 만든 `bare`(사진만)는 **앱 관리자**가 올린 지면에만 있었다. 이번엔 **무대가
    까는 지면**(`seed.pages`)이라 콘솔 컨텐츠 탭에서 항목마다 켠다. 화면 쪽 규칙은 같은
    것을 쓴다(`.cps-bare` — 글자칸과 그라데이션을 **함께** 접는다).
  - ⚠️ **위치·제목과 그라데이션은 한 몸이다.** 그라데이션은 글자를 읽히게 하려고 깔린
    것이라, 글자만 끄고 그것을 남기면 "깨끗한 사진" 이라는 목적이 반만 이뤄진다.
  - ⚠️ **적어 둔 글은 안 지운다** — 껐다 켜면 위치·제목이 그대로 돌아온다(실측).
  - ⚠️ 값이 지나는 자리를 **네 곳 다** 적었다: 콘솔 `PlaySeed.pages` · `toSeed` ·
    `nowhere.ts` 페이로드 타입 · 앱 `nhSanitize` 의 pages map. 마지막 자리가 v2.62.9 에서
    무대 밝기를 삼킨 바로 그 종류의 코드다(새 객체를 짓는 map) — 같은 실수를 안 하려고
    거기부터 확인하고 시작했다.
  - 검증(로컬 :8765 임베드): 두 장을 깔고 하나만 켜서 — sanitize·깔린 항목·화면이 모두
    항목별로 갈리고, 켠 장만 `.cps-bare` 로 **그라데이션과 글자칸이 `display:none`**,
    사진은 그대로. 끄면 위치·제목이 돌아온다.
    콘솔 화면(:3010 데모 모드): 컨텐츠 › 지면 카드 › `＋ 상단 지면 카드` 아래에
    `사진만 — 위치·제목과 그 아래 그라데이션을 뺍니다` 체크칸이 서고 켜고 끄기가 된다.
    `node tools/check.js` PASS.

### 2026-08-18 (30)
- **v2.62.9 — 무대 밝기가 앱에 도착조차 못 하고 있었다 · 피드 상단 지면 · 피드 순서 · 근처 실제 장소 사진 (콘솔 v0.158.0 짝)**
  사용자 요청 넷. 첫째는 **버그**다.
  - **① 무대 설정의 다크가 안 먹던 이유** — 콘솔은 `seed.theme` 을 제대로 보냈고 앱도
    `nhThemeSet` 으로 받을 준비가 돼 있었는데, 그 사이 **`nhSanitize` 가 seed 를 새 객체로
    다시 지으면서 목록에 없는 키를 통째로 버렸다.** D124 와 같은 병이다(적히지 않은 칸은
    조용히 사라진다) — 다만 그때는 **단계**의 칸이었고 이번은 **무대**의 칸이라 계약 검사가
    안 보고 있었다.
    ⚠️ 게이트에도 넣었다: 컨텐츠가 하나도 없는 데모는 `seed` 자체가 null 이 되어 **밝기만
    정한 무대**가 또 조용히 사라진다.
    **다음 것을 막는 자리**: 콘솔 `check:contract` 에 ⑪(무대 칸 대조)을 새로 뒀다 —
    `PlaySeed` 가 선언한 칸이 앱 seed 조립에 다 적혀 있는지 센다. 일부러 `theme` 을 빼고
    돌려 **실제로 잡는 것까지 확인했다**(처음 판은 정규식이 깨져 못 잡았다 — 그것도 고쳤다).
  - **② 피드 탭도 지도 탭과 같은 지면** — 여태 그 줄이 `map` 만 봐서 피드 탭 상단은 손으로
    올린 지면 중 `tab:'feed'` 인 것만 남았고 대개 비어 있었다. 같은 자리가 탭에 따라 다른
    물건이었다. 소셜은 안 얹는다(그 탭 지면은 '커뮤니티 지면' 이라 다른 것을 말한다).
  - **③ 피드에 뜨는 순서를 사람이 정한다** — 컨텐츠 탭 표의 사진 줄에 ↑↓. 값은
    `feedItems[].ord`(0,1,2…)이고 **처음 누를 때 전체에 번호를 매긴다** — 일부만 번호가
    있으면 있는 것과 없는 것이 서로 다른 대역에 놓여 뒤죽박죽이 된다.
    ⚠️ **포커스 구역 우선은 그대로 둔다** — 그건 "지금 보고 있는 동네" 라는 화면의 약속이라
    사람이 정한 목록보다 위다. 순서는 그 안에서 정해진다. 손 안 댄 데모는 하나도 안 바뀐다.
  - **④ 근처 실제 장소 사진** (`burst` 의 사진 고르개에 `근처 실제 장소`) — 사용자: "웹에서
    직접 연관된 걸로 가져올 수 있을까?"
    이 파일이 "웹에서 안 긁어온다" 고 적어 둔 이유(출처·라이선스를 모르고, 죽으면 빈 카드)는
    지금도 유효하다. 그래서 임의의 웹 이미지가 아니라 **Google Places 의 장소 사진**이다:
    그 동네의 **실제 가게 사진**이고, 출처 표기를 API 가 같이 주며, 실패하면 옛 묶음이다.
    ⚠️ **미리 받는다** — burst 는 예약된 시각에 하나씩 까는데 사진은 비동기다.
    ⚠️ **출처 표기는 필수다**(없이 쓰면 약관 위반). 여기서 버그를 하나 잡았다: burst 시작에
    표기를 걸었는데 그 순간 캐시가 비어 안 붙고, 잠시 뒤 사진이 도착해 **표기 없이** 떴다 —
    이제 도착하는 자리에서 다시 건다(`nhPlaceCreditOn`).
    ⚠️ 프리페치 자리도 한 번 틀렸다: `if(!sc.seed)return` **아래**에 뒀더니 컨텐츠 없는
    무대에서 아예 안 돌았다(①과 같은 병) — 위로 올렸다.
    ⚠️ `sampleTone` 이 밝기를 재려고 `crossOrigin` 으로 한 번 더 받는데 Places 는 CORS 를
    안 준다 — 그 주소는 **안 재고** 기본값으로 간다(요청 가로채기로 시도 0건 확인).
  - 검증(로컬 :8765 임베드): ① sanitize 가 `theme` 을 나르고 무대가 걸리며 **저장은 안 되고**
    (`nhThemeRestore` 로 복귀) 컨텐츠 없는 무대에서도 산다. ② 지도 3장 = 피드 3장, 소셜 0장.
    ③ f3 을 두 번 올리면 맨 앞, 두 번 내리면 제자리. ④ **네 장 전부 Places 사진**(회차 시작에
    미리 받아 13장 캐시), 출처 표기가 뜨고 `nhReset` 이 걷으며, `실사진` 모드는 Places 를
    **안 부른다**(과금). 콘솔 에러 0(CORS 시도 0건) · `node tools/check.js` PASS.
  - ⚠️ Places 는 **호출만큼 과금된다.** 쓰는 단계가 있을 때만 부르고 같은 동네는 다시 안
    받지만, 데모를 많이 돌리면 비용이 는다.

### 2026-08-18 (29)
- **v2.62.8 — 다크용 Map ID 를 받아 넣었다 (덮개는 이제 안 걸린다)**
  사용자가 콘솔에서 만든 어두운 지도 ID(`5224…3d50`)를 `CONFIG.MAP_ID_DARK` 에 넣었다.
  v2.62.7 이 만들어 둔 길이 이걸로 채워진다 — **다크에서 `setMapId` 로 그 ID 로 갈아타고
  덮개(`MapTint`)는 안 걸린다.**
  - 검증(로컬 :8765 임베드): 라이트 = 기본 ID·덮개 0 → 다크 = `5224…3d50`·덮개 **0** →
    라이트 = 기본 ID·덮개 0. `setMapId` 호출은 **딱 두 번**(바뀔 때만 — 같은 테마를 다시
    걸어도 안 부른다). 관리자 안내문이 「구글 다크 지도로 바뀝니다」 로 뒤집힘.
    콘솔 에러 0 · `node tools/check.js` PASS.
  - ⚠️ **여기까지가 계약이고, 그림은 아직 아무도 못 봤다.** 이 PC 에서는 API 키 리퍼러
    때문에 지도가 로컬에서 안 뜬다(`renderingType` 이 `UNINITIALIZED` 로 남는다). 그래서
    ①그 ID 의 스타일이 실제로 어두운지 ②ID 를 가는 순간 오버레이(핀·스팟·존)가 제대로
    다시 붙는지는 **배포본에서 볼 것**. 갈아 끼운 뒤 재렌더를 한 번 걸어 두긴 했다.
  - ⚠️ ID 가 잘못 물리면(다른 프로젝트·JavaScript 유형 아님·키 제한) 구글은 조용히 기본
    스타일로 떨어뜨린다 — 그때는 다크에서 **밝은 지도**가 보이고 덮개도 안 걸린 상태다.
    그 증상이면 콘솔의 지도 ID 설정을 먼저 본다.

### 2026-08-18 (28)
- **v2.62.7 — 지도는 구글의 다크로 간다 (`setMapId` 교체) · 덮개는 폴백으로**
  사용자: **"맵 다크 모드가 그냥 검은색 필터를 쓰는 게 아니라, google maps의 다크 모드를
  못 쓰는 거야?"** — **쓸 수 있다. v2.62.6 의 판단이 틀렸다.**
  - **무엇을 안 보고 단정했나.** 그때 근거는 두 개였다: `styles` 는 벡터 지도에서 무시된다,
    `colorScheme` 은 생성 시에만 받는다. 둘 다 맞다 — 그런데 **`map.setMapId()` 가 있는지를
    안 봤다.** 로드된 API 를 직접 찔러 보니(v3.65.12f):
    `google.maps.ColorScheme` = `LIGHT`/`DARK`/`FOLLOW_SYSTEM` **존재** ·
    생성자에 `colorScheme:DARK` **통과**(mapId 유무 무관, 경고 0) ·
    **`map.setMapId` 가 함수로 존재**. 런타임 교체 길이 처음부터 열려 있었다.
  - **그래서 ID 를 간다.** `MAP_ID_DARK`(config.js)에 어두운 클라우드 스타일을 물린 두 번째
    Map ID 를 넣으면, 테마가 바뀔 때 `setMapId` 로 갈아탄다. 지도를 만들 때도 **처음부터**
    그 ID 로 연다(열고 나서 갈면 한 번 깜박인다).
  - **덮개는 폴백으로만 남는다** — `MAP_ID_DARK` 가 비어 있을 때만 걸린다. 지우지 않은
    이유: ID 를 넣기 전까지 다크에서 지도만 하얗게 남는 것보다 낫다. 관리자 화면(표시 옵션
    › 테마)이 **지금 어느 길인지 한 줄로 적는다** — 안 적으면 "왜 라벨까지 눌리지?" 를
    사람이 코드까지 파야 안다.
  - ⚠️ **ID 를 갈면 렌더러가 다시 선다.** 갈아 끼운 뒤 한 번 다시 그린다(핀·스팟·딜·존).
    이 앱은 지도 위 물건이 전부 `OverlayView`·`Polygon`·`Marker`(고전 API)이고
    **AdvancedMarker·WebGLOverlayView·tilt/heading 사용처가 0건**이라(전수 확인) 벡터든
    래스터든 잃는 기능이 없다.
  - 검증(로컬 :8765 임베드): `MAP_ID_DARK` 가 **빈 상태** — 다크에서 기본 ID 유지 + 덮개 2개
    (폰·PC 지도). `MAP_ID_DARK` 를 넣은 상태 — 다크로 갈 때 `setMapId` 가 **정확히 한 번**
    그 값으로 불리고, 라이트로 돌아올 때 기본 ID 로 다시 불리며, **덮개는 0개**.
    같은 테마를 다시 걸면 안 부른다(중복 호출 없음). 콘솔 에러 0 · `node tools/check.js` PASS.
  - ⚠️ **아직 반쪽이다** — `MAP_ID_DARK` 는 **빈 값으로 나간다**. Google Cloud Console 에서
    어두운 지도 스타일과 Map ID 를 만들어 넣어야 진짜 다크 지도가 뜬다(그 전까지는 덮개).

### 2026-08-18 (27)
- **v2.62.6 — 팝업 면은 한 토큰 · 다크 누락분 · 무대가 정하는 밝기 · 지도 타일 덮개 · 딜 라벨과 전환 효과 (콘솔 v0.157.0 짝)**
  사용자 요청 여섯. 하나(answer 편집기)는 콘솔만이고 나머지가 앱이다.
  - **① 팝업 면에 투명도를 없앴다** — "팝업창의 배경은 투명도가 필요없어 … 타임딜 팝업이나
    agent 팝업창과 동일한 테마로". 여태 **셋이 각자**였다: 스팟·딜 카드는 `--frost`(0.6 —
    지도가 비친다), agent 말풍선은 `rgba(255,255,255,.92)` 를 박아 썼다(그래서 다크에서
    흰 채로 남았다). 이제 **`--pop-surface` 한 토큰**이고 스팟 팝업·딜 시트·agent 말풍선·
    컨텐츠 추가 메뉴가 같은 값을 쓴다(실측: 라이트 `#ffffff` · 다크 `#1b1f27`, 셋이 동일).
    뒤가 안 비치므로 **뒤 지도는 `--pop-blur` 가 맡는다** — 역할이 갈렸다.
  - **② 다크 누락분** — 화면을 탭·팝업별로 훑어 **밝은 바탕 + 밝은 글자**를 셌다. 여섯이
    나왔고 원인은 하나였다: v3 스킨이 **잉크색을 버튼 배경으로** 쓰는데(`--v3-ink`),
    다크에서 잉크를 밝게 뒤집자 배경만 밝아지고 글자는 흰 채로 남았다(대비 0.08).
    배경을 되돌리지 않고 **글자를 어둡게** 했다 — 어두운 화면에서 CTA 는 밝은 알약이 맞다.
    ⚠️ **고쳐 놓고 두 번 부쉈다**: ①안 고른 칩 규칙이 `.active` 까지 잡아(둘 다 `.fsc`)
    방금 고친 고른 칩을 덮었고 → `:not(.active)` 로 좁혔다 ②`new` 스킨의 소셜 탭은
    토큰만으로 이미 뒤집혀 있었는데 한 줄에 묶다가 어두운 면에 어두운 글자를 만들었다 →
    그 선택자에서 뺐다. **스킨 3종 × 전 화면 재측정으로 0건**을 확인했다.
  - **③ 지도 타일을 어둡게** — ⚠️ **지도 스타일 자체는 못 바꾼다**(클라우드 mapId 벡터 지도:
    런타임 `styles` 무시 · `colorScheme` 은 생성 시에만). 그래서 **타일 위에 한 겹을 덮는다**:
    자리는 `getPanes().mapPane` — 이 저장소의 오버레이는 전부 `overlayMouseTarget`(맨 위 판)
    에 살고 mapPane 은 그 아래·타일 위라 **타일만 어두워지고 스팟·핀·이름표는 안 물든다**.
    지도 div 에 필터를 걸면 그 안의 컨텐츠까지 같이 물드는 것과 다른 점이 이것이다.
    ⚠️ **이 세션에서는 못 봤다** — 브라우저 패널이 안 떠 OverlayView 가 아예 안 그려진다.
    그래서 실패하면 **아무 일도 안 하게** 짰다(try/catch · 투영 없으면 통과).
  - **④ 무대가 밝기를 정한다** (`seed.theme`) — `zoneCard`·`contentScale` 과 **같은 규칙**:
    회차 동안만 걸리고 `nhReset` 이 되돌린다(`nhThemeRestore`). **저장은 안 한다** —
    단계의 `theme` 액션은 저장하고 이쪽은 안 하는 것이 둘의 차이다: 무대는 회차가 입는
    옷이고 액션은 재생 중에 갈아입는 손이다. 그래서 둘을 같이 써도 안 싸운다.
  - **⑤ 딜 전환 효과가 가게명을 안 가린다** — 앵커가 이름표 루트의 `top:0`(=상호칩 윗선)
    이라 ⏰ 와 링이 **칩 위에서** 솟아 글자를 덮었다. 이제 미니 라벨 자리(`-20px`)에서
    솟는다. **끄는 옵션**도 붙였다(관리자 › 표시 옵션 › 타임딜 › 전환 효과) — 꺼도 바운스와
    소리는 남는다(그건 "지금 바뀌었다" 의 최소한이고, 이 옵션이 끄는 것은 ⏰ 와 링이다).
  - **⑥ 타임딜 미니 라벨을 키웠다** — 9 → **11px**, 여백 2/5 → **3/7px**. 키운 만큼 위로도
    밀어야(-16 → **-20px**) 상호칩과 안 겹친다. 전환 효과의 앵커도 같은 값이다.
  - 검증(로컬 :8765, 임베드 + admin.html): **전이를 끄고**(이 세션은 프레임 합성이 없어
    전이가 옛 값에 멈춘다 — v2.62.5 의 교훈) 스킨 3종 × 다크로 **탭·서랍·팝업 넷·딜 시트·
    매장 페이지·추가 메뉴**를 돌며 대비를 쟀다 → 안 읽히는 자리 **0건**.
    팝업 면 셋이 라이트·다크 모두 같은 값·불투명. 무대 테마가 걸리고(`data-theme`)
    **저장은 안 되며**(localStorage null) `nhThemeRestore` 로 되돌아온다.
    미니 라벨 11px/3·7px, 전환 효과 앵커가 라벨과 **같은 -20px**, 끄면 `display:none`.
    콘솔 에러 0 · `node tools/check.js` PASS.
  - ⚠️ **지도 덮개는 못 본 채로 나간다** (위 ③). 배포본에서 지도가 어두워지는지·핀이
    안 물드는지 눈으로 볼 것. 이상하면 관리자 테마를 라이트로 두면 덮개가 안 걸린다.

### 2026-08-18 (26)
- **v2.62.5 — 네비 아이콘도 다크로 · 지면 카드를 사진만으로 · 데모 프로필 사진 (콘솔 v0.156.1 짝)**
  사용자 요청 셋. 전부 앱이고 둘(지면·프로필)은 **관리자 설정**까지 붙는다.
  - **① 하단 네비가 다크를 안 따라왔다** — 유리 알약은 v2.62.4 에서 어두워졌는데 **그 위의
    선 색이 그대로**였다: `.pn-add` 는 `#3c434e`, 안 고른 탭은 `#5f6672` 를 박아 써서
    **어두운 유리 위에 어두운 아이콘**이 놓였다. v3 스킨은 제 규칙이 흰색이라 안 보였고,
    다른 스킨에서 그대로 드러난다. 토큰 색(`--ink`/`--ink3`)으로 바꿔 세 스킨 다 밝아졌다.
    v3 의 선택 칩 유리(`--v3-glass-ic`·`--v3-glass-on`)도 어두운 값으로 덮었다.
    ⚠️ **고른 탭의 파랑·초록·보라 그라디언트는 그대로 둔다** — 그건 탭의 정체성이고
    어두운 배경에서도 흰 글자 대비가 충분하다.
    ⚠️ **이 판을 재는 데 함정이 있었다**: `.pn-add` 는 `background` 에 트랜지션이 걸려
    있는데 **이 세션은 프레임 합성이 없어 전이가 시작점에 멈춘 채로 남는다** — 처음 잰
    값이 옛 색이라 "안 바뀌었다" 로 보였다. 전이를 끄고(`transition:none`) 다시 재야
    참값이 나온다. 다음에 색을 잴 때도 같다.
  - **② 지면 카드를 사진만으로** — 컨텐츠 탭 › 지면 목록의 항목마다 `사진만` 체크칸.
    ⚠️ **글자칸과 그라데이션을 함께 접는다.** 그라데이션은 글자를 읽히게 하려고 있는
    것이라, 지역 이름만 빼고 그라데이션을 남기면 사용자가 적은 목적("그라데이션 처리가
    안 된 깨끗한 이미지")이 반만 이뤄진다.
    ⚠️ **값은 안 지운다** — 껐을 때 적어 둔 위치·제목이 그대로 돌아와야 "잠깐 감춘 것"
    으로 읽힌다(실측으로 확인).
  - **③ 데모 프로필 사진** — 관리자 › 표시 옵션 › **프로필 사진 (데모)**. 주소(https)나
    파일로 정하고, 비우면 여태 동작(계정 사진 → 이니셜)으로 돌아간다.
    ⚠️ **로그인 계정의 사진을 고치는 것이 아니다.** 우상단 원은 여태 `user.photoURL` 을
    그렸는데 시연에서 보이는 얼굴은 **그 계정의 얼굴이지 데모 속 인물의 얼굴이 아니고**,
    임베드에는 로그인이 아예 없어 늘 이니셜 'P' 였다. 이 값이 있으면 그것이 먼저다.
    파일은 **160px 정사각으로 가운데를 잘라** 담는다(원형 마스크와 같은 그림) — 설정과
    함께 클라우드로 오가는 값이라 원본을 실으면 문서가 커진다(지면이 900px 로 줄이는 것과
    같은 규칙). 실측 1.2KB.
  - 검증(로컬 :8765, 임베드 + admin.html):
    ① **전이를 끄고** 스킨 3종 × 다크에서 아이콘 밝기 대 배경 밝기를 쟀다 — 셋 다
    아이콘이 배경보다 0.25 이상 밝다(legacy 0.92 vs 0.14 · new 0.88 vs 0.12 · v3 1.0 vs 0.18).
    ② 체크칸이 서고 켜면 `.cps-bare` 가 붙어 그라데이션·글자칸이 `display:none` 이 되며
    사진은 그대로, localStorage 에 `bare` 가 남고, **끄면 위치·제목이 그대로 돌아온다**.
    ③ 관리자 칸 5개(주소·올리기·지우기·미리보기·파일)가 서고, `ftp://` 는 막히고(경고 1회,
    값 안 바뀜) https 는 실리며 우상단 원이 사진으로 바뀐다(이니셜 `display:none`).
    파일 압축은 **160×160 JPEG 1.2KB**. 지우기로 되돌아온다. 클라우드 스냅샷에 들어간다.
    콘솔 에러 0 · `node tools/check.js` PASS.
  - ⚠️ 색의 인상은 이번에도 픽셀로 못 봤다.

### 2026-08-18 (25)
- **v2.62.4 — 뒷배경은 흐림만 · 말머리는 연둣빛 · 다크 모드와 `theme` 액션 · burst 도 등장한다 · 폰 안 스크롤바를 감춘다 (콘솔 v0.156.0 짝)**
  사용자 요청 다섯. 전부 앱이고 둘(테마·뒷배경)은 **관리자 설정**까지, 하나(테마)는 **콘솔
  액션**까지 붙는다.
  - **① 뒷배경은 흐림만** — "팝업은 기존과 동일하게 (투명도를 안 줘도 괜찮아, 배경만 효과
    적용)". v2.62.3 이 얹은 어둡기가 **팝업 카드까지 어둡게** 만들고 있었다: 카드가 유리
    (`--frost` 0.6)라 그 뒤의 색이 그대로 비친다. 어둡기 기본을 **0** 으로 내리고 흐림을
    3 → **5px** 로 올렸다(어둡기가 하던 몫을 흐림이 든다). 어둡기 칸은 **없애지 않았다** —
    0 이 기본일 뿐이다.
  - **② 말머리는 연둣빛 파스텔** — 잉크 회색은 아래 의견 말풍선과 **같은 계열**이라 "한 톤
    짙다" 가 잘 안 읽혔다. 색이 다르면 한눈에 갈린다.
    ⚠️ **내가 올린 글이어도 같은 초록**이다: 이 자리는 "누가 썼나" 가 아니라 "이 대화가
    무엇에 관한 것인가" 를 말한다(누가 썼나는 바로 아래 이름줄의 몫이다).
    나머지는 v2.62.3 그대로 — 모서리·글자·여백은 아래 말풍선과 같은 값이다.
  - **③ 다크 모드 + `theme` 액션** — 앱에 테마가 **아예 없어서** 토큰부터 세웠다.
    색이 `:root` 한 곳에 모여 있는 구조가 이걸 싸게 만들었다: 토큰 한 벌을 덮으면 스킨
    3종이 따라온다. 그것만으로는 **앱바·상단 지면 프레임·서랍·요약 상자 넷이 흰 채로
    남았고**(실측), 스킨 둘이 **제 팔레트**를 들고 있는 것이 원인이었다 — 스킨 안쪽 규칙을
    하나씩 고치지 않고 **그 팔레트의 값**을 덮었다.
    ⚠️ **지도 타일은 안 바뀐다.** 이 지도는 클라우드 스타일(mapId)로 그려지는 벡터 지도라
    런타임 `styles` 가 무시되고 `colorScheme` 은 생성 시에만 받는다 — 바꾸려면 지도
    인스턴스를 다시 지어야 하고 그러면 오버레이·카메라·핀이 전부 재생성된다. 이 판은
    **앱 UI 의 다크**다. 관리자 › 표시 옵션 › **테마** 와 단계 액션 `theme`(v=dark|light,
    비우면 뒤집기)이 같은 값을 쓴다.
  - **④ burst 도 등장 효과를 낸다** — v2.12 가 안 붙였던 이유는 **깜박임**이었다: 그때는
    하나 깔 때마다 렌더가 통째로 돌아 이미 있던 오버레이까지 다시 지어졌고 그 재생성이
    바운스와 겹쳤다. **그 원인은 v2.61 에서 없어졌다**(`nhRenderSoon` 이 한 프레임에 한 번,
    이름표는 다시 안 지어진다). 그래서 이제 붙는다 — `drop` 과 **같은 표**(`nhBounceMark`)다.
    ⚠️ 소리는 그 표를 찍는 자리가 낸다(v2.23) — burst 가 따로 부르던 `nhSfxPlay()` 를 **뺐다**.
    안 빼면 한 항목에 **두 번** 운다.
  - **⑤ 폰 안 스크롤바를 감춘다** — 폰 목업 안의 막대는 실제 기기에 없는 물건이라 시연에서
    이물감이 크다. 구르는 것 자체는 그대로다(막대만 감춘다).
    ⚠️ **관리자 사이드바(#sidebar)는 아니다** — 그쪽은 PC 화면이고 막대가 정보다.
  - 검증(로컬 :8765, 임베드 + admin.html):
    ① 기본값이 `blur(5px)` · `rgba(…,0)` 이고 팝업 카드는 `rgba(255,255,255,.6)` 그대로.
    ② 말머리 배경 `rgba(126,200,140,.22)` + 테두리, 모서리·글자·여백은 아래 말풍선과 동일.
    ③ **단계 경로(`nhAct`)로**: dark·light·빈 값 뒤집기·모르는 값 뒤집기 전부 true,
    토큰이 갈리고(`--ink` #20242c ↔ #e8ebf0) 라이트는 속성을 지운다. **스킨 3종 × 다크로
    화면을 훑어 밝은 바탕이 남았는지 셌다** — 처음 4개 → 스킨 팔레트를 덮은 뒤 **0개**
    (v3 의 프로필 원 하나는 일부러 뒤집은 아바타다: 밝은 원 + 어두운 글자).
    관리자 고르개도 change 로 상태·속성·localStorage·클라우드 스냅샷이 함께 움직인다.
    ④ 6개짜리 burst 에서 **등장 표 6개**(id 전부 실물, 지도 수 2/2/1)와 **항목당 소리 1회**.
    ⑤ 폰 안 `scrollbar-width:none`.
    콘솔 에러 0 · `node tools/check.js` PASS.
  - ⚠️ 색의 인상은 이번에도 픽셀로 못 봤다. **다크에서 대비가 충분한지**와 **연둣빛 톤**은
    배포본에서 볼 것 — 둘 다 값 하나로 조절된다.

### 2026-08-18 (24)
- **v2.62.3 — 말머리는 그냥 말풍선 · 팝업 뒷배경을 사람이 정한다 · 리액션은 뿅**
  사용자 요청 셋. 전부 앱이고, 둘째는 **관리자 설정**까지 붙는다.
  - **① 말머리를 되돌렸다** — "너무 AI가 제작한 것처럼 만든 거 같아. 기존 말풍선 느낌이
    나도록 하되, 테두리나 버블 색 정도만으로 하이라이트."
    v2.62.2 가 왼쪽 기둥 + 각진 모서리 + 큰 글씨 + 가로 꽉 채움으로 눌러 놨던 것이
    **채팅창 안의 다른 물건**이었다. 남긴 것은 둘뿐이다: 한 톤 짙은 바탕(0.05 → **0.086**)과
    **1px 테두리**. 모서리·글자 크기·무게·안쪽 여백은 아래 의견 말풍선과 **같은 값**으로
    되돌렸다(실측: radius·font-size·weight·padding 네 값이 모두 동일, 왼쪽 시작점도 0 으로
    같다). 강조는 대비로만 한다.
  - **② 지도 위 팝업의 뒷배경 — 그리고 그 값을 관리자가 정한다** (`popScrim`).
    ⚠️ v2.29 는 이 덮개를 **일부러 없앴다**("지도를 계속 보여 줘야 이 지점의 것으로 읽힌다").
    그 판단은 지금도 맞다 — 그래서 되돌린 것이 아니라 **손잡이를 달았다**. 기본은 약하게
    (블러 **3px** · 어둡기 **18%**)이고, 둘 다 0 이면 v2.29 그대로다.
    같은 값을 **딜 시트 스크림도 쓴다** — 둘 다 지도 위에 뜨는 팝업이라 뒤가 서로 다르게
    덮이면 한 화면에서 규칙이 둘이 된다.
    관리자 › 🖥 화면·디자인 › 📐 표시 옵션 › **팝업 뒷배경**. `uiScale` 과 같은 즉시 적용·
    additive 클라우드 동기 패턴이고, 적용은 body 의 CSS 변수 한 벌이라 재렌더가 없다.
  - **③ 리액션은 바운스 없이 뿅** — "바운스는 사라지고 하트나 코멘트 이모지가 뿅 하고
    떴다가 사라지는 느낌으로 (바운스+사라지는 건 좀 긴 거 같아)."
    `heartPop` 은 커졌다 작아졌다를 두 번 하고(1.2 → .94 → 1.04 → 1) **1.15초**를 쓴다 —
    그 되돌아오는 마디가 곧 바운스다. 지도 위 리액션에 **제 곡선**을 줬다(`reactPop`):
    커지는 마디가 하나뿐이고 **0.62초**, 뜨는 거리도 -120% → **-76%** (짧은 시간에 멀리
    가면 "튕겨 나갔다" 로 보인다). 걷는 시각도 1200 → **700ms** 로 맞췄다 — 안 맞추면
    이미 사라진 글리프가 반 초 더 DOM 에 남아 50개짜리 단계에서 빈 span 이 쌓인다.
    ⚠️ **`.fc-heart`(피드 카드 더블탭 하트)는 여태 곡선 그대로다** — 그쪽은 내 손짓이고
    사진 한가운데 크게 뜨는 서명 같은 연출이라, 사용자가 말한 "지도 위 컨텐츠" 가 아니다.
    문서가 "같은 곡선" 이라고 적어 둔 자리를 일부러 갈랐으므로 그 이유를 CSS 에 남겼다.
  - 검증(로컬 :8765):
    ① 말머리와 의견 말풍선의 radius·font-size·weight·padding **네 값이 동일**, 왼쪽 시작점
    둘 다 0, 다른 것은 배경(0.086 vs 0.05)과 테두리(1px vs 0)뿐.
    ② 임베드에서 값 반영(3px/18% → 10px/45% → 0/0 즉시) · **딜 시트 스크림도 같은 값** ·
    상한 걷기(999→20px, 999→80%). **관리자 화면(admin.html)에서 사람이 바꾸는 길 그대로**:
    표시 옵션 › 팝업 뒷배경 칸이 서고, change 로 상태·CSS 변수·localStorage 가 함께 움직이며,
    범위 밖(999·-5)은 20·0 으로 걷히고 빈 값은 기본(3)으로 돌아온다. 클라우드 왕복은
    `settingsSnapshotFull`·캐시 복원·수신·cloudSave 네 자리에 배선했고 **옛 문서(필드 없음)는
    건드리지 않는다**(additive 확인).
    ③ `reactPop` **4마디/0.62초** 대 `heartPop` 6마디/1.15초, 하트·의견 둘 다 새 곡선,
    걷는 시각(700) ≥ 연출(620), `.fc-heart` 는 **1.15초 heartPop 그대로**.
    콘솔 에러 0 · `node tools/check.js` PASS.
  - ⚠️ 색·움직임의 인상은 이번에도 픽셀로 못 봤다 (브라우저 패널 미표시). 특히 **블러
    기본값 3px/18% 가 적당한지**는 배포본에서 눈으로 볼 것 — 관리자에서 바로 조절된다.

### 2026-08-18 (23)
- **v2.62.2 — 말머리가 줄을 맞춘다 · 의견 문구를 사람이 적는다 · `comment` 액션 (콘솔 D194 짝)**
  사용자 요청 셋. 첫째는 앱만, 나머지 둘은 두 저장소가 같이 움직인다.
  - **① 말머리 하이라이트 + 이모지 생략** — "스팟 메시지의 시작 메시지는 좀 더 하이라이트
    되면 좋겠고, 스팟메시지의 이모지는 생략해도 될거 같아 (메시지 얼라인이 안 맞는 느낌)".
    **얼라인은 진짜로 안 맞았다**: 아바타 26px + 간격 6px 때문에 말머리 말풍선만 왼쪽에서
    **32px 안쪽**에서 시작하고 아래 남의 의견은 0 에서 시작했다 — 같은 왼쪽 줄인데 시작점이
    둘이었다. 아바타를 떼자 저절로 맞았다(실측 32 → **0**). 이모지는 지도 말풍선과 팝업
    헤더가 이미 들고 있어 여기서 한 번 더 적을 것이 아니었다.
    빠진 아바타의 몫은 **무게**가 대신한다: 잉크색 왼쪽 기둥 3px + 짙은 바탕 + 글씨
    12.8px/600 → **14.4px/700**. 말풍선 모서리는 그대로다 — 채팅의 문법을 버리는 것이
    아니라 그 안에서 높이는 것이다.
  - **② `react` 의 의견 문구를 사람이 적는다** — 단계의 `n` 칸에 `|` 로 나눠 적는다.
    ⚠️ **안 적은 자리는 앱 표가 채운다**(사용자: "미작성시 랜덤으로"). 둘만 적고 다섯을
    달면 앞 둘이 내 말이고 나머지 셋은 여태 문구다 — 한 칸 적었다고 장면이 줄면, 문구를
    정하려다 개수를 잃는 셈이 된다.
    ⚠️ **`n` 의 전송 상한을 20 → 200 으로 넓혔다.** 짧은 의견 하나도 20자를 넘긴다.
    이 슬라이스는 전송의 상한일 뿐이고 **이름으로 쓰는 자리는 제가 다시 자른다**
    (nhLayFeed 가 20자) — 한 칸이 두 뜻을 가지므로 짧은 쪽 상한을 여기 둘 수 없다
    (sp 가 12 → 400 으로 넓어졌던 v2.39 와 같은 이유).
  - **③ `comment` 액션 — 사용자가 스팟 팝업에 의견을 쓴다** (v=쓸 말 · i=어느 스팟).
    `answer`(nhAnswerTyped)의 이웃이다 — 같은 손짓, 다른 창. 타이밍 규칙(t0·commitAt·per)도
    **같은 값**을 쓴다: 두 곳이 다르면 같은 연출이 다른 속도로 보인다.
    ⚠️ **스스로 안 닫는다** (사용자 요구 그대로 · v2.48 규칙): 방금 쓴 말이 대화에 앉는
    것까지가 이 장면이고, 얼마나 보여줄지는 대본이 정한다 — 뒤의 `popclose` 가 닫는다.
    커밋 뒤에는 팝업을 다시 열지 않고 `cpopRefresh` 로 목록만 다시 그린다(다시 열면
    스크롤과 입력 상태가 튄다 — 답장 칸을 새로 만들어야 하는 nhAnswerTyped 와 다른 점).
  - 검증(로컬 :8765 임베드) — **전부 단계 경로(`nhAct`)로** 밟았다 (v2.60.1 의 교훈:
    함수로 부르면 ReferenceError 도 "그냥 안 됨" 으로 보인다):
    ① 말머리 말풍선 시작점 32 → **0**(남의 의견과 같은 세로선), 아바타 DOM 0개,
    기둥 2.67px, 글씨 14.4px/700 대 12.8px/600.
    ② `n` 파서(빈 값·빈 칸 건너뜀·60자 자름) · nhSanitize 가 43자 `n` 을 **그대로** 나름 ·
    다섯 개를 달아 **앞 셋만 내 말**, 뒤 둘은 앱 표.
    ③ 콘솔이 보내는 모양 그대로 4단계(`pop`→`react`→`comment`→`popclose`)를 sanitize 부터
    재생까지: 전부 true·경고 0건, 중간 타이핑이 칸에 보이고, 내 말풍선(오른쪽)으로 앉고,
    입력칸이 비워지고, **`comment` 뒤에도 팝업이 열려 있다가 `popclose` 에서 닫힌다**.
    빈 말·없는 번호는 **false 로 드러난다**(조용한 성공 금지).
  - ⚠️ 지도 위 실렌더와 색·간격의 인상은 이번에도 못 봤다 (브라우저 패널 미표시).

### 2026-08-17 (20)
- **v2.60.1 — `dealon` 이 단계 경로에서만 던지고 있었다 (ReferenceError)**
  사용자: **"deal on 은 여전히 동작 안 하는 거 같아."**
  - 원인은 한 글자다: 디스패치가 `nhDealOn(st.i,fast)` 였는데 **`fast` 라는 변수가
    `nhAct` 안에 없다** (형제 액션은 전부 `st.fast` 를 쓴다). `exec()` 를 감싼 try/catch 가
    그 ReferenceError 를 삼켜서, 단계는 **조용히 실패**로만 보였다.
  - **함수는 멀쩡했다.** v2.59 검증이 `nhDealOn()` 을 **직접 불러** 통과시킨 것이 이 판을
    놓친 이유다 — 사용자가 쓰는 길은 함수가 아니라 **단계(`nhAct`)** 다. 이제 그 길로 다시
    확인했다: 무대에 `단계로 뿅 + 단계에서 켠다` 매장을 두고 `drop` → `dealon` 을 `nhAct`
    로만 불러 예약 → 딜중으로 바뀌는 것과 경고 0건.
  - 같은 실수가 더 있는지 디스패치의 `fast` 를 전수로 봤다 — 이 줄 하나뿐이었다. 내친김에
    이번 판에서 손댄 액션 14개(`pop`·`popclose`·`coupon`·`shopsay`·`drop`·`dealon`·
    `focus`·`like`·`page`)를 단계 경로로 쓸어 전부 true·경고 0건을 확인했다.
  - **교훈**: 새 액션은 **함수가 아니라 단계로** 검증한다. try/catch 가 감싼 자리에서는
    ReferenceError 도 "그냥 안 됨" 으로 보인다 — 문법 검사(`node --check`)도 못 잡는다.

### 2026-08-17 (19)
- **v2.60.0 — 번호는 컨텐츠 탭의 줄 번호다 (콘솔 D191) · 쿠폰 연출도 닫을 때**
  사용자: **"dealon 액션 동작 안 하는데, 혹시 이전 단계에서 drop 으로 매장을 호출해서
  dealon 방금 생긴 것에 체크가 안 된 걸까? dealon 뿐만 아니라 무엇을 띄울까는 전반적으로
  모두 불안정한데, 그냥 최근 단계에서 사용한 거 + 컨텐츠 탭에 있는 모든 리스트가 뜨고
  직접 선택할 수 있게 해 줘."** 진단이 정확했다.
  - **`nhSeedIds`(선언 번호 → 깔린 id)** — 무대를 깔 때 다섯 종류 모두 이 표에 적고
    (`nhSeedNote`), `nhPick` 이 `i>=0` 이면 **이 표로** 집는다. 여태는 '깔린 것 중 몇
    번째' 라, 같은 항목이라도 `hold` 를 켜면 그 목록에서 빠져 번호가 통째로 밀렸다.
  - **`nhDrop` 이 띄운 것을 표에 적는다** — 이 한 줄이 없어서 `drop` 으로 띄운 매장을
    `dealon`·`pop` 이 못 집었다. 사용자가 본 바로 그 자리다.
  - **`nhDrop` 의 번호도 줄 번호다** — 보관 목록 안의 순번이 아니라 `item.i`(선언 번호)로
    찾는다. 보관물이 아닌 번호를 주면 실패다(이미 화면에 있는 것을 지금 띄울 수는 없다).
  - **안 깔린 것을 가리키면 null** — 표에 있는데 값이 비어 있으면(선언은 됐는데 아직 안
    깔림) 그 단계는 실패로 드러난다. 가까운 것으로 떨어지면 "번호는 맞는데 엉뚱한 가게가
    열리는" 조용한 성공이 된다 (v2.36 이 클램프를 버린 것과 같은 이유).
  - **`nhOwn` 의 선언 순번 정렬을 되돌렸다** (v2.59 에 넣었던 것) — 그 목록은 이제
    `i:-1`(방금 생긴 것)만 쓰므로 **생성 순서**가 맞다. 선언 번호는 표가 맡는다.
  - **쿠폰 연출도 `popclose` 때** — v2.59 는 티켓을 누르는 순간 날렸는데 시트에 가려진 채로
    날아갔다. 누름(`nhTouch`)만 그 자리에 남기고 티켓·문구는 닫을 때 함께 온다.
  - **⏰타임딜 라벨**: 상호칩 **가운데 위**로 옮기고(`left:50%`) **흰 링을 뗐다** — 딜 칩과
    같은 테두리·그림자다. 흰 링은 "별개의 물건" 이라고 말하는데 이 둘은 한 덩어리다.
  - 검증(로컬 :8766): 매장 4개(처음부터 가게·뿅 가게·뿅 예약딜·처음부터 딜)를 깔고 —
    `dealon(2)` 가 **띄우기 전에는 false**, `drop('deal',2)` 로 띄운 뒤 **true**.
    `nhPick(0..3)` 이 컨텐츠 탭 줄 번호 그대로(안 깔린 1번은 `(없음)`).
    `coupon` 뒤 티켓 0개·시트 열림 → `popclose` 뒤 티켓 1개·문구·시트 닫힘.
  - ⚠️ 지도 위 실렌더는 이번에도 못 봤다 (브라우저 패널 미표시).

### 2026-08-17 (18)
- **v2.59.0 — 딜은 매장의 상태다 (매장 목록 통합 · `dealon` · 쿠폰은 popclose 가 닫는다)**
  콘솔 D190 짝. 사용자: **"컨텐츠 탭에서 매장을 추가하고 그중 어떤 건 타임딜 옵션을 걸 수
  있는 거야. 추가해야 할 단계 액션 중 하나가 매장 타임딜 활성화 액션이 있어야 해."**
  - **매장 목록이 하나가 됐다** — `nhPick('store')` 는 `deal` 로 접히고 보관함도 하나다
    (`nhHeld.store` 삭제). D164 가 둘로 가른 이유("타임딜 1번이 매장을 집는다")는 둘이
    **다른 물건**일 때의 처방이었다.
  - **가르는 동안 앱이 조용히 틀리고 있었다** — 까는 순서가 매장 먼저인데(딜의 `of` 가
    붙으려면 그래야 한다) 번호는 그 **생성 순서**였다. 콘솔은 배열 순서로 세므로 딜과
    매장을 섞은 데모에서 `매장 2번` 이 1번을 집었다. `nhLayDeal` 이 선언 순번(`si`)을
    적고 `nhOwn` 이 그것으로 정렬한다.
  - **`dealon` 액션** — `later:true` 인 매장은 `off:true` 로 깔려 이름표만 서고
    (`dealActive` 가 false), 이 단계가 그 칸을 걷는 순간 딜이 시작된다. **`ts` 를 지금으로**
    다시 찍는다 — 무대를 깐 시각부터 세면 앞 단계가 길었던 데모는 켜자마자 몇 분이 지나 있다.
    이미 켜진 것·그냥 가게를 고르면 **실패로 드러난다**(조용한 성공 금지).
  - **딜 중인 이름표를 누르면 타임딜 팝업**(`openDealSheet`)이다 — 딜이 없거나 끝났으면
    여태처럼 매장 페이지. 이름표가 딜이라고 외치는데 딜 없는 페이지가 열리면, 강조가
    가리킨 것과 열린 것이 다르다.
  - **`coupon` 이 스스로 안 닫는다** — `nhAfterClose` 에 문구를 적어 두고 `popclose` 가
    닫으며 그때 말풍선이 뜬다 (v2.48 이 컴포저에 세운 규칙 그대로). 티켓(`couponFly`)은
    지금 난다 — 누른 버튼의 대답이라서다. `claimDeal` 에서 문구 부분을 `couponSay` 로 갈랐다.
  - **맥동이 글자를 흔들던 것** — `box-shadow` 만 애니메이션했는데도 칩의
    `backdrop-filter:blur` 가 매 프레임 다시 합성돼 글자가 떨렸다. 링을 `::after` 로 빼서
    글자 면은 한 번도 안 건드린다. **⏰타임딜 라벨은 칩 위로 완전히 띄웠다**(`top:-16px`) —
    상호를 물고 여백이 없다는 지적.
  - **`nhSanitize` 에 `later` 를 뚫었다** — 처음에 빠뜨려서 무대가 딜을 **처음부터 켠 채로**
    깔고 `dealon` 이 "이미 켜져 있다" 로 실패했다. v2.15 의 `addr`/`desc` 와 같은 자리다
    (무대만 뚫고 sanitize 를 안 뚫었다). 검증에서 잡혔다.
  - 검증(로컬 :8766, 실제 `nhSanitize`+`nhSeedScenario`): 매장 3개(가게·처음부터 딜·예약)를
    깔아 `nhPick('deal',0..2)`·`nhPick('store',0..2)` 가 **선언 순서 그대로**(연무장길 국수 →
    모락커피 → 로스터리 온) · `dealon(2)` 이 예약을 켜고 재호출·그냥 가게는 false ·
    `coupon` 뒤 시트가 열린 채(대기 1건)이고 `popclose` 가 닫으며 그때 문구가 뜬다 ·
    이름표 탭이 딜=쿠폰 시트 / 가게=매장 페이지(`no-deal`) / 끝난 딜=매장 페이지.
  - ⚠️ **지도 위 실렌더는 이번에도 못 봤다** (브라우저 패널이 안 열려 구글맵이 프레임을
    안 그린다). 화면으로 볼 것: 맥동이 글자를 안 흔드는지 · 라벨 여백 · 켜지는 순간의 바운스.

### 2026-08-17 (17)
- **v2.58.0 — 매장과 타임딜은 한 가게다 (⏰ 원형 핀 삭제 · 딜 이름표 강조 + 미니 라벨)**
  콘솔 D189 짝. 사용자: **"매장과 타임딜을 통합하고, 설정값에서 타임딜을 체크하면 미니
  라벨에 '⏰타임딜' 라벨과 잔여 시간이나 할인 정보를 표시. 타임딜 하는 매장은 다른 매장
  대비 눈에 띄어야 할 것 같다. (기존에 동그란 아이콘 형태로 표시하는 건 삭제)"**
  - **`DealPin` 을 통째로 지웠다** — 클래스·`dealMarkers`·`.deal-pin` CSS(style.css·
    skin-v3.css)·declutter 의 딜 칸·dim 루프·히트테스트 선택자까지. 한 가게가 **핀과
    이름표로 두 번** 서던 것이 이 판의 뿌리다: 둘이 각자 자리를 잡아 겹쳤고, "딜을 하는가"
    는 핀만 알고 이름표는 몰랐다.
  - **`DealLabel` 이 다 한다** — 딜이면 상호칩이 딜 색으로 서고 2.4초 주기로 맥동하며
    (`.dl-live`), 어깨에 미니 라벨이 붙는다 (`.dl-tag`: `⏰타임딜` + `lab` 이 고른 칸).
    라벨은 컨텐츠의 `❤3 💬2` 와 **같은 문법**이다 — `paintTagCell`·`tagBg` 를 그대로
    나눠 쓴다. 맥동은 **box-shadow 로만** 한다: transform 은 `draw` 가 매 프레임 쓰는
    배율(labelScale × 이름표 크기 × 딜 크기)과 싸운다.
  - **`dealLabelTick`(1초)** — 남은 시간을 줄이고, **딜이 끝나는 순간 강조를 걷는다.**
    딜이 하나도 없으면 스스로 선다 (재생 중에는 렌더가 잦아, 매번 새로 걸면 티커가 겹쳐
    쌓여 같은 초를 여러 번 그린다).
  - **딜이 끝나도 가게는 남는다** (`dealShown` 을 넓혔다) — 여태는 0초가 되면 핀도 이름표도
    통째로 걷혀 "마감 순간 가게가 증발" 했다. 이제 강조와 라벨만 걷힌다.
  - **`dealName` 폴백**(상호 → 딜 제목 → `근처 매장`) 과 **`nhSanitize` 필터 완화**
    (상호 **또는** 제목) — 핀이 없어져 이름표가 유일한 자리가 됐으므로, 상호가 빈 옛 딜이
    지도에서 통째로 사라지는 것을 막는다. 콘솔 `toSeed`/`seedLayable` 과 같은 규칙이다.
  - **`lab` 칸 추가** (`time`·`pct`·`both`·`none`, 없으면 `time`) — `nhSanitize`·`nhLayDeal`
    양쪽을 통과한다. 모르는 값은 기본으로 떨어진다.
  - **burst 의 `dealpin` 제거** — 뜻이 "⏰ 아이콘만" 이었는데 그 아이콘이 없어졌다. 콘솔의
    `ACTION_INFO.burst.values` 에서도 같이 뺐다 (check:contract ⑥ 이 둘을 대조한다).
    ⚠️ 저장된 옛 `dealpin` 단계는 폴백 규칙대로 **기본 셋**(지도 글·피드·타임딜)을 쏟는다.
  - **이름표 겹침 우선순위**에 딜을 넣었다 — ①열린 매장 페이지 ②딜 중 ③한마디 ④나머지.
    여태는 먼저 온 평범한 가게에 딜 이름표가 가려 숨는 일이 있었다(그때는 ⏰ 핀이 대신
    서 줬다).
  - 관리자 지도 설정: `핀 크기` → **`딜 매장 크기`**(딜 중인 가게만 배율을 한 번 더),
    `할인율 라벨` → **`⏰타임딜 라벨`**. 저장 키(`deal.size`·`deal.label`)는 그대로 둔다 —
    localStorage·클라우드·settings-default.json 이 그 이름을 쓴다.
  - 검증 (로컬 :8766): `DealLabel.onAdd` 를 실제로 태워 3건 —
    딜은 `dl-live` + `⏰타임딜|14:24`, `lab:'both'` 는 `2:24 · 40%`, `'pct'` 는 `25%`,
    `'none'` 은 빈 칸, 딜 없는 가게는 라벨 숨김·강조 없음. `ts` 를 과거로 밀면
    `live:false`·라벨 숨김으로 뒤집힌다. `nhSanitize` 가 상호만/제목만 항목을 살리고 둘 다
    없는 것만 버린다. `nhBurstKinds('dealpin')` → 기본 셋. 티커가 재렌더에 겹치지 않는다.
  - ⚠️ **지도 위 실렌더는 못 봤다** — 브라우저 패널이 안 열려 구글맵이 프레임을 안 그렸고
    (오버레이 `onAdd` 는 지도 draw 에 매달려 있다), 그래서 강조색·맥동·라벨의 **자리와
    크기**는 화면으로 확인해야 한다. 특히 이름표가 스무 개 깔린 줌아웃에서 딜이 실제로
    도드라지는지, 라벨이 상호칩을 넘어 잘리지 않는지.

### 2026-08-17 (16)
- **v2.57.1 — 모드 전환은 누른 자리에서 터진다 (와이프 → 머테리얼 원형 리빌)**
  사용자: **"베이직/트렌드 모드 전환은 너무 느리고 효과가 잘 안 보이는 것 같아서 취소.
  머테리얼 방식으로."** 맞는 지적이다 — v2.57.0 의 가로 와이프는 0.62s 인데 띠의 **앞뒤가
  투명**해서 화면이 실제로 칠해지는 순간이 짧았다. 오래 끌면서 덜 보이는 최악의 조합이다.
  - **`runModeReveal`** — 누른 토글 버튼 자리에서 **그 모드의 색 원**이 터져 화면을 꽉 덮고
    흩어진다. 0.5s 이지만 **확산은 0.29s 에 끝나고**(그 지점에서 화면이 완전히 덮인다) 남은
    구간은 흩어지며 새 모드를 드러낸다. 덮이는 순간이 확실해서 "바뀌었다"가 눈에 박힌다.
  - **원점은 전달 인자가 아니라 실측이다** — `#phone-mode .pm-btn[data-mode]` 에서 읽는다.
    부르는 쪽이 폰 토글이든 PC 사이드바든 시연 대본이든, **사람의 눈이 가 있는 자리는 늘
    폰 토글**이다. 토글이 숨은 탭(피드·소셜)에서는 폭이 0 이라 지도 상단 한가운데로 폴백한다.
  - **반지름 = 원점 → 가장 먼 모서리** — 어느 자리에서 터져도 화면을 다 덮는다.
  - ⚠️ **width/height 가 아니라 `transform:scale`** 로 큰다 — 레이아웃을 건드리면 지도 위에서 끊긴다.
  - 검증(로컬 :8765, 375×812): 원점이 누른 버튼을 따라감(베이직 x=149.7 / 트렌드 x=224.9 —
    토글의 좌·우 자리 그대로) · 반지름 521px ≥ 최원거리 모서리 520.1px = **전면 커버 보장** ·
    색 실측(트렌드 `#ff4d67→#ff7a45` / 베이직 `#2b6ff0→#4a8bff`) · `modeReveal 0.5s` ·
    z4 / `pointer-events:none` · `#mode-wipe`·`runModeWipe` 잔여 참조 0건. `node tools/check.js` OK.

- **v2.57.0 — 화면이 어디로 갔는지 손에 남긴다 (탭 슬라이드 · 모드 와이프 · 접힌 만큼 따라온다)**
  사용자: **"map/feed/커뮤니티 전환이 조금 더 자연스러웠으면 · 베이직↔트렌드도 더 극적으로
  (스와이프 같은) · 지면을 접으면 축척·모드 토글도 그만큼 위로."** 셋 다 같은 결이다 —
  화면은 바뀌는데 **바뀌었다는 사실이 몸에 안 남는다**.
  - **① 탭 전환 = 방향성 슬라이드** (`animateTabSwap`) — 여태 `display:none/block` 한 줄이라
    화면이 순간이동했다. 하단 네비 순서(지도 0 · 피드 1 · 소셜 2)를 **그대로 방향으로** 쓴다:
    오른쪽 탭으로 가면 새 화면이 오른쪽에서 들어오고 이전 화면은 왼쪽으로 빠진다(0.34s).
    나가는 화면은 `.open` 이 이미 떨어져 있으므로 **`.tp-exiting` 이 display 를 대신 잡고**
    340ms 뒤에 걷는다(`#social-page` 만 flex 라 규칙을 하나로 못 묶는다).
    지도로 돌아올 때는 지도가 `scale(.985)→1` 로 드러난다.
    ⚠️ 구글맵 div 에 **영구 transform 을 남기면 좌표 투영·히트테스트가 밀린다** —
    `mapMorph` 와 같은 규칙으로 **identity 로 끝나는 one-shot** 만 건다.
  - **② 모드 전환 = 컬러 와이프 스윕** ~~(`runModeWipe` · `#mode-wipe`)~~ **→ v2.57.1 에서 머테리얼 원형 리빌로 교체(폐기)** — 색 띠가 지도 위를
    한 번 쓸고 지나가고 그 뒤로 새 모드가 드러난다(0.62s). **→트렌드는 웜톤이 좌→우,
    →베이직은 블루가 우→좌** — 토글에서 트렌드가 오른쪽 버튼이고 색도 그 버튼의
    그라디언트와 같으니, **누른 버튼이 화면을 칠했다**는 인과가 보인다.
    오버레이는 z4 = 지도·탭페이지(3) **위**, 헤더·모드 토글(5) **아래** — 누른 버튼은 띠 위에
    살아 있어야 한다. 기존 `mapMorph` 펄스·AI 아이콘 스핀·`nhSfxPlay('mode')` 는 그대로 두고 위에 얹었다.
  - **③ 지면을 접으면 축척·모드 토글도 접힌 만큼 올라온다** — 지면은 44cqw→15cqw 로 줄어드는데
    둘은 화면에 못 박힌 자리(69/70.1cqw)에 남아 **빈 띠**가 생겼다. 접기가 `body.sum-folded` 를
    심고 CSS 가 정확히 **29cqw** 올린다. v2.28 의 "토글은 화면에 못 박는다" 규칙은 그대로다 —
    캐러셀 종류·존 카드 높이 변화에는 여전히 안 움직이고 **사람이 명시적으로 접었을 때만** 따라온다.
    ⚠️ `:not(.mode-trend)` 가드 필수 — 트렌드 지도 탭은 지면 대신 존 리스트(44cqw)가 서므로,
    접힘이 남아 있으면 토글이 **존 카드 위로 파고든다**(존 요약은 접기 자체가 없다).
  - **`prefers-reduced-motion`** 에서는 ①②의 애니메이션과 ③의 트랜지션을 끈다 — 상태 전환 자체는 그대로 일어난다.
  - 검증(로컬 :8765, 375×812): 접기 리프트 실측 **108.66px / 108.65px = 29cqw 정확**(축척·토글 둘 다),
    접힘+트렌드는 원위치(258.51px)로 복귀 = 가드 동작. 탭 전환 3방향 클래스·애니메이션 실측
    (map→feed `tpInR` / feed→social `tpOutL`+`tpInR`, 나가는 페이지 display 유지 /
    social→map `tpOutR`+`mapSettle`). 와이프 색·방향 실측(→트렌드 `#ff7a45→#ff4d67` `modeWipeR` /
    →베이직 `#4a8bff→#2b6ff0` `modeWipeL`), z 순서 4/5/3 확인. `node tools/check.js` OK.
  - ⚠️ 로컬은 로그인 게이트 앞이라 구글맵이 안 뜬다(`map` undefined) — **모드 전환 실동작은
    배포본/실기기에서 확인 필요**. 위 검증은 DOM·CSS 실측이다.

### 2026-08-15 (15)
- **v2.56.0 — 사람이 만진 것을 알린다 (`nh:touch`, 콘솔 v0.147.0 짝, 콘솔 D186)**
  서베이 응답 화면이 **응답자가 데모에서 한 일**을 응답의 로그로 남긴다. 앱은 그 재료를
  보내기만 한다.
  - **① `nh:touch`** — 신뢰된 클릭(사람의 손)마다 `{unit, v}` 를 부모에게 보낸다.
    단위는 잠금과 같은 어휘(`nhLockHitFor`)이고 분류 밖은 `other` 다. 잠긴 자리를 눌러
    막힌 경우도 보낸다 — **막힌 것도 사람이 시도한 일**이다.
  - **② 지도 제스처는 따로** — 끌기·확대는 클릭이 아니라 문지기 그물에 안 걸린다.
    Maps 의 `dragend`·`zoom_changed` 에서 `map/pan`·`map/zoom15` 로 보낸다.
    지도는 이 함수보다 늦게 만들어질 수 있어 **생길 때까지 몇 번 더 두드린다**.
  - **③ 재생 중에는 안 보낸다** — 그때 화면을 움직이는 것은 대본이지 사람이 아니다.
  - **시각을 안 싣는다** — 받는 쪽이 제 시계로 적는다. 두 시계를 맞출 이유도, 앱이 절대
    시각을 들 이유도 없다. 좌표·화면에 적은 글도 안 보낸다.
  - 검증(로컬 :8765, 같은 오리진 자식 프레임): `.phone-screen` 에 문지기가 붙어 있고,
    자식이 `nh:touch` 를 보내면 부모가 `{unit:'tabs',v:'feed'}`·`{unit:'content'}` 로 받는다.
    `node tools/check.js` OK.

- **v2.55.0 — 이어 달리기 (`nh:run.keep`, 콘솔 v0.145.0 짝, 콘솔 D184)**
  사용자: **"5단계에서 6단계를 누르면 5→6단계만 재생하면 될 것 같은데."** 맞는 말이다.
  `nh:run` 은 늘 `nhStop`+`nhReset`+`nhSeedScenario` 부터 하므로, 콘솔은 앞으로 한 칸
  가려 해도 **0번부터 다시 조립**해 보낼 수밖에 없었다.
  - **① `nh:run` 이 `keep:true` 를 받는다** — 이때는 리셋·시드·카메라 초기화를 건너뛰고
    **받은 단계만** 잇는다. 돌던 회차는 `++nhRunToken` 이 끊으므로 `nhStop` 없이도 안 겹친다.
  - **② `nh:ready` 가 `keep:true` 를 알린다** — 콘솔이 **할 줄 아는 것을 보고** 길을 고른다.
    버전 문자열을 파싱시키지 않는 이유는 그것이 사람이 읽는 값이라서다. 옛 앱에서는 이 값이
    없어(=false) 콘솔이 저절로 옛 길(전체 조립)로 돈다 — 그래서 짝 배포 순서에 안 묶인다.
  - ⚠️ **뒤로 갈 때는 못 쓴다** — 앞 단계가 만든 것을 되돌릴 길이 없다. 그때는 콘솔이
    `keep` 없이 처음부터 보낸다.
  - 검증(콘솔 :3010 ↔ 로컬 앱 :8765): 2단계 클릭 → `nh:begin.total = 2`(조립),
    이어서 3단계 → **`total = 1`**(이어 달리기), 4단계 → `1`, 되돌아 2단계 → `2`(다시 조립).
    이어 달렸을 때도 콘솔의 `재생 중` 표시가 **3행**에 붙는다(번호 어긋남 없음).

- **v2.54.0 — 재생 중에는 사람 손이 안 닿는다 (콘솔 v0.143.0 짝, 콘솔 D182)**
  사용자: **"demo full play 시에는 아예 화면에 어떤것도 조작이 안되게 해줘."** 지금은 재생
  중에 마구 눌린다 — v2.53 의 문지기가 **재생 중이면 통째로 비켰기** 때문이다
  ("대본이 시키는 손짓은 사람의 손이 아니다"). 그 예외가 사람의 손까지 함께 열어 줬다.
  - **① 사람의 손만 가린다** — 대본은 `el.click()` 으로 누르므로 `isTrusted:false` 다.
    재생 중에는 **신뢰된(사람이 만든) 이벤트만** 막고 대본의 것은 지나가게 한다.
  - **② 제스처·스크롤은 그물에 안 걸린다** — 문지기는 `click`·`pointerdown`·`touchstart`
    만 본다. 그래서 재생 중에는 `body.nh-playing` 으로 `.phone-screen` 에
    `pointer-events:none` 을 건다: 지도 끌기·핀치·스크롤까지 함께 멈춘다.
    `el.click()` 은 히트 테스트를 안 거치므로 대본은 그대로 돈다.
    지도는 `nhLockMap()` 에서 재생 중을 함께 보고 `gestureHandling:'none'` 으로 내린다.
  - **③ 흔들기만, 안내는 없다** — 화면이 눈에 보이게 도는 중이라 이유가 분명하고,
    안내 문구("이 문항에서는…")는 서베이 문항을 전제로 쓴 말이라 여기서는 안 맞는다.
  - **④ 재생 상태는 한 자리에서만 바꾼다** (`nhPlaySet`) — 대입이 세 군데 흩어져 있어서
    한 곳을 빠뜨리면 "재생이 끝났는데 안 눌린다" 가 난다.
  - 검증(로컬 :8765 임베드): 재생 시작 → `body.nh-playing` · `.phone-screen` 의
    `pointer-events:none`, 끝나면 `auto` 로 복귀. **그 사이 대본은 그대로 돈다** —
    탭이 map→feed→social→map 으로 옮겨가는 것을 스냅샷으로 확인.
    `node --check app.js` 통과.

- **v2.53.0 — 응답자가 만질 수 있는 것을 문항이 정한다 (M16, 콘솔 v0.133.0 짝, D172)**
  서베이 문항에 붙는 데모 화면에서 **부분 허용**이 필요해졌다. 콘솔에서 iframe 에 덮개를
  씌우는 것으로는 전부 막거나 전부 여는 것뿐이라 "지도만 움직이게" 를 못 만든다 — 앱이
  제 UI 를 잠가야 한다.
  - **① `nh:lock` 수신** — `{allow:[...]}` 의 것만 허용, 배열이 아니면 전부 허용.
    답으로 `nh:locked` 를 돌려준다. **모르는 키는 버린다**(nhSanitize 와 같은 규칙).
    ⚠️ **잠금이 안 오면 전부 허용**이다 — 옛 게시본에는 이 값이 없고, 없다고 잠그면
    지금 도는 서베이가 조용히 먹통이 된다.
  - **② 일곱 단위**: `map`(지도 이동·줌) `tabs`(하단 탭) `mode`(렌즈) `content`(핀·카드 열기)
    `add`(＋ 올리기) `drawer`(메뉴·AI) `page`(상단 지면). 콘솔의 토글과 1:1 이고
    `check:contract` 가 두 목록을 대조한다.
  - **③ 문지기 하나로 거른다** — 일곱 곳에 각자 게이트를 넣으면 하나를 빠뜨리고, 새 버튼이
    생길 때마다 또 넣어야 한다. 폰 화면 뿌리에서 **캡처 단계로** 받아 원래 핸들러보다 먼저
    가로챈다. 지도만 예외다(클릭이 아니라 제스처라 Maps 의 `gestureHandling` 으로 잠근다).
  - ⚠️ **재생 중에는 안 막는다** (`nhPlaying`) — 대본이 시키는 손짓은 사람의 손이 아니다.
    여기서 막으면 시나리오가 제 화면을 못 만든다.
  - **④ 흔들기 → 반복하면 안내** — 조용히 무시하면 고장인 줄 알고, 누를 때마다 문구를
    띄우면 시끄럽다. 4초 안에 3번 누르면 **무엇이 되는지** 한 줄 알린다
    ("이 문항에서는 지도 움직이기만 할 수 있어요").
  - 검증(로컬 :8765 임베드): 잠금 없을 때 탭 클릭 통과 · `['map']` 로 잠그면 탭 클릭이
    실제로 차단되고 `nhCan(map)=true·nhCan(tabs)=false` · body 에 `nh-lock-*` 여섯 개 ·
    1회는 흔들기만/3회에 안내 · 전부 잠금이면 "화면을 보기만 합니다" · `node tools/check.js` PASS.

### 2026-08-15 (14)
- **v2.52.0 — 소리는 이 서비스의 것이다 (M16, 콘솔 v0.127.0 짝, D168)**
  사용자 요구: "프로젝트 단위로 소리가 통합으로 쓰였으면 좋겠다 — now-here-demo 웹서비스
  자체에 소리를 넣을 수 있게, 관리자 콘솔에서 관리." 여태 소리는 **데모마다** 붙는 값이라
  (persona-vc 의 `seed.sfx`) 같은 서비스를 보여주는 데모가 여럿인데 하나씩 따로 올려야 했고,
  안 올린 데모는 조용했다.
  - **① 저장소에 기본음 7개** (`sfx/*.wav`, 합계 33KB) — 합성해서 담았다. 외부 호스팅에
    안 기댄다(시연 중에 죽으면 조용해진다). `nhSfxSrc` 가 **`sfx/이름.확장자` 만** 받게
    넓혔다: 폴더 하나·영숫자 이름·확장자 넷으로 좁혀서 `javascript:`·`//evil.com`·`../` 가
    들어올 자리를 아예 없앴다(실측으로 넷 다 막히는 것을 확인).
  - **② 관리자 콘솔에 `🔊 효과음` 7칸** — 미리듣기(▶)·`↺ 기본 소리로` 포함. 못 쓰는 주소를
    넣으면 **칸에서 지워진다**("적었는데 왜 소리가 안 나지" 를 그 자리에서 알린다).
    값은 `settingsSnapshotFull`·`cloudSave`·`📋 설정 JSON 복사` 셋에 다 실린다.
  - **③ 우선순위: 앱이 기본, 데모가 그 자리만 덮어쓴다.** `nhSfxSet` 이 인자가 비어도
    **더는 끄지 않는다** — 그것은 "이 데모는 소리 설정이 없다" 이지 "조용히 하라" 가 아니다.
  - **④ `nhSfxPlay` 가 은행이 비면 늦게 채운다.** 이 함수는 재생 밖에서도 불린다(시트·팝업·
    렌즈 전환·타이핑·셔터) — 여태는 `nhSfxSet` 이 회차 시작에서만 채워서 **평소의 앱은
    조용했다.** 늦게 채우면 설정이 언제 도착하든(캐시·파일·클라우드) 알아서 맞는다.
  - ⚠️ **`nhSfxMute` 를 새로 뒀다** — v2.25 의 "되돌리는 동안은 무음" 은 **은행을 비우는
    것**으로 구현돼 있었는데, ④가 그 방법을 무력화한다(비면 스스로 채우니까). 그대로 뒀으면
    회차를 끝낼 때마다 "삐- 딸깍 스윽" 하고 울었다. `nhReset` 이 문을 잠그고 **finally 에서**
    연다 — 중간에 던지면 앱이 영영 조용해진다.
  - 검증(로컬 :8765): 기본음 7개가 `sfx/…` 경로로 실제 재생 · 데모가 pop 만 덮으면 pop 은
    데모 것·tap 은 앱 기본 · 데모 소리 없으면 전부 앱 기본 · 관리자가 바꾸면 그 자리부터
    새 소리 · 음소거 창이 실제로 막음(`open` 두 번 중 한 번만 울림) · 나쁜 주소 4종 차단 ·
    관리자 화면 콘솔 에러 0 · `node tools/check.js` PASS.

### 2026-08-15 (13)
- **v2.51.0 — 엔딩에 쏟아지는 것이 여섯 종류 · 타임딜의 아이콘과 이름표를 갈라 고른다 (M16, 콘솔 v0.126.0 짝, D167)**
  사용자 규칙이 하나 섰다: **"drop 이나 burst 에서 고를 수 있는 컨텐츠는 컨텐츠 탭에 있는
  것 중 맵에 표시되는 건 모두"**. 그 규칙으로 재 보니 burst 가 둘을 놓치고 있었다.
  - **① `req`(현장 Request)가 쏟아진다** — `nhBurstKinds` 가 여태 `spot|feed|deal|store`
    넷만 받아 **콘솔에서 고를 수도 없고 보내도 버려졌다**. 표본 `NH_BURST_REQS`(묻는 말 8개)를
    새로 두고 `nhLayReq` 로 깐다. 전부 `mine:false` 다 — 엔딩에 쏟아지는 물음이 전부 내
    것이면 "동네가 묻는다" 가 아니라 "내가 도배했다" 가 된다.
  - **② 타임딜이 셋으로 갈린다** — 앱에서 ⏰ 핀은 `dealActive` 가, 이름표는 `dealShopName`
    이 **따로** 정하는데 콘솔에서 그 둘을 고를 길이 없어 늘 아이콘+이름이었다.
    `deal`=아이콘+이름(여태) · **`dealpin`=아이콘만**(신설) · `store`=이름만(v2.49).
    `dealpin` 은 `shop` 을 **안 준다**: nhLayDeal 이 채우는 '근처 매장' 을 dealShopName 이
    "콘솔이 안 줬다" 로 읽어 이름표를 안 건다(v2.32 규칙을 그대로 탄다).
  - **왜 새 칸이 아니라 값인가**: burst 는 `e`(줌)·`sp`(밀집도)·`n`(사진)이 이미 다 찼다 —
    D165 에서 B안을 접은 그 제약이다. multi 값에 하나 얹으면 스키마를 안 건드리고 저장된
    옛 단계(`deal`)의 뜻도 그대로다.
  - **빈 값의 뜻은 안 바꿨다** — `nhBurstKinds` 는 여전히 `['spot','feed','deal']` 로 떨어진다.
    저장된 데모의 `v` 가 대개 비어 있어서, 늘어난 종류를 끼우면 옛 엔딩에 없던 것이 갑자기 깔린다.
  - ⚠️교차: 콘솔 `tools/check-contract.mjs` 에 **burst 종류 어휘 대조**를 새로 넣었다.
    이 어긋남(콘솔엔 있는데 앱이 모름)이 여태 아무 검사에도 안 걸렸다 — 사람이 "왜 없지" 하고
    물어봐야 발견되는 자리였다. 일부러 `req` 를 빼서 검사가 실제로 잡는 것까지 확인했다.

### 2026-08-15 (12)
- **v2.50.0 — 액션 어휘 정리 A: 한 축의 양끝을 한 액션으로 (M16, 콘솔 v0.124.0 짝, D165)**
  액션 38개를 검토한 결과 중 **잃는 표현 없이 줄일 수 있는 것**만 반영했다(A안).
  - **① `dim` 이 되돌리기까지 든다** — `v:'off'` = 원래 밝기로. 흐리기와 원복은 한 축의
    양끝이라 액션을 둘로 두면 목록만 길어진다. `undim` 은 **계속 받는다**(저장된 단계).
  - **② `popclose` 가 무엇을 닫을지 고른다** — `''`(기본)=팝업·시트·글쓰기 카드 ·
    `'bubble'`=우하단 말풍선 · `'all'`=둘 다. 여태 말풍선은 `bubbleclose` 라는 **다른
    액션**이었는데, 둘은 "지금 걷는다" 라는 한 손짓의 대상만 다르다. `bubbleclose` 도 계속 받는다.
    ⚠️ 첫 값이 **빈 문자열**이어야 한다 — 저장된 단계는 전부 `v:""` 이고 콘솔의 `withAction`
    이 액션을 갈아탈 때 첫 값을 넣으므로, 그 값이 곧 여태의 뜻이어야 한다.
  - **③ 손대지 않은 것**: `hearts`·`addbtn` 은 이미 은퇴 상태였다(고르개 hidden + AI 프롬프트
    부재) — 어휘에서 지우면 `Record<PlayAction,ActionInfo>` 가 깨지고 옛 단계가 편집기에서
    터진다. **감춰 두는 것이 제거보다 안전하고 결과가 같다.**
  - 검증(임베드 :8765): `dim v:'off'` 와 `undim` 이 **같은 상태**로 끝난다(nhDimIds 소멸) ·
    `popclose v:'bubble'` 은 말풍선만 걷고 · `v:''` 는 말풍선을 **안 건드리며**(여태 동작) ·
    `v:'all'` 은 둘 다 · `bubbleclose` 도 그대로 동작 · `node tools/check.js` PASS · 콘솔 에러 0.

### 2026-08-15 (11)
- **v2.49.0 — 매장이 제 종류로 선다 · 딜을 매장에 붙인다 · 닫기에는 손을 안 그린다 (M07/M16/M17, 콘솔 v0.123.0 짝, D164)**
  사용자 5건 중 앱 몫 4건.
  - **① 닫기에는 손을 안 그린다.** `popclose` 의 터치 표식을 뺐다. 표식은 "사용자가 무엇을
    눌렀나" 를 말하는 자리인데 닫기는 그 장면의 끝일 뿐이고, v2.48 부터 이 단계가 글쓰기
    카드까지 닫으므로 **눌릴 버튼이 하나로 정해지지도 않는다**(닫기 버튼·시트 ✕·카드 —
    아무거나 하나에 손이 뜨면 나머지는 거짓말이 된다).
  - **② (버그) `drop v:'deal'` 이 매장을 집었다.** 저장은 `deals` 한 배열인데(D155) 보관함도
    한 통이라, 콘솔이 딜과 매장을 **두 목록으로** 세는 것과 어긋났다 — "타임딜 1번" 이 매장을
    집는 자리다. 이제 `nhHeld.store` 가 따로 있고 `drop v:'store'` 로 꺼낸다.
    ⚠️ `i` 는 여전히 **배열 순번 그대로**다 — 자리(NH_BAND)와 원가(9900+i*5000)가 그것으로
    정해지고, 아래 `of` 도 이 번호로 가리킨다. 깔린 것(`nhPick`)은 여태처럼 한 배열이다.
  - **③ 딜을 매장에 붙인다** (`of` = 매장의 번호). 여태 딜과 매장은 남남이라 "그 가게에서
    하는 딜" 을 만들려면 같은 상호를 두 번 적고 자리도 손으로 맞춰야 했고, 그러고도 지도에
    이름표가 **둘** 섰다. 이제 매장이 이미 서 있으면 **그 항목을 딜로 승격**한다 —
    핀 하나, 이름표 하나, 매장 페이지에 쿠폰까지. 아직 보관 중이면 매장의 상호·이모지·주소·
    소개·사진·자리를 물려받아 혼자 선다(둘 다 "그 가게의 딜" 로 읽힌다).
    **매장을 먼저 깐다** — 딜이 `of` 로 가리킬 때 그 매장이 이미 서 있어야 붙는다.
  - **④ burst 에 매장** — `store` 종류가 늘었다(딜 없이 이름표만 = 상권이 깨어나는 그림).
    ⚠️ **빈 값·모르는 값은 여태 셋 그대로**다(store 를 안 끼운다) — 저장된 데모의 `v` 가 대개
    비어 있어서, 기본값에 넣으면 옛 엔딩에 없던 가게 이름표가 갑자기 깔린다.
  - 검증(임베드 :8765 `?embed=1&clean=1`): 매장2+딜2 무대에서 **딜이 매장에 병합돼 핀이 1개**
    (이름 `강남원주추어탕` + 딜 제목·30%) · 보관함이 갈림(`heldDeal=['독립 딜']`·`heldStore=['블루엘리펀트']`) ·
    `drop v:'store' i:0` = 매장 · `drop v:'deal' i:0` = **딜**(버그 재현 지점) · 같은 것 두 번은 실패 ·
    `nhBurstKinds('spot+store')`=['spot','store'] / 빈 값=여태 셋 · `nhTouchTarget({a:'popclose'})`=null ·
    `node tools/check.js` PASS · 콘솔 에러 0.
  - **이 환경에서 못 밟은 것**: 지도 위 실제 그림(딜이 붙은 이름표에 ⏰ 핀이 서는지, burst 로
    쏟아지는 매장 이름표가 겹침 방지를 타는지)은 오버레이가 안 그려져 미검증 — 실기기 확인 필요.

### 2026-08-15 (10)
- **v2.48.1 — (버그) addreq 가 아무것도 안 남겼다 · 채택 폭죽을 "공이 터지는" 것으로 (M04/M07/M16)**
  v2.48 을 써 본 사용자 보고 2건.
  - **① (버그) `addreq` 가 타이핑만 하고 끝났다** — 핀도 말풍선도 없었다.
    v2.48 은 등록을 **여태 자리(`commitAt` 타이머)** 에 두고 닫기·말풍선만 미뤘다. 그런데 이 단계는
    이제 "타이핑까지" 이고 닫는 것은 **다음 단계**(`popclose`)라, 닫기가 타이머보다 먼저 오면
    `ov.closed` 가 참이 되어 타이머가 그대로 물러났다 — **등록할 기회 자체가 없었다.**
    (짧은 `ms` 든 빠른 `popclose` 든 같은 자리로 떨어진다.)
    이제 커밋·핀·말풍선이 **한 자리**에 있다: 카드를 닫는 그 순간(`finishReq`).
    **등록은 액션이 시작할 때 곧바로 적어 둔다** — 타이머 안에서 적으면 그 타이머가 돌기 전에
    닫힌 회차는 적을 기회가 없다(그것이 이 버그의 정체다). `addspot` 도 같은 구조라 같이 고쳤다.
    - 곁따라: 타이핑 창이 **이 단계의 끝까지**로 늘었다(예전엔 등록 시점 앞까지) · `popclose` 는
      **뒷일을 먼저** 돌린다(카드를 먼저 걷으면 등록할 글이 든 칸이 사라진 뒤에 등록이 돈다) ·
      `SpotComposer.commit({text})` 로 **칸이 아니라 손에 든 글**을 등록한다(칸이 없는 프레임에서
      빈 글이 올라가던 자리 — 이 브라우저에서 실제로 재현됐다).
  - **② 채택 폭죽이 "빨려 들어가는" 느낌이었다** — 사용자가 "작은 폭죽과 원이 사라지는 기존
    효과가 중첩된 느낌" 이라고 한 자리. 원인은 **방향이 반대**였던 것이다: 껍질은 `scale(.2)` 로
    오그라들고 조각은 밖으로 나갔다. 이제 한 사건으로 읽힌다 —
    살짝 움츠렸다(예비 동작) **부풀며 사라지고**(껍질은 70% 지점에 이미 opacity 0),
    그 순간 조각 12개가 **부푼 껍질 밖까지**(46~70px, 껍질 최대 지름 ≈60px) 나간다.
    링도 22% 까지 제자리에서 기다렸다가 터지는 프레임부터 퍼진다. 답글 수 라벨은 터지기 전에 빠진다.
  - 검증(임베드 :8765 `?embed=1&clean=1`): `addreq`(ms 1200) 뒤 **단계 중에는 Request 0개**,
    `popclose` 뒤 1개(글 온전 · `stage` · 회차 임시목록에 적힘)와 「📍 Request 전송!」 말풍선 ·
    바로 뒤 `i:-1` 이 그 Request 를 집는다 · `addspot` 도 같은 순서(글·이모지 온전) ·
    **사람이 직접 쓰는 길은 그대로**(인자 없이 부르면 칸에서 읽고 그 자리에서 닫힌다) ·
    폭죽은 애니메이션을 t=0.19s/0.30s 에 **고정해** 눈으로 확인(팡 하는 순간 조각이 사방으로,
    0.3s 에는 껍질이 사라지고 조각만 남는다) · `node tools/check.js` PASS · 콘솔 에러 0.

### 2026-08-15 (9)
- **v2.48.0 — 팝업을 닫는 것은 대본이다 · 답변자 이름 · 답변 알림 짧게 · 채택 폭죽 (M04/M07/M16, 콘솔 v0.122.0 짝, D163)**
  사용자 4건. 넷 다 "그 화면을 얼마나 보여줄지 내가 정하고 싶다" 는 한 줄기다.
  - **① `answers` 에 응답자 이름.** 줄 형식이 `글\t사진주소` → `글\t사진주소\t이름` 으로 늘었다(`nhAnsPlan`). 글·사진과 **같은 규칙**이다: 안 적은 칸만 앱이 고른다(`NH_ANS_WHO`). 이 이름이 팝업의 답 옆과 채택 말풍선(`…님께 전달됐어요`)에 그대로 쓰인다.
  - **② 답변 알림은 짧게.** `📍 <동네> 답변 도착: <답 내용>` → **`💬 Request 답변이 달렸어요!`**. 말풍선이 답을 미리 읽어 주면 **다음 단계(팝업 열기)가 할 일이 없다** — 알림의 몫은 "가서 봐라" 까지다.
  - **③ 팝업은 `popclose` 가 닫는다** (이 판의 뼈대). 여태 컴포저는 등록이 끝나면 스스로 닫혔고(`addreq`·`addspot`·`request`) 채택은 900ms 타이머로 핀을 터뜨렸다 — 그래서 **보여줄 시간을 단계가 정할 수 없었다**. 이제 뒷일을 `nhOnClose` 에 적어 두고 `popclose` 가 실행한다:
    - `addreq`/`request` → 카드가 남는다. 닫히면 「📍 Request 전송!」 말풍선. `commitFieldRequest` 는 `quiet`(무대)일 때 그 자리에서 말하지 않는다 — 카드가 떠 있는데 "보냈다" 고 하면 손에 든 것을 이미 보냈다고 말하는 셈이다. **사람이 직접 올리는 길은 여태 그대로**(카드가 그 자리에서 닫히므로 지금 말한다).
    - `addspot`/`write` → `SpotComposer.commit({hold:true})` (additive 인자 — 사람이 쓰는 길은 인자 없이 불러 여태처럼 닫힌다).
    - `adopt` → ✅ 표는 지금 팝업 안에 남고, **말풍선과 핀 걷기는 닫힌 뒤**다.
    - `popclose` 가 컴포저 두 종류도 닫는다(여태는 딜 시트·매장 페이지·상세 팝업만).
    - ⚠️ `nhReset` 은 이 판을 **실행하지 않고 버린다** — 앞 회차의 뒷일이 새 회차 화면에서 터지면 없던 말풍선이 뜬다.
  - **④ 채택하면 핀이 불꽃과 함께 걷힌다.** 물음표 원이 오그라드는 것만으로는 "사라졌다" 인지 "가려졌다" 인지 안 읽혔다. `reqSparkBurst` 가 핀 **안에** 조각 10개를 붙인다 — 핀이 렌더에서 걷히면 조각도 같이 사라져 따로 치울 손이 없다(코인 버스트와 같은 `--dx/--dy` 문법, 지도 오버레이라 px).
  - 검증(임베드 :8765 `?embed=1&clean=1`): 이름 파싱 3줄(`김이웃`/자동 `근처 주민`/`박현장`)·이름만 적은 줄도 글은 앱이 채움 · 알림 문구 `💬 Request 답변이 달렸어요!` · **adopt 직후에는 말풍선도 핀 걷기도 없고**(`nhOnClose` 1건 대기) `popclose` 뒤에 둘 다 일어남(`rewarded` false→true, 큐 비움, **내 코인 불변**) · `commitFieldRequest` 가 quiet 면 말 안 하고 사람 경로는 말함 · 불꽃 10개가 `rpSpark` 로 뜸 · `node tools/check.js` PASS · 콘솔 에러 0.
  - **이 환경에서 못 밟은 것**: 컴포저 카드 자체(ReqComposer·SpotComposer)는 지도 오버레이라 이 브라우저에서 안 그려진다 — **카드가 실제로 떠 있는 채로 다음 단계가 흐르는 그림**은 실기기 확인이 필요하다. 그리고 **옛 데모 중 `addreq`·`addspot`·`adopt` 뒤에 `popclose` 가 없는 것은 카드가 뜬 채로 남는다** (의도된 변경이지만, 저장된 데모를 한 번씩 재생해 확인할 것).

### 2026-08-15 (8)
- **v2.47.0 — 미니 라벨 숫자·톤 · 의견도 튄다 · 방금 만든 것을 세워 본다 · 지도 요소 스타일 (M00/M04/M05/M07/M11/M16, 콘솔 v0.121.0 짝, D162)**
  사용자 4건.
  - **① 카운트 숫자는 흰색, 라벨은 연한 톤.** `❤3` 이 **글자열 하나**라 `.mine` 색이 숫자까지 물들였다 — "내가 눌렀나" 와 "몇 개인가" 는 다른 정보인데 한 색이 둘을 덮었다. 라벨 한 칸을 `.tg-e`(이모지) + `.tg-n`(숫자)로 나눈다(`paintTagCell`) — **색이 붙는 것은 이모지뿐이고 숫자는 늘 흰색**이다. 자식은 한 번만 만든다: 이 함수는 팬·줌·리액션마다 불려서, 매번 innerHTML 을 새로 쓰면 Twemoji 가 갈아 끼운 `<img>` 가 그때마다 버려진다(깜박임).
    - 톤은 **한 곳**에서 정한다(`tagBg`). 여태 말풍선은 JS 가 불투명하게 박고(alpha 1) 피드 핀·Request 핀은 CSS 가 단색으로 들어, 톤을 바꾸려면 세 자리를 따로 고쳐야 했다. 기본 72%. ⚠️ 트렌드는 `--heat` 가 **hex** 라 CSS 에서 알파를 못 얹는다 — 알파가 붙은 짝(`--heat-a`)을 JS 가 같이 싣는다.
  - **② 의견도 튄다, 각자 제 이모지 위에서.** v2.40 은 "하트만 튄다 — 의견은 숫자가 오르는 것이 전부" 였는데, 같은 라벨에서 같은 일이 벌어지는 두 수 중 하나만 반응하면 오르는 쪽을 놓친다. `reactPopOn(el,glyph)` 로 넓히고 **부르는 쪽이 칸을 넘긴다**(`tagH`/`tagC`) — 라벨 통째가 아니라 그 칸 위에서 터지므로 `❤3 💬2` 가 나란히 선 라벨에서도 **어느 수가 올랐는지 자리로 읽힌다**(실측 x 간격 20px). Request 핀의 답글 수(`.rp-tag`)도 같은 사건이라 같이 튀되 **첫 페인트에는 안 튄다** — 이미 답이 달린 핀이 화면에 들어올 때마다 터지면 "지금 왔다" 가 거짓말이 된다.
  - **③ 매장을 추가하면 그 자리에 바로 선다** (`nh:peek`, 콘솔 D162). 컨텐츠 탭의 추가 버튼은 목록에만 한 줄을 만들었다 — 화면에서는 아무 일도 없어서 "추가했는데 어디 있냐" 가 됐다. 이제 지금 보고 있는 지도 **한가운데**에 이름표가 뜬다. 상호가 비어 있으면 **'추가된 매장'** 이다: `dealShopName` 이 빈 이름에 이름표를 안 걸므로(v2.32) 그대로 두면 여전히 아무것도 안 뜬다.
    - **임시다**, 세 가지 뜻에서: 하나만 서고(다음 peek 이 앞의 것을 지운다) · `nhTempIds.deal` 에 적혀 재생·리셋이 무대와 같이 걷어가고 · 저장소에 안 남는다. 그래서 다음 회차에 유령으로 안 남는다. 끌어 옮기면 여느 이름표처럼 자리를 기억한다(`DealLabel._onDown`) — 추가하고 바로 자리를 잡는 손이 이어진다.
    - **재생 중에는 안 받는다** — 그때 지도 위에서 벌어지는 일은 대본이고, 거기에 목록 편집이 끼어들면 시연이 거짓말을 한다(`nhPlaying` 신설: 여태 앱은 토큰만 있고 "도는 중인가" 를 묻는 자리가 없었다).
  - **④ 지도 위 요소의 크기·색·투명도를 콘솔에서** (s-pins 확장). 손잡이가 없던 둘이 대상이다 — **가게 이름표**(크기·글자색·배경색·배경 투명도)와 **Request 아이콘**(색·투명도), 그리고 리액션 미니 라벨의 톤. 적용은 CSS 변수 한 벌(`applyPinStyle`)이라 재렌더가 없고 스킨 3종에 그대로 걸린다.
    - ⚠️ **Request 아이콘 색만 인라인**이다 — 스킨 3종이 `.rp-drop` 의 background 를 각자 덮으므로(v3=검정 원) style.css 의 변수로는 못 이긴다. 빈 값이 "스킨 기본" 이라 되돌릴 손잡이(`스킨 기본` 버튼)를 같이 뒀다: 팔레트에는 '없음' 이 없어서 한 번 고르면 돌아갈 길이 없었다.
    - `mergePinView` 가 색(`#rrggbb`)·투명도(0~100)를 알게 되고, 왕복 6지점은 여태 자리 그대로다(additive — 옛 문서는 기본값 유지).
  - 검증(임베드·관리자 :8765, 마크업 실측): 숫자 `rgb(255,255,255)`·이모지만 `rgb(255,92,120)`(mine)·라벨 `rgba(138,145,157,0.72)` · 팝 두 개가 각자 `.tg-h`/`.tg-c` 의 자식이고 20px 떨어져 뜬다 · `paintTagCell` 재호출이 자식을 다시 안 만든다 · `nhPeek` 이 지도 중심에 서고 이름이 '추가된 매장' 이며 `nhTempIds.deal` 에 적히고 **두 번 불러도 하나** · `nhPeekClear` 가 걷는다 · s-pins 새 컨트롤 8개가 다 서고 값→CSS 변수(`--tag-a .3`·`--dln-bg rgba(26,26,26,0.5)`·`--rq-o .4`·`nameScale 1.5`)·localStorage·`settingsSnapshotFull` 까지 왕복 · 색을 비우면 `--rq-c` 가 지워진다 · 이름표가 관리자 색을 탄다(글자 `#ffee00`·배경 `rgba(20,40,160,0.6)`) · `node tools/check.js` PASS · 콘솔 에러 0.
  - **이 환경에서 못 밟은 것**: 실제 Google Maps 타일 위의 그림 — 이 브라우저는 지도가 레이아웃을 안 해 오버레이 `onAdd` 가 안 돈다(그래서 위 실측은 같은 클래스의 마크업을 손으로 세워 쟀다). **미니 라벨의 팝이 줌 배율(CSS zoom)을 탄 채로 제 칸 위에 앉는지, 이름표 크기 배율이 겹침 방지(declutter)와 부딪히지 않는지는 실기기 확인이 필요하다.**

### 2026-08-15 (7)
- **v2.46.0 — 재생이 무대의 시작 위치에서 선다 · 지도 위 컨텐츠 크기 배율 (M16, 콘솔 v0.120.0 짝, D161)**
  - **(근본 버그) 재생이 시작 위치로 카메라를 옮기는 코드가 앱에 아예 없었다.** `nhRun` 은 `nhReset()` → `nhGoHome()` 으로 카메라를 **강남**에 놓고, `nhSeedScenario` 는 `SEED_AREAS[sc.area]` 에 **컨텐츠만** 깔았다 — 카메라는 한 줄도 안 건드린다. 그래서 시작 위치를 아무리 저장해도 화면은 늘 강남에서 시작했고, 그 동네로 가는 유일한 길은 `area` **단계**뿐이었다. 사용자가 "무대 시작 위치가 저장을 해도 계속 강남쪽으로 초기화된다" 고 한 것이 이것이다 — **저장이 안 된 게 아니라 카메라가 안 따라갔다.** D81 이 반대 방향(최상위 area 없이 area 단계만 있으면 카메라만 가고 무대는 강남에 남는다)을 적어 뒀는데 역방향은 아무 데도 없었다.
    - 새 `nhStageHome(sc)` 이 `nhSeedScenario` **직후** 그 자리에 **즉시** 세운다(저장된 배율까지 쓴다 — `nhGoHome` 은 `NH_AREA_ZOOM` 고정이라 배율이 안 실렸다).
    - ⚠️ **`nhGoHome` 은 안 고친다.** 그 함수는 **청소**이고 임베드 부팅·`nh:stop`·회차 리셋이 같이 쓴다 — 거기에 시나리오 상태를 기억시키면 "멈추기를 눌렀는데 앞 회차 동네에 서 있다" 가 된다. 청소는 청소로 두고 **시작을 새로 만들었다**.
  - **`nhSanitize` 보강 — `area` 가 비어 있어도 시작 위치가 왔으면 `custom` 으로 본다.** 여태는 빈 값이 그대로 남아, 콘솔이 좌표를 분명히 보냈는데도 무대가 `nhAreaKey`(회차 리셋 직후라 **강남**)에 깔렸다. `area` 가 어딘가에서 지워진 데모가 정확히 이 길로 강남에 섰다.
  - **지도 위 컨텐츠 크기 배율** (`seed.contentScale`, 0.4~2). `contentDot` **한 곳**에 곱한다 — `contentScale` 의 호출부가 이 함수뿐이고, 이 함수의 소비자가 스팟·피드·Request·딜 **넷뿐**이라 지도 위 컨텐츠 전부가 이 문을 지난다.
    - ⚠️ **`s` 와 `dp`(점 크기) 양쪽에** 곱한다. `dot` 판정이 `s<=floor` 이고 `floor=dp/basePx` 이므로 양변에 같은 배가 곱해져 **점이 되는 시점이 안 움직인다**. `s` 에만 곱하면 램프 경사가 배율만큼 가팔라져 v2.16 이 없앤 "툭 끊김" 이 부분 재발한다.
    - ⚠️ 반대로 `r`(점 전환 임계)에는 **안 넣는다** — 넣으면 같은 데모가 배율에 따라 다른 줌에서 점이 된다. 크기를 바꾸는 것이지 언제 점이 될지를 바꾸는 것이 아니다.
    - 같이 맞춘 곳: 클러스터 임계 `TH`(안 맞추면 200% 에서 눈으로는 겹치는데 56px 임계에 안 걸려 클러스터가 안 된다), 겹침 방지의 점 상자, 점 CSS 두 자리(`--nh-c`). 딜 점은 `--nh-k` 조차 안 타고 있어 같이 태웠다.
    - 회차 값이라 `nhReset` 이 되돌리고 **`settingsSnapshotFull` 에는 안 넣는다** — 넣으면 localStorage 캐시와 `shared/publicSettings` 로 새어 나가 **실앱 관리자 설정이 영구히 바뀐다**. 관리자 설정과는 **곱해진다**(대체가 아니다).
  - 검증(임베드 :8765): 기본 배율 1 · 자르개 0.4~2 이고 비정상 입력은 1 · **줌 3~21 × 배율 4종 전수**에서 점 전환 시점 불변이고 `scale`·점 크기가 정확히 배율배 · 점 CSS 둘이 `8×k×2`·`12×k×2` 이고 1 로 돌리면 절반 · 되돌리면 `--nh-c` 가 1 · 관리자 스냅샷에 안 샘 · 컨텐츠 없는 데모에서도 배율이 살아남음 · 앱도 2 로 자름. 시작 위치: `area` 가 비어도 `custom` 으로 서고, 분명히 적은 `area` 는 그대로이며, 시작 위치가 없으면 여태처럼 빈 값 · `nhStageHome` 이 저장된 배율(15)로 즉시 이동 · **전체 재생에서 카메라가 강남을 거쳐 마지막에 시작 위치에 선다**(청소는 그대로).
  - **이 환경에서 못 밟은 것**: 실제 Google Maps 타일 위에서 배율을 올렸을 때의 그림(스팟의 CSS `zoom` 경로는 `emojiPos` 4방향 × 꼬리 × 점/이모지 갈래가 있어 산술로는 다 못 잡는다), 배율이 올라가 새로 생기는 클러스터가 `like`/`hearts` 의 대상 오버레이를 지우는지.

### 2026-08-15 (6)
- **v2.45.0 — 자리표를 손으로 끈다 · 아이콘이 그 자리를 그대로 가져간다 (M16, 콘솔 v0.119.0 짝, D160)**
  - **(버그 · v2.34 부터 죽어 있었다) 파란 자리표를 끌 수가 없었다.** `style.css` 의 `.add-pin{pointer-events:none}` 때문에 `AddPin.onAdd` 가 붙인 `pointerdown` 이 **영영 안 불렸다** — `pointer-events` 는 상속 속성이라 자식 `.apn-dot` 도 같이 죽는다. 판(pane)은 `overlayMouseTarget` 로 이미 옮겨 뒀는데 CSS 한 줄이 남아 `_onDown`·`nhAddPinMoved` 가 통째로 **도달 불가 코드**였다. WORKLOG v2.34 의 "AddPin 을 끌 수 있다" 와 v2.44 문서의 "순간이 짧아 못 잡았다" 는 **둘 다 사실이 아니었다.** 이제 **원만** 잡힌다 — 링(`.apn-ring`)에는 안 준다: `apnPulse` 가 `scale(2)` 까지 부풀어 손잡이가 26↔52px 로 숨쉬며 이웃을 덮는다.
  - **잡히게 만들자 두 곳이 곧바로 물었다.** ⓐ `document` 클릭 닫기가 **잡는 그 클릭**으로 `closeAddMenu`→`addPinHide` 를 불러 "잡으려는 동작이 곧 지우는 동작" 이 됐다. `pointerdown` 의 `preventDefault` 로는 못 막는다 — click 은 그대로 온다. 자리표를 그 핸들러의 예외로 뒀다. ⓑ 지도 롱프레스(**520ms**)와 자리표 드래그(`LP_MS` **450ms**)가 70ms 차로 경쟁하고, 520ms 가 이기면 `openAddMenu`→`addPinShow` 가 **끌고 있던 인스턴스를 `setMap(null)`** 하는데 `_onDown` 의 클로저는 살아남아 pointerup 에서 **없어진 마커의 자리를 저장한다.** `onContent` 화이트리스트에 `.add-pin` 을 넣었다(`.spot-marker`·`.feed-pin` 과 같은 이유).
  - **아이콘이 자리표 자리를 그대로 가져간다.** 좌표는 원래 같았다 — 어긋난 것은 **앵커**(그림의 어느 점을 좌표에 대는가)다. 자리표는 원의 **중심**(0×0 상자 + `left/top:-13px`), 피드 핀도 중심인데, **스팟**은 상자 아래 변이라 이모지 중심이 `이모지높이/2` 위였고(z16 에서 13px, 줌에 비례해 커진다), **Request** 는 v2.20 이 물방울의 회전을 걷어 **뾰족한 끝을 없앴는데 상자 높이 56px 만 남아** 37px 떠 있었다. 이제 **이모지·물방울의 한가운데가 좌표다**.
    - 스팟은 **기준점**(`div.top` 과 `_ay`)을 내린다 — 그래야 겹침 방지(`dirBox`)가 같은 틀에서 재고, 방향·꼬리·gap 규칙을 한 줄도 안 고쳐도 된다. 상자 높이는 **안 잰다**(재면 draw 마다 = 팬·줌 프레임 × 스팟 수만큼 강제 리플로). `line-height:1` 이라 `emojiSize × 배율` 로 계산된다.
    - 덤: 줌아웃하며 말풍선↔점으로 넘어갈 때 `13·s` px 툭 튀던 것이 없어진다(점은 진작부터 중심 앵커였다).
  - **콕 집은 자리가 먼저다** (`nhAddAtPinned`). 만드는 쪽(`nhWriteSpot`·`nhRequestTyped`·`nhLayFeed`)은 "그 컨텐츠를 전에 끌어 옮긴 자리"(`write_*`·`rqw_*`·`feed_*`)를 **먼저** 봤다(v2.12). 자리표를 못 끌던 시절엔 그게 더 나중의 뜻이라 옳았다. 끌 수 있게 된 지금은 뒤집힌다 — 방금 + 를 옮겼는데 글이 옛 자리에 생기면 화면이 손을 안 따라온다. 자리표를 끌었거나 콘솔 📍 로 정한 자리면 그것이 이긴다.
  - 검증(임베드 :8765): 원이 잡힌다(`pointer-events auto`·`cursor grab`·`touch-action none`) · 링은 `none` · 끄는 중 `grabbing` · 원 중심 = 좌표(0,0) · **자리표를 눌러도 팝업이 안 닫힌다** · 바깥을 누르면 닫힌다 · **자리표 위 꾹은 새 팝업을 안 연다**(빈 지도 위 꾹은 그대로 열린다 — 걸러내기가 과하지 않다) · 스팟 위 꾹도 여태대로. 앵커는 실제 마크업·실제 transform 으로 두 배율에서 쟀다: Request 물방울 중심이 배율 1·2 모두 어긋남 0 (옛 앵커면 37/74px 위), 스팟 이모지 중심도 0 (옛 앵커면 13/26px 위).
  - **이 환경에서 못 밟은 것**: 실제 Google Maps 타일 위에서의 드래그(이 브라우저는 지도가 레이아웃을 안 해 오버레이 `onAdd` 가 안 돈다). **손으로 끌어 자리가 저장되고 다음 재생에 그 자리에 서는지는 실기기 확인이 필요하다.**

### 2026-08-15 (5)
- **v2.44.0 — 지도 위 팝업이 지도 가운데 · 꾹 누를 자리를 콘솔에서 · 답변 줄 여백 (M16, 콘솔 v0.118.0 짝, D159)**
  - **팝업이 지도 칸 가운데에 선다** (`cpopFitMap`). `.modal-overlay` 는 `position:fixed; inset:0` 이라 **뷰포트** 가운데에 섰는데, 폰 화면의 가운데는 지도의 가운데가 아니다 — 위에 헤더·지면이, 아래에 네비가 있어 카드가 지도보다 한 뼘 위에 떴다. 누른 것은 지도 위의 컨텐츠인데 카드가 딴 데 있으니 **무엇을 열었는지가 눈으로 안 이어졌다**. 헤더 높이는 스킨·화면 폭마다 달라 상수로 못 짐작하므로 `#phone-map` 을 **실측**해 오버레이의 top/left/width/height 에 박는다. `display` 를 준 **뒤**에 불러야 rect 가 잡힌다. 지도를 못 찾으면(관리자 화면 등) 인라인을 걷어 여태처럼 화면 가운데다. 카드는 `#content-pop .modal-card{max-height:calc(100% - 24px)}` 로 그 칸을 안 넘고, `closeContentPop` 이 인라인 자리를 지워 다음에 열 때 다시 잰다.
  - **꾹 누를 자리를 콘솔에서 미리 정한다.** ⚠️ **v2.45 정정: 아래의 "v2.34 부터 끌 수 있었지만 순간이 짧아 못 잡았다" 는 틀렸다.** `pointer-events:none` 때문에 **애초에 잡을 수 없었다** — 그리고 이때 넣은 📍 는 `stagePos` 가 Firestore 로 안 나가 **초안에만 살았다**(v0.119 에서 고쳤다). 앱은 v2.34 부터 `AddPin` 을 끌 수 있었지만 그 마커는 **재생 중 그 순간에만** 서 있어 실질적으로 못 잡았다. 이제 콘솔의 단계 값에 `📍 지금 지도 가운데를 꾹 누를 자리로` 가 있고, 저장은 앱과 **같은 약속**이다 — `stagePos.addat_<순번>`. 콘솔의 `holdIndexAt` 이 앱의 `nhAddN` 과 **같은 술어**로 센다(`addmenu` 이고 `v !== 'btn'` 인 단계만). 앱 쪽은 안 고쳤다 — `nhPosGet('addat',idx,…)` 가 진작부터 그 표를 먼저 봤다.
  - **`+` 버튼 갈래가 화면 한가운데를 좌표로 명시한다.** 여태 좌표를 안 주고 각 만드는 함수의 기본값(지역 좌표 +0.0012 · `nhSpread`)에 맡겼는데, 그러면 보고 있는 화면 밖에 생기기도 했다. `+` 의 뜻이 "지금 보고 있는 자리에 올린다" 이므로 `m.getCenter()` 를 준다. 마커는 바로 `addPinHide()` 로 걷는다 — 가리킬 지점이 없는 손짓인데 마커까지 서면 "꾹 누른 자리" 처럼 보인다.
  - **답변 줄에 숨 쉴 자리** — `gap 12px · padding 10px 12px`, 썸네일은 `aspect-ratio:1/1` 64px(들쭉날쭉하던 세로를 정사각으로). ⚠️ **`#cpop-body` 규칙이 style.css 두 곳에 흩어져 있었다**: 위쪽(1018~)에서 고쳐도 아래 블록(1285~)이 같은 선택자로 덮어 안 먹혔다. 특이도가 같으면 **나중 것이 산다** — 팝업의 답변 치수는 이제 아래 한 블록에서만 정한다.
  - 검증(임베드 :8765, `#phone-map` 을 실측 가능한 크기로 세워 둔 채): 오버레이가 지도 칸에 정확히 겹침(180,420 = 180,420) · 카드가 칸을 안 넘음(380 ≤ 420) · `gap 12px · padding 10px 12px` · 썸네일 61×61 · 글이 알약 왼쪽 벽에서 84px · 닫으면 인라인 자리 걷힘.
  - **이 환경에서 못 밟은 것**: 실제 Google Maps 타일 위에서의 팝업 위치(이 브라우저는 지도가 레이아웃을 안 해 `#phone-map` 이 h:0 이다 — 그래서 실측값을 손으로 세워 확인했다), 팝인 애니메이션이 끝난 뒤의 카드 위치(Browser pane 이 가려지면 애니메이션 시계가 안 흘러 `panelIn` 의 시작 프레임 `translateY(-12px)` 에 멈춰 있다. 그 12px 을 되돌리면 위아래 여백이 20/20 으로 대칭이다). 콘솔의 `📍 꾹 누를 자리` 버튼과 저장된 자리가 재생에 실제로 붙는지도 **실기기 확인이 필요하다**.

### 2026-08-15 (4)
- **v2.43.0 — 이름표도 끌어 옮긴다 · 답변에 사진 · 팝업 컴팩트 · (회귀) 올리기 타이밍 (M16, 콘솔 v0.117.0 짝, D158)**
  - **(회귀 · v2.41 이 냈다) `올리기 ›` 가 글자를 안 박고 등록했다.** 팝업을 기다리게 하면서 남은 시간을 뺐는데, 대기가 `nhT(420)` 이라 **배율을 따라 같이 커졌다** — `addreq` 1300ms 면 대기 666, 남은 634, 커밋 514, 타이핑 시작 760. 즉 커밋이 타이핑보다 **먼저** 왔다. 이제 붙잡는 시간은 `nhAddHold` 가 **단계의 30% 로 자른다**(1300 → 390 / 2000 → 600). 콘솔 바닥도 같이 올렸다(addreq 1300→2000 등) — 두 박자가 다 보이려면 그만큼이 든다.
  - **이름표를 끌어 옮긴다** (`DealLabel._onDown`) — 지도 위 컨텐츠는 전부 옮겨졌는데 **이름표만 안 움직였다**: '가게만' 항목은 ⏰ 핀이 안 서므로 잡을 손잡이가 아예 없었다. `DealPin._onDown` 과 같은 문법이고 끝에서 하는 일도 같다(`moveDeal` + `nhPosNote` → `dln_<순번>`). CSS 에 `touch-action:none` 을 줘야 터치 드래그가 지도 팬으로 안 샌다.
  - **답변에 사진** — `answers` 의 `v` 한 칸에 **줄마다 하나씩**(`글\t사진주소`). 새 칸을 안 뚫은 이유: 답은 개수가 정해지지 않은 목록이라 칸 하나로는 못 담고, `v` 는 이 액션에서 비어 있었다. 안 적은 줄은 앱이 채우되 **사진만 적은 줄에는 글을 안 지어낸다** — 팝업이 「사진으로 답했어요」 로 그린다.
  - **답변 한 줄이 가로 셋** — [사진 썸네일][이름·글][시각·채택]. 세로로 쌓으면 답 세 개가 팝업을 채워서, 여러 답을 훑고 하나를 고르는 일이 안 됐다.

### 2026-08-15 (3)
- **v2.42.0 — 가게 이름표에 이모지 (M16, 콘솔 v0.116.0 짝, D155)**
  `DealLabel` 의 이름표가 `e`(이모지)를 상호 앞에 세운다 — 실서비스 지도가 그렇게 생겼다
  (🍴 강남원주추어탕). 종류가 한 글자로 먼저 읽혀서 이름표가 스무 개 깔려도 훑을 수 있다.
  `e` 는 딜의 이모지와 같은 칸이다: 딜에는 ⏰ 핀이 그것을 쓰고, 가게에는 이름표가 쓴다.

### 2026-08-15 (2)
- **v2.41.0 — 내 Request 에 답이 달리고, 그중 하나를 채택한다 · 팝업 타이밍 · 가게 노출 (M16, 콘솔 v0.115.0 짝, D153·D154)**
  사용자 요청 9건 중 남은 4건.
  - **⑧ 추가 팝업이 사라진 뒤에 만든다** (`nhAfterMenu`). 여태 누르는 손과 컴포저가 겹쳤다 — `nhAddDone()` 이 팝업을 `NH_ADD_HOLD` 만큼 붙잡아 두는데(v2.37) 만드는 쪽은 그 자리에서 시작해서, 팝업이 아직 떠 있는데 입력창이 그 위에 뜨는 한 겹이 생겼다. 기다린 만큼 **ms 에서 뺀다** — 안 빼면 단계가 팝업 시간만큼 길어져 다음 단계로 넘친다(측정: 1700 → 829 + 대기 871). 네 개(addspot·addreq·addphoto·addpost) 모두 같은 규칙이다.
  - **⑨-1 `answers` 액션** — 내 Request 에 답이 하나씩 도착한다. `answer`(내가 쓰는 손)의 **반대쪽**이라 손짓이 없다: 지도의 물음표 핀에 **답글 수 미니 라벨**(`.rp-tag`, 컨텐츠의 `.spot-tag` 와 같은 우측 상단)이 오르고, **처음 한 번만** 우하단이 알린다(v2.29 의 규칙 — 곧 뒤따를 말을 미리 하지 않는다).
  - **⑨-2 열면 남의 답이 보인다** — 팝업의 답변 목록에 `who`(작성자)를 붙였다. 무대가 얹는 답만 이름을 든다: 사람이 쓴 답은 여태처럼 이름이 없다.
  - **⑨-3 `adopt` 액션 + 채택 손잡이** — **내 Request 이고 아직 안 골랐을 때만** 각 답에 [채택] 이 선다. 고르면 `best` 표가 남고, 리워드가 **답변자에게** 갔다는 말풍선이 뜬 뒤 Request 가 끝난다(핀이 걷힌다). 코인은 내 주머니에 안 들어온다 — 내가 주는 쪽이다.
  - **① 가게만 항목이 뿅 한다** — `nhBounceTake` 를 소비하는 손이 `DealPin.onAdd` 뿐이라, `drop` 으로 가게를 띄우면 소리는 나는데 아무것도 안 튀었다. `DealLabel.onAdd` 가 **store 일 때만** 표를 가져간다 — 딜이 있는 항목은 핀이 먼저 가져가므로 둘이 부르면 한쪽이 빈손이 된다(표는 소비형이다).
  - ⚠️ 답변에는 여전히 id 가 없다 — 채택은 **배열 순번**으로 가리킨다. 라이브(Firestore arrayUnion)는 원소 수정이 안 되므로 채택은 지금 **무대 전용**이다(로컬 배열을 고친다).

### 2026-08-15
- **v2.40.0 — 리액션 미니 라벨 하나 · 하트+의견을 한 액션이 단다 (M16, 콘솔 v0.114.0 짝, D152)**
  사용자 요청 9건 중 이번 5건 (나머지는 다음 턴).
  - **미니 라벨을 하나로 모았다.** v1.63 은 의견 수를 오른쪽 어깨에, v2.33 은 하트 수를 왼쪽 어깨에 뒀고, 피드 핀은 하트만 왼쪽·클러스터 개수만 오른쪽이었다 — 종류마다 규칙이 달라 시연을 보는 사람이 매번 어디를 볼지 다시 배웠다. 이제 **우측 상단 하나**에 `❤n 💬n` 순서로 든다(`.spot-tag`/`.fp-tag`, 안쪽 `.tg-h`/`.tg-c`). 없는 쪽은 빠지고 둘 다 0 이면 라벨째 사라진다.
  - **칠하는 함수도 하나다** (`paintReactTag`) — 말풍선과 피드 핀이 각자 만들고 각자 칠하던 것을 합쳤다. 하나를 고치면 다른 쪽이 뒤처지던 자리다.
  - **하트가 라벨에서 튄다** — `heartPopOn` 에 넘기는 요소를 오버레이 전체에서 **미니 라벨**로 바꿨다. 숫자가 오르는 자리와 하트가 튀는 자리가 같아진다.
  - **의견이 컨텐츠 종류를 안 가린다** — `contentComments(id)`. 방 이름의 `spot:` 은 옛 접두어일 뿐이고 키는 컨텐츠 id 다(스팟·피드 id 네임스페이스가 안 겹치는 것은 `feedLikes` 가 진작부터 쓰던 사실). 접두어를 안 바꾼 이유: 쌓인 스팟 의견과 `firestore.rules` 의 liveChat 규칙이 그대로 산다.
  - **피드 팝업에도 의견이 열린다** — 라벨이 💬N 을 말하는데 열어 보면 아무것도 없으면 화면이 거짓말이다. 스팟 팝업 안에 있던 UI 를 `cpopComments(body,id)` 로 꺼내 둘이 나눠 쓴다.
  - **`react` 액션** — 남들이 하트와 의견을 단다. `e`=하트 수 · `sp`=의견 수(각각 비우면 1, 0 이면 그쪽은 안 단다). **둘이 같이 오를 때 순서가 섞인다** — 하트를 다 올리고 의견을 다 올리면 봇이 두 번 돈 것처럼 보인다. 다만 섞는 방식은 **결정적**이다(v1.72 규칙): 컨텐츠 id 로 씨를 얹은 LCG 라 같은 데모는 늘 같은 순서다. 개수를 줄여야 할 때는 **비율을 지킨다**(하트만 깎으면 '의견이 하트보다 많은' 없는 장면이 된다).
  - `hearts` 는 `nhReact(…,count,0,…)` 로 남는다 — 저장된 옛 단계가 그대로 돈다.
  - **`like` 에 개수** (`e`) — **내 손은 늘 한 번**이고(D131 이 지킨 뜻) 2 이상이면 나머지가 빠르게 차오른다. "내가 여덟 번 눌렀다" 가 아니라 "내가 누르자 숫자가 따라 올랐다" 는 장면이다.
  - 회차 정리에 `nhTempIds.cmt` 를 더했다 — 방마다 **뒤에서** 그 수만큼 뺀다(앞에서 빼면 원래 있던 의견이 지워진다).

### 2026-08-14 (5)
- **v2.39.0 — 단계의 시간이 연출의 속도가 된다 · 라이브 카메라에 진짜 사진 (M16, 콘솔 v0.112.0 짝, D145·D146)**
  사용자 요청 8건 중 앱 몫 둘.
  - **ms 가 곧 속도다.** 여태 손짓·팝업·찰칵은 제 길이가 고정이라, 단계를 길게 주면 연출이 끝난 화면이 남은 시간을 그냥 서 있었고(대기) 짧게 주면 다음 단계로 넘쳐 흘렀다 — 사용자가 "동작 자체 속도는 그대로고 남은 시간은 대기하는 형식" 이라고 한 것이 이것이다. 액션마다 **제 속도로 다 보이는 길이**(`NH_NAT`)를 적어 두고 `ms/NAT` 를 배율(`nhRate`, [0.4, 2.2])로 삼아, 그 액션의 고정 시간 전부(`nhT()`)에 곱한다. 측정: like 400ms → 0.49× · like 3000ms → 2.2×(클램프) · addphoto 1200ms → 0.8×.
  - **CSS 도 같이 늘어난다.** 표식·번쩍임·날아가는 사진은 keyframes 라 JS 가 못 늘인다 — 각 규칙의 시간을 `calc(기본 × var(--nh-rate))` 로 바꾸고 `nhRateSet` 이 루트에 그 값을 적는다. 측정: .nh-touch 0.55s → 1.21s(2.2×) / 0.33s(0.6×).
  - ⚠️ **배율은 `nhT()` 를 지나는 고정 시간에만 붙는다.** 이미 ms 로 제 길이를 재는 것(타이핑 창·커밋 시각·카메라 트윈·쏟아지는 시간)은 안 지난다 — 거기 또 곱하면 창이 두 배로 늘어 커밋이 스텝 밖으로 나간다(D115 가 막아 둔 자리). 그래서 write·burst·zoom 은 표에 없고(배율 1), ai·add* 처럼 **손짓만 고정인** 액션은 표에 있다.
  - `nhStop` 이 배율을 1 로 되돌린다 — 사람이 손으로 만지는 화면이 마지막 단계의 배율을 물려받으면 앱이 이유 없이 느려진다.
  - **라이브 카메라에 진짜 사진** — `addphoto`·`addpost`·`postfeed` 가 `sp` 로 사진 주소를 받아 `nhLayFeed({img})` 로 넘긴다. 컨텐츠 탭의 피드 카드가 v2.10 에 얻은 길(Storage 에 두고 주소만 싣는다)을 단계도 쓴다. 없으면 여태처럼 테마 색이라 옛 데모는 안 변한다. 날아가는 사진(`nhPhotoFly`)도 그 사진을 그대로 든다.
  - `sp` 의 sanitize 상한 12 → **400**. `sp` 는 D143 이 "곁들이는 값 둘째 칸" 으로 열어 둔 **일반 칸**이라, 뜻은 액션이 정한다(burst=밀집도 · 카드를 만드는 액션=사진). 한 액션이 두 뜻을 갖지 않으므로 갈릴 일이 없다.

### 2026-08-14 (4)
- **v2.38.0 — 사진이 피드 탭으로 들어간다 · burst 100개와 밀집도 (M16, 콘솔 v0.111.0 짝, D142)**
  사용자 요청 4건 (둘은 콘솔 몫).
  - **사진의 경로를 뒤집었다.** v2.37 은 화면 가운데 → 지도 핀이었는데, 그건 "찍혔다" 까지만 말한다. 이제 **지도 위 그 핀 → 하단 피드 탭**으로 간다 — 사진이 **어디로 들어가는지**가 화면의 말이 된다(쿠폰이 버튼에서 Agent 로 가는 v2.31 과 같은 문법). 탭이 받는 링(`.pn-item.nh-catch`)도 붙였다. 측정: 출발 (375,533) → 도착 (374,1177) = 피드 탭 중심과 일치.
  - **burst 상한 50 → 100**. 등장 시각을 ms 에 고르게 펴므로 개수가 늘어도 리듬은 유지된다 (60개 두 번 전부 깔리는 것 확인).
  - **밀집도** (`sp`: tight 0.45 · normal 1 · wide 1.7) — 자리 반경 `rVis` 에 곱한다. 같은 개수라도 좁게 몰면 "이 골목이 찬다", 넓게 펴면 "동네 전체가 깨어난다" 로 읽힌다. 측정: 최대 반경 0.0048 vs 0.0109.
  - ⚠️ **새 스텝 칸(`sp`)을 뚫었다.** 마지막 수단인데(앱 sanitize·타입·화면·프롬프트가 갈라진다) 밀집도는 줌과 **독립된 축**이라 `e` 에 접을 수 없었다 — 같은 줌에서 좁게도 넓게도 쏟아질 수 있어야 한다.
  - `addbtn` 은 앱 어휘에 그대로 남는다 (콘솔이 고르개에서만 감췄다) — 저장된 단계가 계속 돈다.

### 2026-08-14 (3)
- **v2.37.0 — 지도 꾹이 제 자리를 갖는다 · 찰칵 · burst 옵션 둘 (M16, 콘솔 v0.110.0 짝, D139)**
  사용자 요청 5건.
  - **(버그) 지도 꾹이 `+` 버튼과 똑같이 동작했다.** 기본 자리가 **지도 중심**이었는데 `+` 버튼 갈래도 좌표 없이 화면 센터에 만든다 — 둘이 같은 자리에 같은 것을 만들고 있었다. 이제 중심에서 아래로 16%·왼쪽으로 10% 비껴 놓는다(엄지가 닿는 자리). 측정: (-0.0044, -0.0034)° 만큼 어긋나고 팝업도 그 지점에 뜬다.
  - **팝업이 손가락보다 먼저 사라졌다.** 표식(`.nh-touch`)은 0.55초짜리 CSS 애니메이션인데 항목을 누르고 **그 자리에서** 팝업을 닫아 왔다 — 표식이 아직 커지는 중인데 가리키던 메뉴가 없었다. 닫기 경로가 셋이라(무대의 `nhAddDone`·`addSpotContent` 안쪽·바깥 클릭) 부르는 쪽마다 늦추는 대신 **`closeAddMenu` 자체가 기다린다**(`addMenuHoldFor`, 420ms). 눌린 항목에 표시(`.nh-press`)도 남는다.
  - **찰칵** — 라이브 카메라는 찍는 장면이다. 화면이 한 번 번쩍하고(`.nh-flash`), 찍힌 사진이 뷰파인더 가운데서 부풀었다가 지도 위 제자리로 날아가 앉는다(`.nh-shot`). 쿠폰·코인이 쓰던 문법 그대로(`--dx`/`--dy`). `sfx` 에 `shot` 키를 열어 뒀다 — 파일을 안 올리면 조용히 지나간다.
  - ⚠️ 날아가는 사진은 **자식 `img`** 로 넣는다. 처음에 `background-image:url(...)` 로 했다가 빈 카드가 날아갔다 — 테마 일러스트가 SVG data URI 이고 그 안에 괄호가 그대로 있어(`encodeURIComponent` 는 괄호를 escape 하지 않는다) `url()` 이 그 자리에서 끊긴다.
  - **burst 에 줌 유지** (`e:'keep'`) — 이미 맞춰 둔 화면에서는 빠지는 것 자체가 방해다. 그때는 카메라를 **아예 안 건드린다**.
  - **burst 에 실사진** (`n:'photo'`) — 웹에서 긁어오지 않는다. 이 저장소가 이미 들고 있는 `SEED_IMG`(Wikimedia Commons 직링크·핫링크 허용·로드 검증)를 **테마별로 묶어** 쓴다: `cafe` 카드에는 카페 사진이 간다. 임의의 웹 이미지는 출처·라이선스를 그때그때 알 수 없고, 시연 중에 죽으면 빈 카드가 되며, 문구와 그림이 어긋나면 오히려 지어낸 티가 난다.
  - `addbtn` 은 남기되 `addmenu` 의 값(`btn`)으로 되돌렸다 — 콘솔이 액션 하나로 물어보고, 저장된 옛 단계는 그대로 돈다.

### 2026-08-14 (2)
- **v2.36.0 — 카메라가 흐른다 · 무대 번호가 안 어긋난다 · 동네 여럿 (M01+M16, 콘솔 v0.109.0 짝, D135·D136·D137)**
  사용자 요청 5건.
  - **카메라 엔진**(`nhCamTo`). 여태 이동은 `panTo`+`setZoom` 두 줄이었고, 임베드에서는 PC 지도가 `display:none` 이라 투영이 없어 늘 `setCenter` 가지로 떨어졌다 — **중심은 순간이동하고 배율만 스르륵** 하던 이질감의 정체다. `burst` 의 rAF 루프가 유일하게 부드러웠는데 그 이유는 이징이 아니라 세 규칙이었다: ①프레임마다 `moveCamera` ②그동안 미러를 재운다 ③끝에서 한 번만 착지. 그 셋을 공용 엔진으로 뽑아 `area`·`zoom`·`focus`·`burst`·`nhGoHome` 이 전부 쓴다.
  - **멀면 아크로 간다** — 팬 거리가 화면 1.5개를 넘으면 중간에서 줌을 판다(줌아웃 → 슬라이드 → 줌인, 최대 2.2레벨). 측정: 같은 동네 0.08화면 → 파임 0 (순수 슬라이드) · 강남→성수 1.02화면 → 0 · 강남→방학 3.30화면 → **1.14레벨**.
  - ⚠️ **미러를 재우는 것이 핵심이다.** 카메라는 PC → 폰 단방향 미러인데 `sync` 가 `phoneMap.setZoom` 을 부르고, setZoom 은 부를 때마다 Maps 자체 애니메이션을 새로 시작한다 — 16ms 마다 새 애니메이션을 시작하는 꼴이라 v2.13 이 겪은 끊김이 그대로 난다. 잠금은 착지가 **두 지도를 같은 값에 앉힌 뒤에** 푼다(먼저 풀면 첫 idle 이 폰을 흔든다).
  - **세대 번호**(`nhCamSeq`)를 따로 뒀다 — `nhRunToken` 은 같은 회차 안에서 안 변해 앞 단계의 지연 콜백을 못 끊는다. 다음 카메라 명령이 오면 앞 트윈은 목표로 튀지 않고 **그 자리에서 멈추고** 새 트윈이 거기서 이어받는다. `focus` 의 두 박자는 setTimeout 이 아니라 앞 트윈의 `onEnd` 로 이었다 — 앞이 끊기면 뒤도 안 온다.
  - **손이 이긴다** — `dragstart`·`wheel`·`gesturestart`·`pointerdown`(capture)이 트윈을 끊는다. `moveCamera` 는 프레임마다 사람 조작을 지우므로 취소는 선택이 아니라 필수다.
  - **짧은 단계는 안 흐른다** — 바닥(280ms)이 남는 시간보다 길면 즉시 이동이다. 0.4초짜리 단계가 늘어지지 않는다.
  - **(오늘의 버그) `drop` 이 번호를 갉아먹고 있었다.** `nhHeld[kind].splice(n,1)` 로 꺼내서 한 번 띄울 때마다 뒤 번호가 당겨졌는데, 콘솔 고르개는 정적으로 0·1·2 를 저장한다 — 같은 종류를 두 번 이상 띄우는 데모는 **두 번째부터 엉뚱한 것이 뜨고 마지막은 조용히 실패**했다. 이것이 "컨텐츠 탭에 등록한 걸 액션이 못 알아본다" 의 가장 큰 몫이다. 자리를 그대로 두고 `used` 만 표시한다. 측정: 0·1·2 → A·B·C (전에는 A·C·실패).
  - **`nhAt` 의 클램프를 없앴다** — 범위를 넘으면 마지막 항목을 조용히 돌려주던 것이 어긋난 번호를 **실패가 아니라 '엉뚱한 성공'** 으로 만들었다(ok:true 로 보고돼 콘솔 타임라인에도 표시가 안 남았다). 이제 null 이고 그 단계는 빨간 실패로 드러난다.
  - **빈 지면도 버린다** — 다른 다섯 종류는 예전부터 버렸는데 지면만 안 버려서, 손으로 짠 데모와 AI 를 거친 데모의 번호 체계가 달랐다("어제까지 되던 데모가 AI 고치기 한 번 뒤에 밀린다").
  - **`pop` 의 빈 `v` 를 지도 글로** — `focus` 는 이미 기본값을 채우는데 `pop` 만 안 채워 조용히 실패했다.
  - **동네 여럿** (`custom`·`custom2`·`custom3`…). `area` 게이트가 이미 `SEED_AREAS[v]` 조회라 **등록만 넓히면 게이트·액션·배율이 한 줄도 안 고치고 그대로 산다** — 이것이 이 설계를 고른 이유다. ⚠️ `SEED_AREA_ORDER` 에는 안 넣는다(시드 평탄화 순서 = 문서 id). 옛 콘솔의 `areaPlace` 하나는 `custom` 한 칸으로 읽는다.
  - **`addbtn` 액션 신설** — 하단 + 버튼. `addmenu` 의 값으로 접어 뒀더니 액션 목록을 훑는 눈에 안 걸렸다(사용자가 두 번 물었다). 겸사겸사 `.pn-add.open`(＋ → ✕ 회전)이 CSS 에만 있고 붙이는 코드가 없던 것도 살렸다 — 실앱에서도 열림 표시가 안 됐다.
  - **사이드바 '지도로' 버튼이 죽어 있었다** — `goMapCam(g.lat,g.lng,16)` 은 첫 인자가 지도여야 하는데 좌표를 넣어 TypeError 였다. 인자 검증을 넣어 다음엔 조용히 안 죽는다.

### 2026-08-14 (1)
- **v2.35.0 — 화면이 커져도 같은 그림 (M01+M05+M16, 콘솔 v0.108.0 짝, D133)**
  사용자 질문 1건: "접속 환경에 따라 데모 앱의 크기·비율이 달라져 보이는 컨텐츠의 위치·크기가 계속 바뀐다. 일반적인 앱처럼 같은 경험을 유지할 수 없나."
  - **원인은 지도 한 층뿐이었다.** 폰 크롬은 `cqw` 라 이미 폭에 비례해 자라고, 비율(9:19.5)은 콘솔 래퍼가 지킨다 — 즉 **레이아웃은 이미 어디서나 같았다**. 어긋나는 것은 지도다: 구글 지도의 줌은 절대 픽셀 기준이라(`mapMpp` 는 줌·위도만의 함수) 칸이 두 배면 같은 줌에서 두 배 넓은 지역이 들어온다. 무대는 위경도 고정 반경(`nhSpread` 167~345m)에 컨텐츠를 깔고 `NH_AREA_ZOOM=14` 를 못 박으므로, 그 원반이 340px 칸에서는 화면 가로의 **37%** 지만 680px 칸에서는 **18%** 로 쪼그라든다. "동네가 컨텐츠로 차 있다" 가 "빈 지도 한복판의 점 뭉치" 가 되는 자리다.
  - **기준 폭 390px 을 세우고 줌으로 환산한다** — `z' = z + log2(폭/390)` (`nhZ`). 보이는 지리 범위가 어디서나 같아진다(측정: 폭 250·340·390·600·780 전부 가로 **2956m**). 덤으로 컨텐츠 배율이 `2^(z-16)` **지면 고정**이라 줌을 올리면 핀·말풍선도 같은 비율로 커진다 — 크기 보정을 따로 안 해도 화면에서 차지하는 비중이 그대로다(무대 링 **23.3%** 고정).
  - 걸리는 곳은 **절대 줌을 쓰는 다섯**뿐이다: `nhGoHome`·`area`·`zoom`(숫자)·`focus`(17)·`burst`. `in`/`out` 은 지금 줌에서 떼는 상대 이동이라 손대지 않는다. `fitBounds` 계열도 그대로다 — 그쪽은 **원래** 칸 크기를 보고 줌을 고르므로 이미 정답이다.
  - **`nhHere` 는 기준 폭 줌으로 되돌려 보고한다**(`nhUnZ`). 안 그러면 "이 지도 저장" 이 보정된 값을 저장하고 재생 때 `nhZ` 가 한 번 더 붙어 두 배로 줌인된다. `nhCustomArea` 의 반올림도 정수 → 소수 둘째 자리로 풀었다(줌이 이제 소수를 갖는다).
  - **칸이 바뀌면 보이던 범위를 지킨다**(`nhViewSync`) — 목표 줌을 기억하지 않고 **배율의 변화분**만 얹으므로 대본 상태가 필요 없다. 감시는 `ResizeObserver`(콘솔이 패널을 끌면 창 크기는 그대로다) + `window resize` 둘. ⚠️ **두 지도의 줌을 먼저 읽고 나서 건다** — 카메라가 PC → 폰 단방향 미러라 읽으면서 걸면 폰이 보정을 두 번 받는다(실제로 그렇게 났다).
  - 회차 시작(`nhReset`)에도 한 번 맞춘다 — 전체화면으로 들어간 직후처럼 감시가 놓쳤을 수 있는 순간이 있다. ⚠️ **`nhGoHome` 보다 먼저**여야 한다(뒤에 두면 절대 줌 위에 변화분이 또 얹힌다).
  - 남은 고정 px 자산은 `--nh-k` 를 탄다: 점 크기(`contentDot` 의 `dotPx`·`.spot-dotmark`), 점 전환 임계의 축척자 64px, 클러스터 묶임 거리 56px, 손가락 표식 34px.
  - `isFractionalZoomEnabled:true` 를 두 지도에 박았다 — 보정은 소수 줌을 만드는데, 래스터 폴백(`MAP_ID` 없음)에서는 `setZoom` 이 정수로 반올림돼 보정이 통째로 사라진다.
  - **좌표계는 손대지 않았다.** 폰 전체를 고정 논리 크기 + `transform:scale` 로 그리는 길(레터박스)도 검토했는데, `getBoundingClientRect` 와 `clientX` 를 섞어 쓰는 자리가 **25곳** 깨지고(드래그 5·꾹눌러추가·연출 4·인셋 계산 5…) 구글 지도 내부의 팬/핀치 환산은 앱 코드로 못 고친다. 이 보정은 카메라와 크기에만 걸리므로 그 25곳이 전부 그대로 참이다.

### 2026-08-13 (10)
- **v2.34.0 — 지도 피드도 더블탭 하트 · 남들이 누르는 하트 · 컨텐츠를 올리는 손 (M05+M16, 콘솔 v0.107.0 짝)**
  사용자 요청 7건.
  - **지도 피드 핀이 손을 받는다.** `.feed-pin{pointer-events:none}` 이 문을 닫고 있어서, v1.60 이 만든 단일 핀 상세 팝업조차 편집 권한자(`.editable`)에게만 열렸다. 문을 열고 말풍선과 **같은 문법**을 준다 — 한 번=상세 팝업 · 두 번=좋아요(340/360/1200ms 그대로), 카운트는 `.fp-heart` 배지. ⚠️ 배지는 말풍선의 `.spot-heart` 와 달리 **CSS zoom 을 안 탄다**(핀은 zoom 이 아니라 width/height 로 큰다) — 점 모드에서는 감춘다.
  - **더블탭이 지도 줌으로 새던 것을 막았다.** 같은 두 번의 탭을 Maps 도 본다: 오버레이는 지도 컨테이너 **안**이라 이벤트가 위로 흐르고, 마우스는 `dblclick` 이 한 번 더 올라간다. 막는 길이 각각 반쪽이라 둘 다 쓴다 — ①`dblclick` 에 stopPropagation(마우스) ②첫 탭이 컨텐츠에 닿은 순간에만 `disableDoubleClickZoom` 을 켜고 700ms 뒤 되돌린다(`mapDblGuard`). 통째로 끄지 않는 이유는 **빈 지도의 더블탭 줌은 살려야** 해서고, `touchstart` 를 막지 않는 이유는 핀에서 시작하는 **지도 팬**이 같이 죽어서다.
  - **`like` 는 이제 "사용자가 더블탭하는 장면" 이다** (D131). 여태 대상이 피드 카드뿐이었고 화면에 손도 안 보여서 숫자만 조용히 늘었다 — "좋아요 누르기가 동작을 안 한다" 는 말이 여기서 나왔다. `v` 로 종류를 고르고(지도 글·피드 카드), 손가락 표식이 **두 번** 뜨고, 하트가 튄다. 그리고 **끄지 않는다**: `toggleLike` 는 토글이라 이미 눌러 둔 것이면 이 단계가 하트를 빼는 장면이 됐다.
  - **`hearts` 액션 — 남들이 하나둘 누른다** (`v`=종류·`i`=몇 번째·`e`=몇 개·`ms`=오르는 시간). 내 하트(`me`)는 안 켜고 숫자만 오른다. **이 단계 안에서 끝난다** — 하트 사이 60ms 를 바닥으로 두고, 그 박자로도 안 들어가는 개수는 들어갈 만큼으로 줄인다. 회차가 끝나면 `nhTempIds.heart` 에 하나씩 적어 둔 만큼 되돌린다(전역 카드는 살아남아 두 번째 재생부터 숫자가 부푼다).
  - **컨텐츠를 올리는 손** (D132). `write`·`request` 는 컴포저부터 시작해서, 시연을 보는 사람은 "이 앱에서 어떻게 올리는가" 를 끝내 못 봤다. `addmenu`(v=`btn` 하단 + 버튼 / `hold` 지도 꾹)가 팝업을 열고, `addspot`·`addphoto`·`addpost`·`addreq` 가 그 팝업의 **네 항목을 각각** 눌러 컨텐츠까지 만든다. 스팟·Request 는 있는 타이핑 연출(`nhWriteSpot`·`nhRequestTyped`)을 그대로 쓴다 — 같은 일을 두 벌 만들면 둘이 어긋난다. 사진·지면은 파일 고르기가 자동화되지 않아 그 자리에서 카드를 만든다(`nhLayFeed` 에 `kind` 추가 — cam/post).
  - **`AddPin` 을 끌 수 있다.** ⚠️ **v2.45 정정: 이 문장은 사실이 아니었다.** 판(pane)은 옮겼지만 `.add-pin{pointer-events:none}` 이 남아 `pointerdown` 이 영영 안 불렸고, 아래 코드는 v2.45 까지 **도달 불가 코드**로 서 있었다. 실제로 끌 수 있게 된 것은 v2.45 다. 여태 이 마커는 `overlayLayer` 에 있어 손이 안 닿았다. "꾹 눌러 추가" 단계의 자리를 정하는 길이 이것뿐이라 다른 컨텐츠와 같은 규칙으로(터치=롱프레스 뒤 · 마우스=즉시) 옮기고, 옮긴 자리는 `addat_<순번>` 으로 남아 다음 재생에도 그 자리다. 실서비스에서도 뜻이 있다 — 끈 자리가 곧 만들어질 자리다.
  - 자리 우선순위는 **사람이 옮긴 것이 먼저**다: 쓰인 글을 옮긴 자리(`write_<i>`) > 추가 팝업이 가리키던 자리(`addat`) > 지역 오프셋. 뒤엣것이 더 나중에·더 구체적으로 정한 뜻이고, 화면에 남는 것도 그쪽이다.

### 2026-08-13 (9)
- **v2.33.0 — 지도 컨텐츠 더블탭 좋아요 · 하트 수를 무대가 심는다 · 단계 사이의 군더더기 제거 (M04+M05+M16, 콘솔 v0.106.0 짝)**
  사용자 요청 3건.
  - **스팟 말풍선 더블탭 = 좋아요.** 카운트는 의견 수(`.spot-cmt`)와 같은 미니 배지로 반대쪽 어깨에 앉는다(`.spot-heart`). 말풍선의 자식이라 `draw` 의 CSS zoom 을 그대로 타고, 색은 JS 가 인라인으로 박아 모드 규칙·스킨 3종을 공짜로 탄다. 두 번 사이 340ms·싱글탭 대기 360ms 는 피드 카드가 오래 쓰던 값 그대로. ⚠️ 카운터는 **인스턴스**에 둔다 — 진입점이 둘(권한 없는 손은 click, 권한자는 `_onDown` 의 cleanup)이라 클로저에 두면 한쪽이 센 탭을 다른 쪽이 못 본다.
  - **(오늘의 버그) 라이브에서 스팟 하트가 조용히 죽었다** — `toggleLike` 가 `feedItems` 에 없는 id 를 만나면 그 자리에서 돌아섰다. 피드 탭의 스팟 카드는 **이미** 더블탭 하트를 갖고 있었으므로(그 목록은 스팟·지면을 섞어 돈다) 이건 새 기능이 아니라 회귀 수정이다. 이제 피드 문서가 아닌 id 는 로컬 경로로 내려간다.
  - **`rebuildLikes` 가 피드 id 만 다시 짓는다** — 여태 표를 통째로 비워서, 라이브 스냅샷이 올 때마다 지도 하트가 사라졌을 것이다. 스팟·피드 id 는 네임스페이스가 안 겹치므로 표는 하나로 족하다.
  - **하트 수를 무대가 심는다** (`seed.spots[].likes` · `seed.feeds[].likes`, 0~999). 시드 생성기가 오래 쓰던 수법과 같은 자리다. ⚠️ 상한 999 인 이유: 피드 온도가 **최다 좋아요 대비 비율**이라 한 장에 큰 수를 주면 나머지가 전부 식는다.
  - **액션은 안 늘렸다** — `like` 가 이미 `toggleLike` 를 부른다. 어휘를 늘리는 것은 마지막 수단이다. 대신 `nhTempIds.like` 에 같은 id 가 두 번 쌓여 sweep 이 두 번 토글하던 것을 고쳤다(같은 카드에 like 를 세 번 두면 났다).
  - **단계 사이의 군더더기를 걷었다.** ①손가락 표식의 **170ms 리드인 제거** — 표식은 띄우되 액션은 그 자리에서 돈다(tab·ai·scope·popclose·pop v:req 다섯이 늘 이 값을 먹었다). ②`write`·`ai` 의 커밋 시각을 **스텝 안으로 접었다**(`min(…, ms-여유)`) — answer·request 가 이미 쓰던 문법이다. 이 두 번째가 콘솔 하한을 낮출 수 있게 한 전제다(D115 사슬이 ms 와 무관해진다).

### 2026-08-13 (8)
- **v2.32.0 — 가게 이름표 · 가게 한마디 · 이름표에서 매장 페이지로 (M17, 콘솔 v0.105.0 짝)**
  사용자 요청 3건. 설계는 세 안을 겨루게 해서 골랐다(계약·화면·폭발 반경 세 관점이 각각 다른 안을 골라 1-1-1 이었고, 코드를 읽어 tie 를 깼다).
  - **가게는 새 컨텐츠 종류가 아니다** — 딜 항목 안의 칸 두 개(`msg`·`store`)로 산다. 새 종류를 세우면 `nhTempIds`·`NH_MAX`·`NH_BAND`·`nhPosNote`·`nhStore`·`nhSweepTemp`·`nhDrop`·`nhDim` 과 콘솔의 `PlaySeed`·`CONTENT_KINDS`·`OPENABLE_KINDS`·`indexChoices` 까지 **열두 자리**가 는다. `deal` 은 그 열두 자리에 이미 전부 등록돼 있다.
  - **이름표(`DealLabel`)는 앵커 아래**에 선다. 핀은 `translate(-50%,-100%)` 로 앵커 위를 다 쓰고 그 높이가 `contentDot`(0.02~40)으로 변한다 — 위에 두면 줌마다 사이가 흔들린다. 배율은 이름표의 것(`labelScale` 0.7~1.6)이라 **멀어져도 점이 되지 않는다**("계속 표시").
  - **한마디는 스팟 말풍선 몸통을 빌린다**(`.spot-bubble`+`tl-t`) — 스킨 3종이 그 클래스를 알고 있어 유리 룩·꼬리색이 따라온다. ⚠️ 다만 `.map-label-tag` 에는 background 가, `.spot-bubble` 에는 color 가 **없다**(둘 다 호출부가 인라인으로 주던 값이다) — 클래스만 빌리면 투명 배경 흰 글씨와 폴백 파란 말풍선이 된다. 그래서 색을 `.dl-name`/`.dl-msg` 에 명시했다.
  - **`dealActive` 를 좁혔다**(`!d.store &&`). `nhLayDeal` 이 모든 항목에 `secs`·`ts` 를 넣으므로 가게만 있는 항목도 30분간 "진행 중" 이 되고 그 뒤 뒤집힌다 — 한 술어를 좁히니 "⏰ 타임딜 N" 카운트·관리자 표·쿠폰 액션·`pop e:'sheet'`·매장 페이지 칩 **다섯 자리가 손대지 않고** 정직해졌다. 렌더만 `dealShown`(딜이거나 가게)을 쓴다.
  - **이름표를 누르면 매장 전용 페이지**. 딜이 없으면 배너·쿠폰 CTA 를 통째로 접는다(`.no-deal`) — 회색 비활성으로 남기면 "딜이 있는데 못 받는다" 로 읽힌다.
  - **이름표끼리 겹치면 뒤에 온 것을 숨긴다**(자리는 안 옮긴다 — v2.27 규칙). 우선순위는 ①매장 페이지가 열린 것 ②한마디가 있는 것 ③나머지. 줌인하면 그대로 돌아온다.
  - **`shopsay` 액션** — 가게가 한마디 한다(i=어느 가게·v=문구, 비우면 걷는다). ⚠️ 등장 바운스는 공용 표(`nhBounceTake`, 소비형)를 안 쓴다 — 핀이 먼저 가져가면 이름표가 안 튄다.
  - 관리자 표시 설정에 `가게 이름표` 토글(딜 하위 키라 `mergePinView` 의 하드코딩 배열은 그대로다). `근처 매장`(기본값)은 이름이 아니라 빈 값의 표시라 이름표를 안 건다.

### 2026-08-13 (7)
- **v2.31.0 — 쿠폰이 버튼에서 Agent 로 · 말풍선 닫기 액션 · 단계 수 상한 제거 (M17+M16+M08, 콘솔 v0.104.0 짝)**
  사용자 4건 중 앱 몫 3건.
  - **쿠폰 티켓의 두 끝** — 누른 `쿠폰 받기` 버튼에서 나와 하단 Agent 아이콘으로 들어간다. 부르는 쪽이 **시트를 닫기 전에** `claimDeal(d,{from:btn})` 을 부르는 것이 전부다: `couponFly` 가 그 자리에서 rect 를 읽고 티켓을 `.phone-screen` 에 붙이므로 다음 줄에서 닫아도 티켓은 이미 독립이다. 도착 rect 가 0 이면 **연출을 안 한다** — 네비바에 배율(기본 90%)이 걸려 있어 자리를 상수로 못 짐작한다. 받는 링은 `::after` 로 그리고 2.2cqw 를 안 넘긴다(그보다 크면 `overflow:hidden` 에 잘린다).
  - **`bubbleclose` 액션** — 우하단은 말풍선 하나가 아니라 **셋**이다(agent 말풍선 · AI 프리셋 · 현장 Request 수신 카드). 하나만 걷으면 "닫았는데 안 닫혔다" 가 되므로 셋을 함께 걷는다.
  - **단계 20개 상한 제거** — 21번째부터 통째로 버려졌고 그 사실이 어디에도 안 남았다. 재생 루프는 타이머를 쌓지 않으므로 단계가 늘어도 문제가 없다. ⚠️ **이미 20단계를 넘겨 저장해 둔 데모가 있으면 이번부터 뒷단계가 실제로 돈다** — 여태 안 돌던 것이 도는 것이라 한 번 확인해 보는 편이 좋다.

### 2026-08-13 (6)
- **v2.30.1 — 딜 팝업이 지도 가운데로 · 쿠폰 받기가 눌린다 · 딜도 끌어 옮긴다 (M17)**
  사용자 3건(앱 단독).
  - **(버그) 쿠폰 받기가 안 눌렸다** — v2.30 이 시트를 가운데 카드로 만들며 `max-height` + `overflow-y:auto` 를 걸었는데, 내용이 그 높이를 넘는 화면에서 '쿠폰 받기'가 **접힌 아래**로 내려갔다. 보이지도 않고, 그 자리를 누르면 뒤의 스크림이 받아 시트가 닫혔다(그래서 "동작을 안 한다"). 카드를 flex 세로로 바꾸고 **가운데만 구르게** 한다(`.ds-scroll` — index/admin 두 마크업에 함께) — 머리줄과 받기 버튼은 언제나 카드에 붙어 있다.
  - **팝업 자리를 지도 기준으로** — 폰 화면 한가운데(`inset:0`)면 헤더·상단 캐러셀만큼 위로 치우쳐 모드 전환 버튼(69cqw)에 붙었다. `dealSheetFrame` 이 열 때 헤더 높이 + `phoneMapInsets`(카메라가 쓰는 것과 같은 자)로 **보이는 지도**에 맞춘다. ⚠️ `#phone-map` 의 rect 는 안 쓴다 — 지도가 아직 안 붙은 화면에서 높이가 0 이라 프레임이 통째로 어긋난다. 가리는 것이 화면의 3분의 2를 넘으면 프레임을 걷는다(짧은 창 안전판).
  - **쿠폰은 Agent 에서 나온다** — `couponFly(anchor)`. 리워드 코인이 말풍선 위에서 터지는 것과 같은 규칙이라 말풍선을 **먼저** 띄우고 그 자리에서 티켓이 출발한다. 문구 없는 연출(`coupon e:0`)이면 AI 버튼이 그 자리.
  - **딜도 끌어 옮기고 자리를 기억한다** — `DealPin._onDown`(ReqPin 과 같은 문법) + `moveDeal`. 무대 항목이면 `nhPosNote` 가 `dln_` id 를 읽어 다음 재생에도 그 자리다 — 그 계약은 v2.3 부터 있었고 `nhLayDeal` 도 `nhPosGet` 으로 읽고 있었는데 **끄는 손이 없어** 죽어 있었다(Request 가 v2.20 에 겪은 자리와 같다).

### 2026-08-13 (5)
- **v2.30.0 — agent 말풍선을 한 손이 든다 (M07+M08+M16, 콘솔 v0.102.0 짝)**
  사용자 4건 중 앱 몫 3건.
  - **말풍선을 띄우는 자리가 여섯 군데였다** — 유지 시간이 2.6·4·5·6·7초로 흩어져 있어 "이 단계의 말풍선은 몇 초" 를 정할 손잡이가 없었다. `aiSay(text, defMs, hardMs)` 하나로 모으고 `hideAiBubble()` 로 걷는다.
  - **단계가 유지 시간을 정한다 (`bh`, 초)** — additive 필드. `nhAct` 가 **모든 액션 실행 전에** 심는다(액션별 분기 안이 아니라 — 요청이 "모든 action 대상"이다). 실행 직후에 안 지운다: `ai` 의 답은 `ms*0.4` 뒤에 뜨므로 그때까지 같은 단계의 값이 살아 있어야 한다. 우선순위는 **좁은 규칙부터** — `coupon` 의 `e`(표시 초) → `bh` → 그 자리 기본값. ⚠️ 그래서 `nhCouponMs` 가 빈 값에 4000 대신 **null**(= 안 정했다)을 돌려준다. 4초로 굳히면 `e` 를 비운 단계에서 `bh` 가 영영 못 온다.
  - **현장 Request 팝업이 뜨면 말풍선을 걷는다** — 수신 카드(`.req-bubble`)와 말풍선(`.ai-bubble`)은 우하단 **같은 자리**(right:3cqw · bottom:21cqw)에 겹쳐 떴다. 카드가 올라가면 밑에 깔린 말풍선 아랫단만 삐져나와 읽지도 못할 글자가 남았다. 상세 팝업(`openContentPop('req')`)도 같은 규칙이고, 같은 구석에 뜨는 프리셋 패널도 함께 닫는다.
  - **타이핑 소리는 손이 자판에 닿을 때 한 번** — 글자마다 울려 16자 답변에 열한 번 났다. 세 자리(스팟 쓰기·Request 등록·답 달기) 모두 인터벌 **밖**으로 옮겼다.

### 2026-08-13 (4)
- **v2.29.1 — 시연 중에는 지도가 혼자 안 움직인다 · 타임딜이 가운데 팝업으로 (M03+M16+M17)**
  사용자 3건(둘은 앱, 하나는 콘솔 짝 — 콘솔 v0.101.1).
  - **`focusNearbyZones` 를 임베드에서 끈다**: 베이직→트렌드 전환 80ms 뒤에 근접 존 3개를 담는 `fitBounds` 가 **혼자** 걸린다. 실제 앱에서는 친절이지만 시연에서는 무대가 맞춰 둔 화면(`zoom`·`focus`·`burst` 가 앉힌 자리)을 소리 없이 밀어낸다 — "줌인아웃·모드 전환·burst 가 안 먹는다" 의 정체다. `IS_EMBED` 로만 막아 index/admin 은 여태와 같다. 손으로 토글을 눌러도 시연 중이면 안 움직인다(카메라를 움직이는 주체는 단계뿐이어야 한다).
  - **타임딜이 바텀시트 → 가운데 팝업**: 딜은 지도 위 그 핀의 것인데 바닥을 통째로 밀고 올라오면 지도가 반쯤 가려 "어디의 딜인지" 가 사라졌다. 78cqw 카드가 화면 정중앙에 서고(dsPop), 사진 34→24cqw·이모지 18.4→13.4·제목 5.1→4.2·할인율 6.6→5.4·버튼 12.2→10.4cqw 로 한 단계씩 줄였다. 마크업은 그대로 — 손잡이(`ds-grip`)만 숨긴다(시트의 물건이다). `couponFly` 출발점도 화면 중앙으로.
  - 확인한 것(회귀 아님): `zoom`·`mode`·`burst` 자체는 정상이다 — 임베드에 실제 `nh:run` 을 태워 카메라 궤적을 150ms 간격으로 찍었다(14 → mode trend → zoom in 16 → burst 12, 컨텐츠 8개). 카메라를 되돌리던 것은 위의 자동 줌아웃 하나뿐이었다.

### 2026-08-13 (3)
- **v2.29.0 — 지도 컨텐츠 팝업이 유리로 · 추가 자리표 · Agent 배경 정지 · 리워드가 Request 를 닫는다 · 쿠폰 수집 (M15+M17+M07+M08+M03)**
  사용자 요청 7건(앱 단독 — persona-vc 몫이라고 적힌 4건도 전부 앱 화면의 일이라 콘솔 수정 없음).
  - **팝업 문법 통일 마무리 (M15+M17)**: v2.27 이 상세 팝업 카드만 유리로 바꾸고 **뒷배경을 안 건드렸다** — `.modal-overlay` 의 짙은 스크림(rgba(15,23,42,.42)+blur)이 그대로 남아, 같은 유리인데도 컨텐츠 추가 메뉴·에이전트 말풍선과 딴 물건으로 보였다. `#content-pop` 을 투명 오버레이로(카드만 뜬다), 타임딜 시트도 스크림 제거 + `--frost` 유리로. 스크림 div 는 남긴다 — 바깥 클릭으로 닫는 자리다. ⚠️ v3 스킨의 `.ds-card{background:var(--v3-surface)}`, new 스킨의 `.cpop-card{background:rgba(255,255,255,.94)}` 도 같이 유리로 (안 하면 기본 스킨에서만 통일이 깨진다).
  - **여기에 추가 (M00 `AddPin`)**: 꾹 눌러 추가 메뉴가 뜨면 **그 좌표에 자리표**(점선 링 + `+`)가 선다. 컴포저가 곧 그 자리에 앉는 스팟·Request 는 메뉴가 닫힐 때 걷고, 화면이 비는 사진·지면은 파일 고르는 동안 살려 둔다. ⚠️ 함께 고친 것: 사진·지면이 여태 꾹 누른 자리를 무시하고 **화면 센터**에 올라갔다 — 자리를 가리키는 이상 그 말이 참이어야 한다(`feedDropAt`, 동 이름도 그 좌표로). 파일 고르기 취소는 이벤트가 없으므로 창이 돌아온 뒤 확인해 걷는다.
  - **Agent 배경 정지 (M08)**: `aiHeatFlow` 5s 무한 흐름 삭제 — 정지한 115° 두 색 사선만 남는다. v2.15 가 렌즈 무지개를 걷은 것과 같은 이유(구석에서 쉬지 않고 도는 그라디언트가 시선을 끈다). 스킨 두 벌의 재선언·`background-size`·reduced-motion 예외까지 같이 걷었다.
  - **답을 보낸 쪽은 말이 없다 (M07)**: "전달됐어요, 리워드는 잠시 뒤" 는 곧 뒤따르는 리워드 말풍선이 할 말을 미리 하는 중복 안내였다. **내 Request 에 답이 도착한** 경우는 그대로 알린다(내가 모르는 일이다).
  - **리워드가 그 Request 를 닫는다 (M07)**: 지급되면 지도 위 핀이 **펑 터지며** 사라진다(`reqPopAway` · `rp-pop`). 조용히 없어지면 렌더 사고로 읽히므로 사라지는 순간을 남긴다. 데이터는 `rq.rewarded` 로 살아 있어 답변 목록·드로어는 그대로. 대상은 `lastAnsweredReqId`(무대의 `reward` 액션도 이 길을 탄다 — 콘솔 수정 불필요).
  - **쿠폰 수집 (M17 `couponFly`)**: 받았다는 말이 글자로만 남던 것을 — 티켓이 시트 자리에서 부풀었다 우상단 프로필로 날아가 접히고 프로필이 노란 링으로 받는다. 방향은 v2.18 코인 적립과 같다. 문구를 안 띄우는 연출(`coupon e:0`)에서도 쿠폰은 날아간다.
  - **존 카드 설명이 위로 (M03)**: 이름·온도가 **항상 맨 아랫줄**이고 설명이 그 위에 얹힌다. 설명 유무로 이름 줄 높이가 카드마다 달라져 캐러셀 기준선이 들쭉날쭉하던 것도 같이 잡힌다.
  - 남긴 것: **매장 전용 페이지**(`#store-page`, ⏰ 핀 탭이 여는 전면 페이지)는 불투명 흰 면 그대로. 팝업이 아니라 페이지고, 전면 유리로 덮으면 지도 위에서 가격·재고를 읽을 수 없다.

### 2026-08-12 (13)
- **v2.28.0 — 지도 컨텐츠 좌표 고정 · 존 카드 지면형 · 모드 토글 고정 (M00+M03+M09)**
  사용자 요청 3건(앱 단독).
  - **핀을 밀지 않는다**: 겹침 방지가 픽셀로 밀던 값은 그때의 줌·화면에서 잰 것이라 환경이 바뀌면 컨텐츠가 다른 지점에 붙었다. 핀은 장애물로만 세고 좌표에 고정, 겹침은 말풍선 방향이 피한다.
  - **존 카드**: 유리 칩 → 사진 위 흰 글씨 + 그라데이션(베이직 지면과 같은 문법), 이름 옆 **온도 알약**. 선택 테두리는 outline 음수 offset 으로 안쪽에 그려 안 잘린다.
  - **v3 서클** 26.5 → 31cqw, 스크롤 칸 위아래 여백으로 온도 배지·체크가 안 잘린다.
  - **모드 토글·축척**을 69cqw 에 못 박았다 — 캐러셀 높이에 따라 오르내리던 것. v2.27 이 서클 캐러셀을 height:auto 로 만들면서 그 흔들림이 더 커졌다(같은 판에서 함께 잡힌다).

### 2026-08-13 (2)
- **v2.27.0 — 폰 셸 UI 크기 · 롱프레스 마우스 · 캐러셀 여백/원형 썸네일 · 팝업 통일 · 리워드 지급 분리 (M03/M07/M09/M11/M15/M16, 콘솔 v0.101.0 짝)**
  — 사용자 8건 (now-here 6 + persona-vc 기능데모 연동 2).

  ① **모드 토글·하단 네비 축소 + 크기 옵션 (M09+M11)** — `.pa-mode`/`.phone-navbar` 에
  `transform:scale(var(--ui-mode-s/--ui-nav-s))` (CSS `zoom` 은 left/top 을 곱해 앵커가
  밀린다 — v1.64 교훈 그대로 transform 만). 기본 88%/90% = "살짝 줄여 달라"의 값.
  관리자 표시 옵션(s-view)에 `ui-mode-scale`/`ui-nav-scale`(60~120%) — `uiScale` 즉시
  적용·additive 클라우드 동기(mapPinView 와 같은 왕복 배선: cloudSave·applyCloudData·
  settingsSnapshotFull·applyExtraSettings·설정 JSON 복사·파일 백스톱).

  ② **지도 꾹 누르면 그 지점에 컨텐츠 추가 팝업 — 마우스 지원 (M09)** — 터치(520ms)·
  우클릭은 이미 있었는데(`attachAddGestures`) **마우스 길이 없었다**. 데스크톱 서비스·
  persona-vc 임베드 시연은 마우스라 "없는 기능"으로 보였다. mousedown 520ms(이동 12px
  취소) 같은 규칙. (v2.18 의 `request` 꾹 누름 링과는 다른 자리 — 그쪽은 컴포저 연출이다.)

  ③ **지면 접기 버튼이 카드 밖으로 삐져나옴 (M15/v3)** — `#sum-collapse` 의
  `right:2.6cqw` 는 `#phone-content-page` 기준인데 v3 가 그 안에 패딩 4.1cqw 를 더해
  버튼이 카드 우측 밖으로 1.5cqw 나갔다 → v3 에서 `right:6.7cqw`(카드 안쪽 2.6cqw).
  실측 버튼 right 349.9 < 카드 right 359.6 PASS.

  ④ **트렌드 캐러셀 좌우 여백 = 지면과 동일 (M03/v3)** — `#cp-zones` 가 부모 패딩
  4.1cqw 위에 자기 패딩 4.1cqw 를 **또** 얹어 여백이 배였다. 음수 마진으로 부모 패딩을
  되돌리고 스크롤러(.tz-scroll) 안쪽 패딩으로 시작선을 지면과 같게 — 스크롤 중 카드는
  화면 끝까지 흘러가 잘린다(블리드). 실측 첫 카드 left=15.375px=cp-frame left PASS.

  ⑤ **존 카드 스타일 '원형 썸네일'(circle) 신설 (M03+M15)** — v3 전용이던 v1.90 스토리
  서클을 **명시적 선택지**로: `ZONE_CARD_STYLES` 에 'circle'(관리자 고르개 4번째 옵션,
  클라우드 동기, 무대 `seed.zoneCard` 도 그대로 받는다), CSS 는 `#cp-zones.circle` 로
  style.css 이관(전 스킨 공용 — v3 의 glass 는 이제 글래스 캡션 그대로). 이관하며
  **온도 배지가 카드 높이만큼 세로로 늘어나던 버그 수정** — 기본 `.tz-card::after` 의
  `inset:0` 을 물려받아 top 이 살아 있었다(네 변 전부 재선언). 서클 모드는 높이도 auto.
  ⚠️교차: `applyExtraSettings`/`applyCloudData` 의 존 카드 검증을 `ZONE_CARD_STYLES`
  하나로 — v2.26 의 'page' 가 클라우드로 안 다니던 틈도 함께 봉합.

  ⑥ **팝업 문법 통일 (M15)** — 컨텐츠 추가 메뉴·프로필 메뉴·상세 팝업(cpop)을 현장
  Request 버블(.req-bubble)과 같은 프로스트(`--frost`)·라운드 5cqw(폰 밖 fixed 는
  20px — v1.82 cqw 규칙)·그림자 한 벌로. ai-presets 는 원래 같은 문법이라 무수정.

  ⑦ **리워드 지급 분리 (M07)** — 답변 즉시 `addCoins`+`coinFly`(v2.18~19, 프로필로
  날아가는 연출)를 **삭제**. 답변 → "전달됐어요. 리워드는 잠시 뒤 지급됩니다" →
  `REQ_REWARD_MS`(6s) 뒤 **agent 말풍선**(`showRewardBubble`, ai-bubble 자리)이 따로
  뜨며 **코인 버스트**(`coinBurst` — 🪙 12개 부채꼴 비산, CSS `coinPop`)와 함께 적립.
  내 Request 에 답하는 건 여전히 적립 없음. 회차 코인 복원(nhCoinsMark, v2.19)은 그대로 유효.

  ⑧ **시나리오 reward 액션 (M16 ⚠️교차: persona-vc)** — `NH_ACTIONS` 에 `'reward'`
  (v=말풍선 문구, 비우면 앱 기본·120자). answer(답 작성)와 **분리된 스텝**이라 콘솔이
  지급 시점·문구를 정한다. **임베드는 자동 지연 지급을 걸지 않는다**(`!IS_EMBED` 가드) —
  걸면 reward 스텝과 두 번 지급된다. persona-vc `PLAY_ACTIONS`/`ACTION_INFO` 도 같이
  올렸다(v0.101.0) — 옛 앱은 reward 스텝을 nhSanitize 가 버리고 ok:false 로 남긴다.

  검증: `node tools/check.js` PASS · 임베드(?embed=1) 콘솔 에러 0 · 서클/여백/접기 버튼
  실측 PASS(카드 left=frame left=15.375px) · 마우스 롱프레스 → at-point 팝업 ·
  `showRewardBubble('문구')` 코인 12개+잔액 +500 · nhSanitize 가 reward 스텝을 나름 ·
  ⚠️ cloudSave 왕복(uiScale·circle)·라이브 답변 경로는 로그인 게이트 뒤라 라이브에서 확인.

### 2026-08-13 (1)
- **v2.26.0 — 가져온 존이 그린 모양 그대로 · 존 카드 지면형 (M03+M16, 콘솔 v0.100.0 짝)**
  사용자 보고 둘. 하나는 버그였고 하나는 없던 모양이다.
  - **(버그) 콘솔이 보낸 존 모양을 앱이 통째로 버렸다.** 공개 목록은 `[[lat,lng],…]` 로
    납작하게 싣고 콘솔은 그것을 `{lat,lng}` 로 풀어 무대에 넣는데, `nhShape` 가 **쌍만**
    받았다 — 그래서 중심(`at`)만 살아남아 앱이 7칸 로제트를 새로 폈다. 칸 수·배열·크기가
    앱과 달랐던 것이 전부 이 한 줄이다. 이제 두 모양을 다 받는다.
  - **존 카드에 `page`(지면형) 신설.** 트렌드 모드 지도 탭의 상단은 지면 캐러셀이 아니라
    **존 카드 자리**다(`renderSummaryZones` 가 `#cp-frame` 을 숨기고 `#cp-zones` 를 편다) —
    사용자가 본 "2개만 뜨는 것" 이 그 카드다. 지면형은 상단 지면 3번과 같은 문법이고
    (사진이 카드를 꽉 채우고 이름이 유리 캡션) **스킨의 재해석을 안 탄다** — v3 의 원형
    서클 규칙에서 비켜 둔다(`:not(.page)`). 폭 30cqw 로 한 화면에 세 개가 들어온다
    (리스트 40cqw = 2.2개 · 지면형 = 2.9개).
  - **무대가 카드 모양을 고른다** (`seed.zoneCard`) — 여태 관리자 설정이라 시연마다 못
    바꿨다. 회차가 끝나면 원래 값으로 되돌린다(소리·코인과 같은 규칙, localStorage 는 안 건드린다).
  - 관리자 설정(index·admin)의 `존 목록 카드` 고르개에도 지면형을 넣었다.
  - 검증(헤드리스 크롬, 공개 목록의 실제 성수 존 데이터로): 무대 존이 **소스 좌표 그대로**
    2칸·반경 0.5km 로 서고, 세 카드 모양이 각각 적용되며(page/glass/list), 지면형이
    사진+유리 캡션으로 그려지고 한 화면에 2.9개가 든다. 존 안 피드 사진이 카드 썸네일로
    잡히는 것도 함께 확인(`zoneBestPhoto`).
### 2026-08-12 (12)
- **v2.25.0 — 효과음이 여섯 자리로 (M16, 콘솔 v0.99.0 짝, D122)**
  v2.23 은 소리가 **등장음 하나**였다. 시연에서 소리가 필요한 순간은 등장만이 아니다 —
  사용자 요청대로 다섯 자리를 더 냈다: 터치음·팝업 열림·팝업 닫힘·모드 전환·타이핑.
  - `seed.sfx` 가 문자열 하나 대신 **여섯 칸의 표**를 받는다
    (`pop`·`tap`·`open`·`close`·`mode`·`type`). 문자열이 오면 여태처럼 등장음이다 —
    옛 콘솔이 보낸 시나리오가 소리를 잃지 않는다. 자리마다 다른 소리를 걸 수 있고,
    안 건 자리는 조용하다.
  - **우는 자리는 이미 있던 단일 기준을 쓴다**: 등장=`nhBounceMark`(v2.23 그대로) ·
    터치=`nhTouch`(표식이 뜨는 자리) · 열림/닫힘=팝업·시트·매장 페이지의 표시 전환 ·
    모드=`switchMode`(실제로 바뀔 때만) · 타이핑=세 타자 루프(글·질문·답)의 한 틱.
  - **닫는 소리는 열려 있던 것을 닫을 때만** 운다 — `popclose`·`nhReset` 은 열렸든 아니든
    부르므로 그냥 울리면 아무 일 없는 자리에서 소리가 난다.
  - **리셋하는 동안은 무음** — `nhReset` 이 소리를 먼저 끈다. 회차를 시작할 때 도는 청소
    (모드 되돌리기·팝업 닫기)에서 앞 회차 소리가 한 번 울리던 것을 막는다.
  - 최소 간격도 자리마다 다르다 (타이핑 55ms ~ 모드 150ms).
  - 검증(로컬 :8766, `HTMLMediaElement.play` 가로채기): 한 회차가 `mode → open → 타이핑×3
    → tap → close` 순으로 정확히 울고, **두 번째 회차 시작의 리셋에서는 안 운다**.
    `nhSanitize` 가 여섯 칸을 그대로 나르고 https 아닌 값은 버린다. ⚠️ 글쓰기(스팟 컴포저)
    경로의 타이핑음은 지도 오버레이가 안 그려지는 세션이라 미검증 — 세 루프가 같은 한 줄이다.

### 2026-08-12 (11)
- **v2.24.1 — 존을 고치면 공개 목록도 따라 올라간다 (M03)**
  v2.24 를 배포했는데도 콘솔이 가져온 존의 자리가 그대로 엉망이었다(사용자 보고). 원인은
  코드가 아니라 **문서가 안 쓰인 것**이었다: `publicSettings` 는 관리자 **설정 적용 버튼**
  (`cloudSave`)에서만 쓰인다 — 존을 그리고 고치는 일은 `saveZonesToStorage` 로 끝나고
  `markCloudDirty` 만 켰다. 그래서 배포 뒤에도 콘솔이 읽는 목록은 **옛 저장본**(자리 키가
  아예 없다)이었고, 자리 없는 존은 여태처럼 무대가 폈다.
  - `saveZonesToStorage` 가 `publishZoneBook()` 을 부른다 — 공개 문서만 다시 쓴다(merge).
    관리자일 때만, **1.5초로 묶어서** 한 번 (칸 하나 옮길 때마다 쓰면 드래그 한 번에 수십 번).
  - 존 안 컨텐츠(스팟·피드)는 실시간 컬렉션이라 사람 손 없이도 바뀐다 — 그때마다 올리지
    않는다. 존을 고치거나 설정을 적용할 때 함께 새로 찍힌다.

### 2026-08-12 (10)
- **v2.24.0 — 가져온 존이 제 자리에 선다 · 존 안 컨텐츠도 나른다 (M03+M16)**
  콘솔 v0.98.0 과 짝. v2.22 는 존의 **이름·색·크기**만 나르고 자리는 무대가 새로 폈다 —
  그래서 가져온 존이 실제 지도의 그 자리가 아니었다(사용자 지적).
  - `zoneBook` 에 **자리와 모양**을 싣는다: `at`(중심) · `radiusKm` · `shape`(그린 칸 좌표
    [[lat,lng],…], 30칸까지). 칸마다 키를 반복하면 문서가 두 배라 납작하게 보낸다.
  - `zoneBook` 에 **그 존 안의 컨텐츠**도 싣는다(`spots` 10 · `feeds` 6) — 판정은 화면이
    쓰는 것과 같다: 스팟은 좌표(`ptInZone`), 피드는 태깅(`zone`) **또는** 좌표.
    각 항목은 제 좌표를 들고 간다. 사진은 https 주소만(올린 사진은 data URI — 1MB 상한).
  - `nhLayZone` 이 `at`·`shape`·`radiusKm` 을 받으면 **그대로 편다**. 없으면 여태처럼
    지역 중심 둘레에 로제트로 편다 — 손으로 적은 존은 하나도 안 바뀐다.
  - 무대 **스팟·피드도 `at`(제 자리)을 들 수 있다** — 존은 실제 자리에 서는데 그 안
    컨텐츠가 동네 중심에 흩어지면 둘이 따로 논다. 우선순위는 사람이 옮긴 자리(v2.20 pos)
    → 항목의 `at` → 무대가 편 자리.
  - 검증(로컬 :8766): 존 하나와 안팎 컨텐츠를 만들어 `zoneBook` 이 존 안 것만(태깅 포함)
    싣는 것 확인 · 콘솔이 보낼 모양대로 재생해 **자리를 든 존은 그 좌표에 3칸 그대로**,
    자리 없는 존은 여태처럼 7칸 로제트 · 자리를 든 스팟도 그 좌표에 · `node tools/check.js` PASS.

### 2026-08-12 (9)
- **v2.23.0 — 컨텐츠 등장 효과음 (M16)**
  콘솔 v0.97.0 과 짝. 무대의 `seed.sfx` 주소를 받아 **컨텐츠가 뜰 때** 운다.
  - 우는 자리는 `nhBounceMark` 하나 — "지금 생긴 것" 의 단일 기준이라 액션마다 따로 안 붙인다. burst 는 바운스 없이 소리만.
  - 최소 간격 120ms 로 겹침을 막는다(burst 50개가 기관총이 되지 않게). 주소는 https·data:audio 만 통과.
  - **자동재생은 콘솔이 iframe 에 allow="autoplay" 를 줘야 산다**(교차 오리진). 막혀도 재생은 안 멈춘다.
  - 무대 까는 동안은 무음, 회차가 바뀌면 앞 소리를 끈다. 로컬 DOM 계측으로 전 경로 확인.

### 2026-08-12 (8)
- **v2.22.0 — 공개 설정 문서가 트렌드 존 목록도 나른다 (M03+M11)**
  콘솔 v0.96.0 과 짝. 기능 데모의 무대 존을 손으로 하나씩 적던 것을 **앱에서 골라 가져오게** 하기 위한 앱 몫이다.
  - `settingsSnapshotFull` 에 `zoneBook` 추가 — 이름·설명·색·온도·사진(https만)·동 이름·칸 수. 존을 만들거나 고치면 기존 cloudSave 경로로 공개 문서에 함께 실린다.
  - 실제 존이 사는 `shared/mapContent` 는 비로그인 403 이라 콘솔이 못 읽는다 — 공개된 문 하나(`publicSettings`)에 목록을 얹는 것이 유일한 길이었다.
  - hexCenters 는 안 싣고(무대 존의 기하는 앱이 결정적으로 편다), 사진은 https 주소만(올린 사진은 data URI 라 1MB 상한에 닿는다).
  - **적용은 안 한다** — applyExtraSettings 가 zoneBook 을 안 읽는다(내보내기 전용). 로컬 계측으로 확인.

### 2026-08-12 (7)
- **v2.21.0 — dim/undim · fast 스텝 · 무대 트렌드 존 · 드랍 지면 옵션 · 지면 바운스 제외 · AI 버튼 사선 흐름 (M16+M08+M03)**
  사용자 요청 7건 중 앱 몫. 콘솔 v0.94.0 과 짝이다(앱 먼저).
  - **`dim`/`undim` 액션** — dim(v=남길 불투명도 %, 빈 값 22)이 그 순간 깔린 지도 컨텐츠
    (스팟·피드 핀·Request·딜)만 `filter:opacity()` 로 흐린다. 이후 뜨는 것은 제 밝기 —
    "이 다음 것만 봐 달라". 표(`nhDimIds`)는 각 오버레이 onAdd 가 다시 보고(재렌더 대응,
    클러스터=멤버 전원일 때만), undim·nhReset 이 걷는다. 어휘 3중 동기화(NH_ACTIONS·
    콘솔 PLAY_ACTIONS·프롬프트).
  - **스텝 `fast:true`** (additive) — 연출(표식·타이핑·바운스·시트)을 접고 그 자리에서
    동기 커밋. 콘솔의 "단계 클릭 = 그 단계만 재생" 이 앞 단계를 60ms 로 조립하는 길이다.
    write 는 컴포저를 안 연다(컴포저 textEl 이 onAdd 다음 프레임에 생겨 그 자리 commit 은
    빈 글이 된다) — 컴포저 commit 의 비관리자 갈래를 그대로 한다.
  - **무대 트렌드 존** — `seed.zones`(최대 6: name 필수·desc·img·color `#rrggbb`·temp·r 1|2).
    `nhLayZone` 이 지역 둘레 결정적 자리에 헥사 7/19칸을 펴 `trendZones` 에 넣는다 —
    드로어 카드·리스트·온도·포커스 전부 기존 게이트, 트렌드 모드에서만 보인다. sweep 이 걷는다.
  - **`drop v:feed e:'keep'`** — 그 카드를 상단 지면에 안 얹는다(`nonews`,
    `feedSummaryItems` 가 거른다). 빈 값=여태처럼 실린다.
  - **지면의 피드 파생 카드는 바운스를 안 탄다** — renderNews 가 바운스 표를 떼 가서 지면
    사진이 지도 핀과 같이 튀고, 표 2장 중 1장을 훔쳐 지도 핀 하나가 안 튀던 것. 이제
    지도 위 컨텐츠(와 page 드랍의 지면 카드)만 튄다.
  - **트렌드 AI 버튼 = 온도 팔레트 붉은 두 색(#f2862e·#e23b2a) 사선 흐름** — 렌즈
    무지개·발광(v2.15)은 삭제, 선글라스는 정지한 다크 렌즈. new·v3 스킨이 `.pn-ai` 배경을
    축약형으로 덮어서 **스킨 두 벌에도 흐름 배경을 재선언**했다.
  - 검증(로컬 :8765 `?embed=1&clean=1`, DOM 계측): sanitize 통과·fast 동기 커밋·dim/undim
    왕복·nonews 필터·존 3개(7·19·7칸) 깔림/걷힘·2회차 동일·v3 스킨 그라디언트 실측·콘솔 에러 0.
    지도 위 픽셀(핀 흐림·존 폴리곤)은 창이 표시되지 않는 세션이라 라이브에서 확인할 것.

### 2026-08-12 (6)
- **v2.20.0 — 딜을 어떻게 열지 · 쿠폰 받기 · 똑바로 선 Request 핀 · 자리의 저장소 (M17+M07+M16)**
  사용자 요청 5건. 콘솔 v0.93.0 과 짝이다(앱 먼저).
  - **딜 여는 방식을 단계가 정한다** — `pop v:deal` 의 `e` 가 `'sheet'`(쿠폰 시트) / 빈 값·
    `'page'`(매장 전용 페이지)를 가른다. v2.15 부터 핀 탭은 늘 매장 페이지였는데, 매장을
    소개하는 장면과 쿠폰을 받는 장면은 보여줄 화면이 다르다. 실서비스 핀 탭은 그대로 페이지다.
  - **`coupon` 액션 신설** — 시트의 '쿠폰 받기' 를 실제로 누르는 장면이다(시트가 안 열려
    있으면 열고 → 버튼에 터치 표식 → 한 박자 → 받기). **리워드 문구는 따로 정한다**:
    `v`=문구 · `e`=표시 초(`'0'` 이면 안 띄운다). 받는 일과 말하는 일이 갈려야 "쿠폰만 받고
    말은 다음 장면이" 같은 연출이 된다.
  - **시트의 `alert()` 를 걷었다**(`claimDeal`) — 그 창은 브라우저 것이라 화면 밖이고
    **자바스크립트를 멈춰** 재생이 그 자리에 섰다 (v2.18 의 `prompt()` 와 같은 이유).
    말은 하단 AI Agent 말풍선이 한다 — 코인 적립과 같은 문법이다.
  - **Request 핀이 똑바로 선다** — 물방울을 만들려던 `rotate(-45deg)` 가 세 스킨 모두에
    남아 있었다. legacy 는 핀이 기울어 보이고, 원으로 다시 그린 new·v3 에서도 **그림자가
    45° 비껴** 떨어져 기운 것처럼 보였다. 회전을 걷고 원으로 통일했다(글리프의 되돌림
    회전도 함께). 기본 스킨(v3)이 이미 원이라 지금 화면과 가장 가깝다.
  - **Request 핀을 끌어 옮긴다**(`ReqPin._onDown`·`moveRequest`) — 스팟·피드가 오래 하던
    제스처다(터치는 롱프레스 뒤, 마우스는 즉시). 좌표와 **동 이름을 함께** 옮기고(자리만
    옮기고 옛 동 이름이 남으면 카드가 거짓말을 한다), 라이브면 문서까지 갱신한다.
    무대 항목이면 `nhPosNote` 가 자리를 남긴다 — `rqw`·`nhReqIds` 계약은 v2.3·v2.18 에
    적혀만 있고 **끄는 손이 없어 죽어 있었다**.
  - **옮긴 자리의 저장소가 콘솔로 올라갔다** — `NH_POS_KEY`(localStorage)는 이 기기의
    것이라 다른 PC 에서는 없는 값이었다(같은 데모인데 자리가 달랐다). 이제 `nh:run` 이
    `pos` 를 실어 오고(`nhPosRecv`, **받은 값 우선**), 핀을 옮기면 `nh:pos` 로 콘솔에 알린다.
    저장은 콘솔의 데모 문서가 한다(그쪽이 이미 PC 를 넘어 동기화된다). localStorage 는
    콘솔 없이 여는 임베드를 위한 폴백으로 남는다. 둘 다 additive 라 배포 순서와 무관하다.
  - 검증(로컬 :8766 `?embed=1&clean=1`, DOM 계측): `pop v:deal e:sheet`=시트 · 빈 값=매장
    페이지 · `coupon` 이 시트를 닫고 지정 문구를 3초 띄움(**alert 0회**) · `nhPosGet` 이
    받은 값 → localStorage 순, 5km 밖 무시 · `moveRequest` 가 좌표와 동 이름을 함께 옮김
    (역삼1동→서초2동) · `nhSanitize` 가 잘못된 pos 키·좌표를 버림 · `node tools/check.js` PASS.
    핀 **모양**은 puppeteer 로 따로 찍어 확인했다(회전 제거 전/후 비교).
    ⚠️ 핀 **드래그 제스처**는 미검증 — 이 세션은 브라우저 창이 표시되지 않아 지도
    오버레이가 프레임을 못 그린다(핀 자체가 DOM 에 안 뜬다). 드래그가 끝나면 부르는
    두 함수(`moveRequest`·`nhPosNote`)는 직접 불러 계약을 쟀다.

### 2026-08-12 (5)
- **v2.19.0 — Request 두 시나리오 리뷰 반영 (M07+M16, ⚠️교차 M09/M12/M15)** — v2.18 을
  적대적 리뷰(20 에이전트)에 걸어 확인된 것을 고쳤다. 시나리오 자체는 v2.18 대로다.
  - **(버그) 수신 카드 💬Chat 참여가 죽은 팝업을 열었다** — 라이브 리스너는 카드에 필요한
    최소 객체 `{id,q,place,lat,lng}` 만 넘기는데 v2.18 이 답하기를 `openContentPop('req',rq)`
    로 바꾸면서 그 객체가 그대로 팝업에 들어갔다. `ts` 가 없어 `reqActive` 가 false →
    "⏱ 종료 · 답변 0개" 에 답장 칸도 안 그려진다. `reqById(id)` 로 **전체 객체**를 찾아 연다
    (무대 경로 nhDrop 은 전체 객체를 넘겨 무사했다 — 실서비스 라이브만 깨져 있었다).
  - **(버그) 코인 연출이 팝업 뒤에서 재생됐다** — 답을 보내는 두 경로(팝업 답장 줄·무대
    nhAnswerTyped)는 **팝업을 연 채로** answerRequest 를 부른다. 코인은 `.phone-screen`
    (자체 스태킹 컨텍스트)에 z-index 200 으로 붙는데 `#content-pop` 은 z-index 100001 의
    fixed 스크림이라, v2.18 의 핵심 장면이 정작 그 장면을 만드는 경로에서 안 보였다.
    coinFly 가 **열려 있는 팝업 안**에 붙고(자리는 폰 화면 기준 그대로), 폰 밖에는 cqw 를
    재어 줄 컨테이너가 없으므로 크기도 px 로 준다.
  - **(버그) 라이브에서 보낸 답이 팝업에 안 앉았다** — 라이브는 answerRequest 가 Firestore
    만 갱신하고 로컬 배열은 스냅샷이 다시 만든다. v2.18 은 그 자리에서 동기로 팝업을 다시
    열어(스냅샷 전) 빈 입력칸이 되살아났고, 손에 든 객체는 스냅샷이 오면 고아가 됐다 →
    "안 갔나?" 하고 한 번 더 보내면 답변이 **중복 저장**된다. req 팝업에도 `cpopRefresh`
    를 걸고(스팟 의견이 오래 쓰던 길) liveRequests 스냅샷이 그것을 부른다. 다시 열 때는
    항상 `reqById` 로 지금 저장소에 있는 것을 본다.
  - **(버그) 짧은 스텝에서 request→answer(i:-1) 사슬이 끊겼다** — 등록/전송 커밋 바닥이
    1200·900ms 인데 nhRun 은 `st.ms` 뒤에 다음 스텝을 시작한다. ms 가 바닥보다 짧으면
    (사람이 0.6초로 적거나 배속으로 줄면) 커밋 전에 다음 스텝이 실행돼 `i:-1` 이 빈손이거나
    엉뚱한 Request 를 집었다. 커밋을 **그 스텝 안으로 접는다** — 연출은 접혀도 사슬은 안 끊긴다.
  - **컴포저 취소가 안 먹었다** — 무대 등록 타이머가 사람이 취소·직접 등록한 뒤에도
    한 번 더 올렸다. `ReqComposer.closed` 를 보고 물러난다. 전역 Escape 도 이제 이 카드를 닫는다
    (제 입력칸에도 Escape 가 있지만 포커스가 빠지면 그 길이 없다).
  - **무대의 도착 카드가 개인 설정에 지워졌다** — `reqCardShown`('보기' 토글, 이 기기의 취향)이
    꺼져 있으면 drop 의 AI Agent 카드가 조용히 빠졌다. 무대 경로는 force 로 지나간다.
  - **회차마다 코인이 쌓였다** — 잔액은 이 기기에 남는 값이라 재생할 때마다 +500 이 누적돼
    두 번째 회차가 다른 숫자로 시작했다. nhReset 이 그 회차의 적립을 되돌린다(nhCoinsMark/Restore).
  - **팝업 답변 목록이 데스크톱에서 초대형이었다** — `.rqa-*` 가 cqw 인데 `#content-pop` 은
    폰 컨테이너 밖이라 뷰포트로 풀린다(M15 v1.82 "컨테이너 밖은 px" 규칙). #cpop-body 에 px 재정의.
  - 검증(로컬 :8766 `?embed=1&clean=1`, DOM 계측): 시나리오1(seed mine:false → drop → answer)
    = 도착 카드 표시 → 팝업 → 답변 1건 → **코인 노드 8개가 #content-pop 안**에 · myCoins 500.
    시나리오2(request ms=600 → answer i:-1) = 등록이 스텝 안(≈480ms)에 끝나 answer 가 방금 만든
    Request 를 집고 ok=true, 회차 시작에 코인 0 으로 복원. `node tools/check.js` PASS.
    ⚠️ 컴포저 카드의 **픽셀**은 미검증 — 이 세션의 브라우저 창이 표시되지 않아 지도
    오버레이(OverlayView.onAdd)가 프레임을 못 그린다(같은 조건에서 write 의 스팟 컴포저도
    안 뜬다 = 환경 문제). 연출 자체는 v2.18 에서 눈으로 확인된 그대로다.

### 2026-08-12 (4-1)
- **v2.18.1 — 자동 도착(answerIn)은 내 Request 만 (M16)** — 남의 Request(mine:false)에
  answerIn 이 실려 오면 8초 뒤 자동 답이 쏘여 **아무도 안 답했는데 코인이 적립**되는
  화면이 됐다(answerRequest 는 "남의 것에 답함" 으로 읽는다). nhLayReq 가 `r.mine!==false`
  일 때만 자동 도착을 건다. 콘솔 toSeed 도 같은 갈래에서 answer/answerIn 을 키째로 뺀다.

### 2026-08-12 (4)
- **v2.18.0 — Request 두 시나리오: 남의 질문에 답해 코인을 받고, 꾹 눌러 묻는다 (M07+M16 ⚠️교차, 콘솔 D114 짝)**
  — 콘솔 v0.91.0 과 짝(앱 먼저 배포). 사용자 요청: "Request 관련 기능이 꼬여 있다 — 두 시나리오가 되게 전체 정리".
  꼬임의 정체: ①무대 Request 가 전부 **내 것**(nhLayReq 가 myUid)이라 "남이 올린 Request" 를 깔 길이 없었고
  ②`request` 액션은 preset 을 **즉시 등록**해 묻는 손이 화면에 없었고 ③하단 AI Agent 수신 카드(showReqBubble)는
  라이브 리스너 전용이라 무대 재생에서 영영 안 떴고 ④적립은 로직뿐, 문구는 늘 수신자 시점("답변 도착")이었다.
  - **M16 ①** `seed.reqs[].mine:false` = 남이 올린 Request — nhLayReq 가 by 를 `nh_other` 로 적는다.
    팝업이 답장 칸을 그리고(기존 1182 분기 그대로) `answer` 가 "사용자가 답하는" 장면이 되어 코인이 적립된다.
  - **M16 ②** `drop v:req` 로 남의 Request 가 뜨면 **수신 카드도 같이** — 요청자 본인 것이면 안 띄운다(실서비스 규칙 그대로).
  - **M16 ③** `request` 액션 = `nhRequestTyped` — 꾹 누르는 링(560ms) → 컴포저 카드 → 글자별 타이핑 → 등록 → 핀 바운스.
    커밋 바닥 1200ms(write 900 + 링 시간), 자리 기억은 `rqw` 키(write 와 같은 약속), 방금 올린 것은 `i:-1`.
  - **M16 ④** `pop v:req` 의 터치 표식이 **핀 위에** 선다 (reqMarkers 에서 id 로 찾는다).
  - **M07 ①** 등록 경로 단일화 `commitFieldRequest` + **ReqComposer 오버레이** — 사람 경로(+메뉴)의 네이티브 prompt() 도 없앴다.
  - **M07 ②** 수신 카드 시안 반영 — 검정 필 "현장 Request" + 🪙500(REQ_COIN 연동) · 버튼 `📷 사진 제출`·`💬 Chat 참여`
    (답하기 prompt() 제거 — Chat 참여가 상세 팝업의 인라인 답장 칸을 연다). index/admin 양쪽 마크업 동기.
  - **M07 ③** `answerRequest` 문구가 시점을 가른다 — 남의 것에 답하면 "🪙 500 적립! 전달됐어요" + **coinFly**
    (🪙 7개가 프로필로 날아가고 +500 이 떠오른다), 내 것이면 여태처럼 "현장 답변 도착".
  - **M07 ④** 답변 목록을 누구의 Request 팝업에서도 본다 — 답을 쓴 사람도 제 답이 앉는 것을 본다.
  - 위생: nhReset 이 ReqComposer·수신 카드도 걷는다 · 설치 배너를 임베드에서 막는다(IS_EMBED).
  - 검증(headless Chrome 390×844, :8765 `?embed=1&clean=1`): 시나리오1 — drop 946ms 에 핀 바운스+수신 카드(필·🪙500·버튼 2개),
    pop 3012ms(핀 터치 표식), 답 타이핑 5표본, 6012ms 에 코인 0→500·coin-fly 7개·+500·적립 문구, 답변 1건.
    시나리오2 — 링 1010ms → 카드 1509ms → 질문 타이핑 7표본 → 3506ms 등록(by=나, stage:true)+핀 바운스 →
    `answer i:-1` 로 답 도착("현장 답변 도착", 코인 불변). 리셋 후 reqs 0·컴포저 0·카드 숨김. `node tools/check.js` PASS.
  - ⚠️ 인앱 브라우저(합성 멈춤 탭)에서는 지도 오버레이 onAdd 가 안 와서 컴포저·핀이 안 선다 — 검증은 실제 Chrome 으로.

### 2026-08-12 (3)
- **v2.17.0 — 매장 페이지의 글·사진을 콘솔이 정한다 · 액션 한 줄 · 잉크 CTA (M16/M17)**
  — 콘솔 v0.90.0 과 짝(앱 먼저 배포). 사용자가 콘솔 쪽에 요청한 6건 중 **앱 몫 3건**.

  ① **매장 페이지의 글·사진을 사람이 채운다.** v2.15 가 `nhLayDeal` 에 optional
  `addr`/`desc` 를 뚫었는데 **`nhSanitize` 가 그 두 칸을 걷어내고 있었다** — 임베드로 오는
  모든 시나리오는 sanitize 를 지나므로, 콘솔이 보내도 도착하지 않았다(화면만 보면
  "콘솔이 안 보낸 것" 과 구별되지 않는다). 이제 세 칸이 지나간다: `addr`(60자) ·
  `desc`(**120 → 300자** — 매장 소개는 한 문장으로 안 끝난다) · **`photos[]`**(최대
  `NH_MAX.dealPhoto`=9, 시안이 3열이라 세 줄로 꽉 찬다. `nhImgSrc` 를 통과한 주소만).

  ② **`storeFeedPhotos` 는 올린 사진이 있으면 그것만 쓴다.** 근처 피드를 섞으면 "내가
  올린 것" 과 "앱이 주워온 것" 이 한 그리드에 뒤섞여, 무엇을 고쳐야 그 칸이 바뀌는지
  알 수 없다. 안 올렸으면 여태 그대로(같은 매장 → 400m 안 → 결정적 `seedImg`).

  ③ **액션 4버튼이 언제나 한 줄이다.** `flex-wrap:wrap` 이라 폰 폭에서 **'타임딜
  쿠폰받기'(CTA) 가 둘째 줄로 내려가** 접힌 자리에 숨었다. `nowrap` + 알약을 그만큼
  조인다(글자 3.1→2.9cqw · 좌우 3.8→2.8cqw · gap 2→1.6cqw). 넘치는 경우(글자를 키운
  스킨 등)에도 줄바꿈 대신 옆으로 밀린다 — `overflow-x:auto` + 스크롤바 숨김.

  ④ **CTA 하이라이트가 보라(#7b61ff) → 잉크(#1a1a1a).** 딜 시트의 '쿠폰 받기'(`ds-claim`)와
  같은 색이 됐다. 기본형·v3 스킨 양쪽. 뱃지 칩의 보라는 시안 그대로 남겼다(버튼이 아니다).

  검증(로컬 :8765 · `?embed=1&clean=1` · skin v3): `nhSanitize` 가 `addr`·`desc`·`photos`
  를 실어 나름 · 매장 페이지가 콘솔 값으로 **주소줄·소개·그리드 4칸**(data: URI)을 그림 ·
  액션 줄 **1줄**(375px 에서 308/341px · 1146px 폭에서도 1줄) · CTA computed
  `rgb(26,26,26)` · `node tools/check.js` PASS.
  ⚠️ 이 개발 창은 스크린샷이 안 찍혀(브라우저 창이 표시되지 않음) **DOM 실측으로만**
  확인했다 — 색·줄바꿈은 computed 값과 버튼 좌표로 검증했다.

### 2026-08-12 (2)
- **v2.16.0 — 지도 컨텐츠 지면 고정 · 점 전환 이어 붙이기 · 피드 핀 깜박임 제거 · 토글 폭 (M04/M05/M11/M15)**
  — 사용자 4건. 넷 다 "줌할 때 지도 위가 불안하다"는 한 증상의 다른 얼굴이었다.

  ① **피드 사진이 줌마다 깜박였다 (M05)** — `renderFeedMarkers` 가 매번
  `clearFeedMarkers()` 로 **전부 지우고 새로 만들었다**. 줌 변경마다(`reclusterFeedMarkers`
  ← idle) `<img>` 가 새로 붙으니, 디코딩 전 한 프레임이 빈 원으로 그려진다. 그게 깜박임이다.
  이제 **클러스터 멤버 id 집합(`feedPinKey`)을 신원으로 삼아 있는 핀을 이어 쓴다**
  (`syncFeedPins` + `FeedThumb._adopt`). 좌표·사진·온도·개수 뱃지만 덮어쓰고 DOM 은 산다.
  `img` 비교는 `getAttribute('src')` — `.src` 는 절대경로로 정규화돼 매번 달라 보인다.
  실측: 같은 줌 재렌더 29/29 재사용, 줌 16→16.4 28/30, 16→15(클러스터 병합) 22/26.
  전에는 **매번 0%** 였다. 고아가 된 `clearFeedMarkers` 는 제거.

  ② **줌아웃하면 컨텐츠가 지면 대비 커졌다 (M04/M05/M11)** — v1.95 가 `contentScale` 에
  건 **0.7~1.6 클램프**가 원인이다. 지도는 절반으로 줄어드는데 컨텐츠는 0.7 에서 멈추니
  줌 16 에서 건물 하나였던 것이 줌 13 에서 블록 하나를 덮는다. 클램프를 걷어내고
  **`2^(z-16)` 순수 지면 고정**으로 되돌렸다 — 설정한 px 가 어느 줌에서도 같은 미터
  범위를 덮는다. (실측 배율 z11 0.031 / z16 1 / z18 4.)
  ⚠️ **지역·존 이름표는 이 곡선에서 뺐다**(`labelScale`, 0.7~1.6 유지) — 이름표는 지면
  위 컨텐츠가 아니라 지도의 이름표라, 지면에 고정하면 시 단위 줌에서 0.4px 로 사라진다.

  ③ **점으로 바뀔 때 크기가 툭 끊겼다 (M04/M05)** — 점 전환 시점은 관리자 설정
  (`spotDotScaleM`, 현재 클라우드 값 **100m**)인데 크기 곡선과 무관해서, 줌 17 의 50px
  사진이 줌 16 에서 12px 점이 됐다(4배 점프). 이제 **임계값 한 줌 전부터 배율을 점 크기
  쪽으로 당긴다**(`contentDot`, `DOT_RAMP=0.5`) — 임계값에 닿는 순간의 크기가 정확히 점
  크기라 크기는 그대로고 모양만 바뀐다. 그 구간 밖은 순수 지면 고정이다. 아주 멀리서
  설정과 무관하게 점보다 작아질 때도 점 크기에서 멈춘다(`s<=floor`).
  실측 피드 핀 z14→19: 12 · 12 · 12 · 19 · 42 · 71 · 100 · 141 · 200px (전엔 12↔50 스냅).
  스팟(이모지 기준 8px)·딜(34→12px)·Request 도 같은 함수를 쓴다. Request 는 점 모양이
  없어 **점 크기까지만 줄고 사라지지 않는다**. 남는 모양 변화(테두리·불투명도)는 CSS
  트랜지션 .22s 가 받는다(reduced-motion 이면 끈다).

  ④ **콘솔 토글 트랙이 36px 인데 48px 로 늘어나 있었다 (M15)** — `.toggle-switch` 도
  label 이라 `.setting-row label{min-width:48px}`(특정성이 더 높다)를 물려받았다. v2.15
  가 v3 의 132px 건은 잡았지만 `#adm-panels` 안에서만이었다 — 앱 페이지 좌측 패널·
  다른 스킨은 그대로 48px 였다. 베이스 CSS 에 `.setting-row label.toggle-switch{min-width:0}`
  한 줄로 전 페이지·전 스킨 수렴. 실측 48→36px.

  검증(로컬 :8765, `?embed=1`): 배율 곡선·점 전환 크기표를 줌 0.5 단위로 실측, 핀 DOM
  재사용률 측정, `_adopt` 후 `<img>` 동일성·클러스터 뱃지·`--heat` 유지 확인, 토글 폭
  48→36 실측, 콘솔 에러 0, `node tools/check.js` PASS.
  ⚠️ 이 샌드박스 브라우저는 Maps 오버레이 수명주기를 안 돌려(v2.15 와 같은 한계) 라이브
  줌 애니메이션의 육안 확인은 못 했다 — draw() 를 직접 돌려 값으로 검증했다.
  **실기기에서 줌 인아웃 한 번 볼 것.**
  ⚠️ 지면 고정의 당연한 귀결: 현재 설정(피드 아이콘 25px, dotScaleM 100m)에서는
  **줌 16.3 아래가 전부 점**이고 줌 19 에서 피드 핀이 200px 다. 컨텐츠가 덮을 **지면
  범위**가 곧 설정값이라 그렇다 — 크게/작게는 콘솔의 아이콘 크기·점 전환 축척으로 조절.

### 2026-08-12 (1)
- **v2.15.0 — 지도 컨텐츠별 표시 설정 · 타임딜 매장 페이지 · AI 무지개 선글라스 · 콘솔 크롬 정리 (M11+M17+M08+M14/M15)**
  — 사용자 4건.

  ① **지도 컨텐츠별 표시 설정 (M11)** — 콘솔 메뉴 "🖥 화면·디자인"에 **🧷 지도 컨텐츠
  표시**(`s-pins`) 패널 신설. 4종(스팟·피드·Request·타임딜) 각각 표시 on/off, Request·딜은
  핀 크기(%), 딜은 %라벨 토글. 스팟 스타일 풀셋(s-spot)·피드 크기(`#feed-icon-size`,
  s-view 에서 이사)는 기존 노브를 그 자리에 묶었다. 저장은 **additive 최상위 키
  `mapPinView`** — feedIconSize 와 같은 즉시 적용 패턴으로 왕복 6지점(cloudSave ·
  applyCloudData · settingsSnapshotFull · applyExtraSettings · loadFileDefaults ·
  initSettingsExport) 전부 배선(v2.3.1/v2.11 교훈 그대로). 렌더 게이트는 각 render 함수
  머리에서 걷기만 하고 기존 연쇄(스팟→피드→목록·Request→딜→declutter)는 유지.
  크기는 `contentScale` 곡선 공통 유지 × 종류별 배율(`pinScale`)만 곱한다(v1.95 계약 보존).

  ② **타임딜 → 매장 전용 페이지 (M17)** — 핀 탭이 바텀시트 대신 **전면 매장 페이지**
  (`#store-page`, z-29)를 연다(시안: 석촌동 쵸리상경). 뱃지 칩(참여중·°C·타임딜)은
  Overview 칩과 같은 **실값 문법** — 참여중=반경 400m 컨텐츠 수, °C=존 온도(36.5~99.9).
  `#매장명`·주소줄(동네+실측 거리)·소개(optional `desc`, 없으면 템플릿)·액션 4버튼
  (메뉴보기=사진 그리드 스크롤 · 지도보기=cpopGoMap · 커뮤니티=switchTab('social') ·
  **타임딜 쿠폰받기**=기존 딜 시트가 z-30 으로 위에 뜬다)·TIME DEAL 배너(1초 티커)·
  사진 그리드(같은 매장 `shop===feed.name` 우선 → 근처 400m → 결정적 seedImg 폴백).
  마크업은 index/admin 양쪽 복제(딜 시트 관례). 임베드 `pop v:'deal'` 도 매장 페이지를
  열고(구버전 앱이면 시트 폴백), popclose·회차 종료 sweep 이 페이지도 닫는다.
  `nhLayDeal` 에 optional `addr`/`desc` 두 칸 추가(additive — 콘솔이 안 주면 파생값).

  ③ **AI 버튼: 트렌드 색상 변화 삭제 → 선글라스 무지개 (M08)** — v2.6~2.7 의 온도
  팔레트 대각선 흐름(aiHeatFlow)·웜톤 블롭 재도색(AI_PALETTE.trend)·skin-v3 재선언을
  전부 삭제. 트렌드 구분은 **렌즈(rect.sh-lens) fill 무지개 순환 + 그룹 drop-shadow 발광**
  (`aiLensRainbow`/`aiShadeGlow`, 같은 6s 주기) 하나다. SVG fill/filter 애니라 v3 의
  background 축약 명시도 문제(237 주석)와 안 싸운다. reduced-motion 은 보라 한 색 고정.
  렌즈 rect 에 `sh-lens` 클래스 부여(index/admin 양쪽).

  ④ **콘솔 크롬 정리 (M14/M15)** — v3 콘솔 문법 3원칙(카드·알약·높이)으로 남은 곳을
  수렴: 독립 모달 4종·모달 ✕(.acct-btn)·문서 링크 2벌(.ft-docs→.adm-docs 문법)·
  목록 행(.al-item/.room-item)·폼 입력·색상 팝업·토글(켜짐=잉크)을 v3 재도색.
  리터럴 5색을 토큰 승격(--v3-hover/tabface/danger-bg/danger-bg2/dirty). action-btn
  높이 32/28 로 표준화(탑바·셀렉트·탭과 한 벌). **토글 132px 버그 수정** — `.toggle-switch`
  도 label 이라 v3 의 라벨 폭 132px 를 물려받아 트랙이 행 폭만큼 늘어나 있었다(s-region
  등 기존 패널도 같았다). 시스템 패널 h4 이모지 제거(이모지는 내비 캡션에만), 인라인
  스타일 4곳을 클래스로(.btn-block/.row-sep/.auth-role/.setting-row .section-hint),
  number 입력 정식 클래스 `.num-in` 신설(sg-num 임시변통 종결).

  검증(로컬 :8765): 매장 페이지 시안 재현·쿠폰 시트 z층 정합·티커 감소 실측, AI 버튼
  legacy/v3 양쪽에서 배경 애니 제거(computed background-image:none)·sh-lens
  aiLensRainbow 구동·블롭 stop 무채색 유지 실측, 표시 설정 게이트(4종 0↔N)·%라벨·
  배율(0.8/1.5) 상태 검증, s-pins 패널 v3 렌더·클라우드 저장 키 확인, `node tools/check.js` PASS.
  ⚠️ 샌드박스 브라우저는 지도 타일이 막혀 오버레이 라이프사이클이 지연됐다 — 핀 부착은
  수동 사이클로 확인했고, 실지도 시각 확인은 배포 후 한 번 볼 것.

  리뷰(4렌즈+적대 검증)가 잡은 3건도 반영: ① **nhReset 이 매장 페이지를 안 닫았다** —
  sweep 은 이번 회차 임시 딜이 있을 때만 닫으므로, 전역 시드 딜을 열어 둔 채(재생 중단·
  수동 탭) 다음 회차가 오면 z-29 전면 페이지가 화면을 덮은 채 뒤에서 재생된다 →
  nhReset 무조건 닫기 묶음에 closeStorePage+closeDealSheet(승격) 추가. ② 스팟 숨김
  게이트가 declutterMarkers 를 생략 — 새 피드 핀이 옛 offset 핀 위에 겹칠 수 있어
  게이트에도 재계산 한 줄(60ms 디바운스라 중복 무해). ③ c-feed 안내문이 아이콘 크기의
  옛 위치(표시 옵션)를 가리킴 → s-pins 로 안내 수정.

### 2026-08-11 (13)
- **v2.14.0 — burst 줌아웃을 프레임으로 돌린다 (M16)**
  — 사용자: "줌아웃이 끊기기도 하고 자연스럽게 뜨지를 않는다". 콘솔 v0.85.0 과 짝.

  **원인은 `setZoom` 자체였다.** v2.13 은 목표까지를 열두 걸음으로 나눠 `setZoom` 을 열두 번
  불렀는데, 그 함수는 부를 때마다 **Maps 의 자체 줌 애니메이션(0.3초쯤)을 새로 시작한다** —
  266ms 마다 다음 걸음이 그것을 중간에 자르니, 부드러운 한 동작이 아니라 잘린 열두 동작이
  됐다. 걸음을 잘게 쪼개도 같은 문제고, 성기게 하면 계단이 보인다.

  그래서 **애니메이션을 쓰지 않는다**: `moveCamera` 는 카메라를 준 값에 그냥 놓는다.
  `requestAnimationFrame` 으로 매 프레임 조금씩 옮기면 그 자체가 부드러운 줌아웃이다.
  이징은 easeInOutSine(시작·끝이 느려 "빠져나가는" 느낌 — 프레임당 줌 변화 0.051 → 0.388 →
  0.051 로 실측).

  ⚠️ **폰 지도만 매 프레임 움직인다.** PC 지도는 임베드에서 `display:none` 이라 카메라
  호출이 조용히 무시될 수 있는데, 그 상태로 PC 가 idle 을 쏘면 미러(map → phoneMap)가
  폰을 시작 줌으로 되돌린다 — 끊김의 또 다른 근원이다. PC 는 끝에서 한 번만 맞춘다(`camLand`).

  컨텐츠 자리도 고쳤다: 반경을 시간 비례로 **계산**하던 것을 카메라가 **실제로 있는 줌**
  (`zNow`)을 읽는 것으로 바꿨다 — 이징이 붙으면 진행이 시간에 비례하지 않아 다시 어긋난다.

  안전망: rAF 는 탭이 화면에 없으면 아예 안 돈다(백그라운드·숨은 iframe). 그대로 두면
  카메라가 멈춘 채 컨텐츠만 쌓이므로, `zoomWin+400ms` 뒤에도 안 끝났으면 목표 줌에 앉힌다.

  검증(로컬 :8767): 이징 곡선 단조 감소·역행 0 · 안전망 경로에서 두 지도 모두 목표 줌(11)에
  도달하고 컨텐츠 10/10 · 콘솔 에러 0 · `node tools/check.js` PASS.
  ⚠️ 이 창은 rAF 가 안 돌아 **프레임 애니메이션 자체는 실측 못 했다** — 곡선·안전망·최종
  상태로만 확인했다.

### 2026-08-11 (12)
- **v2.13.0 — 스팟 오버레이 재사용 · burst 가 줌아웃과 함께 · 무대 Request 는 안 만료된다 (M04+M16+M07)**
  — 사용자 3건. 콘솔은 안 바뀐다(앱 단독 배포).

  ① **"스팟 메시지는 drop 때마다 기존 것도 바운스된다"** — `renderSpots` 가 매번
  `clearSpots()` 로 전부 지우고 다시 만들고 있었다. 새 DOM 이면 등장 연출이 다시 붙으니
  이미 있던 말풍선도 같이 튄다. 피드 핀도 같은 구조인데 사진 한 장이라 다시 만들어도
  티가 안 나서, 증상이 스팟에서만 보였다(사용자가 정확히 그렇게 봤다).
  이제 **id 로 짝을 맞춰 있는 것은 그대로 쓴다** — 좌표·설정만 새로 물리고, 사라진 것만
  걷고, 새로 생긴 것만 만든다. 실측: drop 뒤에도 기존 오버레이가 **같은 객체**로 남고
  표(nhBounceIds)는 새 id 에만 붙는다. 지운 스팟은 `setMap(null)`, 옮긴 스팟은 좌표 추종.

  ② **burst 가 "줌아웃 뒤에" 뜨던 것** — 원인 둘이었다. (a) 시작하자마자 목표 줌으로
  가버려서 카메라가 0.3초 만에 다 빠졌다 → 앞 80% 를 **12걸음**으로 나눠 소수 줌으로
  내려간다. ⚠️ 처음 한 번은 지금 줌 그대로 자리만 잡는다 — 목표로 먼저 튀면 걸음이 도로
  줌인이 된다(첫 구현에서 실제로 그랬다, 실측으로 잡음). (b) 반경을 목표 줌 기준으로
  한 번에 정해서 **초반 항목이 아직 좁은 화면 밖**에 떨어졌다 → 그 순간의 줌(`zAt`)을
  따라 반경이 넓어진다. 실측(ms=4000, 14→11): 0.5s z13.75·1개 → 1.5s z13.25·3개 →
  2.5s z12.25·4개 → 3.5s z11.25·6개 → 4.5s z11·8개, 중심 거리 앞 절반 평균 766m ·
  뒤 절반 4,713m.

  ③ **"Request 기능이 왜 안 보이나"** — 데이터·마커는 정상이었다(핀 생성까지 확인).
  범인은 **10분 만료**다: 무대가 깐 Request 도 일반 Request 와 같은 `REQ_TTL_MS` 를 타서,
  데모를 만드는 사이 10분이 지나면 핀·드로어·팝업에서 통째로 사라진다. 그 뒤 `pop v:req`
  는 빈손이고 `answer` 는 **"⏱ 종료된 Request예요" 네이티브 alert 로 재생을 멈춰 세운다.**
  `nhLayReq` 가 `stage:true` 를 달고 `reqActive` 가 면제한다. 실측: 11분 전으로 되돌려도
  active·마커 1개(옛 규칙이면 false).

  검증(로컬 :8767 `?embed=1&clean=1`): 위 실측 + 무대 5종 깔기 → 리셋(전부 0) → 다시 깔기
  왕복 · 콘솔 에러 0 · `node tools/check.js` PASS.

### 2026-08-11 (11)
- **v2.12.0 — 빈 무대 누수 차단 · 바운스 소비 · burst 재조정 · write 이모지/자리 · Request 답장 연출 · 딜 사진 (M16+M17+M07+M04)**
  — 콘솔 v0.84.0(D95)과 짝. 사용자 요청 8건.

  ① **"컨텐츠 탭에 없는 타임딜이 뜨고 안 사라진다" 의 정체.** 빈 무대(`clean=1`)는 부팅에서
  한 번 비우는데(`nhEmbedIsolate`), **지도 부팅의 geojson 콜백이 그 뒤에 돌면서**
  `loadLocalSpotsInto` 가 저장된 글을 다시 깔고 있었다 — 임베드는 실서비스·관리자와 **같은
  오리진**이라 거기서 쓴 글이 그대로 새어 든다. 딜은 회차가 끝나도 걷는 사람이 없어 남았다
  (`nhSweepTemp` 는 이번 회차가 만든 것만 걷는다). 고친 방식은 둘이다:
  다섯 loader 를 clean 에서 막고(`loadLocalSpotsInto`·`loadNews`·`loadFeed`·`loadRequests`·
  `loadDeals`), `nhWipeWorld()` 를 만들어 **`nhReset` 이 회차마다 한 번 더 비운다.**
  실측: localStorage 에 유령 딜·스팟·지면·피드를 심어 두고 clean 임베드를 열어도 전부 0,
  지도 로드(mapReady) 후에도 0.

  ② **바운스가 앞 항목까지 튀던 것** — 표를 시간(1.6초)으로 지워서, 그 창 안에 다음 항목이
  깔리면 전체 재렌더가 아직 표가 남은 앞 항목도 다시 튀웠다. 이제 오버레이가 한 번 쓰면
  차감한다(`nhBounceTake`). `nhBounceMark(id,n)` 의 n 은 그 종류를 그리는 지도 수다.

  ③ **burst** — 상한 50, 종류를 `+` 로 여럿(`"spot+feed"`), 등장 시각을 결정적 흔들기로
  흩어 **줌아웃이 도는 동안부터** 마구 생긴다, **바운스 없음**(깜박임의 원인이었다).
  실측: `spot+feed` 로 99개 요청 → 스팟 22·피드 28(합 50)·딜 0, 바운스 호출 0,
  등장 간격 -11~563ms(등간격이면 251ms).

  ④ **write** — `e` 로 이모지를 정하고(컴포저 픽커까지 맞춘다), 사람이 끌어 옮긴 자리를
  기억한다(`nhPosGet('write',n)` · `nhWriteIds` 표). 여태 지역 좌표 +0.0012 고정이라
  옮겨도 다음 재생에 제자리로 돌아갔다.

  ⑤ **Request 답장 연출** — `answer` 가 값을 꽂는 대신 팝업의 **답장 줄에 글자를 하나씩**
  넣고 보낸다(`nhAnswerTyped`, write 와 같은 타이밍 규칙). 상세 팝업의 네이티브 `prompt()`
  도 인라인 입력으로 바꿨다 — 그 창은 스크립트를 멈춰 재생이 그 자리에 서고, 답하는 모습도
  화면에 안 남았다. 무대 Request 는 **내 것**이라 답장 줄이 없어서, 그때는 그 줄을 만든다.

  ⑥ **딜 사진** — `seed.deals[].img` → 바텀시트 위 히어로(`#ds-img`, index/admin 양쪽 마크업).
  없으면 여태처럼 이모지만. 실측: 사진 있는 딜은 표시+실제 로드(960×640), 없는 딜로 바꾸면
  칸이 접히고 src 가 지워진다.

  검증(로컬 :8767 `?embed=1&clean=1`): 위 실측 + `node tools/check.js` PASS.
  ⚠️ 지도 오버레이(핀·컴포저)는 이 창이 화면에 안 떠 `onAdd` 가 안 돌아 DOM 검증이 안 된다 —
  자료구조·계약으로 검증했다(팝업·시트는 일반 DOM 이라 그대로 확인).

### 2026-08-11 (10)
- **v2.11.0 — 임베드가 지면 타입을 받는다 · 딜 점 표시 · 클러스터 중앙값 · 등장 바운스 · burst (M16+M17+M05+M11)**
  — 콘솔 v0.83.0(D94)과 짝. 사용자 요청 6건 중 앱 몫 5건.

  ① **관리자 스킨·스타일이 persona-vc 데모 화면에 안 보이던 것 (특히 상단 지면 타입).**
  스킨·스타일 자체는 `cloudSave → shared/publicSettings` 로 이미 건너간다(실측: 공개 문서에
  `appSkin:"v3"` 있음). **지면 타입(`newsCardVer`)만** `shared/news`(SDK 전용)로 다녀서
  REST 로 읽는 임베드가 못 봤다. `settingsSnapshotFull`·`applyExtraSettings` 에 추가 +
  지면 타입 변경이 `markCloudDirty` 도 부른다.
  ⚠️ **배포 뒤 관리자 페이지에서 지면 타입(또는 아무 설정)을 한 번 다시 적용해야**
  공개 문서에 새 필드가 실린다 — 그 전에는 임베드가 여전히 기본(1)로 그린다.

  ② **타임딜 핀 점 전환** — 스팟·피드와 같은 기준(`spotDotScaleM`)으로 축소 시
  `.dl-dot`(12px 점·% 라벨 숨김·중심 앵커). v3 스킨 3.5px 테두리는 점에서 1.5px.

  ③ **피드 클러스터 핀 = 멤버 중앙값** — 첫 멤버 좌표 고정이라 줌아웃 중 클러스터가
  합쳐질 때마다 "그때의 첫 멤버" 자리로 널뛰던 것. 그룹핑 기준은 안 바꿈(멤버 구성 유지).

  ④ **등장 바운스** — drop·post·postfeed·burst 로 지금 생긴 컨텐츠가 뿅(스케일 오버슈트)
  하고 나타난다. 전체 재렌더 구조라 만든 쪽이 `nhBounceMark(id)` 로 적고 오버레이 onAdd 가
  `.nh-pop-in` 을 붙인다(스팟·피드·딜·Request·지면 슬라이드). 1.6초 시간 만료로 지움 —
  첫 소비 삭제는 PC 지도가 먼저 먹는다. 자식만 흔든다(루트 transform=지도 배치).
  reduced-motion 은 애니메이션 없음.

  ⑤ **`burst` 액션 (엔딩 연출)** — 줌아웃하며 무작위 컨텐츠가 쏟아진다.
  `v`=spot|feed|deal|mix · `i`=개수(1~24) · `e`=줌 11~16(기본 13) · `ms`=쏟아지는 시간
  (**이 액션만 15초까지** — sanitize 특례, 콘솔 MAX_BURST_MS 와 짝). 자리는 줌 레벨 비례
  (`0.028°×2^(13-z)`, 황금각+면적 균등), 문구는 종류별 풀 8·8·4벌에서 시나리오 키로 고른다 —
  전부 결정적. `nhLayDeal` 원가 `%8` 접기(안 접으면 burst 딜 원가 25만 원대).

  검증(로컬 :8767 `?embed=1&clean=1` — 오버레이 onAdd 는 pane 미표시로 컴포지팅이 안 돼
  합성 DOM·자료구조로): `nhSanitize` 가 burst 통과(ms 3000 유지) · mix 9개 → 스팟 3+피드 3+
  딜 3, 줌 12 실이동, 원가 19,900~44,900원 · **같은 데모 재실행 = 완전 동일**(문구·좌표·가격
  JSON 비교) · 합성 `.deal-pin.dl-dot` 12×12px·라벨 display:none·v3 테두리 1.33px ·
  `.nh-pop-in .fp-im` animationName=nhPopIn · 클러스터 3멤버 → pos=산술 중앙값, 줌 20 에서
  낱개는 자기 좌표 · `settingsSnapshotFull().newsCardVer` 존재 + `applyExtraSettings({newsCardVer:3})`
  적용 · `node tools/check.js` PASS.

### 2026-08-11 (9)
- **v2.10.0 — 피드 카드에 올린 사진 · 무대가 상단 지면을 깨운다 · 무대 상한 확대 (M16+M05+M10)**
  — 콘솔 v0.82.0(D93)과 **짝**이다. 앱을 먼저 올린다: 콘솔이 먼저 올라가면 새 필드(`feeds[].img`)와
  10개 넘는 컨텐츠를 옛 `nhSanitize` 가 조용히 버린다.

  ① **피드 카드도 사람이 올린 사진을 쓴다** (`seed.feeds[].img`). 지면 카드가 v2.4 에 얻은
  길을 그대로 준다 — `nhImgSrc` 를 통과한 값이면 그것, 아니면 `seedImg`(테마 색 + 라벨).
  콘솔은 Storage 에 올리고 **주소만** 보낸다. 사진만 있고 글이 없는 카드도 받는다
  (`filter` 에 `img` 추가) — 카드가 곧 사진인 피드가 성립한다.

  ② **"피드 사진이 왜 상단 지면에 안 뜨나" 의 원인** — 로직은 멀쩡했다. 지도 탭의 상단 지면은
  `newsItems`(관리자·무대 지면) + `feedSummaryItems`(사진 있는 가까운 피드 4장)인데,
  `nhSeedScenario` 가 **`pages` 가 있을 때만** `renderNews()` 를 불렀다. 피드 카드만 깐 데모는
  무대를 깐 직후의 지면이 **빈 채**였고, 탭을 갈아타야(switchTab → renderNews) 비로소 떴다.
  조건에 `feeds` 를 넣고, `nhDrop('feed')`·`nhPostFeed` 도 같이 부른다(재생 중 뜬 피드도 지면에
  얹혀야 같은 물건이다). 실측: 피드 2장만 깐 뒤 `newsView` 0 → 3(사진 카드 포함, 실제 이미지 로드).

  ③ **상한을 `NH_MAX` 한 곳으로 모으고 올렸다** — req 10 · spot 40 · feed 40 · deal 10 · page 12.
  콘솔이 컨텐츠를 무대 탭에서 떼어 **컨텐츠 탭**으로 내면서(D93), 무대가 "여는 장면의 전제" 에서
  "동네를 채우는 자리" 가 됐다. 콘솔의 `MAX_SEED_*` 와 **같은 값이어야 한다**.

  ④ **자리 대역(`NH_BAND`)** — `nhSpread` 는 번호 하나로 자리를 정하므로, 상한이 10 이던 시절의
  대역(스팟 `10+i` · 피드 `20+i`)으로 40 개를 깔면 **다른 종류가 같은 자리에** 떨어진다.
  종류당 100 칸을 주고 앞 절반은 무대, `NH_POST_FROM`(50) 뒤는 재생 중 생긴 남의 글로 나눴다.
  보관(`hold`) 항목의 번호도 **배열 순번 그대로**로 통일 — 스팟·피드만 대역을 미리 얹어서 같은
  항목이라도 hold 를 켠 것과 안 켠 것이 다른 자리에 깔렸다(v2.2 의 딜 가격 사고와 같은 결).
  ⚠️ 대역이 바뀌어 **옛 데모의 콘텐츠 자리도 바뀐다**. 사람이 끌어 옮긴 자리(`nowhere_stagepos`)는
  종류+번호로 저장돼 그대로 살아남는다.

  검증(로컬 `:8765` `?embed=1&clean=1`, `javascript_tool` 로 계약 직접 호출): `nhSanitize` 가
  사진만 있는 카드를 살리고 빈 카드는 버림 · `img` 통과 · 스팟 12개(옛 상한 10 초과) 전부 깔림 ·
  피드 src = 올린 사진 / 테마 SVG 로 갈림 · 무대 직후 `newsView` 3 · 슬라이드 3장 렌더
  (`naturalWidth` 960 = 실제 이미지 로드) · 스팟·피드 좌표 충돌 0 · `node tools/check.js` PASS.

### 2026-08-11 (8)
- **v2.9.0 — 시드 생성이 지도를 죽이던 것 · 문구 다양화 · 겹침 방지 전면 개편 (M13+M00+M04+M05+M07+M17)**:

  사용자 넷: 관리자 콘솔에서 지역 시드 생성을 하면 **구글 맵이 안 뜬다** · 시드 문구가 너무
  단순하다("재고 있다 이런 것만") · 지도 위 데이터가 서로 겹친다(말풍선 포함) ·
  (Firestore 규칙 게시 완료 · Places 키 정리).

  ① **시드 생성이 지도를 죽이던 것 (M13).** `sgSearchPlaces` 가 `PlacesService` 에
  **지도 컨테이너 div(`#map`)를 넘기고 있었다.** 그 인자는 "출처 표기를 그릴 곳" 이라
  Maps 가 자기 DOM 을 채워 둔 자리에 표기 노드가 끼어들어 지도가 통째로 사라졌다.
  **Map 객체**를 넘기면 Maps 가 자기 규칙대로 지도 안에 표기를 그린다.

  ② **문구 다양화 (M13).** 템플릿이 종류마다 **한 벌**뿐이라 12곳을 만들면 상호만 다르고
  문장이 전부 같았다 — `SG_TPL` 을 종류마다 3~5벌로 늘리고 순번으로 고른다(결정적).
  AI 프롬프트도 다시 썼다: 장소의 **실제 분류(types)**·**현재 시각**을 같이 주고,
  "같은 문장 틀을 반복하지 마라 · 분류에 맞는 구체적인 것을 말해라 · 시간대를 살려라 ·
  가격·영업시간은 지어내지 마라" 를 명시. ⚠️ 프롬프트의 `'서울 '` 하드코딩도 지웠다 —
  수원 매탄3동에서 돌렸을 때 프롬프트가 거짓을 말하고 있었다.

  ③ **겹침 방지 전면 개편 (M00).** 전에는 **말풍선만** 자리를 골랐고 핀은 고정 장애물이라
  핀끼리는 아무도 안 비켰다(사진 핀 클러스터는 종류를 넘나들면 안 묶인다). 게다가
  **타임딜 핀은 declutter 에 아예 안 들어가 있었다.**
  이제 **모든 마커가 후보 자리를 갖는다**: 말풍선=4방향×3거리, 핀=제자리+황금각 14곳.
  첫 후보가 언제나 제자리라 **안 겹치면 안 움직인다**. 순서가 우선순위다 —
  타임딜 → Request → 사진 핀 → 말풍선(가장 유연한 것이 마지막에 비킨다).
  ⚠️ 후보 거리는 **마커 크기에 비례**해야 한다: 처음엔 고정값(핀 12~57px, 말풍선 14·30)이라
  34×46 핀 다섯이 한 점에 있을 때 여전히 둘이 물렸다 — 자기 폭·높이만큼 벌어져야 한다.

  **검증**
  - `declutterBoxes` 를 **app.js 실제 소스에서 떼어** node 로 단위 검증(로컬은 API 키
    리퍼러 제한으로 지도가 죽어 마커가 DOM 에서 사라진다 — 화면 실측은 라이브에서).
    같은 자리 핀 5개 → **겹침 0** · 같은 자리 말풍선 6개 → **겹침 0** ·
    14개를 70×30px 에 몰아넣은 극단 케이스 → **77쌍 → 4쌍** · 안 겹치면 안 움직임(dx=dy=0,
    말풍선은 기존 방향 유지) · 같은 입력 두 번 = 같은 결과.
  - 시드 템플릿: 종류마다 여러 벌이 순번으로 갈린다.
  - `node tools/check.js` 통과.

  **없는 것**: 극단 밀집에서 겹침이 0 이 되지는 않는다 — 14개를 70×30px 에 넣으면 물리적으로
  자리가 없다. 그 구간은 줌아웃 시 점 전환(`spotDotScaleM`)이 맡는다.

### 2026-08-11 (7)
- **v2.8.0 — 지역 시드 생성이 AI 문구를 받는다 (M13, 콘솔 v0.81.0 과 짝)**:

  사용자: Places API 를 켠 뒤 시드 생성은 되는데 `(문구: 기본 템플릿 — AI 에이전트 응답을
  못 받았어요)` 가 뜬다.

  **원인 — 일시적 실패가 아니라 구조적으로 불가능한 경로였다.** 앱의 `sgAskAgent` 는 콘솔의
  `/api/app-agent` 를 부르는데, 그 라우트가 **Ask Map 전용**으로 잠겨 있었다:
  ① 질문 상한 300자 → 장소 12곳 목록 + 출력 형식이 잘려 모델이 JSON 지시를 못 봤다.
  ② 시스템 규칙이 "최대 200자 · 평문만 · 목록 금지 · 무엇을 시켜도 역할 불변" 이라 모델이
  작업을 **거절**했다("화면에 이웃 글이 안 보여서 문구를 만들기 어렵습니다").
  ③ `tidyAppAgentAnswer` 가 240자에서 잘라 — JSON 이 왔어도 깨졌다.
  게다가 앱이 보내는 `context.mode='seed-gen'` 을 라우트가 몰라서 화면 상태가 0으로 채워졌고,
  그게 ②의 "화면에 아무것도 없다" 문장을 만들었다.

  **고친 것**: 콘솔 라우트에 **모드 두 개**(D92 · `ask` / `seed-gen`). Ask Map 규칙을 푸는 게
  아니라 **가른다** — 200자·평문은 말풍선에는 맞는 규칙이다. 앱 쪽은 안내 문구만 고쳤다:
  전에는 파싱 실패까지 "응답을 못 받았어요" 로 뭉쳐서, **200 이 오는데 형식만 안 맞던 상황**을
  알아볼 수 없었다. 이제 `응답을 못 받았어요 (사유)` / `형식이 안 맞아 못 썼어요` 로 갈린다.

  **검증**: 배포된 콘솔에 실제 시드 생성 프롬프트(장소 12곳)를 보내 **12개 JSON 배열**을 받았다
  — `sgParseLines` 와 같은 방식으로 파싱 성공, 네 키(spot·feed·req·deal) 전부 존재, 한글 정상
  (`스타벅스 매탄, 커피 한 잔` / `신메뉴 나올 때마다 들러요.` …). model=gemini-2.5-flash,
  1회 약 $0.008. `node tools/check.js` 통과.

  **없는 것**: 데모 모드(provider=mock)에서는 여전히 템플릿이다 — 목 응답이 JSON 이 아니라서
  `sgParseLines` 가 걸러낸다. 실배포에서만 AI 문구가 붙는다.

### 2026-08-11 (6)
- **v2.7.0 — 피드 카드 높이 통일(사진 위 캡션) · 온도 팔레트 4색 · AI 대각선 순차 (M05+M00+M08+M15)**:

  ① **피드 카드 높이가 안 맞던 것.** 글 카드(`.txt`)는 1:1 정사각인데 사진 카드만 아래에
  흰 본문이 붙어 더 높았다(그리드가 들쭉날쭉). 사진 카드도 **1:1** 로 두고 설명글·메타를
  **사진 위 글래스 캡션**으로 올렸다 — 상단 지면 카드 V3(풀 이미지 + 하단 그라디언트 +
  흰 텍스트)와 같은 문법이라 화면이 한 벌로 읽힌다.
  ⚠️ 캡션이 `position:absolute` 가 되면서 그 안의 `.fc-like` 기준이 **카드에서 캡션 상자로**
  바뀐다 — 좌상단 배치를 그대로 뒀으면 엉뚱한 자리에 앉았다. 흐름 안(`static`)에 두고
  `margin-left:auto` 로 메타 줄 오른쪽 끝에 붙였다: `동네 · 시간   ♥6`.

  ② **온도 컬러 팔레트 4색** — `#9dc64c · #f2c53d · #f2862e · #e23b2a`(식음→뜨거움).
  `HEAT_STOPS` 를 app.js 맨 위로 올려 **단일 기준**으로 삼고, 색상 팝업 프리셋(`PALETTE`,
  v1.65)도 여기서 파생시킨다 — 전에는 온도 4색이 두 곳에 적혀 빨강이 서로 달랐다
  (`#DE2F2A` vs `#E23B2A`). `heatColor` 는 **정거장 개수와 무관하게** 보간하도록 고쳤다
  (0.5 기준 2구간 하드코딩이라 3색→4색에서 가운데가 통째로 빠질 참이었다).
  CSS 의 `--heat` 폴백 5곳도 새 팔레트로 맞췄다.

  ③ **AI 버튼 = 대각선 순차 이동만.** v2.6 의 플리커(밝기 들썩임)를 빼고 효과를 하나로 —
  팔레트가 135° 방향으로 흐르고 반복한다(5s linear). 글로우는 애니 없는 고정값으로 남겼다.
  팔레트를 두 바퀴 적고 끝을 시작색으로 맞춘다: 대각선 이동은 `0%→100%` 가 정확히 한
  주기가 아니라, 안 그러면 이어 붙는 자리가 튄다.

  **검증**(실제 Chrome)
  - 사진 카드 9장·글 카드 3장 **모두 195×195** — 높이 일치. 캡션이 사진 위(그라디언트·흰 글자),
    좋아요 35×17 이 메타 줄 오른쪽 끝(카드를 안 덮음), 설명글·동 이름이 캡션에 뜬다.
  - 팔레트 램프 `#9dc64c → #ddc541 → #f2a636 → #ee732d → #e23b2a`, 양끝이 정거장과 일치,
    `heatColor(2/3)` 가 정확히 세 번째 정거장(`#f2862e`) — 4색 보간이 맞다.
  - AI: `135deg` · 팔레트 4색 전부 포함 · `aiHeatFlow 5s linear` · 플리커 없음.
  - `node tools/check.js` 통과.

  **없는 것**: legacy 스킨의 피드 카드는 그대로다(사진 위 오버레이 칩) — v3·new 만 캡션.

### 2026-08-11 (5)
- **v2.6.0 — 지도 표시 6종 (M05+M04+M00+M13+M08+M15)**: 사용자가 짚은 여섯.

  ① **베이직에서도 피드 사진은 컬러.** v3 스킨이 `filter:grayscale(1)` 로 사진까지
  무채색으로 눌렀다 — 지도 위에서 무엇을 찍은 사진인지 알 수 없었다. 무채색 규칙의
  대상은 처음부터 **크롬(링·뱃지)** 이고 사진 콘텐츠는 예외였다(v1.58). 링·뱃지는 그대로.

  ② **시드 좌표 겹침 제거** (`seedSpread`, `SEED_MIN_M=85`). 종류별 배열은 각자 떨어져
  있었지만 **종류를 가로지르면 겹쳤다**(스팟 위에 사진 핀). 가까운 쌍을 서로 밀어내는
  완화를 8패스 돌린다 — 결정적이다(Math.random 금지, 시드는 회차마다 같은 자리).
  동 라벨은 어차피 `dongAt` 으로 다시 판정하므로 표기가 어긋나지 않는다.

  ③ **줌해도 말풍선이 안 움직인다** (`spotDirById`). 방향이 오버레이 객체에만 있어서
  줌·팬마다 declutter 가 다시 돌며 뒤집혔고(줌인하면 버블이 커져 겹침 관계가 통째로
  달라진다), `renderSpots()` 때마다 리셋됐다. 이제 방향은 **스팟 id 의 것**이고, 이미
  자리를 정한 말풍선은 declutter 에서 **고정 장애물**로만 센다 — 새 글 하나 때문에
  기존 배치가 흔들리지 않는다. 사라진 스팟의 기억은 `spotDirForget` 이 버린다.

  ④ **존 안 컨텐츠가 저마다 다른 온도** (`contentHeatT`·`HEAT_SPREAD`). 전에는 존 안이
  전부 `zoneHeatT` 한 값이라 **같은 색 한 덩어리**였다. 이제 존 온도를 중심으로 항목마다
  흩는다 — 뜨거운 존일수록 뜨거운 항목 비율이 높다. 수동 `temp` 는 언제나 먼저.
  ⚠️ 흩는 해시가 처음엔 `h*31+c` 였는데 **끝 글자만 다른 id 가 이웃 값으로 몰려**
  같은 존 14개가 전부 66 이었다 — FNV-1a + fmix32 로 교체했다. `nhAutoTemp`(v2.4)도
  같은 결함이 있어 이 함수 한 벌로 모았다.

  ⑤ **피드 좋아요 칩이 카드를 덮던 것.** v3 규칙이 `left`·`top` 만 얹고 바탕 규칙의
  `right`·`bottom` 을 안 지워서 네 변이 다 물려 **칩이 328×576px 로 늘어났다** — 사진 위
  흰 돔이 그것이었고, 글자는 `align-items:center` 로 그 큰 상자 가운데 떠 있었다.
  `right:auto;bottom:auto` 로 되돌리니 114×63(실측 38×21 @390px)의 알약이 됐다.

  ⑥ **트렌드 AI 버튼 = 흐르는 온도색.** 코랄 한 색이었다 — 트렌드의 언어가 '온도' 인데
  AI 버튼만 그 스펙트럼을 안 보여줬다. HEAT_STOPS 를 이어 무지개처럼 흐른다(`aiHeatFlow`
  6s). 활성은 배경을 덮지 않고 링으로 알린다(덮으면 흐름이 멈춘 것처럼 보인다).
  `prefers-reduced-motion` 이면 흐르지 않고 중간색으로 선다.
  ⚠️ v3 스킨에 그라디언트를 **다시 적어야 했다** — `body[data-skin="v3"] .pn-ai` 의
  `background` 축약이 명시도 (0,2,1) 로 style.css 의 `.pn-ai.ai-flame` (0,2,0) 을 이겨
  `background-image` 를 지운다.

  **검증**(실제 Chrome · puppeteer — 인앱 브라우저는 비표시라 지도 오버레이가 attach 안 됨)

  - 시드 102점 최소거리 **85m · 85m 미만 0쌍**.
  - 말풍선 방향 39개가 **z14·z16·z18 전부 동일**, 왕복도 동일.
  - 온도: 뜨거운 존(temp 95) 14개 평균 88 · 전부 ≥50 · **서로 다른 값 10개**(73~100) /
    식은 존(temp 10) 8개 평균 21 · ≥50 은 0개 · 값 6종(0~37). 두 번 계산해도 같음.
    수동 temp 77 은 그대로 77. 색도 10가지로 갈린다.
  - 베이직 피드 핀 `filter:none`(전 `grayscale(1)`) · 링 흰색 유지.
  - 좋아요 칩 38×21 @390px, 카드(195×278)를 안 덮음.
  - AI 버튼: `ai-flame` + `background-image` 그라디언트 + `aiHeatFlow` 애니 확인.
  - `node tools/check.js` 통과 · 콘솔 에러 0.

  **없는 것**: Request 핀은 존 온도를 그대로 쓴다(존 하나의 신호라 흩지 않는다).
  피드 탭 카드 색은 이번 범위 밖 — 지도 위 표시만 다뤘다.

### 2026-08-11 (4)
- **v2.5.0 — 공개 설정 문서(자동 전달) · 관리자 메뉴 재정비 (M11+M12+M15+M16)**:

  사용자 둘: "Firestore 규칙 게시 방법 알려줘, 자동으로 되게 하자" · "관리자 콘솔 메뉴 UI가
  규칙없이 중구난방인데, 카테고리별로 묶고 목적단위로 잘 묶어서 재정비 해줘".

  **① 공개 설정 문서 — persona-vc 임베드에 자동으로 건너가는 실시간 경로.**
  localStorage 캐시는 cross-site iframe 에서 안 보인다(D91 ③). 관리자가 설정을
  적용(cloudSave)할 때 `shared/publicSettings` 문서에 스냅샷을 **json 문자열 하나**로
  남기고(REST 의 타입 달린 필드를 되짚지 않기 위해), 임베드가 부팅에서 **fetch(REST)** 로
  읽는다 — Firebase SDK 를 안 붙이는 임베드 설계(M16)는 그대로다. 우선순위:
  공개 문서 > localStorage 캐시 > settings-default.json > 코드 기본값.
  캐시·공개 문서·(스냅샷 생성)이 `settingsSnapshotFull`/`applyFullSettings` 한 벌을 쓴다.
  ⚠️ **firestore.rules 에 `shared/publicSettings` 비로그인 읽기 매치를 추가했다 —
  콘솔 게시 전에는 403 으로 조용히 실패하고 예전 경로로 산다** (아래 게시 절차).

  **② 관리자 메뉴 재정비 — 그룹 = 하려는 일 (5그룹).**
  📦 컨텐츠(만들고 정리: 전체 표·스팟·피드 사진·존·지면·소셜) / 🌱 한 번에 채우기
  (데모 데이터·지역 시드 생성) / 🖥 화면·디자인(즉시 적용: 디자인 스킨 + **신설 📐 표시
  옵션**) / 🗺 지도 스타일(미리보기→적용: 말풍선·동 구역·존·렌즈) / 🛠 시스템.
  - **표시 옵션(s-view) 신설**: 컨텐츠 패널들에 섞여 있던 "어떻게 보이나" 를 전부 모았다 —
    피드(지도 아이콘 크기·그리드 열·간격·시간·스팟 지도배경) + 카드(존 목록·지면).
    "아이콘 크기 옵션이 안 보인다" 는 말의 원인이 이 섞임이었다. **id·배선은 그대로**
    (getElementById 라 위치와 무관) — 옮긴 자리엔 안내 한 줄씩 남겼다.
  - 적용 바·안내문은 **드래프트 패널(지도 스타일)에서만** 보인다 — 즉시 적용 패널에
    "적용을 눌러야" 안내가 같이 뜨면 헷갈린다.
  - 팝업 제목 = 내비 그룹 캡션에서 파생 (그룹이 다섯이라 키 첫 글자로는 못 가른다).
  - 탑바 3버튼: 📦 컨텐츠(c-table) · 🖥 화면·스타일(s-skin) · 🛠 시스템.
  - index.html 폰 드로어 설정은 손대지 않았다 (v1.66 ④ — 마크업 독립, 값만 공유).

  **검증**(로컬 :8766): 내비 캡션 5개·패널 17개 순서 확인 · 이동한 컨트롤 7개 전부 s-view
  안에 존재+배선 동작(feed-cols·zone-card-style 실측) · 적용 바가 s-view 에서 숨고
  s-spot 에서 보임 · 제목 파생 3케이스 · 임베드 REST 성공 경로(모의 응답 → 스킨·7값 적용,
  settingsRemoteOn) · 규칙 게시 전 403 은 조용히 폴백 · `node tools/check.js` 통과.

  **남은 일(사람 손)**: Firebase 콘솔 → now-here-demo → Firestore → 규칙 탭에
  repo `firestore.rules` 전문을 붙여넣고 게시. 게시 후 관리자 설정을 한 번 적용하면
  `shared/publicSettings` 가 생기고, 그때부터 persona-vc 데모가 자동으로 따라간다.

### 2026-08-11 (3)
- **v2.4.0 — 무대 콘텐츠에 트렌드 온도 · 지면 카드에 올린 사진 (M16+M17+M04+M05, 콘솔 v0.80.0 D91 과 짝)**:

  사용자 둘: "트렌드 모드에서 스팟 메시지·피드는 온도 시스템이 적용되어 있는데 이것도
  사용자가 설정할 수 있거나 따로 설정 안 하면 랜덤으로" · "상단 지면카드는 내가 직접
  이미지를 올릴수도 있게".

  **한 것**

  ① **무대 콘텐츠의 트렌드 온도** (`seed.spots[].temp` · `seed.feeds[].temp`, 0~100).
  `heatTOf` 가 이미 항목의 `temp` 를 자동 계산보다 먼저 보므로 **렌더는 손대지 않았다** —
  `nhLaySpot`·`nhLayFeed` 가 그 필드를 실어 주기만 한다. 안 정하면 `nhAutoTemp` 가
  22~92 사이 값을 붙인다: 무대 콘텐츠는 좋아요가 0 이라 자동 계산이 **전부 같은 식은 색**을
  내고, 그러면 트렌드 모드를 켠 뜻이 화면에 없다.
  ⚠️ **자동 온도의 씨앗에 id 를 쓰지 않는다** — id 에는 회차마다 달라지는 stamp 가 박혀
  있어 처음엔 재생마다 색이 달라졌다(검증 중 잡음). 대신 그 항목의 **글 + 순번**을 섞는다.
  빈 값과 0 을 가른다: 0 은 "가장 식은" 이라는 선택이고 빈 값은 "안 정했다" 다(`nhTemp`).

  ② **지면 카드에 올린 사진** (`seed.pages[].img`). 있으면 그 사진, 없으면 예전처럼 테마
  색으로 그린다. `nhImgSrc` 가 **https 와 data:image 만** 통과시킨다 — 이 값이 `<img src>`
  로 가므로 `javascript:` 같은 것이 지나가면 안 된다. 콘솔은 Storage 에 올리고 **주소만**
  실어 보낸다(문서에 이미지를 담으면 Firestore 상한 1MB 에 닿는다).

  ③ **피드 지도 아이콘 크기 옵션을 찾기 쉬운 자리로.** 피드 컨텐츠 섹션 **맨 위**(그리드 열
  바로 위)로 올렸다 — 전에는 사진 목록과 안내문 아래 맨 끝이라 스크롤해야 보였고,
  사용자가 "안 보인다" 고 했다.

  **검증**(로컬 :8766)

  - 온도: 지정 90 → 90 · 안 정함 → 23·56 등 항목마다 다름 · **0 을 고르면 0**(빈 값과 구분).
    트렌드 색이 실제로 갈린다(`#ff3a28` / `#ff9a4b` / `#ffc24a`).
  - **결정적**: stamp 를 바꿔 3회 재생해도 `57,76,28,23,22` 로 동일, 5개 항목 전부 다른 값.
  - 지면 사진: https 주소 통과 · 없으면 테마 SVG · **`javascript:` 는 막혀 테마 SVG 로 떨어짐**.
  - `node tools/check.js` 통과 · 콘솔 에러 0.

  **없는 것**

  - Request·타임딜에는 온도 칸이 없다 — 그쪽은 속한 존의 온도를 따르는 것이 규칙이다(v1.58).
  - 올린 사진은 **지면 카드에만** 붙는다. 피드 카드 사진은 여전히 테마 색이다.

### 2026-08-11 (2)
- **v2.3.1 — 파일 백스톱도 스킨을 나른다 (M15+M11)**:

  v2.3.0 을 설명하다 드러난 구멍. 캐시(localStorage)는 브라우저 하나에만 있으므로
  **처음 보는 기기**(VC 노트북 등)는 `settings-default.json` 이 유일한 기준인데,
  그 경로가 **스타일 + spotConfig 까지만** 알았다. 스킨(v3/new/legacy)·존 카드·피드 시간·
  스팟 지도배경·피드 아이콘 크기 다섯은 `📋 설정 JSON 복사`가 담지도, `loadFileDefaults`
  가 읽지도 않아서 — 그 JSON 을 파일에 붙여 넣어도 그 기기만 다른 화면으로 떴다.

  **한 것**: 그 다섯을 `applyExtraSettings(s)` 한 함수로 묶어 **클라우드·로컬 캐시·repo
  파일이 같은 범위**를 적용하게 했다(두 벌로 두면 한쪽만 고쳐진다). 내보내기 버튼도 그
  다섯을 평평하게 얹어 담는다 — 파일을 읽는 쪽 형식 그대로다. 안내 문구도 "백업" 이 아니라
  **"처음 여는 기기·데모 임베드가 이 값으로 뜬다"** 로 고쳤다(그게 이 파일의 쓸모다).

  **검증**(로컬 :8766): 캐시 없는 기기 = 파일의 일곱 값이 전부 발현
  (skin new · emojiSize 38 · #00aa77 · list · clock · {0.2,200} · 52).
  캐시 있는 기기 = 캐시가 파일을 이김 (v3 · 19 · #ff0066 · glass · off · {0.55,50} · 88).
  내보내기 JSON 키 14개(기존 9 + 새 5). `node tools/check.js` 통과 · 콘솔 에러 0.
  `settings-default.json` 은 `{}` 로 되돌려 뒀다 — 이 경로는 값을 넣기 전까지 잠들어 있다.

  ⚠️ 검증 중 알게 된 것: **임베드 안에서는 `localStorage.setItem` 이 막혀 있다**
  (`nhEmbedIsolate`, NH_POS_KEY 만 예외). 캐시를 심어 시험할 때는 임베드 밖 페이지
  (dev.html·admin.html)에서 써야 한다 — 임베드에서 쓰면 조용히 삼켜진다.

  ✅ **v2.3.1 배포·검증 완료 (2026-08-11).** 배포본에서 `v2.3.1` · `applyExtraSettings`
  존재 · 내보내기 키 14개(다섯 전부 포함) · 딜 0개 확인.

### 2026-08-11
- **v2.3.0 — 딜 자동 시드 폐지 · 옮긴 무대 자리 기억 · 피드 아이콘 크기 · 관리자 설정이 임베드 기본값 (M17+M16+M05+M15)**:

  사용자 넷: 타임딜을 추가하지 않았는데 2개가 기본으로 떠 있다 · 지도 위 콘텐츠가 겹치지
  않았으면 + 사람이 옮긴 자리가 다음 플레이에도 유지됐으면 · 관리자 콘솔에 피드 아이콘 크기
  옵션 · 관리자 스킨·설정이 persona-vc 데모에도 기본 적용됐으면.

  **한 것**

  ① **딜 자동 시드 폐지 (M17).** `SEED_DEALS`·`ensureDealSeed`·`DEAL_NEAR_M` 삭제 —
  딜은 이제 무대(`nhLayDeal`)와 시드 생성기가 **명시적으로 깐 것만** 뜬다. 옛 자동
  시드가 localStorage 에 남긴 것(`dl_N`·seed:true)은 `loadDeals` 가 걸러낸다 — 코드만
  지우면 seed:true 라 만료도 안 되고 영영 남는다.

  ② **사람이 옮긴 무대 자리를 기억한다 (M16).** 무대 배치(nhSpread)는 결정적이라 겹치지
  않지만, 사람이 임베드에서 핀을 끌어 옮기면 그 자리는 회차가 끝나며 사라졌다. 이제
  드래그 끝(`nhPosNote`)에 시나리오 id + 항목 번호(id 접미사) 단위로 `nowhere_stagepos`
  에 남고, 다음 재생의 `nhLay*` 가 그 자리를 먼저 쓴다. 무대 중심에서 5km 밖 옛 값은
  무시한다(동네를 옮긴 데모가 옛 동네 자리에 깔리지 않게). `nhEmbedIsolate` 의
  localStorage 차단에서 이 키만 예외 — "사람이 정한 연출" 은 남아야 뜻이 있다.
  무대 피드 핀(`fdn_`)은 임베드에 소유자가 없어 못 끌던 것을 끌 수 있게 했다.

  ③ **피드 지도 아이콘 크기 옵션 (M05).** 여태 스팟 이모지 크기(v1.63)와 한 몸이었다 —
  기준 크기만 분리(`feedIconSize`, 피드 컨텐츠 설정의 `#feed-icon-size`). 비우면 예전
  그대로 스팟 크기를 따르고(0), 클라우드 동기는 additive 라 옛 클라이언트는 모르고 지나간다.

  ④ **관리자 적용 설정이 임베드의 기본값 (M15+M16).** 임베드는 Firebase 를 안 붙여서
  관리자가 고른 스킨·스타일이 데모에는 코드 기본값으로 떨어졌다. 임베드는 관리자 콘솔과
  **같은 오리진**이므로, `cloudSave`/`applyCloudData` 가 통째 스냅샷을
  `nowhere_settings_cache` 에 남기고 임베드 부팅(`loadSettingsCache`)이 읽는다.
  우선순위: 코드 기본값 < settings-default.json < 캐시. Firebase 를 안 붙인다는 설계는
  그대로다.

  **검증**

  - `node tools/check.js` 통과 (버전 v2.3.0 동기화 · dev/diagram 스탬프 · app.js 문법).
  - 로컬(:8766) 임베드: 빈 무대·일반 임베드 모두 **딜 0개**(자동 시드 없음) · 무대 딜의
    자동 가격(pct 33 → 6,600원/9,900원/13개·seed:false) · 지면(tab:feed·stage:true) ·
    **옮긴 자리 라운드트립**(nhPosSave → sweep → 재시드가 그 좌표에 깔림) · 5km 가드
    (부산 좌표는 무시) · 설정 캐시 풀세트(skin new·emojiSize 44·zoneCard list·
    feedTime clock·spotMapBg·feedIconSize 60 전부 임베드에 발현, 파일 백스톱 스킵) ·
    admin `#feed-icon-size` 48→기준 48, 비우면 스팟 크기(26) 복귀 · 콘솔 에러 0.
  - ✅ **v2.3.0 배포·검증 완료 (2026-08-11).** 배포본(?embed=1&clean=1)에서 딜 0개 ·
    무대 딜 가격(20% → 7,900원/9,900원/16개) · 위치 오버라이드 재현 · nhPage 존재 확인.
    짝인 콘솔 v0.79.0 도 같은 날 롤아웃 확인.

  **없는 것 / 한계**

  - 딜 핀·Request 핀은 **드래그 장치 자체가 없다** — 옮긴 자리 기억은 스팟·피드 핀만.
  - 설정 캐시는 **그 브라우저에서 앱(로그인)을 한 번이라도 연 기기**에만 있다. 처음 보는
    기기는 settings-default.json 백스톱이 기준 — 관리자 설정의 📋 설정 JSON 복사로
    그 파일을 갱신해 두면 모든 기기가 같은 기본값을 갖는다.
  - 짝인 콘솔 v0.79.0(D90 — page·deal 어휘)은 이 배포 뒤에 올린다 (앱 먼저 — M16 절).

### 2026-08-10 (7)
- **v2.2.0 — 무대가 지면과 타임딜을 안다 · `page` 액션 (M16+M17+M10, 콘솔 v0.79.0 D90 과 짝)**:

  **한 것**

  ① **무대가 타임딜을 깐다.** 사람은 `seed.deals`(최대 3)에 5칸(이모지·제목·가게·할인율·
  남은시간)만 적고, `nhLayDeal` 이 가격 3칸(원가 `9900+i*5000`원 · 지금가 백 원 내림 ·
  수량 `max(3,20-round(pct/5))`개)을 결정적으로 만든다. `seed:false` 로 깔아 카운트다운이
  실제로 돈다. `pop`·`focus`·`drop` 이 `v:'deal'` 을 안다.

  ② **무대가 상단 지면 카드를 깐다.** `seed.pages` 를 `nhLayNews` 가 깐다(사진 테마·
  지역이름·한 줄, 탭별). 기능 데모의 피드 탭 지면이 여태 항상 빈 껍데기였던 것을 고쳤다.
  새 액션 **`page`**(`next|prev` 뿐 — 지도 탭 캐러셀은 무대 카드 뒤에 근처 피드가 최대
  4장 붙어서 번호를 사람이 맞힐 수 없다). 접힌 캐러셀은 먼저 펼친다.

  ③ **리뷰에서 드러난 결함 다섯**: held 딜을 drop 하면 원가가 30만 원대로 튀던 것
  (자리 오프셋 `60+i` 를 보관 인덱스에 미리 얹었던 버그 — `nhLayReq` 방식으로 되돌림),
  일반 임베드에서 지역을 옮기면 `ensureDealSeed` 의 자가복구가 방금 깐 무대 딜을 통째로
  비우던 것(`nhTempIds.deal` 있으면 자가복구 건너뜀), `popclose` 가 딜 시트를 안 닫아
  다음 스텝들 위에 얹혀 있던 것(`#ds-close` 로 터치 표식도 정정), 만료된 딜을 `pop` 이
  열어 `0:00` 유령 시트를 남기던 것, `nhStore`/`nhDrop` 의 마지막 분기가 모르는 종류를
  각각 Request·스팟으로 조용히 잘못 집던 것. 모두 이번 릴리스에 포함됐다.

  ④ **무대 지면이 피드 그리드에 새는 것도 막았다.** `allFeedEntries` 가 `newsItems` 를
  `tab` 구분 없이 그대로 얹어서, 무대가 깐 캐러셀 카드가 `#feed-grid` 에 제목 없는
  사진 카드로 한 번 더 떴다. 무대가 깐 항목에만 `stage:true` 를 달아 그것만 걸러낸다
  (`tab==='feed'` 필터는 기각 — 관리자가 올린 실제 뉴스는 `tab` 필드가 아예 없다).

  ⑤ **버전 v2.1.0 → v2.2.0**: `index.html`(6곳) · `admin.html`(6곳) · `dev.html`(1곳) ·
  `diagram.html`(1곳). `MODULES.md` 갱신 — M16 임베드 계약의 액션 어휘에 `page` 추가 +
  `pop`/`focus`/`drop` 이 `deal` 을 안다는 것과 `drop` 이 `page` 도 안다는 것 명시,
  M17·M10 변경 로그에 위 ①~④ 기록.

  **검증한 것**

  `node tools/check.js` 통과(버전 3곳 동기화 · dev/diagram 스탬프 일치 · app.js 문법).
  위 ①~④ 는 이번 세션이 새로 만든 코드가 아니라 **이미 landed 된 5개 커밋**(06336ad ·
  014ecda · 2065657 · 556f03f · e3ea2eb)의 내용이라, 이번 세션은 그 커밋 diff·커밋
  메시지를 코드와 대조해 문서화했다 — **로컬 :8765 브라우저로 다시 재생해 보지는
  않았다** (이 세션의 범위는 버전 스탬프와 문서다).

  **없는 것**

  **짝인 콘솔(persona-vc) v0.79.0 이 아직 안 올라갔다.** 지금 배포된 콘솔은 `page` ·
  `deal` 을 모른다 — 모델이 그 어휘로 시나리오를 뽑아도 콘솔의 `PLAY_ACTIONS`·프롬프트
  `ACTION_GUIDE` 가 없으므로 애초에 그 액션을 만들 수 없고, 혹시 콘솔이 먼저 올라가
  누군가 `page`/`deal` 을 쓴 시나리오를 저장해도 **지금 이 배포(v2.2.0) 전에는** 앱의
  `nhSanitize` 가 모르는 액션이라 조용히 버렸을 것이다(그래서 이번 순서는 앱을 먼저
  올린다 — HANDOFF 의 "콘솔 먼저" 규칙과 반대 방향인 이유는 MODULES.md M16 절 참고).
  **짝인 콘솔 v0.79.0 이 아직 안 올라갔다** — 그래서 지금은 사람이 콘솔에서 `page`·`deal`
  을 쓴 데모를 만들 수 없다. 앱만 그 말을 알고 있는 상태이고, 그것이 이번 순서(앱 먼저)가
  의도한 안전한 중간 상태다.

  ✅ **v2.2.0 배포·검증 완료 (2026-08-11).** `git push` 후 약 30초에 롤아웃,
  `id="app-version">v2.2.0` 확인. **배포본**(`https://gihoon-mx.github.io/now-here-demo/?embed=1&clean=1`)
  에서 직접 확인한 것:

  - **빈 무대의 딜 0개** — 이번 릴리스의 출발점이던 결함이 실사이트에서 해소됐다.
    `IS_CLEAN_EMBED true` · `newsItems 0` 도 같이 확인.
  - `nhSanitize` 가 `deals`·`pages` 를 받는다(`seed` 키 5종 확인).
  - 무대 딜의 **자동 가격이 화면 값과 일치** — `pct 33` → 원가 `9,900원` · 지금가
    `6,600원` · 수량 `13개`, `seed:false`. 바텀시트에 그대로 렌더됨.
  - **카운트다운이 실제로 흐른다** — `secs 600` 으로 깔고 열었을 때 `9:48 남음`.
  - 피드 탭 지면 **2장**(`추천 컨텐츠 지면`), `page` 가 1칸 넘어가고 **끝에서
    `false`** 를 돌려준다(카드가 2장이므로 두 번째는 실패가 정답이다).
  - **피드 그리드 누출 0건** — `allFeedEntries()` 의 `type:'news'` 가 0개다
    (무대가 깐 카드에 붙인 `stage` 표시가 걸러낸다).
  - `pop:deal` 이 시트를 연다.

  ⚠️ 아직 안 해 본 것: **콘솔을 통한 연동**(`nh:run` 을 실제 콘솔이 보내는 경로)과
  `drop:deal`·`drop:page`·`focus:deal` 의 배포본 확인. 위 확인은 페이지 컨텍스트에서
  앵커를 직접 불러 본 것이다.

  **다음**

  콘솔 쪽 v0.79.0 — 무대 편집기(지면·타임딜 칸)·초안 생성·D90·D91 기록. 그것이 올라가면
  콘솔에서 만든 데모로 위 ⚠️ 항목들을 한 바퀴 돌려 확인한다.

### 2026-08-10 (6)
- **v2.1.0 — 피드 카드도 무대 없이 · 곁들이는 값 · ms 하한 50 (M16, 콘솔 v0.78.0 D89 와 짝)**:

  ① **`postfeed`** — v2.0 의 `post`(남의 지도 글)에 피드판을 맞췄다. `nhPostFeed` 가
  **시드와 같은 함수**(`nhLayFeed`)로 깔고 자리는 `nhSpread(c, 60+n)` 이다
  (시드 스팟 10+i · 시드 피드 20+i · post 40+n 과 안 겹치게).

  ② **곁들이는 값 두 개** — 스텝에 `e` 와 `n` 을 실을 수 있다:
  `e` 는 post 의 이모지이자 postfeed 의 사진 테마, `n` 은 올린 사람 이름.
  additive 다 — 옛 콘솔은 안 보내고 그때는 기본값(💬 · cafe · 동네주민)으로 뜬다.

  ③ ⚠️ **ms 하한을 400 → 50 으로 내렸다.** 콘솔의 "이 단계 화면 보기" 는 앞 단계를 빨리
  감아 지나가는데, 그 하한이 곧 단계당 대기시간이라 여덟 단계짜리는 3초를 기다려야 했다
  (사용자: "그냥 그 화면만 딱 띄워줘"). 사람이 짜는 값은 콘솔이 400 아래로 만들지 못하고
  (`MIN_STEP_MS`), 여기 50 은 0·음수 가드다. **비동기 커밋이 있는 write·ai 는 콘솔의
  `FAST_FLOOR` 가 계속 바닥을 지킨다** — 그게 없으면 `pop i:-1` 이 아직 등록되지 않은
  글 대신 남의 글을 연다.

  ④ **무대 상한**: 지도 글·피드 카드 4 → **10** (Request 는 3 유지 — 답이 도착하는
  연출이라 많을 이유가 없다). `nhSeedScenario` 와 `nhSanitize` 양쪽을 같이 올렸다.

  검증(로컬 :8765, `?embed=1&clean=1`): `post` 에 `e:"☕"` → 스팟이 ☕ 로 뜬다.
  `postfeed` 2건 → 이름 준 것은 `호수러너`, 안 준 것은 `동네주민`, 사진은
  `data:image/svg+xml`(외부 이미지 없음). `nhSanitize` 가 `ms:60` 을 그대로 통과시킨다.
  ⚠️ 빨리 감기의 **체감 시간**은 이 환경에서 못 쟀다 — 창이 백그라운드라 브라우저가
  타이머를 ~500ms 로 묶는다(60ms 요청 → 491ms, `document.hidden` 참).
  `node tools/check.js` 통과.

### 2026-08-10 (5)
- **v2.0.0 — 남이 방금 올린 글 (`post`) (M16, 콘솔 v0.77.0 D88 과 짝)**:

  콘솔 사용자: "내가 필요한 다른 사람이 작성한 컨텐츠가 뜨는 액션이 없는거 같아".

  엄밀히는 v1.98 의 hold+drop 이 그 일을 한다. 그런데 **무대에 항목을 만들고 → '단계로 뿅'
  토글을 켜고 → 단계에서 번호를 맞춰야** 한다. 가장 흔한 장면 하나에 손이 세 번 간다.

  `{a:'post', v:'글 내용'}` 은 **무대를 안 거치고** 그 단계에서 지도 글을 만든다
  (`nhPostSpot`). 여러 번 쓰면 "남의 글이 하나둘 올라온다" 가 된다.

  ⚠️ **깔기는 시드와 같은 함수**(`nhLaySpot`)다. 따로 만들면 뒤늦게 뜬 글만 모양·id·정리
  대상이 달라진다 (v1.98 에서 `nhLay*` 를 떼어낸 것과 같은 이유). 자리는
  `nhSpread(c, 40+n)` — 시드 스팟(10+i)·피드(20+i)와 안 겹치게 하고, `n` 은 회차 시작마다
  0 으로 돌아간다. 중심은 이 회차의 무대 좌표(`nhHeld.c`), 무대가 없으면 지금 지역이다.
  빈 글이면 `ok:false` — 조용히 성공하지 않는다.

  hold+drop 은 남는다: **피드 카드·Request** 를 뒤늦게 띄우거나 미리 적어 둔 것을 정해진
  순서로 띄우는 길이다. `post` 는 지도 글 전용이다.

  액션 어휘 3중 동기화(NH_ACTIONS · 콘솔 PLAY_ACTIONS · 프롬프트 ACTION_LIST).
  콘솔이 먼저 나가서(v0.77.0) 지금 배포본 콘솔은 "앱에 없는 액션(post)" 경고를 띄운다 —
  이 배포로 사라진다.

  검증(로컬 :8765, `?embed=1&clean=1`): `post` 2회 → 스팟 2건이 생기고 `pop i:-1` 이 방금
  것을 연다. 빈 글은 `ok:false`. `nhTempIds.spot` 2건이라 멈추기가 걷어간다.
  `node tools/check.js` 통과.

### 2026-08-10 (4)
- **v1.99.0 — 보고 있는 지도를 콘솔이 그대로 가져간다 (M16, 콘솔 v0.76.0 D87 과 짝)**:

  콘솔 사용자: "구글이나 네이버 지도는 잘 인식이 안되고 어려운거 같아".
  v1.98 에서 낸 `areaPlace` 는 좌표를 **지도 링크에서 읽는** 길이었는데, `naver.me` 단축
  주소와 카카오는 서버가 리다이렉트를 따라가야 읽힌다. 자리를 정하는 것 자체가 관문이었다.

  그런데 **임베드의 지도는 원래 손으로 끌 수 있다** (재생 중이 아닐 때). 맞춰 놓고
  그대로 가져가면 된다:

  ① **새 계약 `nh:where` → `nh:here{lat,lng,zoom}`** (`nhHere()`).
  **폰 지도를 읽는다** — 임베드에서 사람 눈에 보이는 것도 손으로 끄는 것도 폰이고,
  PC 지도는 `display:none` 이라 미러의 출발점일 뿐이다. 지도가 아직 없으면 `nh:error`.

  ② **배율도 같이 싣는다.** `areaPlace.zoom` → `SEED_AREAS.custom.z` → `area` 단계가
  `NH_AREA_ZOOM` 대신 그 값을 쓴다. 자리와 배율은 한 짝이다 — 같은 좌표라도 11 은 서울
  전역이고 17 은 골목 하나라, 좌표만 저장하면 "이 화면 그대로" 가 성립하지 않는다.
  값은 11~18 로 자른다. **배율 없는 옛 시나리오는 14 그대로** (additive).

  검증(로컬 :8765, `?embed=1&clean=1` 에서 자기 자신에게 postMessage):
  `nh:where` → `{37.5050,127.0310,14}`. 폰 지도를 석촌동·17 로 옮기고 다시 물으니
  `{37.5024,127.1063,17}`. `areaPlace.zoom:17` 시나리오를 돌리면
  `SEED_AREAS.custom={…,z:17}` 이고 **PC·폰 지도 둘 다 17**, 중심 37.5024,127.1063.
  배율 없는 잠실 시나리오는 14 그대로. `node tools/check.js` 통과.

### 2026-08-10 (3)
- **v1.98.0 — 사람이 정한 동네 · 콘텐츠가 하나씩 뜬다 (M16, 콘솔 v0.74.0 D85·D86 과 짝)**:

  콘솔 사용자가 꼽은 세 가지 중 둘이 여기 걸렸다 — "내가 원하는 지역" 과
  "다른 사람이 실시간으로 올린 걸 보여주고 싶음, 뿅뿅".

  ① **`areaPlace` — 앱이 미리 아는 네 곳 밖에서도 시연한다.**
  시나리오에 `areaPlace:{name,lat,lng}` 가 오면 `nhCustomArea` 가 `SEED_AREAS.custom` 에
  등록한다. **`nhSanitize` 의 area 검사보다 먼저** 해야 한다 — 순서를 뒤집으면
  `{a:'area',v:'custom'}` 단계가 화이트리스트에서 조용히 버려진다. `SEED_AREA_ORDER` 에는
  넣지 않는다(그 배열은 시드 문서 id 를 정한다 — 시연용 임시 좌표가 저장 구조에 스미면 안 된다).
  매 회차 시작에 지웠다 다시 넣으므로 앞 회차의 좌표가 남지 않는다.

  ② **`hold` + `drop` — 무대가 통째로 깔리는 것이 문제였다.**
  무대는 재생 시작 때 전부 깔린다. 그래서 "빈 지도에서 시작해 남의 글이 하나둘 올라온다" 는
  연출이 **원리적으로 불가능했다** — 화면은 늘 채워진 채로 시작했다. 무대 항목에 `hold:true`
  가 붙으면 `nhSeedScenario` 가 깔지 않고 `nhHeld` 에 쌓아 두고, 새 액션
  `{a:'drop', v:'spot'|'feed'|'req', i:n}` 이 하나를 꺼내 그 자리에서 깐다.
  꺼낸 것은 보관함에서 빠진다 — 연달아 띄울 때 `i:0` 반복이면 되고, 방금 깔린 것은 목록
  맨 뒤라 `pop i:-1` 로 연다(write 로 쓴 글을 여는 것과 같은 손잡이).
  ⚠️ 깔기를 `nhLaySpot`/`nhLayFeed`/`nhLayReq` 로 떼어 **시작 때와 drop 때가 같은 코드**를
  쓴다. 두 경로를 따로 두면 뒤늦게 뜬 글만 모양·id·정리 대상이 달라진다.
  보관함이 비었는데 `drop` 이 오면 **`ok:false`** 로 보고한다 — 조용히 성공하지 않는다.

  **액션 어휘 3중 동기화**: `NH_ACTIONS` + 콘솔 `PLAY_ACTIONS` + 프롬프트 `ACTION_LIST`.
  콘솔이 먼저 나가서(v0.74.0) 지금 배포본 콘솔은 "앱에 없는 액션(drop)" 경고를 띄운다 —
  이 배포로 사라진다.

  검증(로컬 :8765, `?embed=1&clean=1` 에서 자기 자신에게 `nh:run`):
  `area/custom=true` · `drop/spot=true` ×2 · `drop/feed=true` · 보관함이 빈 뒤의
  `drop/spot=false` · `nh:done`. `SEED_AREAS.custom={석촌동,37.5024,127.1063}`,
  지도 중심이 실제로 그 좌표(줌 14). 깔린 순서는
  `["처음부터 있던 글","뿅 하고 뜬 글 A","뿅 하고 뜬 글 B"]` — 뒤늦게 깐 것이 맨 뒤라
  `pop i:-1` 이 방금 뜬 것을 연다. `node tools/check.js` 통과.

### 2026-08-10 (2)
- **v1.97.0 — 멈추기가 화면도 처음 상태로 (M16)**:

  콘솔 사용자 신고: "데모 중에 생긴 데이터들이 데모 처음으로 돌아가도 그대로 살아남아 있음".

  `nh:stop` 이 `nhStop()` 만 불렀다 — 그건 **토큰만 올린다**. 대본은 멈추는데 그 회차가
  만든 것(쓴 글·좋아요·깐 무대)은 화면에 그대로 남아서, 다시 재생하기 전까지 세계가
  지저분한 채로 있었다. (다시 재생하면 `nhRun` 이 `nhReset()` 을 부르므로 깨끗해진다 —
  그래서 "재생은 되는데 멈추면 남는" 모양이었다.)

  콘솔은 진작부터 이걸 초기화로 알고 있었다 — 유저 시나리오의 "처음부터" 버튼이
  `nh:stop` 을 보내며 주석에 "화면도 처음 상태로" 라고 적어 뒀다. **계약을 코드에 맞췄다.**

  `nh:stop` → `nhStop(); nhReset();`. **끝까지 본 회차(`nh:done`)는 안 건드린다** —
  데모의 결말이 곧 보여줄 것이라, 끝나자마자 치우면 방금 만든 글을 볼 수가 없다.
  지우는 것은 멈추거나 다시 재생할 때다.

  검증(로컬 :8765, `?embed=1&clean=1` 에서 자기 자신에게 `nh:run` 을 보내 재현):
  재생 뒤 스팟 2건(깐 무대 + `write` 로 쓴 글) → **멈추기 → 0건**, `nhTempIds` 도 0.
  `node tools/check.js` 통과.

### 2026-08-10
- **v1.96.0 — 빈 무대 임베드 `?embed=1&clean=1` (M16)**:

  콘솔의 **기능 데모**(콘솔 D79)가 "빈 화면에서 시작한다" 는 연출을 못 했다.
  `startEmbed()` 이 부팅 끝에 `seedDemoData({silent:true})` 로 **사이트 데모
  데이터셋을 항상 깔기 때문**이다 (스팟 39 · 피드 56 · Request 7).
  그건 유저 시나리오에는 맞다 — "사람이 실제 앱을 쓴다" 가 전제라 동네에 남의 글이
  있어야 성립한다(콘솔 D25). 기능 데모는 반대로 **제품의 한 기능을 보여주는 연출**이라
  화면에 있어야 할 것은 그 데모가 선언한 것뿐이다.

  게다가 더 나쁜 일이 조용히 일어나고 있었다: 시나리오가 아무것도 안 깔면 `nhPick` 이
  **전역 시드로 폴백**해서, 깔지도 않은 남의 글이 열리며 `ok:true` 로 성공 보고까지 됐다.
  기능 데모 쪽에서 보면 "시킨 적 없는 데이터가 뜨는데 오류는 없는" 상태였다.

  ① `IS_CLEAN_EMBED` 신설 — `?embed=1&clean=1` 일 때만 참.
  ② `startEmbed()` 이 그때 `seedDemoData` 를 건너뛴다. 화면에 뜨는 것은 시나리오가
     깐 것(`nhSeedScenario`)과 재생 중 만든 것뿐이다.
  ③ `nh:ready` 에 `clean` 을 실어 보낸다 — **additive**. 콘솔이 빈 무대를 요청했는데
     이 값이 참이 아니면 앱 배포가 뒤진 것이고, 콘솔이 그것을 화면에 띄운다
     (어휘 드리프트 tripwire 와 같은 결).

  기본 임베드(`?embed=1`)는 **아무것도 안 바뀐다** — 유저 시나리오 재생은 그대로다.

  검증(로컬 :8765): `?embed=1&clean=1` → spots 0 · feeds 0 · reqs 0 · news 0 ·
  `IS_CLEAN_EMBED=true` · 버전 v1.96.0. `?embed=1` → spots 39 · feeds 56 · reqs 7
  (회귀 없음). `node tools/check.js` 통과.

### 2026-08-08
- **v1.95.0 — 줌 배율을 한 곡선으로 · 죽어 있던 데모 액션 셋 (M00 앵커 신설 + M16,
  ⚠️교차 M04/M05/M07/M17/M00 의 `draw`)**:

  사용자 신고 두 가지 — "데모 세션에서 줌인아웃 할 때 지도 위 컨텐츠 크기가 어색하다",
  "제대로 동작 안 하는 데모 액션이 있다".

  **① 줌 배율 (`contentScale` 신설)**

  지도 위 컨텐츠가 전부 같은 `spotScale(z)`(=2^(z-16)) 곡선을 쓰면서 **클램프만 세 벌**로
  갈려 있었다:

  | 컨텐츠 | 클램프 | 줌12→18 배수 |
  |---|---|---|
  | 지역/존 라벨 (`MapLabel`) | 0.7 ~ 1.6 | ×2.3 |
  | Request 핀 · 딜 핀 | 0.34 ~ 1.3 | ×3.8 |
  | **스팟 버블 · 스팟 이모지 · 피드 썸네일** | **없음** (0.02~40) | **×64** |

  그래서 줌 한 단계에 스팟은 2배로 뛰는데 핀은 상한에 걸려 거의 안 움직인다 —
  **컨텐츠끼리의 크기 관계가 줌마다 달라진다.** 이것이 "어색하게 반응한다" 의 정체다.
  실측(420px 폭, 줌13→18): 말풍선 14.6→260px(화면폭의 62%), 이모지 3.7→118.5px,
  피드 핀 10→104px. 반대쪽 끝에서는 이모지가 3.7px 라 안 보인다.

  `contentScale(z)` = `spotScale` 을 **0.7~1.6 하나로** 자른 값. 다섯 군데 `draw` 가
  전부 이것을 쓴다. 범위는 이미 쓰이던 것 중 **가장 보수적인 라벨 값 그대로** —
  새 숫자를 지어내지 않았고, 양 끝에서 읽히는 것이 이미 확인된 폭이다.
  ⚠️ 멀리서 스팟이 **점**이 되는 것은 `mapMpp` 기준 `isDot` 이라 이 변경과 무관하다
  (먼 줌의 "비어 보임" 은 그대로 유지된다 — 우려 시나리오가 그것에 기댄다).

  **② `scroll` 액션 — 처음부터 아무것도 안 하고 있었다**

  선택자 `.pd-body` · `.tabpage` · `.feed-col` 이 **어느 파일에도 없는 클래스**다.
  마크업이 `#feed-page`·`#social-page`·`#phone-drawer-body` 로 바뀐 뒤 선택자만 남았고,
  `nhAct` 가 예외를 삼켜서 조용히 no-op 이었다. v1.94 의 `ok` 보고가 붙은 지금은
  **매 재생마다 `ok:false`** 로 콘솔에 찍힌다. → `nhScrollTarget()` 신설.
  서랍 → 현재 탭 순으로 후보를 훑고 **실제로 넘치는 칸**을 먼저 고른다
  (내용이 짧아 안 밀리는 것과 칸을 못 찾은 것은 다른 일이라 후자만 실패로 본다).

  **③ `chat` 액션 — 엉뚱한 화면을 열고 성공이라 보고했다**

  `socTab` 에 `'local'`/`'topic'` 을 넣고 있었는데 그건 **방의 종류지 세그먼트가 아니다**.
  v1.91 Our/My Talk 분리 이후 세그먼트 값은 `'our'`/`'my'` 다. 어느 탭에도 안 맞으니
  **활성 탭이 하나도 없는 채로** `socRoomsFor` 의 else 가 걸려 **My Talk(1:1·내 Request)**
  이 열렸다 — 시연은 "동네 채팅방" 을 요구했는데. 게다가 `socRoom=null` 이라 방이 아니라
  **목록**이 떴다. 이제 `socTab='our'` 로 두고 방 목록은 `socRoomsFor('our')` 에서 받는다
  (키 규칙을 여기 베껴 두면 또 어긋난다 — 그게 이번 원인이다).

  **④ `request` 액션 — 네이티브 `prompt()` 가 재생을 멈췄다**

  iframe 위에 브라우저 대화상자가 서면 재생이 멈추고, 그 순간만 앱이 아닌 것이 보인다.
  게다가 진행자가 취소하면 Request 가 안 생기는데 스텝은 **성공으로 기록**됐다.
  `openRequestComposer(presetQ)` **optional 인자**(동결 앵커 규칙: 인자 추가는 optional 로만)
  로 시나리오의 질문을 그대로 넘긴다. 사람이 직접 누르는 길은 그대로 prompt 다.

  **없는 것 / 확인 못 한 것**
  - 액션 어휘는 안 늘었다 — **`NH_ACTIONS` 3중 동기화 불필요**, 저장된 시나리오 그대로 동작.
  - 브라우저 패널이 백그라운드면 지도 rAF 가 멈춰 오버레이가 안 그려진다.
    이 세션에서 한참 헤맨 자리다 — **검증은 puppeteer 실제 창**으로 했다.
  - 콘솔(persona-vc) 쪽 `PLAY_ACTIONS`·`ACTION_GUIDE` 는 손대지 않았다.

  **검증** (puppeteer 실제 창, 로컬 :8765 · `node tools/check.js` 통과):
  - 줌12~18 최대/최소 배수 — 말풍선 2.52 · 이모지 2.29 · 피드 3.5 · Request 2.29 · 딜 2.27
    (이모지 20.7 → 29.6 → 47.4 = 정확히 0.7 / 1.0 / 1.6 배)
  - `chat:local` → `socTab='our'` · 활성 탭 "Our Talk" · "역삼1동 채팅방" 열림
  - `chat:topic` → "🍜 맛집 탐방" 열림
  - `scroll` → 대상 `#feed-page` · scrollTop 0 → 220 (실제로 밀림)
  - `request` → 네이티브 대화상자 **0건** · Request 1건 생성 (질문이 시나리오 문구 그대로)

### 2026-08-06
- **v1.94.0 — M16 재생을 실제 사용자처럼 (콘솔 persona-vc v0.60.0 D72 와 짝)**:
  - **`nh:step` 에 `ok`** — 앵커·대상이 없어 화면이 못 따라온 스텝을 콘솔에 알린다.
    그전에는 `console.warn` 으로 삼키고 스텝 메시지를 그대로 흘려서, 화면은 멈췄는데
    콘솔의 대사·평가 기록은 성공한 사용자를 그렸다 (조용한 거짓말 — 콘솔이 fail 로 기록).
    `nhAct` 가 boolean 을 돌려주도록 전 분기를 정리했고, `wait` 는 항상 true 다.
    **additive 라 배포 순서 무관**: 옛 콘솔은 무시, 옛 앱(필드 없음)을 새 콘솔은 성공으로.
  - **터치 표식** — 누르는 액션(tab·ai·scope·popclose)은 `.nh-touch` 손가락 자국을
    먼저 띄우고 170ms 뒤 실행 (`nhTouchTarget` 매핑, 못 찾으면 표식만 생략).
    상태가 "스스로" 바뀌면 유령 조작으로 보이는 것이 화면에서 가장 큰 로봇 티였다.
  - **write 글자별 타이핑** — 220ms 뒤 통째 대입을 interval 타이핑(30~90ms/틱, 1~2자)으로.
    **커밋 시점(max(900, ms*0.55))은 그대로**라 콘솔 페이싱 계약과 안 어긋나고,
    커밋 직전 전체 문자열 보정으로 잘린 글이 등록되지 않는다. 매 틱 토큰 가드.
  - **scroll 3박자** (`nhScrollHuman`) — 60% → 40% → (길면) -15% 되올림.
  - **focus 두 박자** — 지금 줌으로 팬 → ms*0.4 뒤 줌 17 (nhAi 와 같은 패턴).
  - **concern 비네트** — 막힌 스텝에서 화면 가장자리가 한 박자(0.95s) 어두워진다.
  - **좋아요 결정성 누수** — 전역 시드 카드에 남긴 좋아요를 `nhTempIds.like` 에 적고
    sweep 이 되돌린다. 두 번째 재생에서 하트가 이미 차 있어 "채워지는" 장면이
    "꺼지는" 장면이 되던 것.
  - 검증(로컬 3010 동일 오리진 드라이버 + puppeteer): ok 플래그 전 스텝 수신,
    시드 없는 외곽 answer → **ok:false** ✓, 터치 표식 3회 관측 ✓, 좋아요 1→0 원복 ✓,
    write→pop(i:-1) 자기 글 열기 ✓. 타이핑·focus 줌은 로컬에서 Maps 오버레이가 안 떠
    배포본에서 확인할 것.
- **v1.93.0 — 지역 시드 생성기 · 그룹 관리 (M13, ⚠️교차 M01 Maps 로더 · M04/M05/M07/M17)**:
  - 기존 `seedDemoData` 는 **고정 4지역**(강남·잠실·성수·방학)에 미리 써 둔 문구를 깐다.
    시연 지역이 늘 때마다 상수를 고쳐야 하고, **처음 가 보는 동네에서는 아무것도 못 깐다.**
    생성기는 **지금 보고 있는 지역**에 만든다.
  - **어디서 '연관있는' 것을 가져오나** (3단 폴백):
    - ① **Places 근접 검색**이 반경 안의 **실제 상호**를 준다(카페·식당·공원·상점…).
    - ② 그 상호를 **AI 에이전트**(M08 과 같은 엔드포인트)에 넘겨 동네 앱 말투 문구를 받는다.
      ⚠️ **Ask Map 대화 이력은 안 건드린다** — 섞이면 사용자의 다음 질문에 시드 생성
      프롬프트가 문맥으로 딸려 들어간다. 응답 JSON 파싱 실패도 **정상 경로**로 취급한다
      (모델이 앞뒤에 말을 붙이는 건 흔한 일이다).
    - ③ AI 가 없거나 실패해도 **장소 종류별 템플릿**으로 채운다. 생성기가 외부 서비스
      상태에 인질로 잡히면 시연 직전에 못 쓴다.
  - **종류를 고른다**: 스팟 · 사진 · Request · 타임딜 체크박스 + 장소 수 + 반경(m).
    Request 는 3곳마다 하나, 타임딜은 4곳마다 하나 — 흔하면 '현장 질문'도 '특가'도 아니다.
  - **그룹**: 한 번의 생성 = 그룹 하나. 항목은 전부 `sgroup:<id>` 를 달고 목록에서
    **지도 이동 · 숨김 · 삭제**. ⚠️ **기존 시드와 id 공간을 분리했다**(`sg_` vs
    `fs_`/`sps_`/`rqs_`) — 같은 플래그로 묶으면 🧹 비우기가 그룹을, 그룹 삭제가 기존 시드를
    같이 날린다.
  - ⚠️ **Places API 는 GCP 에서 따로 켜야 한다.** 지금 키는 `REQUEST_DENIED` — Maps 는 되는데
    Places 만 막힌 상태다. '실패'만 띄우면 키를 의심하게 되므로 **원인과 할 일을 그대로**
    알리고(“GCP 콘솔 → API 및 서비스에서 Places API 를 켜세요”), 그 자리에서 **동 이름 기반
    기본 장소로 만들지 물어본다** — 켜는 즉시 같은 버튼이 진짜 상호를 가져온다.
  - 좌표는 **황금각 분산**(결정적) — `Math.random` 금지 규칙(v1.72)을 지킨다.
  - 검증(로컬 실측): 폴백 경로로 9곳 생성 → 스팟 9 · 사진 9 · Request 3 · 타임딜 1
    (park·book 은 딜 템플릿이 없어 건너뜀 = 의도대로), 그룹 카운트 일치.
    **숨김**: 피드 노출 16→0→16, 스팟은 목록에 남아 되돌릴 수 있음. **삭제**: 정확히 그
    그룹만(피드 9·스팟 9·Req 3·딜 1·그룹 1) 제거, 잔여 0. 콘솔 패널 렌더·그룹 2행 표시 확인.
    ⚠️ **Places 실검색 결과는 확인 못 했다** — API 가 꺼져 있어서(`REQUEST_DENIED`)
    검색 호출까지만 검증했다. 켜면 바로 그 경로로 붙는다. check.js PASS.

- **v1.92.0 — v3.0 8단계(마지막): 경계 토글 · 코인 · °C 지표 (M09+M07+M03)**:
  - 드로어에 **'보기' 섹션** 신설 — 동 경계(City View) · 현장 Request 도착 카드.
    둘 다 **관리자 설정이 아니라 이 기기의 취향**이라 설정 블록(드래프트→적용)을 타지
    않고 즉시 반영되고 localStorage 에만 남는다. 클라우드로 보내면 한 사람의 보기 취향이
    모든 사용자에게 적용된다.
  - 경계는 `phoneDataVisibility` 에 `boundaryShown` 을 **AND 로 물렸다** — 원래의
    모드 규칙(`currentMode==='local'`)은 그대로 두고 조건 하나만 더한다.
  - **코인**: 남의 Request 에 답하면 `🪙 500` 적립(내 것에 답하는 건 적립 대상이 아니다).
    잔액은 프로필 메뉴 하단에. 실제 정산이 있는 것처럼 보이지 않게 표기는 '적립'까지다.
  - **°C**: 존 리스트 카드에 온도 배지. 시안은 좋아요가 아니라 **온도로 지역을 말한다**.
    값은 v1.90 에서 `makeZoneCard` 가 심어 둔 `data-temp` 를 그대로 쓴다(마크업 무수정).
  - 검증(로컬 실측): 보기 두 줄 렌더·상태 반영, **동 경계 토글이 실제로 데이터 레이어를
    붙였다 뗐다**(`true→false→true`), Request 카드 끄면 `showReqBubble` 이 아무것도 안 함,
    코인 `0→500` + UI `🪙 500`. 콘솔 에러 0. check.js PASS.
- **v1.91.0 — v3.0 7단계: Our Talk / My Talk (M06, ⚠️교차 M07)**:
  - 3탭(동네·주제·프라이빗)에서 **2세그먼트**로. Our=동네 채팅방+주제방,
    My=프라이빗+**내 Request 스레드**.
  - ⚠️ **방의 저장 키는 그대로 둔다**(`local:` `topic:` `private:`) — 키를 바꾸면 이미 쌓인
    대화가 통째로 고아가 된다. Our/My 는 목록을 고르는 **뷰**일 뿐이고 방 자신의 type 은
    예전 그대로다.
  - 예전엔 '동네' 탭이 목록 없이 **바로 방으로** 들어갔는데 시안은 **목록 → 대화 2단**이라
    동네 방도 목록의 첫 줄이 됐다. 그래서 뒤로가기가 항상 있다.
  - 방 카드에 아바타·태그·마지막 메시지·시각·안읽음 배지. 내 Request 는 방이 아니라
    스레드라 탭하면 기존 상세 팝업(질문+답변)이 열린다.
  - 검증: Our 5행(동네+주제 4)·My 빈 목록 안내, 방 진입/뒤로가기, 탭 전환 시 목록 복귀.
- **v1.90.0 — v3.0 6단계: 지역 Overview 패널 + 스토리 서클 (M10+M03)**:
  - 지면 카드를 탭하면 그 지역의 '지금'이 유리판 한 장에 뜬다 — 칩 줄 · AI 한 줄 요약 ·
    사진 서클 · 소식 카드 · 둘러보기.
  - ⚠️ **v1.62 규칙을 하나 바꿨다**: "지면 캐러셀=클릭 액션 없음"이었는데 시안은 지면 카드를
    Overview 로 가는 문으로 쓴다. 스와이프는 그대로 두고 **탭에만** 액션을 붙였다.
  - **칩은 세어지는 것만** 올린다 — 시안의 `💬 40k`·`👥 현장 682명` 은 이 앱에 없는 숫자다
    (v1.81 교훈). 실제로 세는 것: 온도 · 동 · 스팟 · 사진 · Request · 타임딜 · 존.
  - 스토리 서클은 `#cp-zones` 존 카드를 **원형으로 다시 짠 것**. 링 색·온도는 `makeZoneCard` 가
    `--zone-c`·`data-temp` 로 실어 보낸다(마크업 무수정). 배지는 `Hot Rising` 같은 등급이 아니라
    **실제로 있는 값(°C)**.
  - 검증: 칩 6종 실측치, AI 요약 실데이터, 사진 서클 5장, 소식 카드·둘러보기 라벨.
    ⚠️ 시안은 패널이 화면을 꽉 채우지만 이 앱은 칩·사진이 적어 아래가 빈 유리판이 됐다 —
    내용 높이에 맞췄다.
- **v1.89.0 — v3.0 5단계: 타임딜 신설 (M17, ⚠️교차 M07/M11)**:
  - 지도 ⏰ 핀 + 바텀시트(할인율·가격·재고·`mm:ss` 티커·쿠폰 받기/동네 공유).
  - **왜 새 모듈인가**: 스팟·피드·Request 와 달리 딜은 **시간이 핵심**이다 — 남은 시간이 줄고
    0 이면 사라진다. 기존 컨텐츠 배열에 얹으면 그 시간 규칙이 피드·지면 전체로 새어 나간다.
  - 시드 딜은 **만료되지 않는다**(`seed:true` — `reqActive` 와 같은 장치). 대신 남은 시간은
    벽시계를 주기로 접어(`secs - now%secs`) 계속 흐르는 것처럼 보인다.
  - ⚠️ **딜은 무대를 따라와야 한다.** 처음엔 `feedItems` **배열 순서**로 자리를 골랐는데
    시드가 5개 지역에 흩어져 있어 **13.8km 밖**에 세워졌다(실측). 센터에서 **가까운 순**으로
    고르고, 이미 있어도 3km 를 넘으면 다시 세운다(임베드 시나리오가 지역을 옮겨 다닌다).
  - 콘솔 전체 컨텐츠 표에 '타임딜' 종류로 편입(상태 `진행 중` · 남은 시간).
  - 검증: 시트 티커 `17:15→15:08` 실제 감소, 거리 실측(370m), 진행 바 49%, 표 종류 집계에
    타임딜 2건. ⚠️ **핀 렌더는 이 세션에서 육안 확인 못 했다** — 이 환경의 Maps 가 오버레이
    수명주기를 돌리지 않아(순정 `OverlayView` 프로브도 `onAdd` 미호출, Request·피드 핀도
    같이 0) 구조 검증(ReqPin 과 동일 경로·프로토타입·좌표)까지만 했다.
- **v1.88.0 — v3.0 4단계: 전체 컨텐츠 표 (M11, ⚠️교차 M04/M05/M07)**:
  - 시안의 '컨텐츠 관리' 화면. 지금까지 콘솔의 컨텐츠 관리는 **종류별로 흩어져 있었다** —
    스팟은 스팟 패널, 피드는 피드 패널, **Request 는 어디에도 없었다.** 한 표에 모으면
    "지금 이 서비스에 뭐가 올라와 있나"를 한 번에 본다.
  - 종류 탭(전체/사진/LIVE/스팟/Request/지면) · 다중 선택 → **존 이동 · 숨김 · 숨김 해제 ·
    삭제**. 행은 새로 만들지 않고 **기존 데이터를 읽어 조립한다**(`allFeedEntries` M05 앵커
    재사용 + `fieldRequests` M07). **표는 소유자가 아니라 뷰다** — 쓰기는 각 모듈의 함수를
    부른다(`feedUpdate`·`feedDelete`·`removeSpot`). Request·지면처럼 존/숨김 개념이 없는
    종류는 조용히 건너뛰고 몇 건을 건너뛰었는지 알린다.
  - `hidden` 은 **additive 필드**(Firestore 스키마 규칙 — 없으면 공개로 읽힌다).
  - ⚠️ **두 가지가 조용히 틀릴 뻔했다**:
    - **① `allFeedEntries` 매핑이 `hidden` 을 안 실어 보냈다.** 소비하는 쪽에서
      `it.hidden` 이 늘 `undefined` 라 필터가 그냥 통과하고, 표의 상태도 늘 '공개'로
      나왔다. **매핑이 필드를 빠뜨리면 에러가 아니라 무음 실패다** — 필터를 새로 걸 때는
      그 필드가 실제로 그 객체까지 실려 오는지부터 본다.
    - **② 숨김을 `rebuildSpots` 에서 걸렀더니 `spotMessages` 에서 사라져 표에서도 안 보였다.**
      숨긴 것을 다시 공개할 방법이 없어진다. **목록은 원본을 갖고, 화면만 숨긴다** —
      `renderSpots`·`feedEntriesScoped`·`clusterFeedPins`·`feedSummaryItems` 에서 거른다.
  - 검증(로컬 실측): 사진 2 + 숨김 사진 1 + 스팟 1 + Request 1 = **5행**, 시간 내림차순 정렬,
    상태 배지 `공개/공개/공개/숨김/진행 중`, '사진' 탭 필터 2행, 전체선택 시 액션 바 노출
    `5개 선택`, 숨긴 사진은 `feedEntriesScoped()` 에서 0건(서비스 화면에서 빠짐).
    콘솔 에러 0. check.js PASS.
- **v1.87.0 — v3.0 3단계: 콘솔 재도색 (M15+M14, M11 화면)**:
  - **v3 는 폰 스킨이 아니라 제품 전체의 재설계다.** 핸드오프가 웹앱과 콘솔을 같이
    그렸으므로 **스킨 스위치가 콘솔까지 함께 움직인다**(`body[data-skin="v3"].page-admin`).
    v2(new)는 폰 화면만 바꿨었다 — 그래서 스킨 설정 안내 문구도 같이 고쳤다.
    되돌리기는 여전히 속성 하나: v2/legacy 로 두면 콘솔도 예전 그대로다.
  - **콘솔은 폰 컨테이너 밖이라 전부 px.** cqw 를 쓰면 뷰포트 폭으로 풀린다(v1.82).
  - 시안의 콘솔 문법 셋을 가져왔다:
    - **① 종이 캔버스 위에 흰 카드.** 설정 블록이 구분선으로 나뉜 한 장(legacy)에서
      각자 그림자를 가진 **카드**가 됐다. 패널이 아니라 카드다.
    - **② 컨트롤은 전부 알약, 확정 동작만 검정 알약.** `.action-btn` 기본은 회색 알약,
      `.accent`(전체 적용·초대)만 `#1A1A1A`. 위험 동작은 연한 코랄 배경.
    - **③ 좌측 내비는 선택된 행만 흰 카드로 떠오른다.** legacy 는 옅은 파란 배경으로
      **색**으로 표시했는데, 시안은 **높이**로 말한다(흰 배경 + 그림자).
  - 탑바는 유리 바에서 **불투명 흰 바 + 헤어라인** 하나로. '관리자' 배지는 그라디언트에서
    조용한 회색 알약으로 — 배지가 브랜드를 이기면 안 된다.
  - 검증(로컬 실측, 1400×920): 탑바 `#fff`, 모달 라운드 20px, 좌 레일 `#F8F7F5` +
    활성 행 흰 카드, 패널 영역 `#F4F3F1` + 블록 카드 흰색·라운드 16px, 적용 바 흰 카드 +
    초록 점, 셀렉트·버튼 알약. 콘솔 에러 0. check.js PASS.
- **v1.86.0 — v3.0 2단계: 지도 오버레이 · 탭 페이지 (M15, ⚠️교차 M05/M10)**:
  - **사진 핀이 시안의 핵심 마커다.** 원형 + 두꺼운 컬러 링(3.5px), 그리고 **Basic 에서는
    사진이 흑백으로 가라앉고 링이 흰색**이 된다 — 시안이 명시한 규칙: 색은 Trend 의
    언어다. 클러스터 개수 배지는 우상단 원에서 **하단 중앙 알약**으로(Basic 흰 알약 +
    회색 글씨 / Trend 온도색 + 흰 글씨).
  - Request 핀은 **검정 원 + 흰 링**(Trend 에서 링이 오렌지). 스팟 버블은 **컬러 몸통을
    그대로 살린다** — v2 는 흰 유리로 바꿨지만 v3 의 출처는 사용자가 그린 에셋이고
    거기서 말풍선은 컬러다. `--spot-bg` 를 건드리지 않고 모서리·그림자만 손봤다
    (색은 관리자 설정과 베이직/트렌드 규칙이 갖는다 — 설정을 이기는 스킨은 안 만든다).
  - 피드 카드는 **v1.84 의 `.fc-body` 구조를 그대로 쓴다** — 시안의 카드도 정확히 같은
    구조였다(사진 / 제목 / `존 · 시간`). 다른 점 하나는 **좋아요가 사진 좌상단 흰 알약**인
    것. `.fc-like` 는 메타 줄 안에 있지만 `position:absolute` 를 주면 흐름을 벗어나
    **부모(카드) 좌상단 = 사진 좌상단**에 앉는다 — 마크업을 안 건드리고 CSS 로 옮겼다.
    좌상단을 좋아요가 차지했으니 LIVE·존 칩은 우상단으로 보냈다.
  - ⚠️ **v1.84 의 조건이 틀렸다.** `.fc-body`·`.cps-meta` 를 만드는 조건이
    `appSkin==='new'` 였다 — **스킨이 둘일 때만 맞는 식**이다. `!=='legacy'` 로 뒤집어
    v3 도 포함되게 했다(넷째가 생겨도 자동으로 포함된다).
  - ⚠️ **흰색은 헤더가 아니라 앱바가 갖는다.** 처음엔 `.phone-header` 를 통째로 흰색으로
    칠했는데, 이 헤더는 앱바만 들고 있는 게 아니라 **지면 카드와 존 칩 줄까지** 품고 있다 —
    트렌드 모드에서 존이 하나도 없으면 그 자리가 통째로 **빈 흰 판(실측 490px)** 이 됐다.
    시안에서 흰 바는 앱바 한 줄뿐이다. 헤더는 투명, 앱바만 흰색, 헤더에서 뺀 좌우 여백은
    지면·존 칩이 각자 받는다. 지면 카드는 원래 자기 배경이 있는 카드라 그대로 뜬다.
  - 검증(로컬 실측): 세 스킨 독립 — 본문 개수 `0/10/10`, 좋아요 위치
    `absolute(오버레이)/static(메타 줄)/absolute(사진 좌상단)`, 지면 프레임 배경
    `투명/투명/#fff`·라운드 `18px/0 0 24px 24px/16.4px`, 앱바 배경 `투명/투명/#fff`.
    Trend 전환 시 세그먼트·AI 버튼 코랄 전환 확인. 콘솔 에러 0. check.js PASS.
- **v1.85.0 — 새 디자인 v3.0 1단계: 토큰·셸 (M15)**:
  - 사용자가 Claude Design 핸드오프(`Now Here 재설계 v2.dc.html`, 2026-08-03)를 가져왔다.
    **직접 그린 석촌동 UI 에셋을 기준으로 웹앱·콘솔을 전면 재설계**한 것. 이것을
    **v3.0 스킨**으로 반영한다. 범위는 사용자 확인: v3 를 **세 번째 스킨으로 추가하고
    기본값**, 콘솔은 **재도색 + 컨텐츠 관리 표 신설**, 시안의 **새 기능도 전부 구현**.
    한 세션 = 한 모듈 규칙에 맞춰 **8단계 버전**으로 나눈다(이번이 1단계).
  - **스킨이 셋이 됐다** — `APP_SKINS=['legacy','new','v3']`. 전에는 `v==='legacy'?'legacy':'new'`
    처럼 **두 개를 전제로 분기**하던 코드가 세 곳(초기 로드·`setAppSkin`·클라우드 동기)에
    흩어져 있었다. 셋이 되는 순간 그 분기는 전부 틀린다 — 목록 하나를 두고 셋이 같은
    기준을 보게 했다. 넷째가 생겨도 배열에 한 줄이다.
  - **`skin-v3.css` 신설.** style.css 도 skin-new.css 도 안 건드린다. 스킨은 서로를
    모르고 각자 legacy 위에 자기 층을 얹을 뿐이라, **v3 는 v2 가 덮어 둔 것을 되돌릴
    필요가 없다** — 매칭 자체가 안 된다(`body[data-skin]` 하나로 갈린다).
  - **v2 와 무엇이 다른가**: v2 는 밝은 시안 + 물빛 배경, v3 는 **웜 오프화이트(#F4F3F1)
    + 코랄(#E8574A)**. 잉크도 남색기를 뺀 순수 무채색 5단. 온도 램프는
    `#8CC63F→#A8CF5A→#F2C94C→#F2994A→#EB5757`(36.5~99.9°C) — 시안이 좋아요·히트를
    버리고 **°C 하나로 통일**했고, 앱과 콘솔이 같은 눈금을 본다.
  - **가장 큰 구조 차이는 하단 네비.** v2 는 흰 알약 + 액센트 그라디언트였고 v3 는
    **회색 유리(rgba(120,120,120,.42)) 위 아이콘만** — 글자가 없다. legacy 는 선택 시
    라벨이 펼쳐지는 애니메이션을 갖고 있어서 `.pn-lb` 를 숨긴다. **마크업에서 지우지는
    않는다** — 지우면 legacy·new 가 같이 깨진다. 스킨은 CSS 안에서 끝낸다.
  - 헤더도 성격이 바뀐다: legacy=지도 위 프로스트 바, v2=사진 위 투명 앱바,
    **v3=불투명 흰 바**. 위치는 흰 알약 카드가 아니라 **가운데 정렬된 굵은 제목**이고
    핀도 칩도 없다. 프로필은 검정 원형. Basic/Trend 세그먼트는 회색 유리 위에서
    **Basic=진회색 · Trend=코랄** — Trend 는 그 자체가 온도 화면이라 액센트 상태다.
  - 폰트는 v2 와 같은 `PretendardNH`(family 이름을 갈라 legacy 폰트를 안 건드리는 장치).
    같은 src 라 브라우저가 한 번만 받는다. skin-v3.css 에 다시 선언한 건 **이 파일이
    혼자서도 성립하게** 하려는 것.
  - 검증(로컬 실측, 임베드 400×860): 세 스킨이 서로 다른 값으로 독립 —
    헤더 `rgba(255,255,255,.78)` / `transparent` / `#fff`,
    네비 `rgba(255,255,255,.6)` / `#fff` / `rgba(120,120,120,.42)`,
    라벨 `block`/`block`/`none`, 프로필 라운드 `50%`/`13.2px`/`9999px`,
    화면 배경 `#e8eaed`/`#EFF6FC`/`#F4F3F1`. 콘솔 에러 0. check.js PASS.
  - 남은 단계: ②지도 오버레이·탭 페이지 ③콘솔 재도색·구조 ④컨텐츠 관리 표
    ⑤타임딜 ⑥Overview·스토리 서클 ⑦Our/My Talk ⑧경계 토글·코인·°C 통일
- **v1.84.0 — 카드 부제·메타 줄 (M05+M10, ⚠️교차 M15)**:
  - v1.81 에서 "**마크업 변경이라 스킨 밖의 일**"이라며 미뤄 둔 마지막 조각. 그때 남긴
    두 가지 중 풀블리드 히어로는 v1.83 에서 했고, 이번이 부제·메타 줄이다.
  - **① 피드 카드에 사진 아래 흰 본문**(M05). `skin-new.css` 주석에 "본문 분리는
    `renderFeed` 를 고쳐야 해서 스킨의 일이 아니다"라고 적어 둔 그것이다.
    지금까지 **설명글은 상세 팝업에서만 보였다** — 시드 사진마다 다 들고 있는데
    그리드에서는 한 글자도 안 나왔고, 카드에 있는 글자라고는 동 이름뿐이었다.
    - 구조: 사진 / 설명글(2줄 클램프, 부제) / `동 · 시간 … ♥n`(메타 줄).
      사진 위에 있던 위치칩·시간칩·하단 스크림은 할 일이 없어져 `.has-body` 에서 끈다.
      좌상단 LIVE·존 칩과 우상단 ✏️🗑 만 사진에 남는다(자기 배경이 있다).
    - **카드를 세로 flex** 로 만들고 본문에 `flex:1` 을 줬다. 그리드 한 줄의 카드는
      stretch 로 키가 같아지는데, 그 여분을 본문이 먹어야 **설명글이 한 줄인 카드와
      두 줄인 카드의 메타 줄이 같은 높이에 선다.** 안 그러면 아래 줄이 어긋난다.
      사진에는 `flex:none` — 카드가 늘어도 사진은 자기 비율(1:1)을 지킨다.
    - 좋아요는 사진 위 흰 원형(v1.81)에서 **메타 줄 오른쪽 끝 숫자**로 내려왔다.
      사진이 카드 전부였을 때는 떠 있는 버튼이 맞았지만, 이제 같은 성격의 정보가
      한 줄에 모여 있다. `lk` 요소 자체는 그대로라 더블탭 좋아요 로직은 안 건드렸다.
    - **글 카드(`.txt`)는 제외** — 본문이 곧 카드다.
  - **② 지면 카드에 메타 줄**(M10). `거리 · 시간`. 정렬에 쓰던 `d` 는 제곱 좌표차라
    화면에 못 쓴다 — `haversineM` 으로 실측 m 를 따로 뽑아 `dist` 로 넘긴다.
    - **있는 데이터만 쓴다.** 거리·시간은 연관 피드 카드에만 있고 관리자가 올린 지면
      이미지에는 없다 — 없으면 줄 자체를 만들지 않는다. 좋아요는 이미 우측 칩으로
      나가므로 되풀이하지 않는다. (v1.81 의 교훈: 채울 것이 없는 화면은 껍데기다.)
  - **③ 캐러셀 점이 메타 줄과 자리를 다퉜다.** 지금까지 그 높이 띠는 짧은 동 이름뿐이라
    비어 있었다. 흰 본문(cv1·cv2)에서는 점을 **오른쪽 끝으로 보내고 잉크색**으로 칠했다 —
    흰 점은 원래 사진 위(cv3) 색이라 흰 본문 위에서는 테두리 선으로만 보이고 있었다.
    사진 위 캡션(cv3)은 점을 가운데 두고 **본문을 점 위로 들어올렸다**(본문이 바닥 기준
    절대배치라 줄 수가 몇이든 정확히 비켜난다).
  - **legacy 는 마크업까지 그대로다.** `.fc-body`·`.cps-meta` 를 **새 스킨일 때만 만든다** —
    CSS 로 숨기는 대신 아예 안 만든다. 대신 스킨이 이제 마크업까지 가르므로
    `setAppSkin` 이 `renderNews`/`renderFeed` 를 다시 부른다(⚠️교차: M15). 속성만
    바꾸면 다음 렌더까지 옛 구조가 남는다.
  - 검증(로컬 실측, 임베드 400×860): 피드 2열 — 설명글 1줄/2줄 카드가 섞인 줄에서
    메타 줄 바닥 정렬 유지(250px·235px 행), 3열(121px 카드)·1열(373px 카드) 모두 정상,
    글 카드 2개는 정사각 그대로. 지면 — cv1/cv2 메타 `290~302` vs 점 `294~300 left=282`
    (겹침 없음), cv3 메타 `275~286` vs 점 `297~303`(본문이 위로 비킴), 접으면 메타 숨김.
    점 색 `rgba(14,20,32,.16)` / 활성 `#1C9BD4`. **legacy**: `.fc-body` 0개,
    카드 자식 `IMG·fc-region·fc-top·fc-tr·fc-like` 그대로, 카드 186×186·라운드 13.6px·
    `display:block`, 시간은 `fc-tr` 안, 지면 메타 0개·점 흰색 가운데.
    스킨 왕복(new→legacy→new) 후 복구 확인. 콘솔 에러 0(index·admin). check.js PASS.
  - ⚠️ 발견(이번 작업 밖, 미수정): **v1.83 풀블리드 이후 지면 좌상단 LIVE·존 칩이
    앱바와 겹친다** — 사진이 최상단까지 올라오면서 칩이 앱바 뒤로 들어갔다.
    `.cps-chips` 를 앱바 아래로 내리든 사진 하단으로 옮기든 배치 결정이 필요하다(M10/M15).

### 2026-08-02
- **v1.83.0 — 네비 버그 + 지면 풀블리드 히어로 (M09+M15, ⚠️교차 M10)**:
  - **① (버그) 코드로 탭을 바꾸면 하단 네비가 안 따라갔다.** `switchTab` 은 `currentTab`·
    탭 페이지·축척·모드 토글만 건드리고 **`.active` 는 건드리지 않았다.** 그 클래스는
    네비 클릭 핸들러의 인라인 코드와 `setNavActive` 헬퍼가 따로 관리했고, 호출부
    **여섯 곳**이 `setNavActive(x); switchTab(x);` 를 짝지어 부르고 있었다.
    그런데 **M16 임베드 브리지의 `tab` 액션만 짝을 빠뜨렸다** — 콘솔이 시나리오를
    재생하면 화면은 넘어가는데 네비는 이전 탭이 활성으로 남는다. **하필 그 한 곳이
    시연 경로다.**
    - 고친 방식: 한 줄 더 붙이면 *일곱 번째 짝짓기*가 될 뿐이고 여덟 번째를 또 까먹는다.
      `switchTab` 안에서 `setNavActive(tab)` 를 부르게 해 **문을 하나로** 했다.
      클릭 핸들러의 인라인 토글도 지웠다. 호출부 여섯 곳의 `setNavActive` 는 이제
      중복이지만 그대로 둔다 — 지우면 `switchTab` 없이 표시만 바꾸던 경로가 있을 때
      조용히 깨진다. 확인해보니 **탭만 바꾸고 네비는 두고 싶은 경우는 없었다.**
  - **② 지면 풀블리드 히어로.** 사진이 화면 **최상단·가장자리까지** 차고 앱바가 그 위에 뜬다.
    - **앱바를 흐름에서 뺐다**(`position:absolute`). 처음엔 흐름에 두고 사진을 음수
      마진으로 끌어올렸는데, 끌어올릴 값이 앱바 **실측 높이와 정확히 같아야** 하고
      그 높이는 위치칩 내용에 따라 변한다 — 12px 짜리 배경 띠가 남았다.
      absolute 로 바꾸니 계산 자체가 사라지고, 덤으로 헤더 높이 = 지면 높이가 되어
      `layoutTabPages` 가 저절로 맞는다.
    - 헤더의 좌우·상단 패딩을 **앱바로 옮겼다**(헤더가 패딩을 들고 있으면 사진이
      가장자리까지 못 간다) + 헤더 배경 투명 + `#phone-content-page` 의 legacy
      상단 마진 3cqw 제거(안 지우면 사진 위에 띠가 남는다).
    - **높이는 탭마다 다르다** — `switchTab` 이 심는 `body[data-tab]` 으로 가른다
      (지도 78cqw / 피드·소셜 50cqw). 같은 높이면 피드 그리드가 화면 밖으로 밀린다.
      `applySkin` 에서 초기값 `map` 도 심는다 — 없으면 첫 프레임이 legacy 높이로 그려진다.
    - 카드 버전 1·2 의 슬라이드 좌우 여백·사진 라운드를 없애 사진을 가장자리까지 붙이고,
      여백은 본문만 갖게 했다. **버전 자체(관리자 설정)는 안 건드린다** — 배치가 아니라
      여백만 조정.
    - 접으면 다시 카드다. 앱바가 흐름 밖이라 그냥 두면 접힌 카드(15cqw)가 앱바(≈18cqw)
      뒤로 통째로 숨는다 — 위 여백 19cqw 로 앱바 아래에 내려놓는다.
  - 검증(로컬 실측): 지도 탭 프레임 `top=0 · h=304px(78cqw)` · 피드 탭 `h=195px(50cqw)`
    · 피드 페이지 `top=205px` · 접힘 `top=74px h=59px`(앱바에 안 가림) · 펼침 복귀 정상.
    **네비**: `switchTab('feed')` 만 호출 → `feed:true` 로 활성 이동.
    **legacy 는 원래 값 그대로**(프레임 `top=68 h=172 radius=17.55px`, 앱바 `static`,
    `#phone-content-page margin-top=11.7px`). 콘솔 에러 0. `node tools/check.js` PASS.
- **v1.82.0 — (버그) 팝업 모서리가 내용을 잘라 먹던 문제 (M15)**:
  - 증상: 상세 팝업의 라운드가 과해서 모서리 쪽 내용이 가려짐.
  - 원인: **`cqw` 를 폰 컨테이너 밖에서 썼다.** `#content-pop` 은 `.modal-overlay`
    (`position:fixed`)라 `.phone-screen`(`container-type:inline-size`) 밖에 있고,
    컨테이너가 없으면 `cqw` 는 **에러 없이 뷰포트 폭으로 풀린다.** 878px 창에서
    `6cqw` = **52.7px** — 카드 폭이 326px 이니 폭의 16%가 모서리로 깎였다.
    데스크톱이 넓을수록 더 심해진다(1280px → 76.8px). 그림자도 같이 부풀었다.
  - **②지도 오버레이 단계에서 "컨테이너 밖은 px" 규칙을 세워 놓고 ④에서 내가 어겼다.**
    파일 머리말에 그 규칙과 해당 요소 목록을 못 박았다.
  - 고친 것: `#content-pop .cpop-card` 22px(+그림자 px). 같이 훑다가 `.pdh-feature` 도
    걸렸다 — **PC 드로어에도 같은 클래스로 있어서**(컨테이너 밖) 뷰포트 기준으로
    부풀고 있었다. 드로어 안쪽은 legacy 부터 px 로 짜여 있으므로(헤드 `padding:11px 13px`·
    `.drawer-sec` 13px) `#phone-drawer-close`·`.drawer-sec` 도 px 로 통일했다.
  - 검증: 878px·1280px 양쪽에서 카드 반지름 **22px 고정**(폭 대비 6.5~6.7%), 팝업 열어
    헤더·본문 안 잘림 확인. 컨테이너 밖 요소 전수 감사 — 남은 `cqw` 없음. check.js PASS.
- **v1.81.0 — 새 스킨 톤 재조정: 밝은 시안 + 사진 위 글래스 (M15)**:
  - 사용자가 여행앱 레퍼런스 이미지를 주고 "이런 비주얼 느낌으로" 요청. 지금 스킨과
    갈리는 지점이 뚜렷했다 — **액센트가 딥 로열블루 vs 밝은 시안**, 그리고 **사진이
    카드에 담기느냐 vs 사진 위에 유리가 뜨느냐**.
  - 팔레트를 통째로 옮겼다: `--nk-blue` `#1B4FD8` → **`#1C9BD4`**, 그라디언트
    `#5AC8F0→#1E96D2`. 배경도 회색(`#F5F8FD`)에서 **물빛**(`#EFF6FC`)으로.
    토큰만 갈면 버튼·토글·네비·핀·시트가 전부 따라온다 — v1.78 에서 변수로 묶어 둔 값.
  - 유리를 더 투명하게(`.72`→`.66`) 블러는 더(22→26px). **사진 위에 뜨는 것이 전제**라
    뒤가 비쳐야 물 위에 뜬 것처럼 보인다.
  - **사진 카드에 하단 그라디언트 스크림**을 깔고 위치를 칩이 아니라 **흰 글씨**로 얹었다.
    `::after` 는 마지막 자식이라 그냥 두면 칩들을 덮는다 — 스크림 `z-index:0`,
    칩 `1` 로 쌓임 순서를 명시했다. 좋아요는 우하단 **흰 원형 버튼**으로.
  - 소셜 세그먼트를 **시안 트랙 위 흰 알약**으로 (레퍼런스에서 가장 특징적인 컨트롤).
    이 앱에 세그먼트는 여기 하나뿐이라 과하지 않다.
  - Ask Map 시트 상단에 옅은 시안이 깔리고 아래로 흰색이 된다.
  - **가져온 것은 팔레트·유리·사진 처리지 정보구조가 아니다.** 레퍼런스는 여행 예약
    앱이라 별점·리뷰 수·레스토랑 목록이 있는데 이 앱에는 그 데이터가 없다.
    화면만 만들고 채울 것이 없으면 껍데기가 된다.
  - ⚠️ **CSS 재도색까지다.** 지면을 풀블리드 히어로로 바꾸는 것, 카드에 부제·메타 줄을
    다는 것은 `renderNews`(M10)·`renderFeed`(M05) 를 고쳐야 하는 **마크업 변경**이라
    스킨 밖의 일이다.
  - 검증: 로컬 프리뷰 실측 — 지도/피드/소셜/AI 시트 전 화면 시안 전환, 스크림+흰 캡션,
    시안 세그먼트, **legacy 는 원래 값 그대로**(네비 `#6d9bf5→#4a7de4`·투명 세그먼트·
    카드 13.26px·배경 `#e8eaed`), 콘솔 에러 0. `node tools/check.js` PASS.
- **v1.80.0 — 새 디자인 스킨 4단계(마지막): 오버레이 (M15)**:
  - **Ask Map 이 바텀 시트가 됐다.** legacy 는 AI 버튼 위에 뜨는 74cqw 짜리 카드였다.
    원본은 화면 폭을 다 쓰는 시트다 — 요약·질문 목록·대화가 들어가야 해서 카드로는 좁다.
    위치(right → left/right 0)·크기·모서리(전체 라운드 → 위쪽만)·등장 방향(팝 → 아래에서
    올라옴)을 전부 바꿨고, 시트 손잡이는 `::before` 로 그렸다.
  - `max-height` 에 **`cqh` 를 못 쓴다** — 폰 화면이 `container-type:inline-size` 라 높이
    축이 없다. 시트가 `.phone-screen` 기준 절대배치라 `%` 가 곧 화면 높이 비율이다(72%).
  - 제목은 그라디언트 글자에서 **작은 대문자 캡션**(ASK MAP)으로, 🗺 지도 요약은 버튼이
    아니라 **연한 액센트 카드**로, 추천 질문은 연회색 카드 + 오른쪽 갈매기(마스크 SVG)로.
    질문 텍스트가 `textContent` 라 익명 flex 아이템이어서 `flex:1` 을 줄 수 없다 —
    `justify-content:space-between` 으로 갈매기를 오른쪽 끝에 붙였다.
  - 드로어·프로필 메뉴·AI 말풍선·Request 도착 버블도 같은 카드 문법으로.
  - **상세 팝업은 `#content-pop` 으로 좁혔다.** `.modal-card` 를 건드리면 관리자 모달
    (접근권한·스팟 편집)까지 따라 바뀐다 — 스킨의 범위는 폰 화면이다.
  - 검증: 로컬 프리뷰 실측 — 시트 전폭·상단 라운드·손잡이·갈매기 정렬·입력바, 드로어
    카드, **legacy 는 원래대로**(오른쪽 288.6px 카드 · 전체 라운드 19.5px), 콘솔 에러 0.
    `node tools/check.js` PASS.
  - **이로써 ①~④ 전 단계 적용 완료.** 남은 것은 마크업이 필요한 것들뿐이다 —
    피드 카드의 "사진 아래 흰 본문", 위치칩의 영문 캡션·온도 dot·chevron 같은 것.
    그건 렌더 함수를 고쳐야 해서 스킨이 아니라 각 모듈의 일이다.
- **v1.79.0 — 새 디자인 스킨 2·3단계: 지도 오버레이 + 탭 페이지 (M15)**:
  - **지도 오버레이는 `cqw` 를 안 쓴다.** 스팟 버블·핀은 폰 안에만 있는 게 아니라
    **PC 지도(admin.html `#map`)에도 그려지는데** 그쪽은 컨테이너 밖이라 cqw 가 다른
    기준으로 풀린다. 원래 코드도 이 요소들만 px 고 크기를 JS 가 줌에 따라 px 로 준다.
  - **스팟 버블: 색이 몸통에서 테두리로 옮겨간다.** legacy 트렌드는 버블이 온도색이고
    글자가 흰색이었다. 새 디자인은 몸통이 항상 흰 유리고 온도는 얇은 링으로만 말한다.
    `--spot-bg` 를 지우지 않고 **링 색으로 재사용**했다 — 안 그러면 스팟 개별 색 설정이
    새 스킨에서 아무 의미가 없어진다. 글자색은 JS 가 inline 으로 박아서 `!important`
    없이는 못 이긴다(그 한 곳만 썼다). 베이직은 원래도 흰 계열이라 거의 그대로다.
  - 피드 핀: 원 → 라운드 사각 카드(흰 테두리가 곧 카드 여백). 개수 배지는 우상단 회색
    원에서 **하단 중앙 온도색 칩**으로. Request 핀: 물방울 → 액센트 원 + 흰 3px 링
    (legacy 의 `border:2px` 를 지웠다 — box-shadow 링과 겹치면 5px 이 된다).
  - **존 헥사와 동/존 라벨은 손대지 않았다.** 헥사는 CSS 가 아니라 Maps 폴리곤이고,
    라벨 색은 `localLabelConfig`·`zoneLabelConfig` 가 inline 으로 칠한다.
    **설정을 이기는 스킨은 만들지 않는다** — 덮으면 그 설정들이 통째로 죽는다.
    같은 이유로 스팟 버블 둥글기도 설정값(inline)이 그대로 이긴다.
  - 탭 페이지: 지면 카드(라운드+드롭 그림자) · 피드 필터칩(선택=잉크 알약)·카드(테두리
    제거+큰 라운드)·오버레이 칩을 흰 유리로 · 소셜은 **회색 트랙 위 흰 알약 세그먼트**와
    방 카드. 피드 카드의 "사진 아래 흰 본문" 레이아웃은 **안 했다** — `renderFeed` 를
    고쳐야 하는 마크업 변경이라 스킨의 일이 아니다.
  - 검증: 로컬 프리뷰 실측 — 지도(베이직/트렌드) 버블·핀·Request 렌더, 트렌드에서
    버블 링이 스팟 색으로 들어감(계산값 확인), 피드/소셜 탭 렌더, **legacy 는 원래 값
    그대로**(버블 배경 `rgba(28,102,229,.92)`·핀 원형 50%·물방울 `50% 50% 50% 6px`),
    콘솔 에러 0. `node tools/check.js` PASS.
  - ⚠️ 남은 단계: **④ 오버레이** — AI 바텀시트·상세 팝업·Request 컴포저·드로어·프로필 메뉴.
- **v1.78.0 — 폰 셸 새 디자인 스킨, 1단계: 토큰·셸 (M15)**:
  - 출처: Claude Design 프로젝트 **"Now Here 디자인 리빌딩"**(`Now Here Mobile.dc.html`).
    **그대로 옮길 수 없는 물건이다** — `<x-dc>`·`{{ }}` 바인딩에 자체 런타임(`support.js`)이고
    스타일이 전부 스크립트가 계산한 인라인 문자열이다. 로컬에 띄워 전 화면을 보고
    **디자인 언어만 뽑아** 이 앱 마크업에 다시 입혔다.
  - 원본이 Google Maps 대신 그린 **벡터 지도는 따라가지 않는다** — 원본 문서에도
    "프로토타입용"이라 적혀 있다. 실제 앱은 Maps 를 쓰고, 그 위 오버레이만 새 디자인으로 간다.
  - **`style.css` 를 한 줄도 안 고쳤다.** 새 규칙은 전부 `skin-new.css` 의
    `body[data-skin="new"]` 스코프 안에 있다. legacy 로 두면 이 파일은 아무것도 하지 않는다 —
    **되돌리기가 속성 하나여야** 큰 디자인을 단계적으로 넣을 수 있다.
  - **Pretendard 는 family 이름을 갈랐다**(`PretendardNH`). 지금 앱은 이 폰트를 안 받고
    시스템 한글 폰트로 렌더된다 — 원본 이름(`Pretendard Variable`)으로 넣으면
    `style.css` 기본 스택 첫 자리가 그거라 **legacy 까지 같이 바뀐다.** 이름을 가르면
    브라우저가 새 스킨이 실제로 쓸 때만 2MB 를 받는다.
  - 치수는 `px` 가 아니라 `cqw`(폰 폭의 1%)로 옮겼다 — 폰 화면이 컨테이너라 px 로 박으면
    폭이 달라질 때 비율이 깨진다. 환산 기준 390px → 1cqw ≈ 3.9px.
  - **렌즈 전환이 변수 두 개다.** legacy 는 버튼마다 트렌드 색을 따로 칠했는데,
    `body.mode-trend` 에서 `--nk-blue`·`--nk-grad` 만 갈아끼우면 이 파일 전체가 따라온다.
  - **하단 네비의 탭별 색(지도=파랑·피드=초록·소셜=보라)을 없앴다** — 새 디자인에서 색이
    말하는 것은 탭이 아니라 렌즈(베이직/트렌드)다.
  - 설정: 콘솔 **🎨 스타일 › 🎛 디자인**. 드래프트(적용 버튼)를 안 태우고 **고르는 즉시** 바뀐다
    — 보면서 고르는 설정이라 미리보기와 적용을 나눌 이유가 없다. 저장은 `appSkin` 으로
    클라우드 동기. 서비스 페이지엔 컨트롤이 없다(v1.77 컨셉 — 콘솔은 별도 페이지).
  - `applySkin()` 을 정의 직후에 부른다 — 초기화까지 기다리면 legacy 가 한 번 번쩍인다.
  - `tools/check.js` 에 `skin-new.css` 캐시버스트 동기 검사 추가.
  - ⚠️ **1단계까지다.** 지면 카드·지도 오버레이(스팟 버블/핀/헥사)·피드 그리드·소셜·
    AI 바텀시트·상세 팝업·드로어는 **아직 legacy 모양**이다. 기본값이 New 라서
    **배포하면 중간 상태가 그대로 보인다** — 순서대로 2~4단계를 마저 넣거나,
    그때까지 콘솔에서 Legacy 로 두면 된다.
  - 검증: 로컬 프리뷰 실측 — 새 스킨(베이직/트렌드 양쪽) 헤더·위치칩·모드 토글·네비·AI
    버튼 전환 확인, **legacy 는 v1.77 그대로**, 콘솔 셀렉트 → `body[data-skin]` 즉시 반영,
    새로고침 후에도 유지(localStorage), 양 페이지 콘솔 에러 0. `node tools/check.js` PASS.
- **v1.77.0 — 폰 안에 콘솔이 없다 (M09+M11)**:
  - 컨셉: **서비스는 폰이 전부고, 콘솔은 메뉴에서 들어가는 별도 페이지다.** v1.65 가 페이지를
    나눌 때 데스크톱에서 PC 지도·좌측 패널을 숨기고 프로필 메뉴에 `admin.html` 링크를
    붙이는 데까지는 했는데, **한 군데가 어긋난 채였다** — 서비스 페이지의 폰 드로어에
    `🧭 둘러보기 / 🛠 관리자` 탭이 있고, 그 관리자 탭이 `content-section`·`settings-section`
    을 통째로 폰 안으로 끌어왔다. 콘솔이 두 군데 있었던 것이다.
  - **드로어를 둘러보기 전용으로 만들었다** — 탭 생성과 섹션 이동을 `initPhoneMenu` 에서 뺐다.
    설정 섹션은 옮기지 않아도 서비스 페이지에서 보이지 않는다: `#left-panel` 이 데스크톱
    (`body.page-app`)·모바일(≤768px) 양쪽에서 이미 `display:none` 이다. **DOM 에서 지우지는
    않았다** — 배선(`syncSettingsUI`·드래프트·클라우드 동기)이 그 노드들을 잡고 있어서,
    지우면 서비스 페이지에서만 조용히 끊긴다.
  - **🧩 기능 보기의 ⚙ 설정은 콘솔로 넘어간다** — `admin.html?adm=<패널>`. 그냥 열면 사용자가
    목록에서 다시 찾아야 하니 보려던 블록을 같이 넘긴다. 받는 쪽(`openAdmPanelFromUrl`)은
    **로그인이 끝난 뒤에** 연다 — 초기화 때 열면 인증 오버레이 위에 뜬다. URL 값은 셀렉터에
    끼워 넣지 않고 실제 `#adm-nav [data-panel]` 목록과 대조해서 통과시킨다.
  - `setDrawerView` 는 남겼다(M09 동결 앵커 — 콘솔 시나리오가 부를 수 있다). 이제 실질적으로
    `'demo'` 하나뿐이고, 지난 방문에서 `'admin'` 이 남은 localStorage 는 초기화 때 덮인다.
  - 죽은 CSS(드로어 탭·`dv-admin` 분기 12줄)를 지웠다.
  - ⚠️ **모바일 관리자 동선이 짧아졌다** — 폰 브라우저에서 드로어로 관리하던 길이 없어졌고
    `admin.html` 은 모바일 레이아웃이 없다(탑바+PC 지도 전면, `@media(max-width:480px)` 에
    높이 보정 한 줄뿐). 컨셉을 그대로 가는 쪽으로 정했고, **admin.html 모바일 레이아웃은
    별도 작업으로 남는다.**
  - 검증: 로컬 프리뷰 실측 — 서비스 페이지(임베드·일반 양쪽) `#drawer-tabs` 없음·드로어
    자식은 `drawer-demo` 하나·설정 섹션은 `#left-panel`(display:none)에 그대로·PC 지도 숨김,
    관리자 페이지 내비 13패널 정상·`?adm=s-zone` → 메뉴 열림+해당 패널 활성·**위조 값은 무시**,
    양 페이지 콘솔 에러 0. `node tools/check.js` PASS.
- **v1.76.0 — Ask Map 이 실제 모델에 붙는다 (M08)**:
  - 지금까지 Ask Map 의 답은 **전부 앱 안에 적혀 있었다** — `aiPresetPool()` 템플릿 ~50개와
    `aiChatAnswer()` 의 키워드 매칭. 무엇을 물어도 미리 쓴 문장 중 하나가 나왔고, 안 맞으면
    "실제 AI 연결은 준비 중입니다" 였다. 그 문장을 지웠다.
  - **모델은 persona-vc 콘솔에서 빌려 쓴다** — 새 라우트 `/api/app-agent`. 앱에 모델 키를
    두지 않는다(정적 사이트라 넣는 순간 공개된다). 콘솔은 이미 Vertex 인증·재시도·단가표를
    갖고 있으니 그 길을 그대로 탄다.
  - **콘솔의 평가 파이프라인과는 갈라 뒀다** — 프롬프트 파일도, 대화 기억도, 사용량 원장도
    공유하지 않는다. 섞이면 앱 사용자에게 페르소나의 사정이 새고, 평가는 자기가 만든 답을
    자기가 평가하게 된다. 자세한 건 MODULES.md 의 **M08 앱 에이전트 계약**.
  - **보내는 것은 화면에 보이는 숫자뿐**(`aiContextSnapshot`): 지역·렌즈·탭·시각·존 수·
    최고 인기 존·피드/스팟/Request 수. 좌표도 사용자 식별자도 글 내용도 보내지 않는다.
  - **기억은 이 탭 안에서만 산다**(`aiChatHistory`): 저장하지 않고 최근 6턴만 실어 보낸다.
  - **템플릿을 지우지 않았다.** 실패·12초 초과·오프라인·서버 스위치 off 면 그대로 되돌아간다
    (`aiChatAnswer(q,{offline:true})` — 미매칭 문구만 다르다). **롤백도 이 길이다**:
    `config.js` 의 `AI_AGENT.ENABLED=false` 하나면 v1.75 의 동작으로 완전히 돌아간다.
  - **임베드(`?embed=1`)는 원격을 부르지 않는다** — 시연은 매번 같은 답이어야 한다(M16).
  - 추천 질문 탭도 같은 길로 보냈다 — 눌러서 나오는 답이 직접 물었을 때와 다르면 안 된다.
    🗺 지도 요약만 로컬로 남겼다: 화면 실데이터를 조립하는 것이라 모델을 부를 이유가 없다.
  - 늦게 온 답이 새 질문의 답을 덮지 않게 순번(`aiAskSeq`)을 둔다. 패널을 닫아도 순번이
    올라가서 기다리던 답은 버려진다 — 닫은 뒤 말풍선이 튀어나오지 않는다.
  - ✅ **배포본 확인 완료**(2026-08-02, 콘솔 v0.33.0 먼저 → 앱 순서로 배포): 프리플라이트
    204(허용 오리진 에코) · 미허용/무 오리진 403 · 실모델 200(Vertex gemini-2.5-flash,
    1회 $0.0025 — 가게 이름을 지어내지 않고 피드 탭·스팟 메시지로 넘겼다) · IP 분당 6회 초과 429.
    **다만 앱 화면에서 눌러본 것은 아니다** — Ask Map 은 로그인 뒤에 있고, 실패해도 그럴듯한
    템플릿 답이 나와 **화면만 보면 성공과 구분되지 않는다.** 네트워크 탭을 봐야 한다.
  - ⚠️ 콘솔 라우트는 오리진 허용 목록으로 막혀 있다 — **배포 주소가 바뀌면 403 → 조용히
    템플릿**이 된다. 콘솔 쪽 `APP_AGENT_ORIGINS` 에 새 오리진을 더해야 한다.
- **v1.75.0 — 카메라 연출 (M16)**:
  - 콘솔 쪽 요구: 시연이 더 극적이어야 한다. 팝업이 열려도 **보는 사람이 어디를 봐야 할지
    모르는데**, 화면이 그걸 말해줘야 한다.
  - `zoom`(in/out) — 중심은 그대로 두고 줌만. 11~18 로 자른다: 더 멀면 동네가 안 보이고
    더 가까우면 핀만 남는다.
  - `focus`(spot|feed|req, i) — i번째 콘텐츠로 카메라를 옮겨 확대. **팝업은 열지 않는다** —
    여는 것은 `pop` 의 일이고, 둘을 합치면 시나리오가 따로 쓸 수 없다.
  - 자체 지도 조작을 만들지 않고 `goMapCam`(M09 동결 앵커)만 부른다. **양쪽 지도를 같이
    움직인다** — 카메라는 PC → 폰 단방향 미러라 폰만 움직이면 다음 idle 이 되돌린다(area 와 동일).
  - 액션 어휘는 앱 `NH_ACTIONS` · 콘솔 `PLAY_ACTIONS` · 프롬프트 `ACTION_GUIDE`
    **세 곳이 같이** 움직였다. 프롬프트에는 "매 스텝마다 쓰지 말라" 를 같이 적었다 —
    계속 움직이면 어지럽고 강조가 사라진다.

### 2026-08-01
- **v1.74.0 nh:ready 가 표본 시나리오의 스텝 정의도 준다 (M16)**: 콘솔이 앱 표본의 타임라인을 재생 전에 다 보여주고(그전에는 번호만 있는 자리), **단계를 눌러 그 지점부터 다시 보기**를 하려면 콘솔이 정의를 알아야 한다 — inline run 을 만들려면 스텝이 필요하기 때문이다. `nh:ready` 의 각 시나리오에 `plan`(a·v·i·say·concern·key·ms)과 `seed` 를 추가했다. `steps` 는 **개수 그대로** 둬서 옛 콘솔과 호환된다. 검증: `node tools/check.js` PASS, 임베드에서 `nh:list` → `plan` 4종 수신 확인
- **v1.73.0 임베드는 GPS 를 쓰지 않는다 (M16)**: "모든 데모 시나리오가 현재 내 GPS 위치 기반" 이라는 지적. `initMyLocation` 이 임베드에서도 `getCurrentPosition` 을 불러 **지도를 사용자의 실제 위치로** 옮기고 있었다 — 거기엔 시드가 없으니 화면이 비고, 헤더에 실제 동 이름이 뜨고, 시연이 사람·장소마다 달라졌다(D25 "매번 같은 결과" 와 정면 충돌). 임베드면 위치를 **묻지 않고** 기본 무대 `NH_HOME_AREA`(강남)에 선다(`nhGoHome` — PC 지도가 display:none 이라 `goMapCam` 의 투영 폴백을 쓴다). `nhReset` 도 빈 값 대신 기본 무대로 되돌려 회차마다 같은 곳에서 시작한다. 내 위치 점도 찍지 않는다. 검증: 임베드 부팅 후 `myLocation=null`·내 위치 마커 0·중심=강남 앵커(37.505,127.031)·헤더 "역삼1동"(전에는 "위치 확인 중"), 방학동 시나리오 재생 → 중심 이동 확인 → `nhReset` → 강남 복귀. `node tools/check.js` PASS
- **v1.72.0 시나리오마다 다른 콘텐츠 — 무대를 시나리오가 깐다 (M16 + M13 ⚠️교차)**: 재생해도 매번 같은 스팟·같은 피드가 열리던 것을 고쳤다. 원인은 `nhPick` 이 **전역 시드에서 앞에서부터** 골랐던 것 — 지역만 같으면 시나리오가 달라도 같은 콘텐츠가 열려서, 화면상으로는 네 시나리오가 다 같은 이야기였다. ①**`nhOwn` 우선** — 그 시나리오가 깐 것(seed)과 재생 중 쓴 글이 있으면 **그 안에서만** 고른다. 아무것도 안 깔았을 때만 전역 시드(지역 반경)로 간다 ②**`seed.feeds` 신설** — 피드도 시나리오가 깐다(그전에는 `like`·`scroll`·`scope` 가 늘 같은 전역 카드를 건드렸다). 사진은 `seedImg`(테마 색+라벨)로 그려 외부 이미지에 기대지 않는다 ③**음수 인덱스** — `i:-1` = 방금 만든 것. privacy-worry 의 마지막 `pop i:0` 은 **남의 글**을 열면서 "방금 쓴 글에 동 이름이 붙었다" 라고 말하던 조용한 거짓말이었다 → `i:-1` 로 고침 ④**결정적 배치**(`nhSpread` 황금각) — `Math.random` 제거. 시연은 몇 번을 돌려도 같은 자리여야 한다 ⑤표본 4종에 각자 무대 부여(성수=처음 온 사람용 안내·잠실=20분 전 현장 피드·강남=동 이름 찍힌 남의 글 2건). 방학·쌍문은 **일부러 그대로** — 비어 있어야 그 시나리오가 성립한다. 검증: `node tools/check.js` PASS, 임베드에서 시나리오 4종 전환 시 서로 다른 콘텐츠 확인. ⚠️콘솔(persona-vc)도 `seed.feeds` 를 같이 올려야 한다 — 계약이 세 곳(앱 `nhSanitize`·콘솔 타입/파서·프롬프트)에 걸쳐 있다
- **v1.71.0 — 시나리오가 보기만 하지 않고 실제로 쓴다 (M16 + M13, M04/M05/M06/M07/M10/M12 ⚠️교차)**:
  - **문제**: 시나리오 4개가 전부 `area → tab:map → pop → popclose → tab:feed` 였다.
    아무것도 쓰지 않고 누르지 않아서 **사용이 아니라 관람**이었고, 그래서 다 비슷해 보였다.
  - **액션 7종 추가**: `like`(좋아요) · `write`(글 작성) · `answer`(답변 도착) ·
    `chat`(채팅방) · `ai`(AI 질문) · `scope`(피드 범위) · `scroll`. 전부 기존 앵커를 부른다
    (`toggleLike` `addSpotContent`+`SpotComposer.commit` `answerRequest` `renderSocial`
    `.pn-ai` 클릭 `.fsc` 칩 클릭).
  - **시나리오별 `seed` 블록**: 재생 직전에 깔고 리셋 때 걷는다. `answerIn` 을 주면
    **재생 도중에 현장 답변이 도착**한다 — 전역 시드로는 못 만드는 장면이다
    (Request 는 10분 타임아웃이라 미리 깔아둘 수 없다).
  - **시나리오 4종을 서로 다르게 재작성**: 처음온동네=존·AI·좋아요 / 물어보기=답변 도착 /
    프라이버시=**직접 써 본 뒤** 내 글에 동 이름이 찍힌 걸 발견 / 빈동네=빈 채팅방까지.
  - **⚠️ 임베드 격리 (`nhEmbedIsolate`)**. 쓰기를 넣자 두 구멍이 드러났다:
    (a) 쓰기 경로가 localStorage 에 저장해 **회차마다 콘텐츠가 쌓인다**,
    (b) 임베드와 실제 앱이 **같은 오리진**이라 실제 앱을 써 본 사람의 localStorage 가
    임베드로 새어 들어온다. 저장 함수가 다섯 군데라 각각 막는 대신 `nowhere_*` 키의
    저장 자체를 무음으로 만들고, 부팅 때 담겨 있던 콘텐츠를 비운 뒤 시드를 깐다.
  - **회차 원상복구 (`nhTempIds` → `nhSweepTemp`)**: seed 와 재생 중 쓴 글을 전부 걷어낸다.
    안 걷으면 두 번째 재생부터 "내가 쓴 글" 이 이미 있어서 시연이 매번 달라진다.
  - **글쓰기는 지도 중심에 기대지 않는다**: `getCenter()` 는 투영 전·지도 오류 시 없고,
    그러면 `addSpotContent` 가 조용히 아무것도 안 한다("썼는데 아무 일도 없음").
    시나리오가 서 있는 지역 좌표를 직접 준다 — 회차마다 같은 자리에 남는 이점도 있다.
  - `currentRole='user'` 를 임베드 부팅에서 세운다(⚠️교차 M12) — 글쓰기 앵커가 역할을 본다.
    로그인은 여전히 없다.
  - **검증**: `node tools/check.js` 통과. 임베드에서 액션 15종 등록, `localStorage` 쓰기
    무음 확인, seed 주입(Request 1·스팟 1) → **1.2초 뒤 답변 도착(answers 0→1)** →
    `chat` 소셜 탭 전환 → `scope` 피드 전환 → `nhSweepTemp` 로 **전부 0 회수**.
    `like` 0→1 동작.
  - ⚠️ **로컬에서 지도를 못 띄웠다** — 이 PC 의 8765 를 다른 세션이 잡고 있어 8767 로
    띄웠는데 Maps 키 referrer 허용목록에 없다(`RefererNotAllowedMapError`, 지도가 아예
    초기화되지 않음). 그래서 **지도가 필요한 `write`·`area` 는 배포본에서 처음 확인한다.**
- **v1.70.0 — 시나리오가 지역을 옮겨 다닌다 + 시드 4지역 확장 + 임베드 여백 제거 (M16 + M13, M15 ⚠️교차)**:
  - **`area` 액션 신설.** `{a:'area',v:'gangnam'|'jamsil'|'seongsu'|'dobong'}` — 시나리오가
    "어디서 벌어지는 일인지" 를 갖게 됐다. 자체 지도 조작을 만들지 않고 **동결 앵커
    `cpopGoMap`** 을 부른다: 그쪽이 팝업·서랍 닫기 → 지도 탭 → 양쪽 지도 이동까지 이미 한다.
  - **⚠️교차 M09 — `cpopGoMap` 두 가지를 고쳤다** (시그니처는 그대로, 인자만 optional 추가):
    - **`panTo` 는 투영(projection)이 없으면 조용히 무시된다.** 임베드의 PC 지도는
      `display:none` 이라 `getBounds()` 가 없고, 카메라가 **PC → 폰 단방향 미러**라
      (`map.center_changed` → `phoneMap.setCenter`) 폰 지도까지 같이 멈춘다. 처음엔 로그만
      정상이고 화면은 명동에 그대로 있었다. `goMapCam()` 을 두어 투영이 없으면 `setCenter`
      로 간다. 보이는 지도에서는 종전과 동일하다.
    - **줌 인자(optional)**. 기본은 종전대로(줌<15 일 때만 16)이고, `area` 는 14 를 준다 —
      16 은 "이 항목 하나" 를 붙여 보는 값이라 동네가 비었는지 차 있는지가 안 보인다.
    - 중간에 `selectPhoneDong`(동 전체 fitBounds)으로 가려다 되돌렸다. **폰 지도만 움직이면
      map 의 다음 idle 이 미러로 되돌려 놓는다** — 움직여야 하는 건 항상 `map` 쪽이다.
  - **지역이 정해지면 `pop` 도 그 지역에서 고른다** (`nhPick` 반경 4km 필터). 이게 없으면
    방학동으로 옮겨 놓고 강남 스팟을 여는 일이 생긴다 — 지도와 팝업이 서로 다른 동네를
    가리키면 시연이 조용히 거짓말을 한다. 그 지역에 아무것도 없으면 아무것도 열지 않는다.
    `nhReset` 이 지역을 초기화한다 (앞 회차가 방학동에 서 있으면 다음 회차 pop 이 빈손이 된다).
  - **시드 4지역.** 강남·잠실·성수를 보강하고(피드 +15 · 스팟 +9 · Request +3 · 채팅 +3 ·
    지면 +2) **방학·쌍문을 신설**하되 **일부러 희박하게** 뒀다 — 피드 2 · 스팟 1 · Request 0.
    `empty-neighborhood` 우려 시나리오가 이제 **실제로 빈 화면을 보여준다**. 전에는 대사만
    "비어 있다" 였고 화면은 강남 그대로였다.
  - **⚠️ 시드 문서 인덱스가 밀렸다.** `gi` 는 `SEED_AREA_ORDER` 순회의 누적 인덱스라
    앞 지역에 항목을 더하면 뒤 지역 문서 id 가 전부 밀린다. 임베드(M16)는 Firestore 를
    안 쓰므로 시연에는 영향이 없지만, **클라우드 시드는 🧹 비우기 → 다시 채우기가 필요하다.**
    채우기 확인창에도 같은 경고를 넣었다. 새 지역은 `SEED_AREA_ORDER` **뒤에만** 붙일 것.
  - **임베드 여백 제거.** v1.69 는 앱이 폰 폭을 높이에서 계산했다 —
    `min(430px,(100dvh-28px)*9/19.5,100vw-24px)`. 콘솔 iframe 이 420px 일 때 폰은 301px 이
    되어 **119px 이 무대 배경으로 남았다.** v1.70 부터 **비율은 콘솔이 책임지고**(iframe 을
    `aspect-ratio:9/19.5` 로 준다) 앱은 받은 프레임을 꽉 채운다. 양쪽이 다 계산하면 반드시
    어긋나므로 계산하는 자리를 하나로 모은 것이다. 라운드·그림자는 콘솔 래퍼가 들고 있어
    앱에서 뺐다 (둘 다 걸면 모서리에 이중 테두리가 보인다). `:root` 토큰은 안 건드렸다.
  - `nh:ready` 에 `areas[]` 추가 — 갈 수 있는 지역 목록도 앱이 알려준다(콘솔에 복사 금지).
    `nhSanitize` 는 모르는 지역 키가 온 `area` 스텝을 버린다.
  - MODULES 레지스트리의 M16 이 v1.67 에서 멈춰 있던 것(v1.68·v1.69 미반영)도 같이 바로잡음.
  - **검증**: `node tools/check.js` 통과. 임베드 320×693 에서 폰이 프레임을 정확히 채움
    (여백 0), 시드 피드 56 · 스팟 39 · Request 7, 지역별 `nhPick` 이 강남=역삼1동 스팟 ·
    잠실=잠실2동 스팟 · **방학=스팟 1건·Request 0건**, `empty-neighborhood` 재생 시
    동 이동이 명동 → 역삼1동 → 방학2동 으로 실제로 일어남(줌 14 고정), `nhSanitize` 가
    모르는 지역 키(`atlantis`)를 버림.
  - ⚠️ **로컬에서 지도 타일을 못 봤다** — 이 PC 의 8765 를 다른 세션이 잡고 있어 8766 으로
    띄웠는데, 그 포트가 Maps 키의 referrer 허용목록에 없다(`RefererNotAllowedMapError`).
    카메라·좌표·선택 로직은 전부 코드로 확인했지만 **새 지역의 타일 렌더는 배포본에서
    처음 눈으로 보게 된다.**
- **v1.69.0 — 임베드는 폭과 무관하게 항상 폰 UI (M16 + M15 ⚠️교차)**:
  - 콘솔의 iframe 폭은 레이아웃에 따라 390px 이 되기도 700px 이 되기도 한다.
    `page-app` 의 무대 연출(폰을 가운데 띄우기)은 **`@media (min-width:769px)` 안에만**
    있어서, 좁은 프레임에서는 폰이 아니라 모바일 웹처럼 전체를 채워 버렸다.
  - `body.embed-mode` 에 같은 규칙을 미디어쿼리 밖에서 다시 건다. 값은 page-app 과
    동일하고 `.phone-screen` 에 **가로 상한(`100vw - 24px`)만 추가**했다 — 좁은 프레임에서
    폰이 잘리지 않게. `:root` 토큰은 건드리지 않았다 (M15 규칙: 값 변경 금지·추가만).
  - 임베드는 "폰 화면을 보여주는 자리" 라는 것이 이제 CSS 로 고정된다.

- **v1.68.0 — 콘솔이 보내온 시나리오 재생 (M16)**:
  - **왜**: 콘솔이 **서베이 응답에서 시나리오를 뽑기** 시작했다. 그 시나리오는 여기
    `NH_SCENARIOS` 상수에 없다 — 응답이 바뀌면 시나리오도 바뀌므로 앱에 박아둘 수 없다.
    그래서 `nh:run` 이 `scenario` 정의를 통째로 받는다.
  - **`nhSanitize()` 를 반드시 통과한다**: 액션은 `NH_ACTIONS` 화이트리스트뿐(모르는
    액션은 조용히 버린다), 대사 300자·간격 50~6000ms 로 자른다(스텝 수 상한은 v2.31 에서 삭제). 밖에서 온
    정의로 **임의 코드가 돌지 않는다** — 실행되는 것은 여전히 우리 앵커 호출뿐이다.
    간격 하한을 두는 이유는 0ms 스텝이 시연을 눈에 안 보이게 지나가기 때문이고,
    상한을 두는 이유는 잘못된 값 하나가 시연을 멈춘 것처럼 보이게 하기 때문이다.
  - `nh:ready` 에 `actions` 를 실어 **앱이 아는 액션 목록을 콘솔에 알려준다** — 콘솔이
    액션 이름을 따로 적어두면 앱이 바뀔 때 조용히 어긋난다.
  - 검증: 인라인 시나리오 재생 시 모르는 액션(`evil`) 1건이 버려져 5→4스텝, `ms:999999`
    가 상한에 걸려 11초 안에 완주, concern 플래그 유지, `nh:done` 도달. check.js 통과.

- **v1.67.0 — 임베드 모드 + 시나리오 브리지 (M16 신설, M13 ⚠️교차)**:
  - **왜**: Persona VC(콘솔)의 컨셉이 바뀌었다. 페르소나가 앱을 상상해서 쓰는 게 아니라
    **실제 Now Here 를 콘솔 안에 띄워 놓고**, 서베이에서 페르소나가 말한 시나리오 —
    특히 **우려하는 상황** — 을 그 위에서 재생해 보여준다. 그래서 Now Here 쪽에
    ① 로그인 없이 뜨는 임베드 모드와 ② 밖에서 조작을 받는 통로가 필요해졌다.
  - **`?embed=1` 임베드 모드** (`IS_EMBED`·`startEmbed`): `initAuth()` 대신 타는 경로.
    Firebase 를 아예 붙이지 않고 `hideAuthOverlay()` → `bootMap()` → 경계 로드 완료
    (`mapReady`) 후 시드를 무음으로 깐다. **실데이터를 쓰지 않는 게 의도다** — 시연은
    매번 같은 화면이어야 하는데 실데이터는 그날 비어 있을 수도 달라질 수도 있고,
    로그인·allowlist·규칙 재게시가 전부 시연 중 실패 지점이 된다. `firestore.rules` 는
    건드리지 않았다 (익명 쓰기 구멍을 만들지 않기 위해).
  - **`seedDemoData(opts)`** — optional `silent` 추가(시그니처 동결 규칙 준수).
    `IS_EMBED` 일 때만 유효하다: 임베드가 아니면 시드가 공유 컬렉션에 쓰이므로
    관리자 확인창을 그대로 거친다. `markCloudDirty()` 는 fbDb 가 없어 자연히 no-op.
  - **시나리오 브리지** (`initScenarioBridge`·`nhRun`·`nhAct`): postMessage 로
    `nh:list`/`nh:run`/`nh:stop` 을 받고 `nh:ready`/`nh:begin`/`nh:step`/`nh:done` 을 돌려준다.
    `EMBED_ORIGINS` 에 있는 오리진의 메시지만 받는다. 계약은 MODULES.md "M16 임베드 계약".
  - **앱 조작은 전부 기존 동결 앵커**(`switchTab`·`switchMode`·`openContentPop`·
    `openRequestComposer`·`openPhoneDrawer`)로만 한다. M16 은 순서와 페르소나 대사만 들고
    있어서, 화면 로직이 두 벌이 되지 않는다.
  - **`nhReset()` 를 재생 앞에 둔다** — 앞 회차가 열어둔 팝업·드로어가 남으면 다음 시연이
    그 뒤에서 조용히 흘러간다 (실제로 겪고 넣었다). 재생 중 다른 시나리오를 걸면
    토큰으로 앞 회차를 무효화해 스텝이 섞이지 않는다.
  - **시나리오 4종**: `first-visit`(처음 온 동네 둘러보기) · `field-request`(지금 거기 어떤지
    물어보기) · **`privacy-worry`(내 위치가 얼마나 드러나나)** · **`empty-neighborhood`
    (우리 동네엔 아무것도 없다)**. 뒤 둘이 우려 시나리오이고, 스텝에 `concern` 플래그가
    붙어 콘솔에서 따로 표시된다.
  - **검증**(로컬 :8765 · 1280px): 로그인 오버레이 없이 부팅, 시드 30스팟·40피드·4Request,
    `privacy-worry` 재생 시 스팟 팝업에 **동 이름이 찍히는 것**을 그대로 보여줌(그게 그 페르소나의
    우려 지점), `first-visit` 완주 후 mode=trend·tab=feed, 재생 중 다른 시나리오로 전환 시
    스텝 섞임 없음·상태 초기화 확인, 콘솔 에러 0. `node tools/check.js` 통과.
  - **남은 것**: 지도 타일은 로컬(:8765)에서 API 키 리퍼러 제한에 막혀 회색으로 뜬다 —
    실제 Pages 도메인에서는 정상. 콘솔(Persona VC) 쪽 재생 패널은 그쪽 저장소 작업.

### 2026-07-07
- **v1.55.1 — 스플래시 앱 아이콘 (M12)**: .auth-mark div(📍 그라디언트 배경)→img(icon-192.jpg), CSS 68px·border-radius 15px·object-fit:cover·옅은 보더+브랜드 섀도. dev/diagram data-app-ver 스탬프 동반 갱신.
- **v1.55.0 — 모듈 인프라 (구조 검토 결과 반영)**:
  - **구조 검토 결론**: 전 코드=전역 함수+호이스팅이라 모듈 간 로드 순서 리스크 없음. 실위험 3가지를 장치로 상쇄 — (a)섹션 경계 부재→[M##] 태깅, (b)공유 전역/헬퍼 오염→MODULES.md 소유권 계약+M00 동결, (c)버전·문서 동기화 누락→check.js CI 강제.
  - app.js 섹션 주석 59곳 [M##] 태깅(주석만, 동작 무변). `tools/check.js` 신설 + pages.yml에 'Version sync check' 스텝(Checkout 직후). dev/diagram/deck에 `data-app-ver` + 헤더 반영 버전 표기(dev=JS로 body dataset 표시). dev.html 워크플로우 규칙 6조로 확장+프롬프트 템플릿에 grep 탐색·check.js·페이지 갱신 반영. diagram: AI 블럭 'Ask Map·요약·프리셋·채팅'(core), Request '질문(10분)→AI 팝업 응답', 헤더에 dev 링크.
- **v1.54.0 — Request 핀·드로어 정돈 + 존 리스트 정렬 + Ask Map (M07+M03+M08+M09 ⚠️교차)**:
  - **M07**: `ReqPin` OverlayView(initReqPinClass, initMap에서 초기화) — .req-pin(.rp-drop ? 티어드롭 rotate -45°, .rp-ring×2 rpPulse 2s 교차) 보라(#8ed0ff→#7b61ff)=AI Agent 채널 계열. renderRequestMarkers가 MapLabel→ReqPin. `deleteRequest(id)`(live=doc.delete/로컬=filter, confirm) + 카드 우상단 .rqc-del(본인∥관리자). rqc-place 📍 제거.
  - **M09**: dsSection(key,title,sub) 3-인자 — 타이틀 이모지 제거('트렌드 존'/'현장 Request'/'스팟 메시지'), .ds-tl 래퍼+.ds-sub(스팟=포커스 지역명). .ds-tt .92rem·900·-0.02em.
  - **M03**: `focusedZoneId`(선택>렌즈>센터 존)+`sortedZonesForList`(포커스→zoneTotalHearts→거리²). buildZoneScroll 정렬 적용+makeZoneCard(zone,focused) — .tz-card.focus(2.5px 액센트 링)+.tzf-check(✓ 원형 뱃지). phoneMap idle의 renderSummaryZones 갱신을 list 한정→트렌드 지도탭 상시로, selectPhoneZone 끝에 renderSummaryZones().
  - **M08**: 패널 재구성 — .aip-head 'Ask Map'(브랜드 그라디언트 텍스트, 트렌드=화염), `#aip-summary`(.aip-special 풀폭 그라디언트)→`aiMapSummary()`(지역·렌즈·존 수·최고 인기 존 ❤·피드·스팟·활성 Request 브리핑, 9s), 템플릿 풀 40 공통+상황별(트렌드 6/베이직 3/탭·Request 1~2)≈**45~50개**, `aiRandomPresets(5)`. 하단 .aip-chat(input+➤)=`aiChatAnswer`(공백 분리 키워드 스코어 매칭). 입력/버튼 stopPropagation으로 패널 유지.
  - **요청자 팝업 제외**: openRequestComposer의 `setTimeout(showReqBubble, 2600)` 시뮬레이션 제거, 로컬 폴백 rq에도 `by:myUid()` 저장(내 Request 판정용). 전송 확인 버블에 '10분간 답변 수신' 명시.
  - **10분 타임아웃**: `REQ_TTL_MS=600000`, `reqActive(rq)`(seed=상시 활성), `isMyReq(rq)`. 적용: 지도 마커 필터·수신 팝업 게이트(3분→TTL, 시드 제외)·answerRequest 차단 가드·드로어 타인 카드 필터. 30s setInterval로 renderRequestMarkers(→드로어 포함) 재렌더.
  - **내 Request 답변 보기**: 드로어 카드 mine 분기 — .rqc-badges(🙋 내 Request 그라디언트 필+⏳/⏱ 상태), [💬 답변 N개 보기] 토글 → .rqc-answers 목록(.rqa-item: 텍스트/사진/timeAgo). 타인 카드는 기존 답하기/사진 유지. 리스너 매핑에 seed 필드 추가.
- **v1.53.0 — 현장 Request 실시간 팝업 개편 + 개발 모듈 체계 (M07+M08+M14)**:
  - **상태 노출 제거**: 드로어 rq-card의 rqc-a(⏳ 응답 대기 중/💬 답변)·rqc-thumb 삭제, renderRequestMarkers 라벨 항상 '❓ Request'. 응답 버튼(💬답하기/📷사진)은 카드에 유지.
  - **실시간 수신 팝업**: liveOn의 liveRequests 리스너에 `docChanges()` 처리 추가 — `reqsPrimed`(첫 스냅샷=기존 데이터, 알림 없음)+`reqAnsSeen`(id→답변수). added: by≠나·ts 3분 내·`reqNearMe`(가시 센터 기준 haversine 1500m ∥ dongAt 동일) → `showReqBubble`. modified: by=나·답변수 증가 → 요청자 ai-bubble '현장 답변 도착'(6s). 재구독 시 상태 리셋. 요청 직후 2.6s 자기 시뮬레이션 팝업은 데모용으로 유지.
  - **팝업 리디자인**(#req-bubble): AI Agent 칩(✦ 그라디언트 원, 트렌드=화염)+질문+📍위치+버튼 2개 — 컨테이너·버튼 모두 네비바 문법(--frost/--glass-blur/--glass-line, primary=pn-item.active 파랑 그라디언트). 퀵응답 3종·직접답장 버튼 폐지(.rq-quick→.rq-btn). answerRequest는 라이브 시 '답변 전송!' 확인으로 변경(도착 알림은 요청자 기기).
  - **모듈 체계(M14)**: `MODULES.md` 신설 — M01~M15 레지스트리(범위·grep 앵커·의존·상태·최근 버전)+세션 규칙 6조(한 세션 한 모듈, 교차 표기, 물리 분할 보류). `dev.html` 신설 — 모듈 카드 그리드+[📋 세션 프롬프트 복사](repo·모듈 범위·앵커·규칙 포함 템플릿, clipboard API+prompt 폴백). 기능 보기 ft-docs 3버튼(덱/다이어그램/🛠개발 관리, CSS :last-child→:not(:first-child)). FEATURES req 설명 현행화.

### 2026-07-06
- **v1.52.0 — UX 7종(스플래시·AI 프리셋·불꽃·스와이프·드로어 탭·Request 응답·피드 칩)**:
  - **피드 칩**: renderFeed의 존 칩 생성을 `feedScope==='zone'` 가드로 한정 — 전체보기/현재 동네에선 우하단 fc-region(동)만.
  - **스플래시**: #auth-overlay 다크→서비스 라이트 프로스트(브랜드 radial 배경+유리 카드+📍 auth-mark), `.auth-ver`에 부팅 시 #app-version 복사(버전 소스 단일 유지).
  - **AI Agent**: `initAiAgent`+`aiPresetPool`(공통 10 + 트렌드=존 이름 질문/베이직=동 질문 + 탭·Request·시간대 분기)→`aiRandomPresets(10)` 셔플, #ai-presets 프로스트 패널, 프리셋 탭=답변 말풍선 7s. `updateAiVisual`이 setAiActive 대체(AI_PALETTE 모드별 그라디언트).
  - **트렌드 불꽃**: switchMode에서 body.mode-trend 토글+재도색. CSS .pn-ai.ai-flame(aiFlicker drop-shadow 1.8s)+.ai-on 화염 배경, 프리셋 패널 헤드/호버도 주황.
  - **네비 스와이프**: .phone-navbar touchstart/touchend(수평 40px+·수직우세 억제) → ORDER[map,feed,social] 이웃 탭 전환.
  - **드로어 탭**: initPhoneMenu가 #drawer-tabs를 바디 최상단 삽입, `setDrawerView`(dv-admin 클래스+nowhere_drawerview). CSS: 기존 `role-admin #drawer-demo 숨김` 전역 규칙 폐지→탭 뷰 기준, 데스크톱(≥769px) 폰 미러는 탭 숨김+데모 고정(!important)으로 기존 미리보기 유지.
  - **Request 응답**: `answerRequest(id,text,img)` 확장(Firestore arrayUnion에 img 포함, undefined 방지 조건 부착)+`answerRequestPhoto`/`initRequestAnswer`(#rq-photo-input, compressNews). 드로어 카드 button→div(중첩 버튼 방지), 💬답하기/📷사진 rqc-btn+첫 답변 사진 썸네일(rqc-thumb), 수신 버블(showReqBubble)에 📷 사진 올리기 추가.
  - 검증(프리뷰, 헤드리스): 스플래시 스크린샷+버전 v1.52.0 / 프리셋 10개·답변 말풍선·트렌드 팔레트(idle #ffb37a→on #ffd24a)·존 질문 포함 / 모바일 뷰포트: 관리자 탭 flex·뷰 전환(demo↔admin 상호배타)·데스크톱 미러 데모 고정 / 스와이프 map→feed→map / Request 버블 버튼 5종·드로어 rqc-btn 2·사진 썸네일 / 존 태그 피드가 all 스코프에서 존 칩 0·동 표시 — 전부 PASS, 콘솔 에러 0. **실기기 터치 스와이프·사진 답변 업로드는 라이브 확인 권장.**
  - **시드 분산**: 먹자골목·카페로드·학동공원에 뭉쳐 있던 좌표를 역삼1·역삼2·논현1·논현2 일대로 재배치 — 피드끼리 최소 85m, 피드+스팟 전체 최소 69m. 동 라벨은 dong_boundary.geojson의 `dongAt`으로 전수 검증(플리마켓·파이브가이즈 Request 등 라벨↔실좌표 불일치 4건 보정).
  - **존 시드 제거**: `SEED_ZONES`·`seedHexCluster` 삭제, SEED_FEED의 zone 태그 전부 null — 시드는 피드 20·스팟 16·Request 2·채팅만. `clearDemoData`의 tzs_* 존 삭제는 유지(이미 라이브에 시딩된 존을 지우는 용도). ⚠️**라이브 반영 = 관리자 접속 → 데모 데이터 → 🧹 비우기(기존 존·구 좌표 시드 제거) → 🌱 채우기**.
  - **피드 썸네일 핀 클러스터**: `clusterFeedPins(m)` — 현재 줌의 월드픽셀 좌표로 56px 근접 그룹핑(팬과 무관, 줌에만 의존). 클러스터=대표 사진(.fp-im 스택 섀도)+개수 뱃지(.fp-n), 탭=`_expand()`(멤버 fitBounds+타이트하면 강제 줌인, 폰은 phoneFitPadding). 줌 변경 시 양 지도 idle에서 `reclusterFeedMarkers`(줌 동일하면 스킵). 시드 기준 z13≈4그룹→z15 낱개 18→z17 전부 낱개로 자연스럽게 펼쳐짐. 핀 DOM 구조 변경: 라운드/보더가 .feed-pin→**.fp-im 내부 래퍼**로(뱃지가 원 밖으로 나오게).
  - **콘텐츠 이동=롱프레스 후 드래그**: 스팟 버블·피드 핀의 `_onDown`을 터치=롱프레스 `LP_MS`(450ms)+허용오차 8px 후 드래그 시작(진동 15ms, 그전 움직임=지도 팬으로 양보, 리스너는 document에 부착해 팬 중 이탈 추적), 마우스=기존 즉시 드래그 유지. 짧은 탭=스팟 편집 모달(롱프레스 후 제자리 해제는 무동작). `attachAddGestures`에 `.spot-marker,.feed-pin` 가드 — 콘텐츠 위 롱프레스가 '컨텐츠 추가 메뉴'(520ms)와 충돌하지 않게. 터치 탭이 지도 click(강조 해제)으로 새지 않게 편집 가능 요소는 click stopPropagation. CSS: .spot-marker/.feed-pin에 touch-action:none+user-select:none(pointercancel·iOS 콜아웃 방지).
  - **피드탭 view 옵션**: 상단 1·2·3 버튼(.fcq) 제거 → **view 아이콘 버튼 1개**(#feed-view-btn, 4분할 그리드 SVG) → 팝오버(#feed-view-pop): 가로 배열 1~3(.fvc) + **컨텐츠 종류 필터**(.fvt: ✍️피드 작성/📸라이브 카메라/💬스팟 메시지/📰소식 지면, `feedTypes`+nowhere_feedtypes, 최소 1종 강제, `feedTypeOf`로 feedEntriesScoped에서 필터). 바깥 클릭 닫힘. 범위 필(전체보기/현재 동네/Trend Zone)은 기존 유지.
  - 검증(프리뷰, 지도 미기동 헤드리스): 동 매칭 20/20·간격(85/69m)·클러스터 수학(z13~z18)·view 팝오버(5→4→3장 필터·복원·열수 3 적용·localStorage·바깥닫힘)·클러스터 핀 CSS 렌더 스크린샷 전부 PASS, 콘솔 에러 0. **터치 롱프레스 드래그·라이브 지도 클러스터 렌더는 실기기/라이브 관측 필요.**
- **v1.50.0 — 시드 실사진·확장 + 이메일 기반 소유권(byEmail)**:
  - **실사진**: `SEED_IMG` 24장 — Wikimedia Commons 직링크(핫링크 허용, 프리뷰 브라우저에서 전장 로드 검증. Google 검색 이미지는 핫링크 차단·URL 만료가 잦아 Commons로 소싱). 존 3·지면 3·피드 19장 실사진, 벽화 1장만 생성 유지. 부적절 후보(추모 사진 등) 수동 배제.
  - **확장**: 피드 20(존 태깅 9, cam 11/post 9, 좋아요 4~18 분산 총 ~200, 1~50h ts)·스팟 16(존 안 배치 강화)·닉네임 다양화.
  - **소유권**: 시드 doc에 `byEmail:'shoomerion@gmail.com'`(SEED_OWNER). 일반 업로드도 `byEmail:myEmail()` 저장(feedAdd/liveSpotDoc/컴포저/로컬 저장·복원/스냅샷 왕복). 판정 통일 `ownsContent(o)`=uid 일치 ∥ 이메일 일치 — canEditSpot/FeedThumb._canEdit/피드 카드 mine이 공용. 시드 by='seed_u*'라 uid 매칭은 없고 이메일로만 소유 → **shoomerion@gmail.com 로그인 시 모든 시드 콘텐츠 수정·드래그·삭제 가능**(타 계정 불가 검증).
  - 검증(프리뷰): 채우기→피드20(wiki19/gen1)·스팟16·존/지면 wiki·좋아요200 / shoomerion 시뮬=feed·spot 소유 true, 타계정 false / 실사진 피드·핀·지면 렌더 스크린샷, 콘솔 에러 0.
- **v1.49.0 — 데모 시드 데이터 (강남·역삼·논현, 실사용 느낌)**:
  - **UI**: 컨텐츠 설정 새 섹션 '데모 데이터' — `#seed-fill`/`#seed-clear`(`initDemoSeed`). 관리자 전용(권한 체크), confirm 후 실행.
  - **데이터**(`SEED_*` 상수): 트렌드 존 3(강남 먹자골목·역삼 카페로드·학동공원 러닝코스 — `seedHexCluster`로 현재 그리드 정렬 헥사 ~4개, 사진·설명 포함) / 피드 16(kind cam·post 혼합, 설명글, 존 태깅, 좋아요 4~16 시드, 1~50시간 분산 ts, 닉네임 by=seed_u*) / 스팟 12(이모지·색·좌표 분산) / Request 2(응답 1건 포함) / 동네채팅 시드 4 + liveChat 문서 5(역삼1동 방·주제방).
  - **저장**: 라이브=liveFeed/liveSpots/liveRequests/liveChat 문서(`seed:true` 태깅, 고정 id fs_*/sps_*/rqs_*/cs_*) + 존·지면·채팅시드는 공유문서(markCloudDirty/saveNews). 폴백=localStorage. **비우기**=`where('seed','==',true)` 삭제 + tzs_*/ns_* 제거 — 유저가 직접 만든 컨텐츠는 유지.
  - **이미지**: `seedImg(theme,label)` — 10팔레트(food/cafe/run/…) 그라디언트+대형 이모지+라벨 칩, 640² SVG data URI(~1KB — Firestore 부담 최소). 교체=기존 편집 UI(존 카드 편집 사진/링크·피드 관리)에서 URL 입력.
  - 검증(프리뷰, 로컬 폴백): 채우기→존3(사진)·피드16·스팟12·Request2·지면3·채팅4 + 지면 카드/핀/스팟/Request버블 렌더 스크린샷, 비우기→시드 전부 0(직접 컨텐츠 보존), 콘솔 에러 0.
- **v1.48.1 — 드로어 스팟 칩 스타일 일원화**: 텍스트가 "멋대로"였던 원인=v1.48.0이 `hashStr(id)%3`로 크기·굵기·색 3티어를 **콘텐츠와 무관하게 랜덤 배정**(워드클라우드 연출) → 티어·hashStr 제거, `.sp-word`=단일 스타일(2.9cqw/600/--ink) **필 버블**(border 1px --line + `hexToRgba(스팟색,0.07)` 옅은 채색, nowrap+ellipsis, hover 액센트). 검증: 전 칩 fontSize/fontWeight 동일·radius 999px·개별 틴트 PASS, 스크린샷 확인.
- **v1.48.0 — 스팟 선명도/흐림 원인 수정 + 지역 워드클라우드 + 본인 콘텐츠 수정권**:
  - **저해상도 원인**: `SpotBubble.draw`가 `transform:scale(2^(z-16))` — 브라우저가 1배로 래스터한 레이어를 GPU로 확대해 줌인(z>16)에서 글자·이모지가 흐릿. → **CSS `zoom`**(레이아웃 스케일 → 목표 크기로 재래스터=선명)을 버블/이모지 자식에 적용, gap도 배율 반영. `CSS_ZOOM_OK`(CSS.supports) 미지원 브라우저는 기존 transform scale 폴백.
  - **흐림 미적용 원인**: v1.47.1의 blur(6px)는 적용됐지만 기본 `bgOpacity 0.92`가 거의 불투명이라 비쳐 보일 배경이 없었음. → `_render`의 `--spot-bg` 알파에 **상한 0.82**(설정이 더 낮으면 설정대로) — 프로스트 항상 가시화.
  - **드로어 스팟 = 현재 지역 워드 클라우드**: `spotsInFocusedRegion()`(베이직=regionAt 동 매칭(normRegion)/트렌드=선택·렌즈·센터 존 ptInZone) + `.sp-cloud`/`.sp-word`(무채색 3티어 — `hashStr(id)%3`로 크기 고정, 색 말풍선 `.sp-list/.sp-bub` 폐지, hover만 액센트). 섹션 제목에 현재 지역명 표기, 탭→포커스 유지.
  - **본인 콘텐츠 수정(demo 포함)**: `canEditSpot(s)`=관리자 ∥ (s.live && by===myUid()) — liveSpots 스냅샷/로컬 저장/컴포저에 `by` 저장 추가(레거시 by 없는 문서는 허용). SpotBubble mousedown→**pointerdown**(모바일 터치 드래그, 포인터 캡처)+지도별 규칙(관리자=메인만/본인=보는 지도 어디서든), 드롭 저장=`persistSpotEdit`(live merge/로컬/클라우드 자동 분기), 클릭=기존 편집 모달(role-user 게이트 없음 확인). 피드 카드 우상단 `.fc-tr`=본인 것만 **✏️(설명 수정 prompt)·🗑(삭제)** + 시간 칩 통합.
  - 검증(프리뷰): zoom=4·transform에 scale 없음·bg 알파 0.82·blur(6px)/본인 드래그 이동·타인 차단/클라우드 지역 필터(먼 스팟 제외)·제목 '· 명동'/피드 칩 본인 3장만 노출 전부 PASS, 콘솔 에러 0, 스크린샷 확인.
- **v1.47.1 — 스팟 버블 그림자 제거 + 배경 흐림**: `.spot-bubble`의 box-shadow(기본 0 1px 4px + hover/`.spot-sel` 0 5px 18px) 삭제, transition에서 box-shadow 제거, `.spot-emoji`의 drop-shadow 필터 삭제. 버블에 `backdrop-filter:blur(6px)`(+웹킷) 추가 — 배경색 불투명도(bgOpacity)를 낮추면 뒤 지도가 흐려 보이는 프로스트 효과(불투명 1.0이면 시각 변화 없음). 검증: computed style box-shadow=none·backdrop-filter=blur(6px)·emoji filter=none PASS.
- **v1.47.0 — 피드 컨셉 개편: 속성 모델 + Feed 작성 + 지면 연동 + 핀 드래그**:
  - **데이터 모델**: `feedAdd(src,region,zone,lat,lng,kind,desc)` — liveFeed 문서에 `kind`('cam' 라이브 카메라/'post' 피드 작성·업로드)·`desc`(설명글 ≤120자)·`name`(만든이=chatName)·`by`·`ts` 저장, 스냅샷 복원 포함. 구버전 항목은 kind='post'·desc='' 폴백.
  - **올리는 방식**: +메뉴 = 스팟 메시지/라이브 카메라(kind cam)/**✍️ Feed 작성**(신규 `#feed-post-input` 갤러리+`initFeedPost`: 압축→설명글 prompt→업로드, 취소 시 중단)/현장 Request. 관리자 컨텐츠 관리의 업로드·링크=kind post(기존 유지).
  - **피드 탭**: 전 열수 **1:1**(one-col 4:5 제거). 카드 칩 재배치 — 좌상단 `.fc-top`(LIVE+존 칩), 우상단 `.fc-time`(올린 시간), 좌하단 동, 우하단 ♥. **시간 표시 옵션**(`feedTimeMode`: ago 상대/clock 시각/off — 피드 컨텐츠 설정 `#feed-time`, nowhere_feedtime+클라우드 동기, `timeAgo`/`fmtTime`).
  - **지역 컨텐츠 지면**(지도 탭 한정): `newsView`=관리자 지면 이미지(수동, 기존 유지)+`feedSummaryItems()`(현 위치 가까운 순+최신 상위 4, **스팟 메시지 제외**). 피드 슬라이드(.cp-feed)=위치+설명글(title 자리)+칩(.cps-chips: LIVE/존 색/♥). 갱신 시점=탭 전환·피드 변경·지면 변경(팬 중 재정렬은 캐러셀 점프 방지 위해 안 함).
  - **핀 드래그**: FeedThumb에 `_canEdit`(관리자 또는 `by===myUid()`) — editable이면 overlayMouseTarget pane+포인터 드래그(`.feed-pin.editable`, touch-action:none), 드롭 시 **동(dongAt)/존(zoneObjAtCenter) 자동 재태깅**+feedUpdate(라이브 merge)+전 뷰 동기. 비편집자는 기존 비인터랙티브 pane.
  - **관리자 컨텐츠 관리**: 행 확장 — 종류 select(라이브/피드 작성)+존 select / 동+설명글 입력 / 메타(👤만든이·🕒시각·♥수, 읽기 전용)+삭제.
  - ※ '+버튼 우하단' 요구는 우하단에 AI 버튼이 있어 **기존 좌하단 유지**(스왑 원하면 별도 결정). 검증(프리뷰): +메뉴 4항목·kind/desc/name 저장·LIVE 칩 1개·시간 3모드(상대/시각/숨김)·지면 피드 4슬라이드+칩·편집기 필드·핀 editable 구분·**드래그 실이동+존 해제 재태깅** 전부 PASS, 콘솔 에러 0.
- **v1.46.1 — 설정 초기화 버그 근본 수정(세션 에코 가드) + settings-default.json 백스톱**:
  - **원인**: v1.41의 자기 저장 에코 가드가 `updatedBy===내 이메일` 비교 — shared 문서는 관리자만 쓰므로 마지막 저장자=항상 본인 → **관리자가 새로 접속(새 버전 push 후 리로드 포함)하면 첫 스냅샷부터 전부 스킵** → 코드 기본값으로 시작 → 존/스팟/방 등 아무 편집 시 `cloudSave`가 전체 페이로드(기본값 settings 포함)를 저장 → **클라우드의 튜닝 설정이 통째로 초기화**. 데모 유저도 그 초기화된 문서를 받아 전원 리셋.
  - **수정**: 페이지 로드마다 `SESSION_SID` 발급, `cloudSave`/`newsCloudSave` 페이로드에 `updatedSid` 포함. mapContent·news 리스너의 에코 가드를 `updatedSid===SESSION_SID`로 교체 — 같은 세션의 자기 에코만 무시(편집 보호 유지), 새 접속·다른 기기·구버전 문서(sid 없음)는 항상 적용.
  - **백스톱**: ①repo에 `settings-default.json`(기본 `{}`) — 부팅 시 fetch(no-store)해 코드 기본값 위에 병합(클라우드가 이미 적용됐으면 무시, 이후 클라우드 도착 시 클라우드가 덮음). 미로그인/로컬 프리뷰에서도 튜닝 룩 유지 가능 ②관리자 설정 상단 **📋 설정 JSON 복사** 버튼(`initSettingsExport`, snapshotSettings 전체) — 붙여넣어 커밋하면 배포 기본값 백업. `applyCloudData`의 설정 병합을 `applySettingsData()`로 추출(클라우드/파일 공용).
  - 검증(프리뷰, fbDb 스텁): 타 세션 저장(updatedBy=본인·sid 다름)=적용✓ / 같은 세션 에코=스킵✓ / sid 없는 구문서=적용✓ / cloudSave에 updatedSid 포함✓ / 파일 백스톱 병합(fog·spotFont·hexR)✓ / 복사 버튼(9키 JSON)✓, 콘솔 에러 0.
- **v1.46.0 — 피드↔지도 연결 4종 (열 3칸·존 칩·지도 썸네일 핀·탭=지도 이동)**:
  - **가로 배열 최대 3칸**: `applyFeedCols` 클램프 4→3, 피드 상단 버튼 [1,2,3], 설정 select 4 옵션 제거. 기존 저장값 4는 초기화 시 자동 3으로.
  - **트렌드존 칩**: `feedZoneOf(it)`(존 태그 우선 → 좌표 ptInZone) 존이면 카드 좌상단 `.fc-zonechip`(존 색상 bg 0.92, 흰 글자).
  - **지도 썸네일 핀**: 신규 `FeedThumb` OverlayView(`overlayLayer` 비인터랙티브 pane — 지도 제스처 방해 없음) + `renderFeedMarkers()`(feedItems 최근 30, 메인+폰). 원형 사진+흰 링 2.5px+옅은 드롭섀도(글로우 없음), 줌별 44/30/18px. 갱신 훅: liveFeed 스냅샷/feedAdd·feedDelete 로컬/renderSpots(지도 준비·모드 전환·클라우드 반영에 피기백). **좌표 저장**: `feedAdd(src,region,zone,lat,lng)` 확장, 업로드/링크/라이브 카메라 3곳이 지도 센터 좌표 전달, liveFeed 문서에 lat/lng 포함(스냅샷 복원도). 좌표 없는 구버전은 `feedItemLatLng` 폴백(존 중심→동 중심).
  - **피드 탭=지도 이동**: 카드 싱글탭(360ms 타이머, 더블탭 좋아요와 공존) → `focusFeedEntry`: 지도 탭 전환+메인·폰 panTo(줌<15→16, 폰 인셋 보정). 스팟 항목은 focusSpot(선택 강조 포함). 위치 없는 항목(구 지면)은 무동작.
  - 기능보기 피드 카드 설명 현행화. 검증(프리뷰): 존 칩(태그/좌표 판정/존 밖 없음)·핀 렌더(메인3+폰3, 줌 축소)·열 버튼 3개+4클램프·싱글탭 지도 이동(좌표 일치·줌16)·더블탭 좋아요 회귀 없음 전부 PASS, 콘솔 에러 0.
- **v1.45.0 — 소셜 채팅 실시간 공유(liveChat)** ⚠️**Firestore 규칙 콘솔 배포 필요**: 실시간 공유 전수 점검 결과 소셜 채팅 메시지(`nowhere_chat` localStorage)만 미공유로 남아 있었음 → `liveChat` 컬렉션으로 전환. ①`liveOn()`에 liveChat 리스너 추가(orderBy ts desc limit400 → `socLiveMsgs`에 방(room key)별 그룹·시간순), 전송=`liveChat` 문서 set(`{room,t,by,name,ts}`, name=구글 displayName/이메일 앞부분) — 낙관적 푸시 없이 스냅샷 로컬 에코가 즉시 렌더 ②렌더 단일화 `roomMsgs()`: 라이브=`seedFor()`(연출용 시드, 저장 안 함)+공유 메시지 / 폴백=기존 localStorage+1.6초 데모 자동응답(**라이브 모드에선 자동응답 비활성** — 실제 상대 계정이 응답) ③방 목록 대화 수=라이브 카운트 ④관리자 '비우기'=공유된 동네 채팅(liveChat의 `local:*` room) 문서도 삭제 ⑤기능보기 소셜 카드 demo→**live** 뱃지·설명 갱신, deck.html 유저 컨텐츠에 liveChat 추가, diagram.html 데이터 블록 liveChat+채팅 동기화 점선+v1.45 표기. 검증(프리뷰): 폴백 경로(시드+전송+데모응답)·라이브 경로(stub Firestore로 쓰기 페이로드/스냅샷 병합/me·타계정 구분/방 카운트/시드 프리픽스) 전부 PASS, 콘솔 에러 0.

### 2026-07-03
- **v1.24.5 — 네비 활성 칩 플랫화**: 글래스 하이라이트/그림자/채도 제거, 배경 블러만 살짝(4px). 반투명 파스텔(블루/그린/보라)·흰 아이콘·글자 유지.
- **v1.24.4 — 네비 활성 칩 파스텔+글래스, 소셜=보라**: 선택 칩을 **반투명 파스텔**(지도=블루 rgba(120,168,240,.62)·피드=그린 (112,206,156,.6)·소셜=**보라** (182,150,236,.62)) + **backdrop-blur 글래스**(inset 하이라이트). 어두운 네비 위라 중간톤이 되어 흰 아이콘·글자 대비 유지.
- **v1.24.3 — 네비 활성 = 컬러 배경 칩 + 흰 아이콘/글자**: 아이콘만 3색으론 여전히 글자가 안 보여서, **선택 칩 배경을 3색**(지도=#3f7fd6 블루·피드=#2ea36c 그린·소셜=#d0774a 코랄)으로 하고 아이콘·글자는 **흰색**으로 → 대비 확실. 색상별 소프트 섀도우.
- **v1.24.2 — 네비 활성 라벨 시인성**: v1.24.1에서 활성 아이템 전체를 3색으로 칠해 **라벨 글자까지 옅어져 안 보이던 문제** → **아이콘(`​.pn-ic`)만 3색, 라벨(`.pn-lb`)은 흰색 유지**로 수정.
- **v1.24.1 — 폰 하단 팝업 겹침·축척/위치칩 정리·네비 아이콘 색상**: ①하단 팝업(`.content-add-menu`·`.ai-bubble`) `bottom`에 `env(safe-area-inset-bottom)` 더해 **iOS에서 하단 네비와 겹치지 않게**. ②축척 자(`.psc-bar`) **흰색 아웃라인(box-shadow) 삭제**. ③축척·위치칩 텍스트 **글로우(text-shadow) 삭제**. ④하단 네비 활성 아이콘 **3색**(지도=#78a6e6 블루·피드=#5fbf95 그린·소셜=#e0a077 코랄, 은은하게). (CSS만, 하네스 확인.)
- **v1.24.0 — 테두리 제거 + 동네소식 구역(동) + 실제 표시영역 기준 중심 정비**:
  1. **테두리 삭제**: 모드칩(`.pa-mode`)·프로필 아바타(`.pa-profile`)·동네소식 프레임(`#cp-frame`) border 제거.
  2. **동네소식 구역(동)**: 각 아이템에 `region` 필드 추가 — 업로드/링크 추가 시 **지도 중심의 동 자동 캡처**(`currentCenterDong`), 사이드바 리스트에서 **구역(동) 입력으로 추가/수정**(폰엔 미표시). 공유문서(`shared/news`)에도 region 저장.
  3. **실제 표시영역 기준 중심 정비**: 동네소식 지면 때문에 폰 지도 상단이 흰 헤더로 가려져, 지도 div 중심과 '보이는' 중심이 어긋나 **트렌드존 fitBounds가 짤리던 문제** → `phoneMapInsets()`(헤더/네비 높이)·`phoneVisibleCenter()`·`phoneFitPadding()` 신설. `selectPhoneZone`은 **인셋 패딩 fitBounds**로 보이는 영역 안에 존 전체 표시, 위치칩·베이직 스포트라이트는 **보이는 중심**의 동 사용, `focusSpot`은 폰에서 `panBy`로 보이는 영역 중앙 보정.
  - 검증: 하네스로 테두리 제거·구역(동) 입력행 확인. 중심/fit 로직은 node 문법 OK, 실제 렌더는 라이브(Maps) 관측(트렌드존 목록 클릭 시 짤림 해소 확인 요망).
- **v1.23.1 — 동네소식 이미지 링크(URL) 추가 지원**: 업로드(이미지 저장)와 별개로 **이미지 링크(URL) 붙여넣기**로 추가 가능(`addNewsUrl`). **URL 문자열만 저장** → Firestore 저장부담 거의 0, 뷰어 브라우저가 외부 호스트에서 직접 로드(우리 저장/대역폭 비용 0). `https://`만 허용 + `new Image()` 프로브 로드 성공 시에만 등록(핫링크 차단/깨진 링크는 알림). 관리자만. (검증: 동네소식 블록에 URL 입력행 렌더 확인, node 문법 OK. 실제 링크 로드는 라이브.)
  - ⚠️ 주의: 외부 링크는 사라지거나 바뀌면 깨짐(link rot), 일부 사이트는 외부 로드 차단. 구글 이미지 검색은 "이미지 주소 복사"로 얻은 **직접 이미지 주소(.jpg/.png…, https)** 사용. 저작권 유의.
- **v1.23.0 — 동네소식 이미지 공유(Firestore) + 무료 티어 안전장치**: 기기 로컬(localStorage)만이던 지면 이미지를 **`shared/news` Firestore 문서로 공유**(관리자 저장 / 로그인 사용자 모두 열람) → 데모유저·타기기에도 표시. **Firebase Storage/Blaze 미사용 = 결제계정 불필요·과금 0.**
  - **안전장치(무료 범위 강제)**: ①개수 상한 `NEWS_MAX_COUNT=6` ②1장 상한 `NEWS_MAX_ITEM_BYTES≈170KB`(900px 리사이즈 + 품질 백오프, 초과 시 거부) ③문서 총합 상한 `NEWS_DOC_BUDGET≈900KB` ④**Firestore 문서 1MB 하드리밋**(서버 강제 백스톱) ⑤저장은 `currentRole==='admin'`만(`markNewsDirty`), 규칙은 기존 `match /shared/{docId}`(admin 쓰기/로그인 읽기) 그대로. 초과 시 사용자 알림.
  - 흐름: 로컬 캐시(즉시 렌더) → 로그인 후 `loadNewsFromCloud`가 공유본으로 갱신. 관리자 추가/삭제/순서변경 → `saveNews`(localStorage) + 디바운스 `newsCloudSave`.
  - 검증: 하네스로 8장 시도 시 개수(6)·1장 용량·총합(≈900KB) 상한 모두 작동, Firestore 1MB 안쪽 확인(worst-case 노이즈 이미지로도 PASS). node 문법 OK. Firestore 실제 read/write는 라이브(로그인) 관측.
  - ⚠️ Firestore Spark(무료) 기준: 저장 1GB · 읽기 50K/쓰기 20K per day — 데모 규모로 여유. 이미지는 Firestore에만(작게) 저장, Storage 미사용.
- **v1.22.1 — 동네소식 이미지 추가 버그픽스**: 파일 change 핸들러에서 `var fs=this.files; this.value='';` 순서 때문에 **입력 비우기(value='')가 라이브 FileList를 같이 비워** 선택 파일이 0개로 처리되던 버그 → **파일을 먼저 배열로 복사 후 value 비우기**로 수정. localStorage 한도 초과 시 조용히 실패하던 것 → 경고 알림 추가. (검증: DataTransfer로 실제 파일 2장 주입 시 캐러셀/리스트에 정상 반영 확인.)
  - 📌 **동네소식 이미지 저장/과금**: 이미지는 **브라우저 localStorage(`nowhere_news`)** 에만 저장(canvas로 ≤1000px JPEG 압축). **서버/클라우드 미전송 = 과금 0.** `cloudSave`(Firestore)에는 이미지 미포함 확인. 한계: 기기 로컬(타기기/데모유저 공유 안 됨), 용량 ~5MB. 공유하려면 Firebase Storage 연동 필요(무료 티어 존재).
- **v1.22.0 — 동네소식 캐러셀 + 위치칩 텍스트화 + 존 병합 외곽선 + 토글 아이콘 제거**:
  1. **동네소식 지면 = 이미지 캐러셀 + 사이드바 관리**: 폰 지면(`#cp-frame`)을 **여러 이미지 좌우 스와이프 캐러셀**(트랙+점 인디케이터)로. 업로드/편집 인라인 컨트롤 제거 → **사이드바 컨텐츠 설정에 `동네소식` 블록 신설**(관리자만): 이미지 추가(다중), 리스트에서 순서 이동(↑↓)·삭제(🗑). `newsItems[]` localStorage 저장(공유는 후속 Storage).
  2. **위치칩 배경/아이콘 제거**: `.pa-loc`을 다크 글라스+핀 → **텍스트만**(어두운 글자+흰 헤일로), 축척과 가로 정렬 유지.
  3. **트렌드 존 내부 경계 숨김 옵션**: 관리자 설정>트렌드 `존 표시 > 내부 경계 숨김` 토글. 켜면 헥사곤 stroke=0(내부선 감춤) + **합집합 외곽선**만 렌더(`zoneOutlineLoops`: 공유 변 제거 후 경계 변 체인 → 루프별 Polygon). `zoneMergeBlocks` 공유문서 저장. 메인/폰 지도 모두 적용.
  4. **상단 모드 토글 아이콘 제거**: 폰 `.pm-btn`의 📍/🔥 제거(텍스트만). 색상(베이직 블루/트렌드 핫)은 유지.
  - 검증: 하네스로 캐러셀+점·동네소식 리스트·아이콘 없는 토글·텍스트 위치칩 확인. 존 외곽선 로직 node 테스트(1헥사=6점 루프, 5헥사 블롭=18점 단일 루프). node 문법·참조 정합성 OK, 실 페이지 콘솔 에러 없음. 이미지 업로드·존 병합 렌더는 라이브(로그인/Maps) 관측.
- **v1.21.0 — 폰 상단 리디자인(프로필/지역 콘텐츠 지면) + 위치칩 글라스**:
  1. **우상단 프로필(P)**: 앱바 3분할 = 햄버거·모드토글·**프로필 아바타**(사진 있으면 표시, 없으면 이니셜). 클릭 시 **프로필 메뉴**(계정 이메일·역할·버전·로그아웃) 드롭다운. **기존 폰 햄버거 드로어의 계정 블록(`#drawer-account`)을 프로필로 이동**(제거). `showUserChip`이 프로필·메뉴를 채움, 로그아웃은 `#ppm-logout`.
  2. **지역 콘텐츠 지면**: 헤더(불투명 흰색) 안에 콘텐츠 카드(`#phone-content-page` + `#cp-frame`) 공간 확보(플레이스홀더 '지역 콘텐츠 지면'). **관리자만** 이미지 업로드 + 크기조절(줌 슬라이더) + **크롭(프레임 내 팬·줌, overflow clip)**. 업로드 시 canvas로 최대 1000px JPEG 다운스케일 후 **localStorage 저장**(공유는 추후 Firebase Storage 필요 — 지금은 기기 로컬).
  3. **축척·위치칩**: 헤더 하단(`top:100%`)에 앵커해 지도 위 표시. **가로 정렬** 맞춤(축척 좌 · 위치칩 중앙). 위치칩을 **하단 네비와 유사한 다크 글라스**(`rgba(24,26,34,0.58)`+blur, 흰 글자)로 변경.
  - 검증: 하네스(관리자)로 헤더(P)·콘텐츠 지면·이미지 컨트롤·프로필 메뉴·축척/위치칩 정렬·글라스 확인. node 문법·참조 정합성 OK, 실 페이지 콘솔 에러 없음. 이미지 업로드/크롭 실동작·프로필 사진은 라이브(로그인) 관측.
  - ⚠️ **콘텐츠 지면 이미지는 현재 기기 로컬(localStorage)** — 데모유저/타기기 공유 원하면 Firebase Storage 연동 필요(후속).
- **v1.20.0 — 폰 지도 오버레이(위치·축척) + PC 사이드바 메뉴 + 설정 재편 + 스팟 점전환 축척화**:
  1. **폰 축척/위치를 지도 위(over) 오버레이로**: 헤더 슬롯에 있던 축척과 앱바 우측 위치표시기를 **`.phone-screen` 직속 오버레이**로 이동 → 위치=상단 중앙 흰 플로팅 칩, 축척=좌상단 작게(배경/테두리 없이 자+수치, 흰 헤일로). 헤더는 햄버거+모드토글만(컴팩트). `top:calc(env(safe-area-inset-top)+…cqw)`로 헤더 아래 앵커.
  2. **PC 전체 지도 사이드바 메뉴**: 메인 지도 좌상단 햄버거(`#pc-menu-btn`) → 좌측 슬라이드 드로어(`#pc-drawer`). **폰 햄버거 드로어와 동일 바디(`#phone-drawer-body`)를 열리는 쪽으로 이동**해 공유 → 항상 싱크(한 번에 하나만 열림). 모바일에선 숨김.
  3. **설정 재편**: `선택 구역`의 `강조 축척`을 `강조외 구역`으로 이동하고 그 섹션명을 **`강조 구역`**으로 변경(주변 배경/테두리 색 포함). 
  4. **스팟 점 전환도 축척(m) 기준**: `spotConfig.dotZoom`(줌) → **`dotScaleM`**(기본 1000m). 축척자(64px) 거리가 이 값 초과로 축소되면 점으로. **강조 구역 축척과 독립**. 설정 `점 전환 축척(m)`(100~5000).
  - 검증: 하네스로 폰 오버레이(위치 중앙칩/축척)·PC 드로어 chrome+데모리스트·설정 재편(강조 구역/점전환 축척) 확인. node 문법·참조 정합성 OK. 지도 강조/점전환/관리자 드로어 설정은 라이브 관측.
- **v1.19.0 — 스포트라이트 축척 기준화 + 강조외 색상 + 폰 축척 표시 + 네비 마진**:
  1. **하단 네비 마진 50%↓**: `.phone-navbar` 하단 패딩 `5.5cqw→2.75cqw`(모바일 safe-area는 유지) → 메뉴가 너무 위로 뜨던 것 완화.
  2. **강조 기준: 동 개수 → 축척(m)**: `spotMaxDongs`(개수) 폐지, `styleConfig.highlight.spotScaleM`(기본 200m)로 교체. 폰 지도 축척자(64px 기준) 거리가 이 값 이하로 확대되면 스포트라이트 발동. 설정 `강조 축척(m)`(50~3000, 숫자입력). `countVisibleDongs` 제거, `mapMpp()` 헬퍼 신설(중심 위도 m/px).
  3. **강조외 구역 색상 편집**: `styleConfig.dim{fillColor,fillOpacity,strokeColor,strokeOpacity}` 신설, `getBasicDimStyle`가 이를 사용. 관리자 설정에 **`강조외 구역`** 섹션(배경/테두리 색상 컨트롤) 추가. `styleConfig` 직렬화로 공유문서 자동 저장(applyCloudData에 dim mergeInto 추가).
  4. **폰 헤더에 심플 축척**: 헤더 슬롯(`#phone-header-slot`)에 `#phone-scale`(자 ⊔ + 수치만, 배경/테두리 없음) 표시. `updatePhoneScale()`가 폰 지도 idle마다 갱신(니스 거리 + px 바). 관리자 메인 범례와 동일 계산.
  - 검증: 하네스로 축척바·설정로우(축척m/강조외 색상)·네비 마진(2.75cqw=9.3px 계측) 확인. node 문법·참조 정합성 OK. 스포트라이트 실제 렌더는 라이브 관측.
- **v1.18.2 — iOS 웹앱 상하단 세이프에어리어 마감**: 아이폰 홈화면 웹앱에서 **상단 회색 상태바 띠 + 하단 검정 띠**로 지도가 가운데 갇혀 보이던 문제.
  - ①**상태바 스타일 `black-translucent`→`default`**: 상단바가 흰색 헤더라 translucent(흰 글자)면 시간/배터리가 안 보임 → default(어두운 글자)로 바꿔 흰 상태바와 이음새·가독성 확보. (⚠️ **iOS는 이 메타를 홈화면 추가 시점에 캐시** → 기존 설치 아이콘은 삭제 후 재추가해야 반영. viewport-fit=cover는 그대로.)
  - ②**세이프에어리어 배경 검정→밝은 회색**: 모바일 `#sidebar`·`.phone-map` 배경을 `#e8eaed`로 → 지도가 못 닿는 노치/홈 인디케이터 가장자리가 지도색과 섞여 **띠처럼 안 보이게**(CSS는 매 실행 새로 로드되므로 재설치 없이도 즉시 적용).
  - 검증: 코드 정합성·버전 동기화 확인. 실제 상태바 렌더는 iOS 실기기 재설치 후 관측 필요.
- **v1.18.1 — 스포트라이트 기준 설정화 + 강조 스타일 통일**: ①**관리자 설정 > 선택 구역**에 **`강조 기준`(동 개수)** 설정 추가 → 임계값을 상수에서 **`styleConfig.highlight.spotMaxDongs`**로 이전(기본 6, 숫자 직접입력 1~50). `styleConfig` 직렬화에 그대로 실려 **공유문서 자동 저장/로드**(`mergeInto`로 구버전 데이터도 기본값 유지). ②센터 동 강조 스타일을 전용(`getBasicCenterStyle`)에서 **선택 구역 스타일(`getHighlightStyle`)과 동일**하게 통일(요청 반영). 그 외 동 그레이(`getBasicDimStyle`)는 유지. (검증: 설정 로우 렌더/숫자입력 변환 하네스 확인, node 문법·참조 정합성 확인. 지도 강조 렌더는 라이브에서 관측.)
- **v1.18.0 — 베이직 폰: 센터 동 스포트라이트**: 베이직 모드 + 폰 화면에서 **화면에 보이는 동 개수가 임계값(`BASIC_SPOTLIGHT_MAX_DONGS`=6) 이하**로 줄면(=충분히 줌인), **센터가 속한 동의 외곽을 강조**(하이라이트 stroke)하고 **나머지 동은 약간의 그레이 처리**(`#33373f` fill 0.42). 폰 지도 `idle`마다 재계산.
  - 구현: 기존 `dongIndex`(동별 bbox)로 뷰포트에 걸치는 동 수를 세고(`countVisibleDongs`), `regionAt(센터)`로 센터 동을 찾아(`buildDongIndex`에 `key`=featKey 규칙 추가) `refreshPhoneMapStyles`에서 센터=`getBasicCenterStyle`/그 외=`getBasicDimStyle`로 렌더. 트렌드 전환/모드 무관하게 로컬 아닐 땐 자동 해제.
  - 임계값 6은 상수(튜닝 가능). 검증: 실 geojson(497개 동)으로 센터 판별·개수 카운트 노드 테스트 통과(≈1.2km뷰=6동 ON, 3km=15동 off). 지도 렌더는 라이브(Maps+로그인)에서만 관측.
- **v1.17.1 — 햄버거 드로어 가독성 수정**: v1.17.0에서 드로어 글래스를 너무 투명(`rgba(...,0.6)`)하게 잡아, **밝은 지도 위에서 배경이 중간 회색으로 떠 dim-gray 설정 텍스트**(`.settings-section h4` #6b7280, `.setting-row label` #9ca3af, `.section-hint`)의 대비가 무너지던 문제 → 드로어 배경을 **`rgba(12,13,20,0.9)`로 상향**(블러는 유지). "약간의 글래스" 느낌은 남기되 지도가 배경을 밝히지 못하게 해 텍스트 가독성 복구. (검증: 밝은 지도 배경 하네스로 0.6 washed-out ↔ 0.9 readable 비교 확인)
- **v1.17.0 — 폰 UI 4종 마감 (웹앱 풀스크린·저작권 숨김·모드토글 강조·드로어 글래스)**:
  1. **iOS 웹앱 하단 빈공간 제거**: PWA 메타는 이미 있었으나 standalone에서 하단이 남던 문제 → 모바일(≤768px) `.phone-screen`을 `height:100dvh`(iOS에서 하단 여백 유발)에서 **`position:absolute;inset:0`로 고정된 `#sidebar`(inset:0)를 그대로 채우도록** 변경. dvh 의존 제거 → 노치/홈 인디케이터까지 꽉 참.
  2. **폰 지도 구글 저작권/로고 숨김**: `.phone-map` 스코프로 `.gm-style-cc`·구글 로고·약관 링크 `display:none`. **데스크톱 메인 `#map` 저작권은 유지**(폰 미리보기만 실제 앱처럼).
  3. **상단 모드 토글(베이직/트렌드) 강조**: 아이콘 추가(📍/🔥). 흰색 헤더에 맞춰 세그먼트 트랙을 옅은 그레이로, **베이직=차분한 블루**(`#2f7bff`), **트렌드=Hot 그라데이션**(오렌지→레드 `#ff9d42→#ff3d5e`)+은은한 글로우, 트렌드 활성 시 🔥 미세 flicker. 플랫 디자인 유지.
  4. **햄버거 드로어 글래스화**: `#phone-drawer` 불투명 배경(0.98) → **반투명 다크 글래스**(`rgba(14,16,24,0.6)` + `blur(26px)`)로 → 하단 네비처럼 뒤 지도가 비침(드로어 텍스트는 라이트온다크라 가독성 유지).
  - 검증: 로컬 정적 프리뷰 하네스로 토글(베이직/트렌드)·드로어 글래스 시각 확인, 실 index.html 콘솔 에러 없음. iOS safe-area·구글 저작권 숨김은 실기기/라이브 Maps 타일에서만 관측되나 표준 방식 적용.
- **v1.16.0 — 컴포저 위치버그 수정 + 이모지 삭제 + 햄버거 메뉴(데모 리스트·관리자 설정)**:
  - **컴포저 위치 버그(핵심)**: `mapId`(벡터 지도)에선 `fromLatLngToDivPixel`이 (0,0) 반환·pane 정렬 안 됨. 게다가 `.spot-composer`의 `animation:cpIn ... both` 최종 키프레임 `transform:none`이 위치용 `transform:translate(-50%,-100%…)`을 **덮어써서** 팝업이 생성점 우하단으로 밀림. → 컴포저를 **지도 컨테이너(`getMap().getDiv()`)에 부착 + `draw()`가 `fromLatLngToContainerPixel`로 left/top 직접 계산**(팝업 하단=생성점, transform 미사용, `scFade` 애니메이션으로 교체). 생성점에 **펄스 점**(`.sc-dot::after`) 강조. (검증: 점이 지도 센터와 오차 −1px)
  - **이모지 삭제**: `buildEmojiPicker`의 각 이모지에 **롱프레스(500ms)/우클릭 → 삭제**(`delEmoji`, 최소 1개 유지, 삭제 후 재렌더+공유문서 저장).
  - **폰 햄버거 드로어 = 메뉴 허브**: `initPhoneMenu`가 `#content-*`/`#settings-*`(관리자 설정)를 **드로어로 이동**, 상단에 `#drawer-demo`(트렌드 존/스팟 리스트) 생성. `renderDrawerDemo`가 `trendZones`/`spotMessages`로 리스트 렌더(존 탭→트렌드 전환+`selectPhoneZone`, 스팟 탭→`focusSpot`, 후 드로어 닫힘). 역할별: `body.role-admin #drawer-demo{display:none}`(관리자는 실제 섹션), 데모는 role-user CSS로 섹션 숨김·리스트만. ⚠️ **관리자 설정이 이제 사이드바가 아니라 폰 햄버거에** 있음(데스크톱 사이드바엔 계정/모드/안내만).
- **v1.15.1 — 스팟 추가 버그픽스(누른 지점 팝업·유지) + 글래스 조정**:
  - **롱프레스/우클릭 = 누른 지점에 팝업 + 그 자리에 생성**: 이전엔 팝업이 좌하단 고정이라 손 떼면 사라져 추가 불가. 이제 `positionAddMenuAt`으로 **누른 좌표(폰스크린 기준)에 팝업**을 띄우고, `ProjHelper`(OverlayView) `getProjection().fromContainerPixelToLatLng`로 **누른 지점 latLng**를 구해 그 자리에 컴포저. **자동닫힘 방지**: 롱프레스 직후 emulated click이 닫던 문제 → document click 핸들러에 `Date.now()-addMenuOpenedAt<600` 가드.
  - **+버튼/사이드바 버튼 = 화면 센터**: `addSpotContent`가 `addAtLatLng`(제스처) 있으면 그 자리, 없으면 `m.getCenter()`. (지오메트릭 센터로 단순화 — visibleCenter는 헤더>네비라 오히려 아래로 치우쳐 제거.)
  - `attachAddGestures(el,mapObj)`가 contextmenu·롱프레스에서 `clientToLatLng`로 좌표 산출. `ProjHelper` 인스턴스는 `mapProjHelper`/`phoneProjHelper`(각 지도 생성 직후).
  - **글래스 조정**: 설정 패널(`#left-panel`) 글래스 **원복(불투명 `#f4f6f9`)**. 폰 메뉴 버튼은 **프로스티드 글래스**로 — 하단 네비/+/AI `rgba(24,26,34,0.58)+blur(22px)`, 모드토글 `rgba(255,255,255,0.42)+blur(8px)`. 폰 헤더 글래스는 유지.
- **v1.15.0 — 마스코트 아이콘 + 타이틀 + 스팟 강조/추가방식 개편 + 글래스**:
  - **앱 아이콘/파비콘 교체**: 첨부 마스코트(구름) 이미지 → `icon-512.jpg`/`icon-192.jpg`/`apple-touch-icon.jpg`/`favicon.jpg`. 브라우저 canvas로 마스코트 중심 크롭·리사이즈(sx300 sy180 1500²)해 JPEG 생성(툴 없이). manifest 아이콘 png→jpg(image/jpeg), HTML apple-touch/icon 링크·`?v` 갱신. 옛 `icon-*.png` 제거.
  - **페이지 타이틀** '동 단위 행정구역…' → **'Now Here Demo'**.
  - **스팟 롤오버/선택 강조**: 테두리(box-shadow 링) 제거 → **`transform:scale(1.16)`**(hover + `.spot-sel`). `selectedSpotId`+`setSelectedSpot`(_render서 `.spot-sel` 토글), `focusSpot`이 선택. `.spot-marker` pointer-events auto로 전 스팟 hover 가능.
  - **스팟 추가 방식 개편**(클릭 배치 폐지): `placingSpot`/`startPlacingSpot`/`onMapClickForSpot`/`setPlaceCursor`/`cancelPlacingSpot` 제거. 대신 **화면 롱프레스(touch 520ms)·우클릭(contextmenu)** → `attachAddGestures`가 좌측하단 `#content-add-menu` 팝업(제스처가 있던 지도를 `addTargetMap`으로). **+버튼/사이드바 추가버튼** → `openAddMenu`. 팝업 '스팟 메시지' → `addSpotAtCenter`가 **`addTargetMap`(없으면 `primaryMap`=모바일 폰/데스크톱 메인) 센터**에 컴포저. ⚠️ 데스크톱 메인맵 우클릭 시 팝업은 폰 미러(우측)에 뜸(폰 중심 설계).
  - **약한 글래스**: `#left-panel` bg `rgba(245,247,250,0.8)`+`backdrop-filter blur(18px)`, `.phone-header` bg `rgba(255,255,255,0.8)`+`blur(14px)`. 시인성 유지 위해 불투명도 0.8 유지.
- **v1.14.0 — 스팟 점색=버블색 + 데모 스팟추가 + 스팟목록 + 상단바 3분할 + 범위확대 + 버블여백**:
  - **점 색상 = 버블 색상 자동**: `dotColor` 설정/컨트롤(`ct-spot-dot`) 제거, `_render`가 `s.color||bgColor`로(개별 변경 색 포함).
  - **데모(뷰어)도 스팟 추가**: `startPlacingSpot`의 admin 게이트 제거(로그인만 요구). 데모가 만든 스팟은 `local:true` 플래그로 이 기기 `localStorage('nowhere_localSpots')`에 저장(`persistSpotChange`=admin→클라우드/데모→로컬). `applyCloudData`·초기로드에서 `loadLocalSpotsInto`로 병합, `cloudSave`는 `!local`만 저장(공유문서 오염 방지). ⚠️ **Firestore 규칙은 그대로**(공유문서 쓰기=관리자만). 데모 스팟을 '공유'로 하려면 규칙 완화+콘솔 배포 필요(보안 검토).
  - **스팟 메시지 목록**(`renderSpotList`/`focusSpot`, `#spot-list-area` 컨텐츠 설정 내): 이모지·텍스트·색점 + 이동(panTo)/삭제. `renderSpots` 끝에서 갱신.
  - **스팟 외 수치 설정 범위 확대**(시스템 최대): 선굵기 →0~50, 헥사곤 반경 →0.1~50, 로컬/존 라벨 크기 →4~200. (스팟 설정은 이전 요청대로 별도.)
  - **버블 좌우 여백 축소**: `.spot-bubble` padding 5px 10px→**4px 7px**.
  - **하단 네비 위로**: `.phone-navbar` padding-bottom 3.6cqw→**5.5cqw**(모바일 safe-area calc도), ai버블·컨텐츠추가 팝업 bottom 19→21cqw.
  - **상단바 3분할**: `.phone-appbar` flex→**grid `1fr auto 1fr`**. 햄버거 `justify-self:start`(좌), 모드토글 `justify-self:center`(가운데), 위치 `justify-self:end`(우). HTML도 순서 재배치(hamburger→pa-mode→pa-loc). 위치 폰트/핀 약간 축소.
- **v1.13.1 — 스팟 글자/이모지 크기 범위 확대**: 너무 큰 최소값 제한 완화. `spot-font-size` 9~22→**4~80**, `spot-emoji-size` 16~48(step2)→**4~120(step1)**. 숫자 입력이라 주의문구(`정수 4~80` 등)·clamp도 자동 반영.
- **v1.13.0 — PWA(전체화면 앱처럼)**: 폰에서 '홈 화면에 추가' 시 브라우저 크롬 없이 전체화면(standalone) 실행.
  - 신규 파일: `manifest.webmanifest`(display:standalone, portrait, theme_color #fff, background #0b0d16, 아이콘 3개), `icon-192.png`/`icon-512.png`(any+maskable)/`apple-touch-icon.png`(180). 아이콘은 위치핀(블루→퍼플 그라디언트+흰점, 다크 그라디언트 배경) — 스크래치패드 `genicons.js`(zlib로 PNG 직접 인코딩)로 생성(스크립트는 repo에 없음, 재생성 시 참고).
  - `index.html <head>`: viewport에 `viewport-fit=cover, user-scalable=no` 추가, `theme-color`, `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style=black-translucent`, `apple-mobile-web-app-title`, `manifest`/`apple-touch-icon`/`icon` 링크.
  - CSS(≤768px): `.phone-header` top / `.phone-navbar` bottom / 드로어 헤드에 `env(safe-area-inset-*)` 안전영역 패딩(노치·홈인디케이터 대비).
  - **서비스워커는 의도적으로 미포함** — 자주 배포하는 데모라 캐시 구버전 문제 회피. iOS는 SW 없이도 '홈 화면 추가' 전체화면 됨(안드로이드도 메뉴에서 수동 추가 시 standalone). 자동 설치배너만 없음.
  - ⚠️ 알려진 한계: standalone(iOS)에서는 `signInWithPopup`이 막힐 수 있음(팝업 차단). 실제 모바일 PWA 로그인까지 필요해지면 `signInWithRedirect`로 전환 검토.
- **v1.12.0 — 숫자입력 전용 + Light 설정테마 + 스팟 고정크기 + 모드무관 스팟 + 폰 네비/컨텐츠추가**:
  - **모든 수치 설정 슬라이더 제거 → 숫자 직접입력만**(`enhanceRangeInputs`가 range를 `display:none`, `.range-num`만). 각 칸 옆 **주의문구**(`.num-hint`: `정수/소수` + `min~max`, 음수 여부는 범위로 표시). 정수 필드는 입력 시 반올림·clamp. 기존 range 핸들러는 `input` 이벤트 디스패치로 재사용(로직 유지).
  - **설정 패널 Light 테마**: `#left-panel` 및 하위(계정/모드토글/토글버튼/아코디언/입력/버튼/토글스위치/존관리)를 밝은 배경+어두운 글자로 오버라이드(`#left-panel …` 스코프). 폰 미러·색상팝업·모달은 기존 다크 유지.
  - **스팟 = 지도 고정 크기**: `SPOT_REF_ZOOM` 14→**16**, 클램프 0.02~40으로 넓혀 사실상 무제한 → `2^(z-16)` 순수 배율(줌아웃=절반씩 축소, 줌인=2배씩). 설정 크기가 어느 줌에서도 같은 '미터 범위'를 덮음(건물 블럭 크기 유지).
  - **스팟 모드 무관 표시**: `renderSpots`의 `currentMode!=='local'` 게이트 제거, `switchMode` 트렌드 분기 `clearSpots→renderSpots`, `applyCloudData`/`initPhoneMirror`도 항상 `renderSpots`. 모드는 지도 구획(동경계 vs 헥사곤)만 바꾸고 스팟 등 컨텐츠는 양쪽 유지.
  - **폰 하단 네비 개편**: `미션` 탭 제거(지도/피드/소셜). 네비 왼쪽에 **컨텐츠 추가(+) 버튼**(`.pn-add`, AI 버튼과 동일 원형칩) → 클릭 시 `#content-add-menu` 팝업 2개: **스팟 메시지**(`startPlacingSpot`) / **사진 올리기**(`#feed-photo-input` 파일선택, 피드 업로드는 데모 alert). `.phone-navbar` justify-content center→space-between(+ 좌 / 그룹 / AI 우).
  - **스팟 배치 폰맵 지원**: `SpotComposer(latLng,targetMap)`·`onMapClickForSpot(latLng,targetMap)`로 클릭한 지도 위에 팝업. 커서(crosshair)를 메인·폰 지도 둘 다 적용(`setPlaceCursor`). 폰맵 click 리스너에 placing 분기 추가(모바일 배치).
- **v1.11.1 — 전체 폰트 Pretendard로 교체**: `style.css` 상단 `@import`를 Inter(Google Fonts)→**Pretendard Variable**(jsDelivr `orioncactus/pretendard@v1.3.9`)로, `html,body` font-family를 `'Pretendard Variable','Pretendard',…`로. 나머지는 전부 `font-family:inherit`라 body만 바꿔 전체 적용(커스텀 지도 오버레이 `.map-label-tag`/`.spot-bubble` 포함). Google Maps 타일 자체 텍스트는 구글 관할이라 무관.
- **v1.11.0 — 베이직 리네임 + 메뉴 블록화 + 스팟 배율/점옵션 + 존 스포트라이트 + 동 라벨**:
  - **로컬→베이직 리네임**(모드 토글·폰 토글 UI 텍스트만; `data-mode="local"`/`currentMode='local'` 등 내부 식별자·CSS 클래스는 그대로).
  - **메뉴 가독성**: 아코디언 섹션을 **블록(카드)**으로(`.acc-section` 테두리+배경+라운드, 헤더 크게·소문자·펼침 시 하이라이트). 최상위 토글(`#content-toggle`/`#settings-toggle`)도 큰 카드 버튼으로.
  - **스팟 배율 스케일**: `SpotBubble.draw`가 `spotScale(z)=2^(z-기준줌)` 적용, 상한만 6배(하한 0.16). **줌인=확대·줌아웃=축소**(기존 1배 상한 제거).
  - **점 표시 옵션**: 점 전환 줌 미만일 때 `spotConfig.dotStyle`='dot'(점) | 'emoji'(이모지 축소). **점 색상** `spotConfig.dotColor`(색 컨트롤 `ct-spot-dot`). 설정에 "작을 때/점 색상" 추가. 공유문서 자동 저장.
  - **위치 라벨 소스 교정**: `updatePhoneLocation`이 **폰맵 센터**(`phoneMap||map`) 기준. 베이직=`dongAt`(동, 기존 `guAt`서 변경), 트렌드=`zoneAtCenter`(존명). 폰맵 idle에도 라벨 갱신(모바일 팬 반영).
  - **트렌드 존 스포트라이트(데모)**: 폰 존 폴리곤 `clickable:true`+클릭→`selectPhoneZone` = **`fitBounds`로 존 전체 표시 + 마스크 폴리곤**(존 주변 사각 outer[시계]+헥사곤 holes[반시계]로 존 밖만 그레이 `#0a0c14`/0.62). 재클릭·빈곳 클릭 해제(`clearPhoneSpotlight`), `syncPhoneZones`/모드전환 시 해제. ⚠️ outer는 전세계 링 쓰면 안/밖 모호로 **반전**돼서 존 주변 사각형(존 span×8 패딩)으로 사용.
- **v1.10.0 — 컨텐츠/관리자 설정 분리 + 모드 토글 단순화 + 스팟 지도 팝업**:
  - **최상위 메뉴 2개**로 재편(같은 레벨): **🗂 컨텐츠 설정**(`#content-toggle`/`#content-section`, `initContentPanel`) = ①스팟 메시지(추가 버튼) ②트렌드 존 관리(존 저장/편집/목록). **⚙ 관리자 설정**(`#settings-section`) = 서비스 스타일 9섹션.
  - **모드 토글은 지도 전환 전용**: `#local-settings`/`#trend-settings` 래퍼 제거 → 두 모드 스타일 설정을 `#settings-section`에 평면화해 **모드 무관 항상 표시**(`switchMode`에서 display 토글 삭제). 존 목록(`renderZoneList`)도 모드 게이트 제거—어느 모드서든 열람, 존 편집(수정) 클릭 시 트렌드 모드 자동 전환.
  - **스팟 추가 = 지도 위 팝업**(`SpotComposer` OverlayView, `initSpotComposerClass`): 버튼→`map.setOptions({draggableCursor:'crosshair'})`(타일 위에서도 커서 확실 변경)→지도 클릭→**클릭한 포인트 옆 팝업**(이모지 픽커+메시지+등록/취소, 앵커 점+꼬리). 사이드바 입력폼(`#spot-form`) 및 `confirmSpot/showSpotForm/cancelSpotForm` 제거. 이모지 픽커는 `buildEmojiPicker`로 재사용화. 스팟은 로컬 콘텐츠라 추가 시 로컬 모드 자동 전환.
  - **JSON 내보내기/불러오기 제거**: `exportZones/importZones/initZoneIO` + `#zone-io-row` 삭제(콘텐츠가 Firestore `shared/mapContent`에 자동 저장돼 불필요).
  - 데모(role-user) 숨김 목록을 `#content-toggle-row`/`#content-section`/`#settings-*`로 갱신.
- **v1.9.0 — 관리자 설정 UI 정비 + 스팟 줌 스케일 + 숫자 직접입력**:
  - **설정 패널을 폰 미리보기 하단으로 분리**: `initPhoneMenu`에서 `#left-panel`을 폰 드로어로 옮기던 로직 제거 → 사이드바(폰 아래)에 상시 표시. `#settings-toggle` 라벨 "⚙ 설정"→**"⚙ 관리자 설정"**. 폰 햄버거 드로어는 이제 데모 앱 '메뉴'(계정 블록)만 담당(`drawer-account` 관리자도 노출).
  - **설정 섹션 아코디언화**(`initSettingsAccordion`): `#settings-section` 내 모든 `.settings-section`을 `.acc-section`으로, `h4`를 클릭 헤더(`.acc-head`, ▾ 회전)로. **기본 접힘**—탭처럼 필요한 것만 펼침. 9개 섹션 UX 일관화.
  - **스팟 줌 스케일**(지도에 붙어 보임): `SpotBubble.draw()`에서 `transform:scale` 적용. 기준 줌 `SPOT_REF_ZOOM`(=`CONFIG.MAP_ZOOM`+3=14) 이상은 **설정한 크기(1배) 유지**, 줌아웃 시 `2^(z-ref)`로 축소(최소 0.16배). 폰 지도도 동일(줌 동기화).
  - **점 전환 줌 관리자 설정화**: 하드코딩 `SPOT_DOT_ZOOM`(13) 제거 → `spotConfig.dotZoom` 슬라이더(8~18, 기본 13). 이 줌 미만이면 점으로 표시. 공유문서(`spotConfig`)에 자동 저장/복원.
  - **이모지 자간 음수 허용**: `spot-emoji-letter` min `0`→**`-8`**(붙이기 가능).
  - **슬라이더 숫자 직접입력**(`enhanceRangeInputs`): 모든 range 옆에 `.range-num` 숫자 필드 추가—슬라이더↔숫자 **양방향 동기화 + min/max clamp**. 기존 range 핸들러를 그대로 재사용(숫자 입력 시 `input` 이벤트 디스패치). `hex-radius`엔 `km` 단위 표기. `setRange`도 숫자필드 동기화.
  - ⚠️ 부수효과: 관리자 설정이 사이드바(데스크톱)에만 있음 → **모바일(≤768px)에선 관리자 설정 접근 불가**(폰 화면만 표시). 관리자 작업은 데스크톱 전제. 데모 뷰잉은 영향 없음.
- **v1.8.1 — 스팟/네비/AI 디테일 개선**: ①스팟 기본스타일 메뉴를 `#local-settings` 밖으로 이동→**모드 무관 항상 표시**(트렌드에서 사라지던 문제 해결) ②스팟 **이모지 자간**(`emojiLetterSpacing`, 2개+ 이모지 간격) 추가 ③스팟 편집 시 **컬러팝업 z-index 100010**로 모달 위에 표시 ④**줌아웃(z<`SPOT_DOT_ZOOM`=13) 시 스팟을 약한 점**으로 표시(`.spot-dot`, draw에서 토글) ⑤**데모 드로어 계정 블록**(`#drawer-account`: 이메일·버전·로그아웃, role-user만) ⑥하단 네비 커뮤니티→**소셜**(라벨 2자 균일)로 hug-content→**아이콘간격==외곽마진 항상 동일·팝핑 없음** ⑦AI 말풍선 꼬리를 **AI 아이콘 중심 위**(right 6.7cqw) 정렬 ⑧AI 클릭 시 **아이콘 360° 회전+AI색(블롭 stop #8ed0ff→#a78bfa, .ai-on 틴트)**, 재클릭/타임아웃 시 말풍선 사라지며 원복.
- **v1.8.0 — 스팟 편집/드래그 + 네비 고정폭 + AI 말풍선 + 플랫화**:
  - **하단 네비 고정폭**(`.pn-group` width:58cqw, justify center) → 메뉴 전환해도 바 크기 불변(팝핑 방지). gap==padding(3cqw) 균등·확대. [그룹+AI] 통째 가운데.
  - **스팟 메시지 편집/이동(관리자)**: 메인지도에서 스팟을 **드래그로 이동**(`SpotBubble._onDown`, fromContainerPixelToLatLng), **이동 없이 클릭하면 편집 모달**(`#spot-edit-modal`)—해당 스팟의 **텍스트·버블색·이모지** 개별 수정 + 삭제. per-spot `color` 필드 추가(`_render`가 `s.color||bgColor`), cloudSave/applyCloudData에 color 포함.
  - **AI 말풍선(데모)**: AI 아이콘 클릭 시 `#ai-bubble` "오늘 우리 동네 일상이 궁금하신가요?" 역동적 팝(overshoot bounce, 4.5s 후 자동 숨김, 재클릭 토글).
  - **플랫 디자인 패스**: 네비 그룹·AI·헤더·모드버튼·스팟버블·모달/팝업의 과한 그림자/inset 하이라이트/blur 깊이감 제거 → 플랫.
  - **데모 드로어 계정**: 데모(뷰어) 햄버거 드로어에 버전+로그인 이메일(· 뷰어)+로그아웃 노출 확인(showUserChip/account-row, 설정은 role-user로 숨김).
- **v1.7.1 — 폰 UI 미세조정**: ①하단 네비 그룹을 아이콘에 맞춰 컴팩트(`.pn-group` flex:0 0 auto), [그룹+AI] 통째로 가운데(`.phone-navbar` justify-content:center) ②네비 아이콘 간격 확대(gap 1.7cqw) ③상단 헤더 상단여백 확대(padding-top 5.5cqw)+앱바 세로중앙(min-height 9cqw, align-items center) ④상단 위치표시=로컬은 **구(sggnm)**, 트렌드는 **중심 트렌드존**(`regionAt`/`guAt`/`zoneAtCenter`, dongIndex에 gu 추가) ⑤AI 버튼을 좌측 네비와 통일—다크 글래스 원형+플랫 라이트 블롭(#cbd0d8, 그라디언트/그림자 제거).
- **v1.7.0 — 폰 UI 정리 + 스팟 디자인 메뉴 + 축척 범례**:
  - **폰 상단 = 흰색 헤더**: 상태바(시간/신호/배터리) 제거. `.phone-topscrim`(어두운 스크림) → `.phone-header`(불투명 흰색). 햄버거·위치라벨·모드토글을 흰배경용으로 배경 제거+재스타일(햄버거 라인 #33373f, 위치 텍스트 #1f2430, 모드토글 라이트 세그먼트). 헤더 하단에 **예약 흰색 슬롯 `#phone-header-slot`**(추후 컨텐츠에 따라 가변).
  - **하단 네비 컴팩트**: `.pn-group` space-between→center+gap축소로 아이콘 중앙 클러스터, 활성 라벨 확장 시 밀착.
  - **스팟 메시지 디자인 메뉴**: 설정에 이모지 위치(위/아래/좌/우)·간격, 말풍선 둥글기, 꼬리 표시 토글 추가(기존 글자/이모지/색 설정과 통합). `SpotBubble._render`가 flex-direction/order/gap/borderRadius/꼬리클래스(no-tail·tail-up) 반영. `spotConfig`에 emojiPos/emojiGap/bubbleRadius/tail 추가.
  - **이모지 팔레트 + 커스텀 추가**: 이모지 픽커를 `spotConfig.emojis`(기본 12개) 기반으로 렌더 + `＋`로 사용자 이모지 추가(prompt). 공유 문서에 저장/복원.
  - **축척 범례**: 관리자 지도 좌상단에 간단한 축척 바+거리(`#scale-legend`), zoom 변경 시 갱신(m/px 계산→nice 반올림). 모바일에선 숨김.
- **v1.6.0 — 폰 앱화 (햄버거 메뉴 + 모드 토글)**: 폰 상단바에 **좌=햄버거(≡)**, **우=로컬/트렌드 모드 토글** 추가. 햄버거 클릭 시 기존 "폰 아래 설정 패널"(`#left-panel`)이 **폰 안 드로어**로 슬라이드 인(닫기 X 버튼). 패널을 런타임에 `#phone-drawer`로 이동(`initPhoneMenu`). 데모 노출 규칙 동일 — 햄버거·모드토글은 노출, 설정·스팟추가 등은 `role-user`로 숨김. (부수효과: 데스크톱 사이드바 하단이 비게 됨 — 폰 중심 UI)
- **v1.5.0 — 스팟 메시지(관리자 기능)**: 로컬모드에서 지도 위 위치에 **이모지+말풍선** 메시지. `💬 스팟 메시지 추가`(설정 위, 관리자만)→지도 클릭으로 위치 선택→이모지 선택+텍스트 입력→등록. 스팟 클릭 시 삭제(관리자). 설정: 최대 글자수/글자크기/이모지 크기/글자색/배경색·투명도. 메인+폰 지도 양쪽 렌더(`SpotBubble` OverlayView).
  - **저장 구조 변경**: 관리자 콘텐츠(존+스팟+설정)를 `users/{uid}` → **`shared/mapContent`** 공유 문서로 이전. 관리자만 쓰기, **로그인 사용자(데모 포함) 모두 읽기** → 데모유저도 관리자가 만든 존/스팟을 볼 수 있음.
  - ✅ **Firestore 규칙**: `firestore.rules`에 `match /shared/{docId}`(관리자 쓰기/로그인 읽기) 추가 → **콘솔 배포 완료(2026-07-03)**. 스팟 저장·데모 뷰잉 동작.
- **v1.4.1 — 폰 UI 6종 개선**: ①폰을 사이드바 상단 `position:sticky`로 고정(설정 스크롤해도 폰 유지) ②폰 화면 접기/펴기 버튼(`#phone-collapse`) 추가 ③하단 네비 좌우 끝 여백 추가(균형 정렬) ④비선택 아이콘 컴팩트(패딩 축소, 선택 항목만 라벨 확장) ⑤AI 버튼 무채색화(그라데이션 #aab3c0→#7b8494, 눈/입 #2b3038) ⑥폰 버튼 탭 시 선택박스 안 뜨게(user-select:none + tap-highlight 투명).
- **repo 이전: `now-here-map-demo-pages` → `now-here-demo`** (배포 URL `/now-here-demo/`). 기존 repo의 GitHub Pages 배포가 계속 큐에서 멈춰(당시 **GitHub Pages 전체 장애 `degraded_performance`** 진행 중이던 영향) 깨끗한 새 repo로 이전. 새 repo는 legacy(브랜치 직접) 배포 + Actions 비활성 + `.nojekyll`. GitHub 장애 복구되면 자동 배포됨. 로컬 origin은 새 repo로 전환(기존은 `oldrepo` remote로 보존).
- **v1.4.0 — 사이드바/폰/역할/모바일 4종 개선**:
  - **사이드바 폭 드래그 조절**(map↔sidebar 사이 `#sidebar-resizer`) → 폰 크기 변경. 폰 내부 UI는 `.phone-screen`을 `container-type:inline-size`로 만들고 상태바/앱바/네비를 **cqw 단위**로 바꿔 **폭이 바뀌어도 비율 유지**. 폭은 localStorage(`nowhere_sidebarW`) 저장.
  - **폰 하단 네비 정리**: navbar/그룹/아이템/AI를 cqw 기반으로 일관 정리.
  - **데모유저(role='user') 뷰어 모드**: 로그인 시 지도 부팅 + 로컬/트렌드 토글만 가능, 설정·존편집·allowlist UI는 `body.role-user`로 숨김, 헥사곤 선택(`toggleHex`) 차단. (클라우드 로드/저장 없음)
  - **모바일(≤768px) 접속 시 폰 화면만 전체표시**: map/패널 숨김, `.phone-screen`이 뷰포트 채움. 모바일에선 폰 지도 `gestureHandling:'greedy'`로 상호작용 가능.
- **레이아웃 재정의 v1.3.0**: 관리자 화면을 2컬럼으로 분리. **왼쪽=전체화면 지도만**, **오른쪽=사이드바**(상단: 폰 화면(프레임/레일 제거), 하단: 컨트롤/설정 패널). 기존 플로팅 폰 미러/좌측 플로팅 패널 → 사이드바에 도킹. HTML `#app-shell`(flex)+`#sidebar`, CSS만 변경. 폰 컨트롤 JS(드래그/크기/접기)는 요소 제거해도 방어적 가드라 무변경. 캐시버스트 `?v=1.3.0`.
  - TODO(후속): 폰 위 저장/뷰상태, 사이드바 폭 조절, 데모유저 화면 등.
- **Google 로그인 복구**: Firebase 브라우저 키 리퍼러에 `now-here-demo.firebaseapp.com`(+`web.app`)이 빠져 403으로 로그인이 깨졌던 것 → 리퍼러 추가로 해결 (위 ⚠️⚠️ 참고).
- **Firestore 규칙을 repo로 편입**: `firestore.rules` 추가. 기존 `allow read,write: if false`(전면 차단)에서 → 로그인/allowlist/유저데이터 접근을 실제 앱 패턴에 맞춰 허용하는 규칙으로 교체(콘솔 배포). allowlist(본인 문서 읽기/관리자 관리) + users/{uid}(관리자 본인 데이터) 구조.

### 2026-07-02
- **GitHub username 변경: `gihoonmx-source` → `gihoon-mx`.**
  - 배포 URL이 `gihoon-mx.github.io/now-here-map-demo-pages/`로 변경 (옛 주소는 404).
  - 위 외부 설정 4곳에 `gihoon-mx.github.io` 도메인 추가 완료.
  - 로컬 remote를 새 주소로 갱신 완료.
- 이 `WORKLOG.md` 추가 (cross-machine 작업 연속성용).

### ~v1.2.0 (기존 커밋 히스토리 참고)
- Google 로그인 + 계정 저장/접근제어 (Firebase)
- 관리자 뷰포트 오버레이 / 폰 미러 / AI 캐릭터 등
- 자세한 내역은 `git log --oneline` 참고.

---

## ✅ 다음 작업 (TODO)

_(여기에 진행 중/다음 할 일을 적어두면 다른 PC에서 바로 이어갈 수 있음)_

### 🎨 새 디자인 v3.0 — 8단계 중 4단계 완료 (v1.85~v1.88)

핸드오프: `Now Here 재설계 v2.dc.html` (Claude Design, 2026-08-03 · 석촌동 에셋 기준 재설계).
사용자 확정 범위: **v3 를 세 번째 스킨으로 추가하고 기본값 / 콘솔은 재도색 + 컨텐츠 관리 표 /
시안의 새 기능도 전부 구현.**

**8단계 전부 완료 (v1.85 ~ v1.92).**

- [x] ① 토큰·셸 (v1.85) — `skin-v3.css`·`APP_SKINS`·웜 오프화이트+코랄·유리 네비
- [x] ② 지도 오버레이·탭 페이지 (v1.86) — 원형 컬러링 핀·피드 카드 본문
- [x] ③ 콘솔 재도색 (v1.87) — 스킨이 `.page-admin` 까지 함께 움직인다
- [x] ④ 전체 컨텐츠 표 (v1.88) — 종류 탭·다중선택·존 이동/숨김/삭제
- [x] ⑤ 타임딜 (v1.89) — M17 신설·⏰ 핀·바텀시트·콘솔 표 편입
- [x] ⑥ 지역 Overview 패널 + 스토리 서클 (v1.90)
- [x] ⑦ Our Talk / My Talk (v1.91)
- [x] ⑧ 경계 토글 · 코인 · °C 지표 (v1.92)

남은 것(디자인 밖):
- [ ] **GCP 콘솔에서 Places API 켜기** — 지역 시드 생성기(v1.93)가 실제 상호를 가져오려면
      필요하다. 지금은 `REQUEST_DENIED` 라 '동 이름 기반 기본 장소' 폴백으로 동작한다.
      (프로젝트 `now-here-demo` · 키는 config.js 의 `GCP_API_KEY` 하나를 공용으로 쓴다.)
- [ ] **드로어 자체의 v3 재도색** — '보기' 섹션은 넣었지만 드로어 카드·칩은 legacy 문법 그대로다.
- [ ] **타임딜 핀 육안 확인** — v1.89 검증 때 이 환경의 Maps 가 오버레이 수명주기를 돌리지
      않아 핀을 못 봤다(Request·피드 핀도 같이 안 떴다). 실기기/정상 환경에서 확인 필요.
### 그 밖에

- [ ] **지면 좌상단 칩이 앱바와 겹친다** (M10/M15) — v1.83 풀블리드로 사진이 최상단까지
      올라오면서 `.cps-chips`(LIVE·존)가 앱바 뒤에 깔린다. 앱바 아래로 내릴지 사진 하단으로
      옮길지 배치를 정해야 한다.
- [ ] **소개 덱(deck.html)이 v1.44.0에서 멈춰 있다** (M14) — v1.65 페이지 분리, v1.78~84
      새 스킨, M16 임베드/시나리오가 하나도 안 들어가 있다. 대외 소개용이면 우선순위 상향.
- [ ] **클라우드 시드 재생성** (M13, 운영) — v1.70에서 시드 항목이 늘며 `gi`(문서 인덱스)가
      밀렸다. admin.html 에서 🧹 비우기 후 다시 채우기 필요.
